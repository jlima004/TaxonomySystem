# Milestone v2.9: Alias Target Integrity & Descriptor Hygiene

**Status:** ✅ SHIPPED 2026-06-06
**Phases:** 49–51
**Total Plans:** 3
**Git range:** 2026-06-05 → 2026-06-06

## Overview

Resolve dangling descriptor alias targets and establish an automated integrity gate — scoped strictly to alias hygiene, with no new low-support curation. The milestone closed the long-standing `ylang ylang -> ylang_ylang` gap carried over from Phase 8 and re-surfaced in v2.8 Batch 2 decision matrix row #4 (`cananga`).

## Phases

### Phase 49: Alias Target Integrity Inventory

**Goal**: Auditar `descriptor_aliases.seed.json`, compiled `descriptor_aliases.json` e `taxonomy.json` para listar todos os alias targets que não resolvem para descriptors compilados, começando por `ylang ylang -> ylang_ylang`. Zero mutation.
**Depends on**: Phase 48
**Requirements**: Inventory support for HYG-01, HYG-02, HYG-03
**Plans**: 1 plan

Plans:

- [x] 49-01: Produce evidence-backed alias target integrity inventory with seed-vs-compiled equivalence and dangling target classification.

**Details:**
- Confirmed seed and compiled alias maps are identical at exactly 18 entries each.
- Verified 340 compiled descriptor IDs, 17 valid alias targets, and one dangling target: `ylang ylang -> ylang_ylang`.
- Classified `ylang ylang -> ylang_ylang` as `remediation_required`, not an exception candidate.
- Preserved zero-mutation scope: no taxonomy, alias, compiled artifact, or source-code changes.
- Created `.planning/phases/49-alias-target-integrity-inventory/49-ALIAS-TARGET-INVENTORY.md` for downstream Phase 50 and Phase 51.
- Completed: 2026-06-05.

### Phase 50: Alias Target Integrity Automation

**Goal**: Implementar o gate automatizado e o suporte a policy de exceções para falhar quando um target de alias não existir sem exceção documentada.
**Depends on**: Phase 49
**Requirements**: HYG-02, HYG-03
**Plans**: 1 plan

Plans:

- [x] 50-01: Implement automated alias target integrity gate and documented exception policy support.

**Details:**
- Implemented `validateAliasTargetIntegrity` pure validator and `npm run alias:integrity` proof CLI.
- Created `data/taxonomy/alias_target_exceptions.v1.json` with valid empty policy envelope.
- Gate validates all 18 seed aliases against compiled taxonomy descriptor IDs; fails closed on unresolved targets without documented exceptions.
- Scope excluded remediating `ylang ylang -> ylang_ylang`; that mutation belongs to Phase 51.
- Completed: 2026-06-06.

### Phase 51: Legacy Alias Remediation

**Goal**: Resolver `ylang ylang -> ylang_ylang` e qualquer outro dangling target confirmado na Phase 49, usando o gate da Phase 50 como prova.
**Depends on**: Phase 50
**Requirements**: HYG-01
**Plans**: 1 plan

Plans:

- [x] 51-01: Resolve confirmed dangling alias targets and prove the Phase 50 integrity gate passes.

**Details:**
- Resolved `ylang ylang -> ylang_ylang` via `add_target`: appended `ylang_ylang` under `floral/floral_white` in seed.
- Preserved alias map and empty exception policy; published v2.9.0 artifacts via explicit `--version 2.9.0`.
- Final gate proof: `341/18/0` (compiled descriptors / valid targets / unresolved targets).
- Full Vitest suite green: 56 files, 389 tests.
- Completed: 2026-06-06.

---

## Milestone Summary

**Key Decisions:**

- Read-only inventory before automation: Phase 49 proved counts and equivalence before any gate or remediation work.
- Pure validator + opt-in CLI proof: `alias:integrity` is separate from default compile/test pipeline.
- `add_target` remediation path for `ylang_ylang`: preserves legacy alias map, keeps exceptions empty, satisfies semantic fit under `floral/floral_white`.
- Explicit-version publication: v2.9.0 published via `--version 2.9.0`; `DEFAULT_PATHS.version` remains `2.1.0`.

**Issues Resolved:**

- Dangling alias target `ylang ylang -> ylang_ylang` remediated with curated seed descriptor.
- Automated alias target integrity gate operational with empty exception policy.
- v2.9.0 compiled artifacts published with 0 unresolved alias targets.

**Issues Deferred:**

- FUT-01: 243 remaining `corpus_candidate_low_support` items — deferred to future milestone.
- FUT-02: 13 remaining `seed_corpus_conflict` items — deferred to future milestone.
- Retroactive `50-VERIFICATION.md` for HYG-02/HYG-03 formal verification (implementation complete; verification documentation gap only).
- Wire `alias:integrity` into default CI pipeline (currently opt-in proof command).

**Technical Debt Incurred:**

- Phases 49 and 50 lack `VERIFICATION.md`; Phase 50 SUMMARY lacks `requirements-completed` frontmatter.
- Inventory test duplicates gate logic instead of calling shared validator.
- Pre-remediation FAIL state (340/17/1) not encoded as automated regression test.
- Phase 49 inventory artifact historically stale (340/17/1); live data reflects post-51 state (341/18/0).

---

## Cross-References

- Milestone audit: `.planning/milestones/v2.9-MILESTONE-AUDIT.md`
- Requirements archive: `.planning/milestones/v2.9-REQUIREMENTS.md`
- Safe-fit rationale: `.planning/phases/51-legacy-alias-remediation/51-SAFE-FIT-RATIONALE.md`
- Published artifacts: `data/compiled/v2/{taxonomy,descriptor_aliases,similarity_matrix}.json` (version `2.9.0`)

_For current project status, see `.planning/ROADMAP.md`_
