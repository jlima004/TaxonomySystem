# Milestone v2.8 Requirements Archive

**Status:** ✅ SHIPPED 2026-06-04
**Original file:** `.planning/REQUIREMENTS.md`
**Milestone:** v2.8 Low-Support Review Queue Triage Batch 2

---

## v2.8 Requirements

### Inventory

- [x] **INV-01**: Curator can inventory the current compiled v2.7 review_queue and confirm the remaining low_support candidate count before selection.
  - **Outcome:** Validated — Phase 44 validated 259 `corpus_candidate_low_support` items in the compiled v2.7 review queue, with strict exclusion of 10 `seed_corpus_conflict` items and 6 v2.7-promoted descriptors.

- [x] **INV-02**: Curator can exclude candidates already explicitly decided in v2.7 unless they still appear unresolved as low_support in the current compiled v2.7 review_queue.
  - **Outcome:** Validated — Phase 44 inventory is the only eligible source for Batch 2 selection; the no-all-259 curation guardrail is preserved.

### Batch Selection

- [x] **SEL-01**: Curator can select a bounded Batch 2 of 25-50 low_support candidates.
  - **Outcome:** Validated — Phase 45 fixed Batch 2 at exactly 40 candidates (within the 25–50 envelope) using a weighted evidence model.

- [x] **SEL-02**: Curator can justify each selected candidate using evidence priority, semantic clarity, low polysemy, and curation value.
  - **Outcome:** Validated — Phase 45 selected-table row entries record weighted-score qualification and explicit manual sanity-review verdict per candidate.

### Decision Matrix

- [x] **DEC-01**: Curator can produce a decision matrix for every selected candidate before mutation.
  - **Outcome:** Validated — Phase 46 produced `46-DECISION-MATRIX.md` with exactly 40 rows mapped 1:1 to Phase 45 candidates; row count, id ordering, and Phase 45 mapping verified by inline parser.

- [x] **DEC-02**: Curator can assign each selected candidate an explicit disposition: safe seed addition, alias, reject, defer, or manual_review.
  - **Outcome:** Validated — Phase 46 disposition counts: 12 `promote_to_seed`, 0 `add_alias`, 2 `reject`, 17 `defer_manual_review`, 8 `defer_future_batch`, 1 `needs_external_reference`. All enums enforced by the parser.

- [x] **DEC-03**: Curator can document rationale and evidence for each disposition so downstream mutation is traceable.
  - **Outcome:** Validated — Phase 46 matrix records target fields, confidence, investigation depth, evidence, and mechanical `phase47_instruction` per row.

### Controlled Mutation

- [x] **CUR-01**: Curator can apply only explicitly approved safe seed additions or aliases from the decision matrix.
  - **Outcome:** Validated — Phase 47 applied exactly 12 `promote_to_seed` rows from `46-DECISION-MATRIX.md`; zero `add_alias` rows; parser confirmed 12 additions, 0 deletions, 0 structural changes to non-target nodes.

- [x] **CUR-02**: Curator can preserve rejects, defers, and manual_review outcomes as non-promoted decisions without automatic promotion.
  - **Outcome:** Validated — Phase 47 ignored all 28 non-executable rows; pre-publication gate (tsc + Vitest + sandbox compile) passed before any mutation could be persisted as official data.

- [x] **CUR-03**: Curator can verify protected boundaries remain unchanged unless explicitly authorized: seed_corpus_conflict reopening, scoring redesign, Graphify, UI, MVP, SaaS, Knowledge Engine, and frequency-only promotion.
  - **Outcome:** Validated — Phase 47 protected-diff guard and `scripts/check-safety-guards.sh` passed before/after mutation; no protected boundary changed.

### Artifact Publication

- [x] **PUB-01**: Curator can validate v2.8 compilation in a sandbox before official artifact publication.
  - **Outcome:** Validated — Phase 48 sandbox compile in `/tmp/compile-2.8-validate/` returned `validation_status=ok` and `quality_gate_status=PASS` before any official compile.

- [x] **PUB-02**: Curator can publish v2.8 compiled artifacts with updated taxonomy, aliases, similarity graph, review_queue metrics, and artifact version alignment.
  - **Outcome:** Validated — Phase 48 published `data/compiled/v2/{taxonomy,descriptor_aliases,similarity_matrix}.json` at `version 2.8.0 / 2.8.0 / 2.8.0`. Final published metrics: 10 families, 18 subfamilies, 61 seed descriptors, 340 compiled descriptors, 18 aliases, 13 graph edges, 256 review queue items.

- [x] **PUB-03**: Curator can produce a v2.8 closure report whose metrics are measured from the published compiled JSON artifacts.
  - **Outcome:** Validated — Phase 48 produced `.planning/releases/v2.8.0-CLOSURE.md` with metrics re-read from published `data/compiled/v2/*.json` (not `/tmp`); pre/post deltas vs v2.7.0 are explicit and traceable.

## Future Requirements

Deferred to later milestones. Tracked but not in the v2.8 roadmap.

### Low-Support Continuation

- **FUT-01**: Continue triaging the remaining `corpus_candidate_low_support` items (243 left after v2.8) in bounded future batches.

### Conflict Resolution

- **FUT-02**: Separately plan and resolve the 13 remaining `seed_corpus_conflict` items (10 carry-over + 3 newly observed) after explicit authorization.

### Alias Target Integrity (NEW follow-up)

- **FUT-03 (carry-over from Phase 8 + re-surfaced in Phase 47 row #4)**: Inspect `descriptor_aliases` target integrity, especially the legacy `ylang ylang → ylang_ylang` alias whose target is not present as a compiled taxonomy descriptor. The next milestone must decide between (a) adding a `ylang_ylang` seed target and migrating the alias, (b) dropping the alias with rationale, or (c) documenting a permanent exception. Until resolved, `data/compiled/v2/descriptor_aliases.json` carries a known dangling alias target.

## Out of Scope

Explicitly excluded to prevent scope creep. Carried forward from v2.7.

| Feature | Reason |
|---------|--------|
| Curating all 243 remaining `corpus_candidate_low_support` candidates in v2.8 | Bounded batch is required to preserve review quality and avoid over-curation. |
| Automatic promotion based only on frequency | Frequency is evidence, not explicit curation approval. |
| Reopening `seed_corpus_conflict` items in v2.8 | v2.8 is strictly low_support Batch 2 unless conflict work is explicitly planned later. |
| Reconsidering v2.7 explicit decisions unless still unresolved in current compiled v2.7 review_queue | Preserves Batch 1 decisions and avoids churn. |
| MVP, SaaS, Knowledge Engine, UI, Graphify, or scoring redesign | Reserved for future milestones outside this curation batch. |
| Switching `DEFAULT_PATHS` to v2.8 | v2.8 publication remains explicit via `--version 2.8.0`. |

## Traceability

| Requirement | Phase | Status | Outcome |
|-------------|-------|--------|---------|
| INV-01 | Phase 44 | ✅ Complete | 259 candidates validated against v2.7 compiled review queue |
| INV-02 | Phase 44 | ✅ Complete | 10 conflicts + 6 v2.7 decisions excluded |
| SEL-01 | Phase 45 | ✅ Complete | Bounded batch of 40 selected |
| SEL-02 | Phase 45 | ✅ Complete | 219 not-selected candidates covered with closed reason codes |
| DEC-01 | Phase 46 | ✅ Complete | 40-row decision matrix produced |
| DEC-02 | Phase 46 | ✅ Complete | 12 promote + 0 alias + 28 non-executable (closed enum) |
| DEC-03 | Phase 46 | ✅ Complete | Target, confidence, evidence, mutation gate per row |
| CUR-01 | Phase 47 | ✅ Complete | 12 approved seed mutations applied |
| CUR-02 | Phase 47 | ✅ Complete | 28 non-executable rows ignored; no auto-promotion |
| CUR-03 | Phase 47 | ✅ Complete | Protected-diff guard + safety script PASS |
| PUB-01 | Phase 48 | ✅ Complete | Sandbox compile validation `ok` |
| PUB-02 | Phase 48 | ✅ Complete | v2.8.0 artifacts published (61 seed, 340 compiled) |
| PUB-03 | Phase 48 | ✅ Complete | Closure report measured from published JSON |

**Coverage:**
- v2.8 requirements: 13 total
- Mapped to phases: 13
- Unmapped: 0
- Outcome: 13/13 Validated
