---
phase: 53-alias-integrity-gate-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/inventory/alias_target_inventory.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
user_setup: []
must_haves:
  truths:
    - "Developer can maintain alias target inventory coverage through a test that reuses validateAliasTargetIntegrity directly where appropriate per D-11 and TEST-01."
    - "Historical inventory fixture coverage remains present and still validates documentary sections and ylang rationale per D-12."
    - "Production validator behavior remains unchanged during the inventory test refactor per D-13."
    - "Operational baseline remains PASS 341 compiled / 18 valid alias targets / 0 unresolved per D-14 and D-18."
    - "D-01: Historical fixture stabilization is already satisfied, so this plan preserves existing fixtures and does not create a fixture-fix task."
    - "D-02: Existing stable fixtures, focused planning-artifact tests, full src suite precondition, and baseline 341/18/0 are preserved during the inventory refactor."
  artifacts:
    - path: "src/tests/inventory/alias_target_inventory.test.ts"
      provides: "Inventory regression contract using shared alias integrity validator plus documentary fixture checks"
      contains: "validateAliasTargetIntegrity"
    - path: "src/compiler/alias_target_integrity.ts"
      provides: "Existing shared validator contract consumed by inventory tests"
      exports: ["validateAliasTargetIntegrity", "ExceptionPolicy"]
  key_links:
    - from: "src/tests/inventory/alias_target_inventory.test.ts"
      to: "src/compiler/alias_target_integrity.ts"
      via: "direct import of validateAliasTargetIntegrity"
      pattern: "validateAliasTargetIntegrity"
    - from: "src/tests/inventory/alias_target_inventory.test.ts"
      to: "data/taxonomy/alias_target_exceptions.v1.json"
      via: "exception policy fixture read"
      pattern: "alias_target_exceptions\\.v1\\.json"
    - from: "src/tests/inventory/alias_target_inventory.test.ts"
      to: "src/tests/fixtures/inventory/49-ALIAS-TARGET-INVENTORY.md"
      via: "fixture-first documentary assertion"
      pattern: "49-ALIAS-TARGET-INVENTORY\\.md"
---

<objective>
Refactor the alias target inventory regression test to reuse the shared alias integrity validator without weakening historical documentary fixture coverage.

Purpose: Satisfy TEST-01 and contribute to TEST-02 by removing duplicated dangling-target logic from `alias_target_inventory.test.ts` while preserving the Phase 49 inventory artifact contract and the current `341/18/0` operational baseline per D-11 through D-14.
Output: Updated inventory test that imports `validateAliasTargetIntegrity`, reads the existing exception policy, asserts `PASS 341/18/0`, and keeps the documentary inventory and ylang rationale checks intact.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-alias-integrity-gate-hardening/53-CONTEXT.md
@.planning/phases/53-alias-integrity-gate-hardening/53-RESEARCH.md
@.planning/phases/53-alias-integrity-gate-hardening/53-VALIDATION.md
@.planning/phases/53-alias-integrity-gate-hardening/53-PATTERNS.md
@src/tests/inventory/alias_target_inventory.test.ts
@src/compiler/alias_target_integrity.ts
@src/tests/compiler/alias_target_integrity.test.ts
@src/tests/fixtures/inventory/49-ALIAS-TARGET-INVENTORY.md
@src/tests/helpers/resolve_existing_path.ts
@data/taxonomy/alias_target_exceptions.v1.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 53-02-01: Replace duplicated dangling-target assertions with shared validator assertions</name>
  <files>src/tests/inventory/alias_target_inventory.test.ts</files>
  <read_first>
    - `.planning/phases/53-alias-integrity-gate-hardening/53-CONTEXT.md` decisions D-11 through D-14 and D-18 through D-23.
    - `.planning/phases/53-alias-integrity-gate-hardening/53-PATTERNS.md` sections for `src/tests/inventory/alias_target_inventory.test.ts`, shared validator result contract, and fixture-first historical artifact resolution.
    - `src/tests/inventory/alias_target_inventory.test.ts` first live alias-data test and the two documentary fixture tests.
    - `src/compiler/alias_target_integrity.ts` exports `validateAliasTargetIntegrity` and `ExceptionPolicy`.
    - `src/tests/compiler/alias_target_integrity.test.ts` validator assertion style.
    - `data/taxonomy/alias_target_exceptions.v1.json` existing exception policy shape.
  </read_first>
  <behavior>
    - Test 1: Live seed aliases and compiled aliases still both contain 18 entries and remain equal.
    - Test 2: The live compiled taxonomy still provides 341 descriptor IDs.
    - Test 3: `validateAliasTargetIntegrity(seedAliases, descriptorIds, exceptionPolicy)` returns `status: 'PASS'`, `seed_alias_count: 18`, `compiled_descriptor_count: 341`, `valid_target_count: 18`, `unresolved_target_count: 0`, and `unresolved: []`.
    - Test 4: The inventory artifact test still checks `Sources Inspected`, `Completeness Proof`, `Zero-Mutation Statement`, `remediation_required`, and `Handoff to Phase 50/51`.
    - Test 5: The ylang rationale test still checks the historical content and confirms `ylang_ylang` and `ylang` live IDs.
  </behavior>
  <action>Update only `src/tests/inventory/alias_target_inventory.test.ts`. Import `validateAliasTargetIntegrity` and, if useful for strict typing, `type ExceptionPolicy` from `../../compiler/alias_target_integrity.js`. Add `exceptionPolicyPath = path.join(repoRoot, 'data/taxonomy/alias_target_exceptions.v1.json')`. In the first live alias-data test, read `exceptionPolicy` together with `seedAliases`, `compiled`, and `taxonomy`; keep the existing 18-entry seed/compiled alias equality assertions and `descriptorIds.size` assertion. Remove the local `valid` and `dangling` `Object.fromEntries` calculations and replace them with a shared-validator result assertion for exact `PASS`, `seed_alias_count: 18`, `compiled_descriptor_count: 341`, `valid_target_count: 18`, `unresolved_target_count: 0`, and `unresolved: []` per D-11 and D-14. Do not modify `src/compiler/alias_target_integrity.ts` per D-13. Do not remove or weaken the documentary inventory artifact test or the ylang near-match rationale test per D-12.</action>
  <acceptance_criteria>
    - Source assertion: `src/tests/inventory/alias_target_inventory.test.ts` imports `validateAliasTargetIntegrity`.
    - Source assertion: `src/tests/inventory/alias_target_inventory.test.ts` reads `data/taxonomy/alias_target_exceptions.v1.json`.
    - Source assertion: the first inventory test no longer contains local `const valid = Object.fromEntries` or `const dangling = Object.fromEntries` calculations.
    - Behavior assertion: the first inventory test asserts shared-validator `PASS`, `341`, `18`, and `0` counts.
    - Source assertion: the file still contains documentary strings `Sources Inspected`, `Completeness Proof`, `Zero-Mutation Statement`, `remediation_required`, `Handoff to Phase 50/51`, and `ylang_ylang`.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix src test -- tests/inventory/alias_target_inventory.test.ts</automated>
  </verify>
  <done>The inventory regression uses `validateAliasTargetIntegrity` for live target resolution, production validator behavior is unchanged, documentary fixture coverage remains intact, and the focused inventory test passes.</done>
</task>

<task type="auto">
  <name>Task 53-02-02: Run focused alias integrity regression after inventory refactor</name>
  <files>src/tests/inventory/alias_target_inventory.test.ts</files>
  <read_first>
    - `src/tests/inventory/alias_target_inventory.test.ts` after Task 53-02-01.
    - `.planning/phases/53-alias-integrity-gate-hardening/53-VALIDATION.md` Per-Task Verification Map and Sampling Rate.
    - `src/tests/compiler/alias_target_integrity.test.ts` existing validator PASS/FAIL coverage.
    - `src/tests/cli/alias_integrity.test.ts` existing CLI proof and script-wiring coverage.
  </read_first>
  <action>Run the required focused regression command from D-18 and `53-VALIDATION.md` after the inventory refactor. If a failure points to weakened documentary fixture coverage, restore the documentary assertions rather than deleting them. If a failure points to shared-validator input shape, adjust only the test's data loading or typing; do not change `src/compiler/alias_target_integrity.ts` solely to satisfy this refactor per D-13.</action>
  <acceptance_criteria>
    - Command assertion: `npm --prefix src test -- tests/inventory/alias_target_inventory.test.ts tests/compiler/alias_target_integrity.test.ts tests/cli/alias_integrity.test.ts` exits `0`.
    - Command assertion: `npm --prefix src run typecheck` exits `0`.
    - Source assertion: no production validator file changes are needed for this plan unless a real bug unrelated to the test refactor is discovered and documented.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix src test -- tests/inventory/alias_target_inventory.test.ts tests/compiler/alias_target_integrity.test.ts tests/cli/alias_integrity.test.ts && npm --prefix src run typecheck</automated>
  </verify>
  <done>Focused alias integrity regression and typecheck pass after the inventory test refactor, preserving equivalent or stronger coverage for TEST-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test data files → shared validator | Live taxonomy and compiled JSON are read into validator assertions. |
| Historical fixture → documentary assertions | Stable planning fixture content proves the inventory contract without relying on mutable planning archives. |
| Test refactor → production validator | The test consumes the existing validator and must not reshape production behavior for convenience. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04 | Tampering | `src/tests/inventory/alias_target_inventory.test.ts` live alias assertions | mitigate | Replace duplicated valid/dangling logic with `validateAliasTargetIntegrity` and assert exact `PASS 341/18/0` counts so test logic cannot drift from the production proof contract per D-11 and D-14. |
| T-53-05 | Repudiation | Documentary inventory fixture assertions | mitigate | Preserve assertions for mandated inventory sections and ylang rationale so the historical fixture contract remains auditable per D-12. |
| T-53-06 | Tampering | `src/compiler/alias_target_integrity.ts` | mitigate | Do not modify production validator behavior merely to satisfy the inventory test refactor per D-13; verification reads validator tests and focused regression. |
| T-53-07 | Denial of Service | Focused/full regression usability | mitigate | Keep the change limited to one test file and run focused regression plus typecheck inside this plan; full suite runs in Plan 03 per TEST-02. |
| T-53-SC | Tampering | npm installs | accept | No package installs are planned; Package Legitimacy Audit is not applicable and no install task exists. |
</threat_model>

<verification>
- `npm --prefix src test -- tests/inventory/alias_target_inventory.test.ts`
- `npm --prefix src test -- tests/inventory/alias_target_inventory.test.ts tests/compiler/alias_target_integrity.test.ts tests/cli/alias_integrity.test.ts`
- `npm --prefix src run typecheck`
</verification>

<success_criteria>
- TEST-01: Inventory test reuses `validateAliasTargetIntegrity` directly where appropriate.
- TEST-02: Focused alias integrity regression remains green and no documentary fixture coverage is weakened.
- D-11 through D-14 and D-18 through D-23 are covered without changing production validator behavior or protected data/artifact scopes.
</success_criteria>

<output>
Create `.planning/phases/53-alias-integrity-gate-hardening/53-02-SUMMARY.md` when done.
</output>
