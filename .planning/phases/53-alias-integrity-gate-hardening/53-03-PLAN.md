---
phase: 53-alias-integrity-gate-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 53-01
  - 53-02
files_modified:
  - .planning/phases/53-alias-integrity-gate-hardening/53-03-SUMMARY.md
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - TEST-01
  - TEST-02
user_setup: []
must_haves:
  truths:
    - "Developer can run focused tests, typecheck, full suite, alias JSON proof, verify JSON proof, compile:quality, and compile smoke successfully per D-15 through D-22."
    - "Reviewer can inspect `src/package.json` and prove normal `compile` does not call `alias:integrity`, `verify:integrity`, or `compile:quality` per D-07 and D-20."
    - "Reviewer can prove Phase 53 did not stage Graphify changes, did not claim preexisting graphify-out dirty state as Phase 53 work, and did not mutate taxonomy truth, descriptor alias policy files, compiled v2 artifacts, scoring, UI, MVP, Knowledge Engine, or CI scope per D-23 through D-26."
    - "All Phase 53 requirements GATE-01, GATE-02, GATE-03, TEST-01, and TEST-02 have executable proof before `/gsd-verify-work`."
    - "D-02: Final verification preserves stable fixtures, focused planning-artifact tests, the full suite, and the 341/18/0 baseline."
    - "D-19: Required full regression command is exactly `npm --prefix src test`."
    - "D-21: Required compile smoke is exactly `npm --prefix src run compile -- --out /tmp/phase53-compile-smoke` and must exit 0."
    - "D-24: Allowed touched scope is limited to `src/package.json`, local quality-script wiring, `src/tests/**`, and fixtures only if needed."
    - "D-25: CI wiring is not included in Phase 53 because CI belongs to Phase 54."
  artifacts:
    - path: "src/package.json"
      provides: "Static script proof for guardrail inclusion and compile isolation"
      contains: "verify:integrity"
    - path: "src/tests/cli/alias_integrity.test.ts"
      provides: "Guardrail script and CLI proof tests"
      contains: "compile:quality"
    - path: "src/tests/inventory/alias_target_inventory.test.ts"
      provides: "Shared-validator inventory coverage"
      contains: "validateAliasTargetIntegrity"
  key_links:
    - from: "npm --prefix src run verify:integrity -- --json"
      to: "dist/cli/alias_integrity.js"
      via: "official local guardrail command"
      pattern: "PASS.*341.*18.*0"
    - from: "npm --prefix src run compile -- --out /tmp/phase53-compile-smoke"
      to: "src/package.json scripts.compile"
      via: "normal compile smoke without alias gate"
      pattern: "node dist/cli/compile.js"
    - from: "git diff --name-only"
      to: "protected scope boundary"
      via: "boundary diff proof"
      pattern: "no protected paths changed"
---

<objective>
Run the Phase 53 proof package and boundary checks after Wave 1 changes land.

Purpose: Close GATE-01, GATE-02, GATE-03, TEST-01, and TEST-02 with concrete automated proof commands, static compile isolation proof, and protected-scope boundary diff proof required by D-15 through D-26.
Output: A completed verification pass showing alias JSON proof `341/18/0`, new `verify:integrity` proof, `compile:quality` proof, compile smoke, typecheck, focused regression, full suite, static script proof, and clean protected boundary diff.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-alias-integrity-gate-hardening/53-CONTEXT.md
@.planning/phases/53-alias-integrity-gate-hardening/53-RESEARCH.md
@.planning/phases/53-alias-integrity-gate-hardening/53-VALIDATION.md
@src/package.json
@src/tests/cli/alias_integrity.test.ts
@src/tests/inventory/alias_target_inventory.test.ts
@src/tests/compiler/alias_target_integrity.test.ts
@scripts/check-safety-guards.sh
</context>

<tasks>

<task type="auto">
  <name>Task 53-03-01: Prove local guardrail, quality gate, typecheck, and compile usability</name>
  <files>No source files modified; verification-only task reads src/package.json and command outputs.</files>
  <read_first>
    - `.planning/phases/53-alias-integrity-gate-hardening/53-CONTEXT.md` decisions D-15 through D-22.
    - `.planning/phases/53-alias-integrity-gate-hardening/53-VALIDATION.md` Sampling Rate and Per-Task Verification Map.
    - `src/package.json` final script wiring after Plan 01.
    - `src/tests/cli/alias_integrity.test.ts` and `src/tests/inventory/alias_target_inventory.test.ts` final assertions after Plans 01 and 02.
  </read_first>
  <action>Run the required local proof commands exactly: `npm --prefix src test -- tests/inventory/alias_target_inventory.test.ts tests/compiler/alias_target_integrity.test.ts tests/cli/alias_integrity.test.ts`; `npm --prefix src run typecheck`; `npm --prefix src run alias:integrity -- --json`; `npm --prefix src run verify:integrity -- --json`; `npm --prefix src run compile:quality`; and `npm --prefix src run compile -- --out /tmp/phase53-compile-smoke`. Inspect the JSON outputs for `status: PASS`, `compiled_descriptor_count: 341`, `valid_target_count: 18`, `unresolved_target_count: 0`, and `unresolved: []` per D-15 and D-16. Confirm `compile:quality` exits `0` if Plan 01 changed it per D-17. Confirm compile smoke exits `0` per D-21. Do not add `alias:integrity`, `verify:integrity`, or `compile:quality` to `compile` to fix any verification failure per D-07.</action>
  <acceptance_criteria>
    - Command assertion: focused regression command exits `0` per D-18.
    - Command assertion: `npm --prefix src run typecheck` exits `0` per D-22.
    - CLI assertion: `npm --prefix src run alias:integrity -- --json` exits `0` and JSON includes `PASS`, `341`, `18`, `0`, and empty `unresolved` per D-15.
    - CLI assertion: `npm --prefix src run verify:integrity -- --json` exits `0` and provides the same machine-readable proof and failure semantics surface per D-05 and D-16.
    - Command assertion: `npm --prefix src run compile:quality` exits `0` per D-17.
    - Command assertion: `npm --prefix src run compile -- --out /tmp/phase53-compile-smoke` exits `0` per D-21.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix src test -- tests/inventory/alias_target_inventory.test.ts tests/compiler/alias_target_integrity.test.ts tests/cli/alias_integrity.test.ts && npm --prefix src run typecheck && npm --prefix src run alias:integrity -- --json && npm --prefix src run verify:integrity -- --json && npm --prefix src run compile:quality && npm --prefix src run compile -- --out /tmp/phase53-compile-smoke</automated>
  </verify>
  <done>Focused regression, typecheck, alias JSON proof, verify JSON proof, compile quality, and compile smoke all pass with the required Phase 53 baseline and no normal compile usability regression.</done>
</task>

<task type="auto">
  <name>Task 53-03-02: Run final phase-gate full regression, static compile isolation, and protected boundary cleanliness</name>
  <files>No source files modified; verification-only task reads src/package.json and git diff outputs.</files>
  <read_first>
    - `.planning/phases/53-alias-integrity-gate-hardening/53-CONTEXT.md` decisions D-20, D-23, D-24, D-25, and D-26.
    - `.planning/REQUIREMENTS.md` v2.10 Out of Scope section.
    - `src/package.json` final scripts after Plan 01.
    - `scripts/check-safety-guards.sh` protected path boundary, confirming `safety:guard` remains separate.
  </read_first>
  <action>Run the full regression command `npm --prefix src test` per D-19 and TEST-02 as a final phase-gate check, not as the primary per-edit feedback loop; the faster focused regression, typecheck, JSON proof, compile:quality, and compile smoke from Task 53-03-01 remain the fast feedback commands for investigation and fixes. Then run a static script proof using Node to parse `src/package.json` and fail if `scripts.compile` contains `alias:integrity`, `verify:integrity`, or `compile:quality`, or if `scripts.compile` is not `node dist/cli/compile.js`, per D-07 and D-20. Run a boundary diff proof that treats `graphify-out/**` specially because dirty state may preexist Phase 53: `graphify-out/**` must not be staged and must not be claimed in `53-03-SUMMARY.md` as Phase 53 work, but preexisting unstaged dirty state may remain. For all other forbidden Phase 53 scopes, staged and unstaged diffs remain forbidden: `data/taxonomy/taxonomy-seed.v2.json`, `data/taxonomy/descriptor_aliases.seed.json`, `data/taxonomy/alias_target_exceptions.v1.json`, `data/compiled/v2`, `.github`, `src/scoring`, `src/ui`, and `src/knowledge-engine`. If the boundary proof reports staged `graphify-out/**` or forbidden staged/unstaged non-Graphify files, stop and revert only the forbidden unintended changes; do not mutate taxonomy truth, publish compiled artifacts, wire CI, or claim preexisting Graphify dirty state per D-23 through D-26.</action>
  <acceptance_criteria>
    - Final phase-gate assertion: `npm --prefix src test` exits `0` per D-19 and TEST-02; expected latency is about 90 seconds, so this is retained as a final gate rather than per-edit feedback.
    - Static assertion: parsed `src/package.json` has `scripts.compile === 'node dist/cli/compile.js'`.
    - Static assertion: parsed `scripts.compile` contains none of `alias:integrity`, `verify:integrity`, or `compile:quality` per D-20.
    - Boundary assertion: staged diffs contain no `graphify-out/**` paths; preexisting unstaged `graphify-out/**` dirty state may remain and must not be claimed by Phase 53.
    - Boundary assertion: working-tree and staged diffs contain no non-Graphify forbidden Phase 53 scope paths from D-23, D-25, and D-26.
    - Scope assertion: no CI workflow files are modified because CI wiring belongs to Phase 54 per D-25.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix src test && node -e "const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('src/package.json','utf8')); const compile=pkg.scripts.compile; if (compile !== 'node dist/cli/compile.js') { throw new Error('compile script changed: '+compile) } if (/alias:integrity|verify:integrity|compile:quality/.test(compile)) { throw new Error('compile script contains forbidden integrity/quality token') }" && bash -lc 'set -e; graphify_staged=$(git diff --cached --name-only -- graphify-out 2>/dev/null); if [ -n "$graphify_staged" ]; then printf "staged graphify-out forbidden changes:\n%s\n" "$graphify_staged"; exit 1; fi; for mode in unstaged staged; do if [ "$mode" = staged ]; then files=$(git diff --cached --name-only -- data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/taxonomy/alias_target_exceptions.v1.json data/compiled/v2 .github src/scoring src/ui src/knowledge-engine 2>/dev/null); else files=$(git diff --name-only -- data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/taxonomy/alias_target_exceptions.v1.json data/compiled/v2 .github src/scoring src/ui src/knowledge-engine 2>/dev/null); fi; if [ -n "$files" ]; then printf "%s forbidden changes:\n%s\n" "$mode" "$files"; exit 1; fi; done'</automated>
  </verify>
  <done>Focused checks remain the fast feedback path, final phase-gate full suite passes, static script proof confirms normal compile is unchanged and alias-gate-free, and boundary diff proof confirms no staged Graphify changes plus no taxonomy truth, alias policy, compiled v2, scoring, UI, MVP, Knowledge Engine, or CI scope mutation occurred. Any preexisting unstaged `graphify-out/**` dirty state is preserved and not claimed as Phase 53 work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local proof commands → repository state | Verification commands read and may write temporary compile outputs; protected scopes must remain clean. |
| Static package inspection → compile usability | Parsed `src/package.json` is the source of truth for whether normal compile remained alias-gate-free. |
| Full regression → phase completion | Full test suite proves TEST-02 after both Wave 1 plans have landed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-08 | Repudiation | Phase 53 proof package | mitigate | Run focused tests, full suite, typecheck, alias JSON proof, verify JSON proof, compile:quality, compile smoke, static package proof, and boundary diff proof; record outputs in `53-03-SUMMARY.md`. |
| T-53-09 | Denial of Service | Normal `compile` script | mitigate | Parse `src/package.json` with Node and fail if `scripts.compile` differs from `node dist/cli/compile.js` or contains forbidden integrity/quality tokens per D-07 and D-20. |
| T-53-10 | Tampering | Protected taxonomy/compiled/Graphify/deferred scopes | mitigate | Run a staged-only check for `graphify-out/**` because preexisting unstaged Graphify dirt may remain, and run staged plus unstaged checks for all other explicit forbidden paths; stop if forbidden Phase 53 changes appear per D-23 through D-26. |
| T-53-11 | Tampering | CI boundary | mitigate | Include `.github` in boundary checks because CI wiring is deferred to Phase 54 per D-25. |
| T-53-SC | Tampering | npm installs | accept | No package installs are planned; Package Legitimacy Audit is not applicable and no install task exists. |
</threat_model>

<verification>
- `npm --prefix src test -- tests/inventory/alias_target_inventory.test.ts tests/compiler/alias_target_integrity.test.ts tests/cli/alias_integrity.test.ts`
- `npm --prefix src run typecheck`
- `npm --prefix src run alias:integrity -- --json`
- `npm --prefix src run verify:integrity -- --json`
- `npm --prefix src run compile:quality`
- `npm --prefix src run compile -- --out /tmp/phase53-compile-smoke`
- `npm --prefix src test` final phase gate, expected around 90 seconds and not intended as per-edit feedback
- Node static script proof for `src/package.json` compile isolation
- staged-only git boundary diff proof for `graphify-out/**`; staged and unstaged git boundary diff proof for all other forbidden scopes
</verification>

<success_criteria>
- GATE-01, GATE-02, GATE-03, TEST-01, and TEST-02 all have concrete automated evidence.
- Required proof commands from D-15 through D-22 pass.
- Boundary proof from D-23 through D-26 passes while preserving any preexisting unstaged `graphify-out/**` dirty state without claiming it as Phase 53 work.
- No source files are modified by this verification-only plan.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
------ | -- | ------------------- | ---- | ------ | -----
GOAL | — | Integrate `alias:integrity` into an appropriate local guardrail without breaking normal compile | 01,03 | COVERED | `verify:integrity` and `compile:quality` are wired; `compile` static and smoke proofs protect normal compile.
REQ | GATE-01 | Local quality/safety command includes alias integrity without normal compile path | 01,03 | COVERED | `verify:integrity`, `compile:quality`, static compile proof.
REQ | GATE-02 | Machine-readable `alias:integrity -- --json` proof shows `341/18/0` | 01,03 | COVERED | CLI tests and command proof.
REQ | GATE-03 | Local alias integrity guard fails non-zero when unresolved targets are introduced | 01,03 | COVERED | Existing temp-fixture FAIL test preserved and guard proof runs.
REQ | TEST-01 | Inventory coverage reuses `validateAliasTargetIntegrity` directly where appropriate | 02 | COVERED | Inventory test imports and calls shared validator.
REQ | TEST-02 | Existing suite passes with equivalent or stronger coverage | 02,03 | COVERED | Focused regression, typecheck, and full suite.
RESEARCH | — | Add `verify:integrity` wrapper over existing base proof command | 01 | COVERED | Script and tests.
RESEARCH | — | Wire `compile:quality` to alias proof without changing `compile` | 01,03 | COVERED | Quality script plus static/smoke proof.
RESEARCH | — | Keep `safety:guard` separate | 01,03 | COVERED | Script source assertions and boundary review.
RESEARCH | — | Refactor inventory test to shared validator while preserving documentary assertions | 02 | COVERED | Direct validator use; fixture strings preserved.
RESEARCH | — | Prefer non-mutating temp output or boundary diff proof for quality compile | 01,03 | COVERED | Explicit temp output preference and boundary diff proof.
CONTEXT | D-01 | Historical fixture stabilization already satisfied; do not create fixture-fixing Task 1 | 02 | COVERED | Plans preserve fixtures; no fixture-fixing task.
CONTEXT | D-02 | Preserve stable fixtures, full suite, focused tests, alias baseline | 02,03 | COVERED | Focused/full/proof commands.
CONTEXT | D-03 | Add `verify:integrity` as official local guardrail | 01 | COVERED | Script wiring.
CONTEXT | D-04 | Keep `alias:integrity` as base command | 01 | COVERED | Base command preserved.
CONTEXT | D-05 | `verify:integrity` has same machine-readable proof and fail semantics | 01,03 | COVERED | Wrapper and JSON proof command.
CONTEXT | D-06 | Include alias integrity in `compile:quality` if clean | 01,03 | COVERED | Quality wiring and proof.
CONTEXT | D-07 | `compile` unchanged and alias-gate-free | 01,03 | COVERED | Static and smoke proof.
CONTEXT | D-08 | Avoid redundant rebuilds where possible | 01 | COVERED | Direct alias CLI after precompile in quality path.
CONTEXT | D-09 | Keep `safety:guard` separate | 01 | COVERED | No safety script merge.
CONTEXT | D-10 | Keep `safety:guard` focused on protected paths | 01 | COVERED | Source assertions preserve command.
CONTEXT | D-11 | Refactor inventory test to reuse validator | 02 | COVERED | Direct import and call.
CONTEXT | D-12 | Preserve historical artifact/fixture coverage | 02 | COVERED | Documentary tests retained.
CONTEXT | D-13 | Do not modify production validator merely for tests | 02 | COVERED | Plan forbids validator change for convenience.
CONTEXT | D-14 | Preserve `341/18/0` baseline | 01,02,03 | COVERED | Tests and JSON proofs.
CONTEXT | D-15 | Alias JSON proof required | 01,03 | COVERED | Command proof.
CONTEXT | D-16 | New guardrail command passing required | 01,03 | COVERED | Command proof.
CONTEXT | D-17 | `compile:quality` passing required if changed | 01,03 | COVERED | Command proof.
CONTEXT | D-18 | Focused regression command required | 02,03 | COVERED | Exact command included.
CONTEXT | D-19 | Full regression required | 03 | COVERED | Full suite command.
CONTEXT | D-20 | Static package proof for compile isolation required | 01,03 | COVERED | Test and Node static proof.
CONTEXT | D-21 | Compile smoke required | 03 | COVERED | Exact smoke command.
CONTEXT | D-22 | Typecheck required | 01,02,03 | COVERED | Included in verification.
CONTEXT | D-23 | Boundary proof required | 03 | COVERED | staged/unstaged diff proof.
CONTEXT | D-24 | Phase may touch package scripts/tests/fixtures only if needed | 01,02 | COVERED | Only package/test files planned; no fixture changes needed.
CONTEXT | D-25 | Do not wire CI | 03 | COVERED | `.github` boundary check.
CONTEXT | D-26 | Do not publish artifacts or mutate taxonomy truth | 03 | COVERED | protected boundary diff proof.
</source_audit>

<output>
Create `.planning/phases/53-alias-integrity-gate-hardening/53-03-SUMMARY.md` when done.
</output>
