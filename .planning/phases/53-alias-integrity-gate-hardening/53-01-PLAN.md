---
phase: 53-alias-integrity-gate-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/package.json
  - src/tests/cli/alias_integrity.test.ts
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
user_setup: []
must_haves:
  truths:
    - "Developer can run npm --prefix src run verify:integrity -- --json as the official local alias integrity guardrail per D-03, D-04, D-05, D-16."
    - "Developer can run npm --prefix src run compile:quality and receive alias integrity verification on the quality path without changing npm --prefix src run compile per D-06, D-07, D-17, D-20."
    - "alias:integrity remains the existing base command and still produces PASS 341 compiled / 18 valid alias targets / 0 unresolved per D-04, D-14, D-15."
    - "The local guard preserves non-zero failure semantics for unresolved alias targets per D-05 and GATE-03."
    - "D-08: Technical wiring avoids redundant rebuilds while preserving external behavior: verify:integrity remains the official local gate and compile stays alias-gate-free."
    - "D-09: safety:guard remains separate from Phase 53 alias integrity wiring."
    - "D-10: safety:guard is not merged with alias semantic integrity checks."
  artifacts:
    - path: "src/package.json"
      provides: "npm script wiring for alias:integrity, verify:integrity, compile:quality, compile, and safety:guard"
      contains: "verify:integrity"
    - path: "src/tests/cli/alias_integrity.test.ts"
      provides: "Vitest assertions for JSON proof, script isolation, compile path isolation, and failure semantics"
      contains: "verify:integrity"
  key_links:
    - from: "src/package.json scripts.verify:integrity"
      to: "dist/cli/alias_integrity.js"
      via: "forwarding-safe semantic guard using the same base proof CLI as alias:integrity"
      pattern: "verify:integrity.*alias_integrity\\.js"
    - from: "src/package.json scripts.compile:quality"
      to: "dist/cli/alias_integrity.js"
      via: "quality-flow integrity proof after precompile"
      pattern: "compile:quality.*alias_integrity\\.js"
    - from: "src/package.json scripts.compile"
      to: "dist/cli/compile.js"
      via: "normal compile only"
      pattern: "compile.*node dist/cli/compile\\.js"
---

<objective>
Create the official local alias integrity guardrail and quality-flow wiring for Phase 53.

Purpose: Satisfy GATE-01, GATE-02, and GATE-03 by promoting `verify:integrity` as the semantic local guardrail while preserving `alias:integrity` as the base proof command and keeping normal compile free of alias integrity checks per D-03 through D-10.
Output: Updated npm scripts and CLI/script regression tests proving JSON proof, compile isolation, quality-flow inclusion, and non-zero failure semantics.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-alias-integrity-gate-hardening/53-CONTEXT.md
@.planning/phases/53-alias-integrity-gate-hardening/53-RESEARCH.md
@.planning/phases/53-alias-integrity-gate-hardening/53-VALIDATION.md
@.planning/phases/53-alias-integrity-gate-hardening/53-PATTERNS.md
@.planning/phases/52-retroactive-verification-closure/52-01-SUMMARY.md
@src/package.json
@src/tests/cli/alias_integrity.test.ts
@src/cli/alias_integrity.ts
@src/compiler/alias_target_integrity.ts
@scripts/check-safety-guards.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 53-01-01: Implement alias integrity script wiring and executable tests together</name>
  <files>src/tests/cli/alias_integrity.test.ts, src/package.json</files>
  <read_first>
    - `.planning/phases/53-alias-integrity-gate-hardening/53-CONTEXT.md` decisions D-03 through D-10 and D-15 through D-22.
    - `.planning/phases/53-alias-integrity-gate-hardening/53-PATTERNS.md` sections for `src/tests/cli/alias_integrity.test.ts` and `src/package.json`.
    - `src/tests/cli/alias_integrity.test.ts` existing `runAliasIntegrityCli` JSON proof, temp-fixture FAIL test, and `alias:integrity npm script wiring` describe block.
    - `src/package.json` existing script names and current compile/quality/alias wiring.
  </read_first>
  <behavior>
    - Test 1: `src/package.json` exposes `scripts['verify:integrity']` and the script uses the same build-safe alias proof command as the existing `alias:integrity` base command, avoiding nested npm forwarding ambiguity per D-03, D-04, and D-05.
    - Test 2: `scripts.compile` does not match `alias:integrity`, `verify:integrity`, or `compile:quality` per D-07 and D-20.
    - Test 3: `scripts['compile:quality']` includes an alias integrity verification path while `scripts['safety:guard']` remains `bash ../scripts/check-safety-guards.sh` and does not mention alias integrity per D-06, D-09, and D-10.
    - Test 4: existing JSON proof assertions still cover `PASS`, `seed_alias_count: 18`, `compiled_descriptor_count: 341`, `valid_target_count: 18`, `unresolved_target_count: 0`, and `unresolved: []` per D-14 and D-15.
    - Test 5: existing temp-fixture unresolved-target test continues to assert exit code `1` and unresolved alias details per GATE-03 and D-05.
  </behavior>
  <action>Update only `src/tests/cli/alias_integrity.test.ts` and `src/package.json` so this task is executable without an expected-failing gate. In `src/package.json`, add `scripts['verify:integrity']` as the official local guardrail using the forwarding-safe direct proof shape `npm run precompile && node dist/cli/alias_integrity.js`, matching the existing `alias:integrity` base proof behavior while avoiding nested npm forwarding ambiguity per D-03, D-04, and D-05. Update `scripts['compile:quality']` to run the quality compile with explicit non-mutating output `/tmp/phase53-compile-quality` and then call `node dist/cli/alias_integrity.js` after the already-run `precompile` per D-06, D-08, D-17, and the resolved research question. Do not change `scripts.compile`; it must remain exactly `node dist/cli/compile.js` and must not include `alias:integrity`, `verify:integrity`, or `compile:quality` per D-07 and D-20. Do not merge or alter `safety:guard` per D-09 and D-10. In `src/tests/cli/alias_integrity.test.ts`, rename or expand the `alias:integrity npm script wiring` describe block so it covers both `alias:integrity` and `verify:integrity`. Add source assertions for exact script keys: `alias:integrity`, `verify:integrity`, `compile`, `compile:quality`, `safety:guard`, `build`, `test`, and `precompile`. The test must assert `verify:integrity` invokes `dist/cli/alias_integrity.js` with a build-safe command; `compile:quality` uses `/tmp/phase53-compile-quality` and contains an integrity proof path via `alias_integrity.js`; `compile` does not contain any of `alias:integrity`, `verify:integrity`, or `compile:quality`; `safety:guard` remains separate and does not contain alias integrity strings. Keep the existing real-data JSON proof test and temp-fixture FAIL test; do not change production CLI behavior per D-13.</action>
  <acceptance_criteria>
    - Source assertion: `src/tests/cli/alias_integrity.test.ts` contains the literal string `verify:integrity`.
    - Source assertion: `src/package.json` contains a `verify:integrity` script that invokes `dist/cli/alias_integrity.js` with a build-safe command.
    - Source assertion: `src/package.json` `compile:quality` contains `/tmp/phase53-compile-quality` and `dist/cli/alias_integrity.js`.
    - Source assertion: `src/tests/cli/alias_integrity.test.ts` asserts `pkg.scripts.compile` does not match `alias:integrity|verify:integrity|compile:quality`.
    - Behavior assertion: the file still asserts JSON proof fields `seed_alias_count: 18`, `compiled_descriptor_count: 341`, `valid_target_count: 18`, `unresolved_target_count: 0`, and `unresolved: []`.
    - Behavior assertion: the unresolved temp-fixture test still expects exit code `1`.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix src test -- tests/cli/alias_integrity.test.ts</automated>
  </verify>
  <done>The CLI/script tests pass after wiring `verify:integrity` and `compile:quality`; existing JSON proof and FAIL semantics remain intact, normal `compile` stays unchanged, and this task has no expected-failing verification gate.</done>
</task>

<task type="auto">
  <name>Task 53-01-02: Prove verify:integrity forwarding and compile isolation</name>
  <files>No source files modified; verification-only task reads src/package.json and command outputs.</files>
  <read_first>
    - `.planning/phases/53-alias-integrity-gate-hardening/53-CONTEXT.md` decisions D-03 through D-10 and D-15 through D-22.
    - `.planning/phases/53-alias-integrity-gate-hardening/53-RESEARCH.md` Pattern 1, Common Pitfalls 1, 2, and 4, and Open Questions (RESOLVED) 1 and 2.
    - `.planning/phases/53-alias-integrity-gate-hardening/53-PATTERNS.md` `src/package.json` current script wiring pattern.
    - `src/package.json` current `compile`, `compile:quality`, `safety:guard`, and `alias:integrity` scripts.
  </read_first>
  <action>Run the focused proof commands after Task 53-01-01. Prove the forwarding-safe standalone guard with `npm --prefix src run verify:integrity -- --json`; inspect the output and require JSON with `PASS`, `compiled_descriptor_count: 341`, `valid_target_count: 18`, `unresolved_target_count: 0`, and `unresolved: []` per D-05 and D-16. Run `npm --prefix src run compile:quality` and confirm it exits `0` while using the explicit `/tmp/phase53-compile-quality` output path from the resolved research strategy per D-06 and D-17. Run a Node static check over `src/package.json` proving `scripts.compile === 'node dist/cli/compile.js'` and contains none of `alias:integrity`, `verify:integrity`, or `compile:quality` per D-07 and D-20. Run typecheck for script/test edits per D-22. If any proof fails, fix only `src/package.json` or `src/tests/cli/alias_integrity.test.ts`; do not alter normal `compile`, `safety:guard`, production CLI behavior, taxonomy truth, or compiled v2 artifacts.</action>
  <acceptance_criteria>
    - Source assertion: `src/package.json` contains a `verify:integrity` script.
    - Source assertion: `src/package.json` still contains `alias:integrity` as a base command that invokes `dist/cli/alias_integrity.js`.
    - Source assertion: `scripts.compile` is exactly `node dist/cli/compile.js`.
    - Source assertion: `scripts.compile` contains none of `alias:integrity`, `verify:integrity`, or `compile:quality`.
    - Source assertion: `scripts['safety:guard']` remains `bash ../scripts/check-safety-guards.sh` and contains no alias integrity command.
    - CLI assertion: `npm --prefix src run verify:integrity -- --json` exits `0` and prints JSON with `PASS`, `341`, `18`, and `0` counts.
    - Command assertion: `npm --prefix src run compile:quality` exits `0` using the explicit temp output strategy.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix src test -- tests/cli/alias_integrity.test.ts && npm --prefix src run verify:integrity -- --json && npm --prefix src run compile:quality && npm --prefix src run typecheck</automated>
  </verify>
  <done>`verify:integrity -- --json` proves forwarding-safe JSON output, `compile:quality` passes with non-mutating temp output plus alias proof, `compile` stays unchanged and alias-gate-free, `safety:guard` remains separate, and focused CLI tests plus typecheck pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer shell → npm scripts | Local command strings in `src/package.json` execute build, compile, and alias proof commands. |
| npm scripts → repository data | Quality and integrity commands read taxonomy/compiled data and may write compile outputs if output paths are not constrained. |
| Test assertions → proof claims | Vitest source assertions become the auditable proof that local guardrails are wired as intended. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | `src/package.json` `compile:quality` | mitigate | Use an explicit temporary `--out /tmp/phase53-compile-quality` or equivalent non-protected output for quality compile, then verify boundary diff in Plan 03 so protected taxonomy and compiled artifacts remain untouched per D-23 and D-26. |
| T-53-02 | Repudiation | `verify:integrity -- --json` and `alias:integrity -- --json` | mitigate | Add script tests and command verification requiring JSON proof with `PASS`, `341` compiled descriptors, `18` valid targets, `0` unresolved, and empty `unresolved` per D-14 through D-16. |
| T-53-03 | Denial of Service | `npm --prefix src run compile` | mitigate | Keep `scripts.compile` exactly `node dist/cli/compile.js`; add tests that reject `alias:integrity`, `verify:integrity`, and `compile:quality` in `compile`; run compile smoke in Plan 03 per D-07, D-20, and D-21. |
| T-53-04 | Tampering | `safety:guard` boundary | mitigate | Preserve `safety:guard` as `bash ../scripts/check-safety-guards.sh` and assert it does not include alias integrity commands per D-09 and D-10. |
| T-53-SC | Tampering | npm installs | accept | No package installs are planned; Package Legitimacy Audit is not applicable and no install task exists. |
</threat_model>

<verification>
- `npm --prefix src test -- tests/cli/alias_integrity.test.ts`
- `npm --prefix src run verify:integrity -- --json`
- `npm --prefix src run typecheck`
</verification>

<success_criteria>
- GATE-01: A local quality/integrity command includes alias integrity without changing normal compile.
- GATE-02: JSON proof still reports `PASS` with `341/18/0`.
- GATE-03: The guard path preserves non-zero failure semantics for unresolved targets through existing temp-fixture tests.
- D-03 through D-10 and D-15 through D-22 are covered without touching CI, taxonomy truth, compiled v2 artifacts, Graphify, scoring, UI, MVP, or Knowledge Engine scope.
</success_criteria>

<output>
Create `.planning/phases/53-alias-integrity-gate-hardening/53-01-SUMMARY.md` when done.
</output>
