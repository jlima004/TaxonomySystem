---
phase: 55-graph-contract-boundary-decisions
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph_read_model/contract.ts
  - src/tests/graph_read_model/contract.test.ts
  - docs/olfactory_graph_contract.md
  - .planning/phases/55-graph-contract-boundary-decisions/55-01-SUMMARY.md
autonomous: true
requirements:
  - GCON-01
  - GCON-02
  - GCON-03
  - GCON-04
must_haves:
  truths:
    - "GCON-01: Maintainer can inspect a fixed olfactory graph schema contract with schema version, node kinds, edge kinds, required properties, ID rules, and Phase 56 invariants."
    - "GCON-02: Maintainer can verify the production graph inputs are exactly data/compiled/v2/taxonomy.json, data/compiled/v2/descriptor_aliases.json, and data/compiled/v2/similarity_matrix.json."
    - "GCON-03: Maintainer can verify all graph node IDs are type-prefixed with family:, subfamily:, descriptor:, or alias: to prevent raw-ID collisions."
    - "GCON-04: Maintainer can identify data/read-models/olfactory-graph/v2.11/ as the only sanctioned source-of-truth output path, /tmp as verification-only support, and protected prefixes as forbidden."
    - "Phase 55 remains contract-only and creates no builder, loader, writer, CLI, generated graph JSON, query proofs, structural validator, Graphify integration, Neo4J/database integration, or runtime integration."
  artifacts:
    - path: "src/graph_read_model/contract.ts"
      provides: "Static graph contract constants/types only for GCON-01 through GCON-04"
      contains: "olfactory_graph_read_model.v1"
    - path: "src/tests/graph_read_model/contract.test.ts"
      provides: "Vitest executable assertions for schema, IDs, inputs, outputs, forbidden scope, and Phase 56 invariant names"
      contains: "OLFACTORY_GRAPH_CONTRACT"
    - path: "docs/olfactory_graph_contract.md"
      provides: "Maintainer-readable schema and boundary contract"
      contains: "data/read-models/olfactory-graph/v2.11/"
  key_links:
    - from: "src/tests/graph_read_model/contract.test.ts"
      to: "src/graph_read_model/contract.ts"
      via: "ESM .js-suffixed import"
      pattern: "../../graph_read_model/contract.js"
    - from: "docs/olfactory_graph_contract.md"
      to: "src/graph_read_model/contract.ts"
      via: "shared exact contract values"
      pattern: "olfactory_graph_read_model.v1"
    - from: "Phase 56 planning/execution"
      to: "src/graph_read_model/contract.ts"
      via: "Phase 56 invariant names and baseline stats"
      pattern: "duplicate_node_id_detection|10 families|13 subfamily-similarity edges"
---

<objective>
Lock the Phase 55 graph schema, ID namespace, allowed input, output boundary, forbidden-scope, and Phase 56 invariant contract for GCON-01, GCON-02, GCON-03, and GCON-04.

Purpose: Protect every later v2.11 graph read-model phase by making boundary decisions inspectable, executable, and non-ambiguous before any builder, writer, validator, query proof, Graphify integration, Neo4J/database integration, or runtime integration exists.
Output: `src/graph_read_model/contract.ts`, `src/tests/graph_read_model/contract.test.ts`, and `docs/olfactory_graph_contract.md` only.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-graph-contract-boundary-decisions/55-RESEARCH.md
@.planning/phases/55-graph-contract-boundary-decisions/55-VALIDATION.md
@.planning/phases/55-graph-contract-boundary-decisions/55-PATTERNS.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.agents/rules/graphify.md
@graphify-out/GRAPH_REPORT.md
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source Type | Item | Coverage |
|-------------|------|----------|
| GOAL | Maintainers have a fixed graph schema, ID and boundary contract that safely constrains every v2.11 graph read-model artifact. | Covered by Task 1 static contract, Task 2 executable assertions, and Task 3 maintainer docs. |
| REQ | GCON-01: documented schema version, node kinds, edge kinds, required properties, ID namespace rules and invariants. | Covered by Tasks 1, 2, and 3. |
| REQ | GCON-02: exact allowed read-only input artifacts. | Covered by Tasks 1, 2, and 3. |
| REQ | GCON-03: type-prefixed graph IDs preventing cross-kind collisions. | Covered by Tasks 1, 2, and 3. |
| REQ | GCON-04: sanctioned output path, /tmp verification-only support, and forbidden protected prefixes. | Covered by Tasks 1, 2, and 3. |
| RESEARCH | Phase 55 must remain zero-dependency, static TypeScript, contract-only, detached from Neo4J, Graphify, runtime systems, builder, writer, validator, and CLI scope. | Covered by all task actions and threat model. |
| RESEARCH | Required values: schema_version, node/edge kinds, required properties, allowed inputs, output path, forbidden prefixes, forbidden node prefixes, edge ID format, Phase 56 invariants, and baseline stats. | Covered by Task 1 constants/types and Task 2 assertions. |
| CONTEXT | Operator locked Phase 55 as contract-only and explicitly prohibited builder/read/write/query/validation/runtime integration scope. | Covered by all task actions; no deferred/out-of-scope item is planned. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the static graph contract module for GCON-01 through GCON-04</name>
  <files>src/graph_read_model/contract.ts</files>
  <read_first>
    @src/graph_read_model/contract.ts if it exists; otherwise confirm it is absent before creating it.
    @.planning/phases/55-graph-contract-boundary-decisions/55-RESEARCH.md lines 72-115 and 179-288.
    @.planning/phases/55-graph-contract-boundary-decisions/55-PATTERNS.md lines 21-143 and 328-417.
    @.planning/REQUIREMENTS.md lines 11-17 and 72-90.
    @.planning/STATE.md lines 44-50.
  </read_first>
  <behavior>
    - The module exports static constants/types only; it performs no filesystem, graph-building, graph-loading, graph-writing, graph-validation, query, CLI, Graphify, Neo4J/database, or runtime behavior.
    - GCON-01 values are exact: schema version `olfactory_graph_read_model.v1`; node kinds `family`, `subfamily`, `descriptor`, `alias`; edge kinds `contains_subfamily`, `contains_descriptor`, `resolves_to`, `similar_to`.
    - GCON-02 values are exact: production inputs are only `data/compiled/v2/taxonomy.json`, `data/compiled/v2/descriptor_aliases.json`, and `data/compiled/v2/similarity_matrix.json`.
    - GCON-03 values are exact: ID prefixes are `family:`, `subfamily:`, `descriptor:`, and `alias:`; forbidden graph node prefixes are `material:`, `molecule:`, `pubchem:`, `review_item:`, `score:`, `graphify:`, and `neo4j:`.
    - GCON-04 values are exact: output path is `data/read-models/olfactory-graph/v2.11/`; `/tmp` is verification-only support; forbidden output prefixes are `data/compiled/`, `data/taxonomy/`, `data/inference/`, and `graphify-out/`.
  </behavior>
  <action>Create `src/graph_read_model/contract.ts` as a strict ESM TypeScript module using readonly types, UPPER_SNAKE_CASE exported constants, and `as const` immutable values. Export a single aggregate `OLFACTORY_GRAPH_CONTRACT` plus named constants/types for: `GRAPH_SCHEMA_VERSION`, `GRAPH_NODE_KINDS`, `GRAPH_EDGE_KINDS`, `GRAPH_ID_PREFIXES`, `GRAPH_FORBIDDEN_NODE_PREFIXES`, `GRAPH_EDGE_ID_FORMAT`, `GRAPH_NODE_REQUIRED_PROPERTIES`, `GRAPH_EDGE_REQUIRED_PROPERTIES`, `GRAPH_EDGE_OPTIONAL_PROPERTIES`, `GRAPH_EDGE_ENDPOINT_KINDS`, `GRAPH_ALLOWED_PRODUCTION_INPUTS`, `GRAPH_OUTPUT_POLICY`, `GRAPH_PHASE_56_INVARIANTS`, and `GRAPH_EXPECTED_BASELINE_STATS`. Set `GRAPH_EDGE_ID_FORMAT` to the exact contract string `edge:&lt;edge_kind&gt;:&lt;source_graph_id&gt;-&gt;&lt;target_graph_id&gt;`. Required node properties must be exactly: family = `family_id`, `name`; subfamily = `subfamily_id`, `family_id`, `name`; descriptor = `descriptor_id`, `family_id`, `subfamily_id`, `source`, `frequency`, `status`, `review_required`, `corpus_derived`; alias = `alias`, `target_descriptor_id`. Required edge properties must be exactly: contains_subfamily = `family_id`, `subfamily_id`; contains_descriptor = `subfamily_id`, `descriptor_id`; resolves_to = `alias`, `target_descriptor_id`; similar_to = `source_subfamily_id`, `target_subfamily_id`, `score`, `dimensions`, `evidence`; optional edge properties must include only `similar_to.final_score`. Endpoint kinds must be `family` to `subfamily`, `subfamily` to `descriptor`, `alias` to `descriptor`, and `subfamily` to `subfamily`. Phase 56 invariant names must include duplicate node ID detection, duplicate edge ID detection, missing edge endpoints, wrong endpoint kinds, invalid alias targets, invalid subfamily similarity endpoints, and expected baseline stats: `10` families, `18` subfamilies, `341` descriptors, `18` aliases, and `13` subfamily-similarity edges. Do not import from any file, do not read the allowed inputs, and do not add builder, loader, writer, CLI, generated graph JSON, query proof, validator, boundary audit hashing, Graphify, Neo4J/database, or runtime code.</action>
  <verify>
    <automated>npm --prefix src run typecheck</automated>
    <automated>test ! -e src/graph_read_model/build_graph.ts &amp;&amp; test ! -e src/graph_read_model/validate_graph.ts &amp;&amp; test ! -e src/graph_read_model/write_graph_outputs.ts &amp;&amp; test ! -e src/cli/graph_read_model.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/graph_read_model/contract.ts` exists and exports `OLFACTORY_GRAPH_CONTRACT` plus the named constants/types listed in the action.
    - Source assertion: no `import` statements appear in `src/graph_read_model/contract.ts` because this phase is static contract-only.
    - Behavior assertion: `npm --prefix src run typecheck` exits 0.
    - Scope assertion: no files named `build_graph.ts`, `validate_graph.ts`, `write_graph_outputs.ts`, or `src/cli/graph_read_model.ts` exist.
  </acceptance_criteria>
  <done>Contract constants/types compile and lock all exact GCON-01, GCON-02, GCON-03, and GCON-04 values without implementing any builder or side-effecting graph behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add executable Vitest assertions for contract values and boundaries</name>
  <files>src/tests/graph_read_model/contract.test.ts</files>
  <read_first>
    @src/tests/graph_read_model/contract.test.ts if it exists; otherwise confirm it is absent before creating it.
    @src/graph_read_model/contract.ts.
    @.planning/phases/55-graph-contract-boundary-decisions/55-VALIDATION.md lines 16-45 and 50-72.
    @.planning/phases/55-graph-contract-boundary-decisions/55-PATTERNS.md lines 146-264 and 328-417.
    @.planning/phases/55-graph-contract-boundary-decisions/55-RESEARCH.md lines 418-459.
  </read_first>
  <behavior>
    - Tests assert exact GCON-01 schema, node kind, edge kind, required property, edge endpoint, edge ID format, and Phase 56 invariant values.
    - Tests assert exact GCON-02 production input list and prove there is no fourth input, including no `data/enriched_materials.json`, `data/taxonomy/`, `data/inference/`, or `graphify-out/` production input.
    - Tests assert exact GCON-03 node ID prefixes, known collision examples (`family:floral` vs `descriptor:floral`, `subfamily:amber` vs `descriptor:amber`, and `family:fresh_spice` vs `subfamily:fresh_spice`), and forbidden graph node prefixes.
    - Tests assert exact GCON-04 output policy, `/tmp` verification-only text, and forbidden output prefixes.
  </behavior>
  <action>Create `src/tests/graph_read_model/contract.test.ts` using existing Vitest style with `import { describe, expect, it } from 'vitest'` and an ESM `.js` suffix import from `../../graph_read_model/contract.js`. Add focused test cases for `OLFACTORY_GRAPH_CONTRACT` and named exports. Assert arrays with `toEqual` in exact order. Assert the allowed production inputs equal exactly the three locked paths and have length `3`. Assert the forbidden output prefixes equal exactly `data/compiled/`, `data/taxonomy/`, `data/inference/`, and `graphify-out/`. Assert forbidden graph node prefixes equal exactly `material:`, `molecule:`, `pubchem:`, `review_item:`, `score:`, `graphify:`, and `neo4j:`. Assert the baseline stats are exactly `10`, `18`, `341`, `18`, and `13` for families, subfamilies, descriptors, aliases, and subfamily-similarity edges. Add a source-scope test that reads only `src/graph_read_model/contract.ts` and asserts it does not contain builder/loader/writer/CLI/runtime implementation identifiers such as `buildGraph`, `loadGraph`, `writeGraph`, `validateGraph`, `createReadStream`, `readFile(`, `writeFile(`, `from 'node:fs'`, `from \"node:fs\"`, `graphify-out/GRAPH_REPORT`, `neo4j-driver`, `GraphDatabase`, or `driver.session`. Do not reject the literal `neo4j:` forbidden node prefix, because that locked value is required by the contract. Do not read or traverse compiled JSON artifacts in these tests.</action>
  <verify>
    <automated>npm --prefix src run typecheck &amp;&amp; npm --prefix src test -- tests/graph_read_model/contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: contract tests fail if any locked value for schema version, kinds, required properties, ID prefixes, allowed inputs, output policy, forbidden prefixes, invariants, or baseline stats changes.
    - Behavior assertion: contract tests fail if `data/enriched_materials.json`, `data/taxonomy/`, `data/inference/`, or `graphify-out/` becomes an allowed production input.
    - Behavior assertion: contract tests fail if `src/graph_read_model/contract.ts` drifts into builder, loader, writer, validator, CLI, Graphify, Neo4J/database, or runtime behavior.
    - Command assertion: `npm --prefix src run typecheck &amp;&amp; npm --prefix src test -- tests/graph_read_model/contract.test.ts` exits 0.
  </acceptance_criteria>
  <done>Executable contract tests prove GCON-01 through GCON-04 and block Phase 55 scope creep without touching protected data, compiled, inference, Graphify, or read-model output paths.</done>
</task>

<task type="auto">
  <name>Task 3: Document the maintainer-readable graph contract and Phase 56 handoff</name>
  <files>docs/olfactory_graph_contract.md, src/tests/graph_read_model/contract.test.ts</files>
  <read_first>
    @docs/olfactory_graph_contract.md if it exists; otherwise confirm it is absent before creating it.
    @src/graph_read_model/contract.ts.
    @src/tests/graph_read_model/contract.test.ts.
    @.planning/phases/55-graph-contract-boundary-decisions/55-RESEARCH.md lines 51-115 and 270-319.
    @.planning/phases/55-graph-contract-boundary-decisions/55-PATTERNS.md lines 249-327.
    @.planning/ROADMAP.md lines 98-119 and 133-142.
  </read_first>
  <action>Create `docs/olfactory_graph_contract.md` as a maintainer-readable contract document, preferably in Portuguese-compatible repository documentation style but preserving exact English identifiers. Include sections for: schema version, node contract, edge contract, ID namespace rules, edge ID format, allowed production inputs, sanctioned output path, `/tmp` verification-only policy, forbidden output prefixes, forbidden graph node prefixes, Graphify separation, zero-dependency/static contract scope, explicit no-builder/no-loader/no-writer/no-CLI/no-generated-graph/no-query-proof/no-structural-validator/no-boundary-audit-hashing/no-Neo4J-or-database/no-runtime statement, Phase 56 invariant handoff, expected baseline stats, and zero-mutation statement. Copy the exact values from `src/graph_read_model/contract.ts`: `olfactory_graph_read_model.v1`; node kinds `family`, `subfamily`, `descriptor`, `alias`; edge kinds `contains_subfamily`, `contains_descriptor`, `resolves_to`, `similar_to`; ID prefixes `family:`, `subfamily:`, `descriptor:`, `alias:`; edge ID format `edge:&lt;edge_kind&gt;:&lt;source_graph_id&gt;-&gt;&lt;target_graph_id&gt;`; allowed inputs `data/compiled/v2/taxonomy.json`, `data/compiled/v2/descriptor_aliases.json`, `data/compiled/v2/similarity_matrix.json`; sanctioned output `data/read-models/olfactory-graph/v2.11/`; forbidden output prefixes `data/compiled/`, `data/taxonomy/`, `data/inference/`, `graphify-out/`; forbidden graph node prefixes `material:`, `molecule:`, `pubchem:`, `review_item:`, `score:`, `graphify:`, `neo4j:`; Phase 56 invariants for duplicate node IDs, duplicate edge IDs, missing endpoints, wrong endpoint kinds, invalid alias targets, invalid subfamily similarity endpoints; and baseline stats `10 families`, `18 subfamilies`, `341 descriptors`, `18 aliases`, `13 subfamily-similarity edges`. Extend `src/tests/graph_read_model/contract.test.ts` with a docs-content test that reads only `docs/olfactory_graph_contract.md` and asserts the required sections and exact boundary phrases are present. Do not create data/read-models output and do not edit any protected data, compiled, inference, taxonomy, or Graphify files.</action>
  <verify>
    <automated>npm --prefix src run typecheck &amp;&amp; npm --prefix src test -- tests/graph_read_model/contract.test.ts</automated>
    <automated>npm --prefix src run safety:guard</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `docs/olfactory_graph_contract.md` exists and contains every exact value listed in the task action.
    - Behavior assertion: contract tests fail if the documentation omits schema/version, input, output, `/tmp`, forbidden prefix, Graphify separation, zero-mutation, no-builder/no-writer/no-runtime, Phase 56 invariant, or baseline-stat content.
    - Boundary assertion: `npm --prefix src run safety:guard` exits 0 after the phase files are created.
    - Scope assertion: this task introduces no modifications under `data/compiled/**`, `data/taxonomy/**`, `data/inference/**`, `graphify-out/**`, or `data/read-models/olfactory-graph/v2.11/**`; if `graphify-out/**` was already dirty before execution, leave it untouched and unstaged.
  </acceptance_criteria>
  <done>Maintainers can inspect the docs and tests to understand the fixed contract, protected boundaries, and Phase 56 handoff without any graph builder or writer implementation existing in Phase 55.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer/developer → repository contract | Human edits to static contract files can accidentally weaken graph boundaries. |
| Contract module → downstream Phase 56 builder | Future builder work will consume these static decisions; weak or ambiguous constants create semantic integrity failures. |
| Repository docs/tests → protected artifacts | Contract work must not mutate source taxonomy, compiled artifacts, inference artifacts, Graphify artifacts, or generated read-model output paths. |
| Graphify navigation artifact → v2.11 graph read model | `graphify-out/**` may inform codebase navigation only and must never become a production graph input/output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | `GRAPH_OUTPUT_POLICY` and protected paths | mitigate | Lock `data/read-models/olfactory-graph/v2.11/` as sanctioned output, lock `data/compiled/`, `data/taxonomy/`, `data/inference/`, and `graphify-out/` as forbidden output prefixes, and run `npm --prefix src run safety:guard`. |
| T-55-02 | Tampering / Information Boundary Confusion | Graphify separation | mitigate | Contract and docs state `graphify-out/**` is neither input nor output; tests fail if `graphify-out/` appears as an allowed production input or if Graphify implementation terms appear in the contract module. |
| T-55-03 | Elevation of Privilege / Supply Chain | Dependency and scope creep | mitigate | No new packages, no schema library, no CLI package, no database driver; Phase 55 files are limited to contract module, contract tests, and docs. |
| T-55-04 | Spoofing / Tampering | Raw ID collision semantic spoofing | mitigate | Lock type-prefixed node IDs and collision tests for `family:floral` vs `descriptor:floral`, `subfamily:amber` vs `descriptor:amber`, and `family:fresh_spice` vs `subfamily:fresh_spice`. |
| T-55-05 | Tampering | Builder/writer/validator drift | mitigate | Tests/source assertions reject builder, loader, writer, validator, CLI, query, Graphify, Neo4J/database, runtime, and generated artifact identifiers in Phase 55 contract scope. |
| T-55-SC | Tampering | npm installs | accept | Phase 55 installs no packages; package legitimacy audit says N/A/no new install. |
</threat_model>

<verification>
Run after all tasks:

<automated>npm --prefix src run typecheck &amp;&amp; npm --prefix src test -- tests/graph_read_model/contract.test.ts</automated>
<automated>npm --prefix src run safety:guard</automated>
<automated>git diff --quiet -- data/compiled data/taxonomy data/inference data/read-models/olfactory-graph/v2.11</automated>
<automated>git diff --cached --quiet -- data/compiled data/taxonomy data/inference data/read-models/olfactory-graph/v2.11</automated>
<automated>git diff --cached --quiet -- graphify-out</automated>

Expected: all commands exit 0; the protected data/read-model working-tree diff check fails on any protected path modification, the protected data/read-model staged diff check fails on any staged protected path modification, and the Graphify staged diff check fails on any staged `graphify-out/**` modification. If `graphify-out/**` has pre-existing working-tree dirtiness, do not stage or alter it during Phase 55.
</verification>

<success_criteria>
- GCON-01: `src/graph_read_model/contract.ts` and `docs/olfactory_graph_contract.md` expose the exact schema version, node kinds, edge kinds, required properties, ID namespace rules, edge ID format, and Phase 56 invariants.
- GCON-02: The allowed production input list is exactly the three compiled v2 artifact paths and no other production input.
- GCON-03: Type-prefixed graph IDs and forbidden graph node prefixes are locked and tested.
- GCON-04: The sanctioned output path, `/tmp` verification-only policy, and forbidden output prefixes are locked and tested.
- Scope: Phase 55 creates no builder, loader, writer, graph CLI, generated graph JSON, query proof, structural validator, boundary audit hashing, Neo4J/database/runtime integration, or Graphify integration.
- Boundary: Phase 55 introduces no files under `data/compiled/**`, `data/taxonomy/**`, `data/inference/**`, `graphify-out/**`, or `data/read-models/olfactory-graph/v2.11/**` and stages no `graphify-out/**` changes.
</success_criteria>

<output>
Create `.planning/phases/55-graph-contract-boundary-decisions/55-01-SUMMARY.md` when done.
</output>
