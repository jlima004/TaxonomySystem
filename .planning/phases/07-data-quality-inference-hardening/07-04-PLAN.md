---
phase: 07-data-quality-inference-hardening
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - data/inference/curated_relations.v1.json
  - data/inference/accord_map.v1.json
  - src/inference/build_similarity_graph.ts
  - src/compiler/quality_gates.ts
  - src/compiler/validate_output.ts
  - src/compiler/compile_all.ts
  - src/cli/compile.ts
  - src/cli/parse_args.ts
  - src/package.json
  - src/tests/inference/build_similarity_graph.test.ts
  - src/tests/compiler/quality_gates.test.ts
  - src/tests/compiler/compile_all.test.ts
  - src/tests/cli/compile.test.ts
autonomous: true
requirements: [DQ-05, DQ-06, DQ-07]
must_haves:
  truths:
    - "Minimal curated relation and accord inputs produce positive similarity edges while empty curated inputs remain technically valid with review warnings."
    - "Default compile runs schema validation, deterministic ordering guarantees, and hard semantic gates before all-or-nothing writes."
    - "CLI output includes concise review and quality status without creating extra artifacts by default."
  artifacts:
    - path: "data/inference/curated_relations.v1.json"
      provides: "Locked minimal manual relation bootstrap"
    - path: "data/inference/accord_map.v1.json"
      provides: "Locked minimal manual accord bootstrap"
    - path: "src/compiler/quality_gates.ts"
      provides: "Pure hard/soft semantic quality gates"
      exports: ["runArtifactQualityGates"]
  key_links:
    - from: "data/inference/curated_relations.v1.json"
      to: "src/inference/build_similarity_graph.ts"
      via: "curatedRelations input produces tradition dimensions and edge evidence"
      pattern: "curated_relation"
    - from: "src/compiler/quality_gates.ts"
      to: "src/compiler/compile_all.ts"
      via: "validation combines schema errors and quality gate errors before writes"
      pattern: "runArtifactQualityGates"
---

<objective>
Bootstrap explicit curated graph inputs and enforce artifact quality gates before Phase 7 artifacts are considered reliable upper-layer taxonomy data.

Purpose: Exercise positive similarity graph paths, surface curation gaps, and prevent unsafe generated data acceptance while preserving all-or-nothing writes and the three-artifact boundary.
Output: Curated relation/accord data, graph warning behavior, quality gates, compile quality command surface, and CLI review/quality summary.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md
@.planning/phases/07-data-quality-inference-hardening/07-RESEARCH.md
@.planning/phases/07-data-quality-inference-hardening/07-PATTERNS.md
@.planning/phases/07-data-quality-inference-hardening/07-VALIDATION.md
@.planning/phases/07-data-quality-inference-hardening/07-03-SUMMARY.md
@data/inference/curated_relations.v1.json
@data/inference/accord_map.v1.json
@src/inference/build_similarity_graph.ts
@src/compiler/validate_output.ts
@src/compiler/compile_all.ts
@src/compiler/write_outputs.ts
@src/cli/compile.ts
@src/cli/parse_args.ts
@src/package.json

<interfaces>
Existing graph input types from src/types/inference.ts:
- CuratedTraditionRelation has source_subfamily_id, target_subfamily_id, relation, score, optional evidence.
- CuratedAccordReference has source_subfamily_id, target_subfamily_id, accord, score, optional reference.
Quality gates must return CompilerValidationResult-compatible data and compose with validateAllOutputs before writeCompileResults.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap curated relations and accords with edge tests</name>
  <files>data/inference/curated_relations.v1.json, data/inference/accord_map.v1.json, src/inference/build_similarity_graph.ts, src/tests/inference/build_similarity_graph.test.ts</files>
  <read_first>
    - .planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md lines 54-63 for DQ-D-27 through DQ-D-34.
    - data/inference/curated_relations.v1.json.
    - data/inference/accord_map.v1.json.
    - src/inference/build_similarity_graph.ts.
    - src/types/inference.ts.
    - src/tests/inference/build_similarity_graph.test.ts.
  </read_first>
  <behavior>
    - Test 1: with non-empty curated relations and accord map, buildSimilarityGraph returns edges.length > 0 per DQ-D-30.
    - Test 2: empty curated relations and empty accord map remain ok but emit review items empty_curated_relations and empty_accord_map per DQ-D-29.
    - Test 3: missing accord/tradition data remains undefined/neutral and does not become zero per DQ-D-31.
    - Test 4: corpus/co-occurrence may appear in evidence summaries but does not create tradition or accord compatibility by itself per DQ-D-32.
  </behavior>
  <action>Populate data/inference/curated_relations.v1.json manually with exactly these locked relation records per DQ-D-33, with fixed field names, directionality, ordering, relation name, score, and evidence value `manual_phase_7_bootstrap`:

1) { source_subfamily_id: floral_rose, target_subfamily_id: floral_white, relation: same_family_tradition, score: 0.85, evidence: manual_phase_7_bootstrap }
2) { source_subfamily_id: citrus_fresh, target_subfamily_id: citrus_bitter, relation: same_family_tradition, score: 0.85, evidence: manual_phase_7_bootstrap }
3) { source_subfamily_id: woody_dry, target_subfamily_id: woody_mossy, relation: same_family_tradition, score: 0.85, evidence: manual_phase_7_bootstrap }
4) { source_subfamily_id: citrus_fresh, target_subfamily_id: floral_white, relation: cross_family_tradition_bridge, score: 0.65, evidence: manual_phase_7_bootstrap }
5) { source_subfamily_id: woody_dry, target_subfamily_id: floral_rose, relation: cross_family_tradition_bridge, score: 0.65, evidence: manual_phase_7_bootstrap }
6) { source_subfamily_id: woody_mossy, target_subfamily_id: floral_rose, relation: cross_family_tradition_bridge, score: 0.65, evidence: manual_phase_7_bootstrap }

Populate data/inference/accord_map.v1.json manually with exactly these locked accord records per DQ-D-34, with fixed field names, ordering, accord name, score, and reference value `manual_phase_7_bootstrap`:

1) { source_subfamily_id: citrus_fresh, target_subfamily_id: floral_white, accord: compatible_accord_pair, score: 0.70, reference: manual_phase_7_bootstrap }
2) { source_subfamily_id: citrus_fresh, target_subfamily_id: floral_rose, accord: strong_accord_pair, score: 0.75, reference: manual_phase_7_bootstrap }
3) { source_subfamily_id: woody_dry, target_subfamily_id: floral_rose, accord: strong_accord_pair, score: 0.75, reference: manual_phase_7_bootstrap }
4) { source_subfamily_id: woody_dry, target_subfamily_id: citrus_fresh, accord: compatible_accord_pair, score: 0.70, reference: manual_phase_7_bootstrap }
5) { source_subfamily_id: woody_mossy, target_subfamily_id: floral_rose, accord: compatible_accord_pair, score: 0.70, reference: manual_phase_7_bootstrap }

Do not generate relations or accords automatically by heuristic per DQ-D-28. Update buildSimilarityGraph to emit empty_curated_relations and empty_accord_map review warnings when the corresponding inputs are empty and to preserve valid empty graph behavior per DQ-D-29.</action>
  <acceptance_criteria>
    - Data assertion: curated_relations.v1.json contains six relation records for the exact locked pairs and no heuristic-generated extras.
    - Data assertion: accord_map.v1.json contains five accord records for the exact locked pairs and no heuristic-generated extras.
    - Behavior assertion: non-empty curated inputs produce at least one edge in test fixtures.
    - Behavior assertion: empty curated inputs produce review warnings without failing graph construction.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run typecheck && npm test -- tests/inference/build_similarity_graph.test.ts</automated>
  </verify>
  <done>Curated relation and accord bootstrap data exercises positive graph edges while empty-input cases remain valid and visible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add hard and soft artifact quality gates</name>
  <files>src/compiler/quality_gates.ts, src/compiler/validate_output.ts, src/compiler/compile_all.ts, src/tests/compiler/quality_gates.test.ts, src/tests/compiler/compile_all.test.ts</files>
  <read_first>
    - .planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md lines 72-79 for DQ-D-41 through DQ-D-46.
    - .planning/phases/07-data-quality-inference-hardening/07-PATTERNS.md section for quality_gates and validate_output.
    - src/compiler/validate_output.ts.
    - src/compiler/types.ts.
    - src/compiler/compile_all.ts.
    - src/compiler/write_outputs.ts.
    - src/tests/compiler/quality_gates.test.ts if it exists before editing.
    - src/tests/compiler/compile_all.test.ts.
  </read_first>
  <behavior>
    - Test 1: hard gates fail for hard-exclude terms in taxonomy.json descriptors: at, in, de, hour_s, dipropylene, glycol, substantivity_232, and general_comment_at_100_00_lime.
    - Test 2: hard gates fail for descriptor source/status/review_required/corpus_derived inconsistency, alias candidates in descriptor_aliases.json, final_score outside [0,1], divergent generated_at across artifacts, and descriptor id uniqueness violations per DQ-D-44.
    - Test 3: soft warnings do not set ok false for seed descriptor zero frequency, empty curated inputs, low-support corpus candidates, high-frequency generic candidates, alias merge opportunities, sanitizer rejections, candidates excluded for low score, and edges [] when curated inputs are empty per DQ-D-45.
    - Test 4: non-empty curated relations or accords with zero edges emits at least a high-severity warning and does not hard fail per DQ-D-46.
  </behavior>
  <action>Create src/compiler/quality_gates.ts with runArtifactQualityGates({ taxonomy, aliases, similarity, inputs? }) returning CompilerValidationResult. Compose this result with validateAllOutputs in compileAll before ok is computed and before writeCompileResults can write.

Hard runtime gates in runArtifactQualityGates must fail for: hard-exclude terms in taxonomy.json, descriptor status/source inconsistency, alias candidates in descriptor_aliases.json, final_score outside [0,1], divergent generated_at across taxonomy/aliases/similarity, and descriptor id uniqueness violations. Keep schema/null/undefined validation in existing validators.

Nondeterministic output checks are test/CI checks (including compile:quality checks), not a single-run runtime gate inside runArtifactQualityGates. Soft warnings must populate validation.warnings and not fail compilation per DQ-D-42 and DQ-D-45. Preserve all-or-nothing writes by keeping writeCompileResults unchanged except for consuming result.ok.</action>
  <acceptance_criteria>
    - Behavior assertion: hard quality gate errors make compileAll(...).ok false and writeCompileResults rejects without writing outputs.
    - Behavior assertion: soft warnings are present in validation.warnings while compileAll(...).ok remains true.
    - Source assertion: quality_gates.ts has no filesystem I/O and returns CompilerValidationResult-compatible data.
    - Behavior assertion: two compileAll calls with fixed generatedAt remain byte-stable under JSON.stringify for all three artifacts.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run typecheck && npm test -- tests/compiler/quality_gates.test.ts tests/compiler/compile_all.test.ts</automated>
  </verify>
  <done>Compiler validation includes pure semantic quality gates and still writes nothing when any hard gate fails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add concise CLI review summary and compile quality command</name>
  <files>src/cli/compile.ts, src/cli/parse_args.ts, src/package.json, src/tests/cli/compile.test.ts</files>
  <read_first>
    - .planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md lines 64-79 for DQ-D-35 through DQ-D-46.
    - src/cli/compile.ts.
    - src/cli/parse_args.ts.
    - src/package.json.
    - src/tests/cli/compile.test.ts.
    - src/compiler/compile_all.ts.
    - src/compiler/types.ts.
  </read_first>
  <behavior>
    - Test 1: successful runCompileCli logs total review items, counts by severity, counts by type, validation status, and quality gate status per DQ-D-37.
    - Test 2: soft warnings and review queue items do not return non-zero by default per DQ-D-38 and DQ-D-42.
    - Test 3: npm script compile:quality exists and runs the built CLI in quality-report mode without creating any extra final artifacts by default per DQ-D-43.
    - Test 4: default compile still writes exactly taxonomy.json, descriptor_aliases.json, and similarity_matrix.json under the requested output directory.
  </behavior>
  <action>Extend compile CLI output after successful compile to print a concise review summary with total review items, counts by severity, counts by type, validation status, and quality gate status per DQ-D-37. Add a parse_args option such as --quality-report and an npm script compile:quality that invokes node dist/cli/compile.js --quality-report after precompile/build.

Default `npm run compile` must run schema validation, deterministic ordering guarantees, and hard semantic runtime gates. Byte-for-byte determinism is verified in tests/CI and may be checked by compile:quality.

In quality-report mode, print warning breakdown, candidate counts, rejected noise count, and quality metrics to console only; do not persist a sidecar report or any extra final artifact per DQ-D-43. Preserve default compile behavior where soft warnings and review items do not fail compilation per DQ-D-38 and DQ-D-42.</action>
  <acceptance_criteria>
    - CLI assertion: runCompileCli logs Review summary, severity counts, type counts, validation status, and quality gate status.
    - CLI assertion: runCompileCli returns 0 when only soft warnings/review items are present.
    - Source assertion: src/package.json contains a compile:quality script.
    - Filesystem assertion: default compile and compile:quality create no output files beyond taxonomy.json, descriptor_aliases.json, and similarity_matrix.json unless an explicit output path is already writing those three artifacts.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run typecheck && npm test -- tests/cli/compile.test.ts && npm run build && npm run compile:quality -- --generated-at 2026-01-01T00:00:00.000Z</automated>
  </verify>
  <done>CLI exposes deterministic review/quality status and a console-only quality command while preserving the three-artifact default boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated inference JSON -> graph builder | Manual curated inputs affect graph edge scores and evidence. |
| compiled artifacts -> filesystem writer | Generated taxonomy, aliases, and similarity data cross from pure compiler output to local files. |
| CLI flags -> local paths/output | User-supplied paths determine local input/output locations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering | data/inference/curated_relations.v1.json and accord_map.v1.json | mitigate | Use locked manual bootstrap records only; no heuristic generation; tests require positive edges from non-empty curated inputs. |
| T-07-12 | Tampering | src/compiler/quality_gates.ts | mitigate | Enforce hard gates for invalid generated data before writeCompileResults performs all-or-nothing writes. |
| T-07-13 | Repudiation | generated artifacts | mitigate | Validate generated_at consistency, deterministic ordering, and byte-stable fixed-timestamp outputs in tests. |
| T-07-14 | Denial of Service | CLI output paths | accept | Existing CLI writes only three deterministic files to explicit output directory; no broad deletes or glob writes are introduced. |
| T-07-SC | Tampering | npm installs | mitigate | No package-manager install tasks are present; preserve zero-runtime-dependency implementation. |
</threat_model>

<verification>
Run from src: npm run typecheck && npm test -- tests/inference/build_similarity_graph.test.ts tests/compiler/quality_gates.test.ts tests/compiler/compile_all.test.ts tests/cli/compile.test.ts && npm run build && npm run compile:quality -- --generated-at 2026-01-01T00:00:00.000Z
</verification>

<success_criteria>
- DQ-05 is satisfied by manual curated relation and accord bootstrap plus positive-edge tests.
- DQ-06 is reinforced by review queue warnings for empty curated inputs and CLI review summaries.
- DQ-07 is satisfied by hard semantic gates, soft warnings, deterministic output checks, and all-or-nothing writes.
- No extra final artifacts are created by default.
</success_criteria>

<output>
Create `.planning/phases/07-data-quality-inference-hardening/07-04-SUMMARY.md` when done.
</output>
