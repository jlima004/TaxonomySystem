---
phase: 07-data-quality-inference-hardening
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - src/inference/placement_scoring.ts
  - src/compiler/compile_taxonomy.ts
  - src/compiler/compile_all.ts
  - src/inference/build_similarity_graph.ts
  - src/types/inference.ts
  - src/types/similarity.ts
  - src/tests/inference/placement_scoring.test.ts
  - src/tests/compiler/compile_taxonomy.test.ts
  - src/tests/compiler/compile_all.test.ts
  - src/tests/inference/build_similarity_graph.test.ts
  - src/tests/inference/seed_expansion_review.test.ts
autonomous: true
requirements: [DQ-04, DQ-06, DQ-08]
must_haves:
  truths:
    - "Corpus candidate placement uses support, normalized support, placement_score, and noise penalty thresholds instead of support >= 1."
    - "Failed corpus placements are absent from taxonomy.json and present as deterministic review_queue items in similarity_matrix.json."
    - "Seed expansion signals are review-only suggestions and do not mutate taxonomy-seed.v1.json or create automatic families/subfamilies."
  artifacts:
    - path: "src/inference/placement_scoring.ts"
      provides: "Pure conservative candidate placement scoring"
      exports: ["scoreCandidatePlacement", "DEFAULT_PLACEMENT_THRESHOLDS"]
    - path: "src/compiler/compile_taxonomy.ts"
      provides: "Taxonomy compilation using placement decisions"
    - path: "src/inference/build_similarity_graph.ts"
      provides: "Deterministic similarity review queue merge target"
  key_links:
    - from: "src/compiler/compile_taxonomy.ts"
      to: "src/inference/placement_scoring.ts"
      via: "scoreCandidatePlacement for each inferred descriptor/subfamily candidate"
      pattern: "scoreCandidatePlacement"
    - from: "src/compiler/compile_all.ts"
      to: "src/inference/build_similarity_graph.ts"
      via: "merge profile, placement, sanitation, alias, and seed-gap review items into similarity.review_queue only"
      pattern: "review_queue"
---

<objective>
Replace permissive candidate placement with conservative scoring and populate the final review queue with actionable deterministic data-quality items.

Purpose: Improve inference confidence by keeping weak/noisy candidates out of taxonomy.json while preserving evidence for manual curation in similarity_matrix.json.review_queue.
Output: Placement scorer, compile integration, deterministic review queue merge, and review-only seed expansion suggestions.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md
@.planning/phases/07-data-quality-inference-hardening/07-RESEARCH.md
@.planning/phases/07-data-quality-inference-hardening/07-PATTERNS.md
@.planning/phases/07-data-quality-inference-hardening/07-VALIDATION.md
@.planning/phases/07-data-quality-inference-hardening/07-01-SUMMARY.md
@.planning/phases/07-data-quality-inference-hardening/07-02-SUMMARY.md
@src/compiler/compile_taxonomy.ts
@src/compiler/compile_all.ts
@src/inference/build_similarity_graph.ts
@src/inference/seed_profile.ts
@src/types/inference.ts
@src/types/similarity.ts

<interfaces>
Existing review item shape from src/types/inference.ts:
- ReviewQueueItem has type, severity, affected, evidence, suggested_action, optional confidence/source/reason.
Placement decisions must use these locked values:
- support threshold >= 3.
- normalized_support threshold >= 0.05.
- placement_score threshold >= 0.35.
- placement_score = 0.60 * min(1, support / 10) + 0.30 * min(1, normalized_support / 0.20) - 0.10 * noise_penalty.
- noise_penalty = 0 for non-noisy descriptors and 1 - downweight_value for downweighted descriptors.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement conservative placement scoring as pure inference logic</name>
  <files>src/inference/placement_scoring.ts, src/types/inference.ts, src/tests/inference/placement_scoring.test.ts</files>
  <read_first>
    - .planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md lines 43-53 for DQ-D-18 through DQ-D-26.
    - .planning/phases/07-data-quality-inference-hardening/07-PATTERNS.md section for placement_scoring.
    - src/compiler/compile_taxonomy.ts.
    - src/types/inference.ts.
    - src/inference/noise.ts.
    - src/inference/placement_scoring.ts if it exists before editing.
    - src/tests/inference/placement_scoring.test.ts if it exists before editing.
  </read_first>
  <behavior>
    - Test 1: support 3, candidate frequency 30, downweight value 1 produces normalized_support 0.10 and placement_score 0.33, so pass is false because placement_score < 0.35.
    - Test 2: support 4, candidate frequency 20, downweight value 1 produces normalized_support 0.20 and placement_score 0.54, so pass is true.
    - Test 3: hardExcluded true always returns pass false with reason hard_excluded_descriptor_blocked per DQ-D-19.
    - Test 4: downweighted descriptor with downweight_value 0.25 applies noise_penalty 0.75 and reduces placement_score by 0.075 per DQ-D-23 and DQ-D-24.
  </behavior>
  <action>Create src/inference/placement_scoring.ts with exported DEFAULT_PLACEMENT_THRESHOLDS, CandidatePlacementEvidence, CandidatePlacementDecision, and scoreCandidatePlacement(candidate, subfamily, evidence, options). Implement support as supplied evidence support, normalized_support = support / frequency(candidate), and the locked placement_score formula per DQ-D-20 through DQ-D-23. Include pass/fail reasons for support_below_threshold, normalized_support_below_threshold, placement_score_below_threshold, and hard_excluded_descriptor_blocked. Keep this module pure with no I/O and no runtime dependencies. Add or extend types in src/types/inference.ts only when shared by compiler/review queue consumers.</action>
  <acceptance_criteria>
    - Behavior assertion: locked thresholds and formula produce the expected pass/fail decisions from the tests.
    - Behavior assertion: hard-excluded descriptors are blocked regardless of support.
    - Behavior assertion: semantic noise penalizes score but does not remove valid descriptors automatically.
    - Source assertion: placement_scoring.ts exports scoreCandidatePlacement and DEFAULT_PLACEMENT_THRESHOLDS.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run typecheck && npm test -- tests/inference/placement_scoring.test.ts</automated>
  </verify>
  <done>Candidate placement scoring is available as an inference-owned pure function with locked Phase 7 thresholds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate placement decisions and review queue items</name>
  <files>src/compiler/compile_taxonomy.ts, src/compiler/compile_all.ts, src/inference/build_similarity_graph.ts, src/tests/compiler/compile_taxonomy.test.ts, src/tests/compiler/compile_all.test.ts, src/tests/inference/build_similarity_graph.test.ts</files>
  <read_first>
    - .planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md lines 43-70 for DQ-D-18 through DQ-D-40.
    - src/compiler/compile_taxonomy.ts.
    - src/compiler/compile_all.ts.
    - src/inference/build_similarity_graph.ts.
    - src/inference/placement_scoring.ts.
    - src/inference/seed_profile.ts.
    - src/types/inference.ts.
    - src/types/similarity.ts.
    - src/tests/compiler/compile_taxonomy.test.ts.
    - src/tests/compiler/compile_all.test.ts.
    - src/tests/inference/build_similarity_graph.test.ts.
  </read_first>
  <behavior>
    - Test 1: compileTaxonomy excludes a corpus candidate with support 1 and produces a corpus_candidate_low_support review item with candidate descriptor, support, normalized_support, placement_score, thresholds, and suggested_action review_candidate_placement.
    - Test 2: compileTaxonomy includes a corpus candidate that meets all thresholds and emits descriptor fields source corpus, status candidate, review_required true, corpus_derived true.
    - Test 3: compileAll puts placement review items into result.similarity.review_queue and does not add review_queue to taxonomy.
    - Test 4: merged review_queue is sorted deterministically by type, severity, affected entity, and stable evidence JSON.
  </behavior>
  <action>Replace compile_taxonomy.ts choosePlacement support >= minCooccurrenceSupport default 1 with scoreCandidatePlacement per DQ-D-18 through DQ-D-26. Preserve descriptor flags in taxonomy.json exactly as source corpus, status candidate, review_required true, corpus_derived true per DQ-D-36. Route failed placement decisions into ReviewQueueItem objects of type corpus_candidate_low_support with severity medium by default, affected.descriptor, affected.subfamily when a target was evaluated, evidence containing support, normalized_support, placement_score, thresholds, candidate_frequency, noise_penalty, and suggested_action review_candidate_placement per DQ-D-25 and DQ-D-40. Update compileAll or a compiler-level helper to merge profile review items, sanitizer/alias audit-derived review items if present, placement review items, and graph review items into similarity.review_queue only per DQ-D-35. Add one shared deterministic review item comparator and use it consistently per DQ-D-39.

Explicitly enumerate and assign creation responsibility for all 9 review item types from DQ-D-40:

1. **seed_descriptor_zero_frequency** (type: seed_descriptor_zero_frequency, severity: medium) — Created by existing buildSeedCorpusProfiles in seed_profile.ts; verify it survives the alias-aware refactoring and is merged by compileAll.
2. **hard_excluded_descriptor_detected** (type: hard_excluded_descriptor_detected, severity: high) — Created in 07-01 Task 2: when sanitizeDescriptor returns keep: false with reason hard_exclude, emit a review item with affected.descriptor, evidence.raw, evidence.normalized, evidence.matched_rule, evidence.material_id, evidence.source, and suggested_action confirm_sanitizer_rule.
3. **corpus_candidate_low_support** (type: corpus_candidate_low_support, severity: medium) — Created in this task (07-03 Task 2): when scoreCandidatePlacement returns pass false due to support/normalized_support/placement_score below thresholds, emit with full placement evidence.
4. **corpus_candidate_high_frequency_generic** (type: corpus_candidate_high_frequency_generic, severity: low) — Created in this task (07-03 Task 2): when a corpus candidate has high frequency but low placement score and matches a generic olfactive descriptor (e.g., sweet, fresh, green, floral, fruity, woody, spicy, herbal, natural, clean), emit with evidence.frequency, evidence.placement_score, evidence.noise_downweight_value, and suggested_action review_generic_descriptor_placement.
5. **empty_curated_relations** (type: empty_curated_relations, severity: low) — Created by buildSimilarityGraph in 07-04 Task 1; verify it is merged by compileAll.
6. **empty_accord_map** (type: empty_accord_map, severity: low) — Created by buildSimilarityGraph in 07-04 Task 1; verify it is merged by compileAll.
7. **alias_frequency_merge_opportunity** (type: alias_frequency_merge_opportunity, severity: medium) — Created in 07-02 Task 2: when two distinct canonical descriptors have high individual frequencies but could plausibly be a single concept (detected by co-occurrence patterns or semantic noise overlap), emit as review-only per DQ-D-15. Merge into compileAll review queue.
8. **suspicious_descriptor_from_ingestion** (type: suspicious_descriptor_from_ingestion, severity: medium) — Created in 07-01 Task 2: when a descriptor passes sanitizer but matches a pattern that suggests ingestion artifact (e.g., contains underscores with numeric suffixes not matching pattern_exclude, or contains known technical substrings like dipropylene/glycol as part of a longer token), emit with evidence.descriptor, evidence.material_id, evidence.pattern_hint, and suggested_action review_ingestion_source.
9. **technical_token_in_descriptor_field** (type: technical_token_in_descriptor_field, severity: high) — Created in 07-01 Task 2: when a descriptor that passed sanitizer still contains a technical token substring (e.g., a hard-exclude term embedded in a longer descriptor like "at_room_temperature"), emit with evidence.descriptor, evidence.embedded_token, evidence.matched_rule, and suggested_action review_descriptor_origin.

All 9 types must be merged deterministically into similarity.review_queue by compileAll using the shared comparator. No other final artifact may contain a review_queue per DQ-D-35.</action>
  <acceptance_criteria>
    - Behavior assertion: weak support candidates are absent from taxonomy descriptors and present in similarity.review_queue.
    - Behavior assertion: taxonomy output has no top-level or nested review_queue property.
    - Behavior assertion: review_queue order is byte-stable across two compileAll calls with fixed generatedAt.
    - Source assertion: compile_taxonomy no longer uses default minCooccurrenceSupport 1 as the placement acceptance rule.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run typecheck && npm test -- tests/compiler/compile_taxonomy.test.ts tests/compiler/compile_all.test.ts tests/inference/build_similarity_graph.test.ts</automated>
  </verify>
  <done>Conservative placement controls taxonomy inclusion and failed placement evidence is visible only through similarity_matrix.json.review_queue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add review-only seed gap suggestions without seed mutation</name>
  <files>src/compiler/compile_all.ts, src/types/inference.ts, src/tests/inference/seed_expansion_review.test.ts, src/tests/compiler/compile_all.test.ts</files>
  <read_first>
    - .planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md lines 80-85 for DQ-D-47 through DQ-D-51.
    - .planning/REQUIREMENTS.md DQ-08.
    - src/compiler/compile_all.ts.
    - src/types/inference.ts.
    - src/tests/compiler/compile_all.test.ts.
    - src/tests/inference/seed_expansion_review.test.ts if it exists before editing.
    - data/taxonomy/taxonomy-seed.v1.json.
  </read_first>
  <behavior>
    - Test 1: high-frequency generic corpus descriptors that fail placement can produce review-only seed gap items with type corpus_candidate_high_frequency_generic or seed_taxonomy_gap_suggestion and severity low/medium.
    - Test 2: compileAll output seed-derived family and subfamily counts remain exactly the input seed counts when gap suggestions are emitted.
    - Test 3: no taxonomy-seed.v2.json file is created and taxonomy-seed.v1.json is not modified by compileAll.
  </behavior>
  <action>Add a pure review producer for seed taxonomy expansion evaluation per DQ-D-47 through DQ-D-51. Emit review-only gap suggestions into similarity.review_queue when alias-canonicalized corpus evidence indicates high-frequency descriptors repeatedly fail placement or indicate under-covered semantic areas, using affected.descriptor, evidence.frequency, evidence.reason, and suggested_action review_seed_taxonomy_gap. Do not add families, subfamilies, or descriptors to the seed; do not create or modify taxonomy-seed.v2.json; do not implement automatic corpus-derived family or subfamily creation, which is explicitly deferred.</action>
  <acceptance_criteria>
    - Behavior assertion: seed gap suggestions appear only in similarity.review_queue.
    - Behavior assertion: compiled taxonomy family_count and subfamily_count are derived from the input seed and do not increase due to suggestions.
    - Filesystem assertion: no data/taxonomy/taxonomy-seed.v2.json is created by this plan.
    - Source assertion: no code path creates automatic families or subfamilies from corpus descriptors.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run typecheck && npm test -- tests/inference/seed_expansion_review.test.ts tests/compiler/compile_all.test.ts</automated>
  </verify>
  <done>Seed expansion is evaluated through deterministic review-only signals without mutating curated seed truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analysis maps -> taxonomy compiler | Corpus-derived evidence can become candidate descriptors if placement accepts it. |
| review producers -> final similarity artifact | Multiple producers contribute curation evidence to one final review queue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | src/inference/placement_scoring.ts | mitigate | Enforce locked support, normalized_support, placement_score, and hard-exclude checks before taxonomy inclusion. |
| T-07-08 | Information Disclosure | similarity_matrix.json.review_queue | accept | Review items contain descriptor evidence from local corpus artifacts only and no secrets/env vars are in phase scope. |
| T-07-09 | Repudiation | review queue merge | mitigate | Use one deterministic comparator over type, severity, affected entity, and stable evidence JSON. |
| T-07-10 | Elevation of Privilege | seed expansion review | mitigate | Emit seed gap suggestions as review-only items; never mutate seed hierarchy or create automatic families/subfamilies. |
| T-07-SC | Tampering | npm installs | mitigate | No package-manager install tasks are present; preserve zero-runtime-dependency implementation. |
</threat_model>

<verification>
Run from src: npm run typecheck && npm test -- tests/inference/placement_scoring.test.ts tests/compiler/compile_taxonomy.test.ts tests/compiler/compile_all.test.ts tests/inference/build_similarity_graph.test.ts tests/inference/seed_expansion_review.test.ts
</verification>

<success_criteria>
- DQ-04 is satisfied by conservative pure candidate placement scoring and taxonomy inclusion only when all thresholds pass.
- DQ-06 is satisfied by deterministic review queue population in similarity_matrix.json only.
- DQ-08 is satisfied by review-only seed expansion suggestions without curated seed mutation.
</success_criteria>

<output>
Create `.planning/phases/07-data-quality-inference-hardening/07-03-SUMMARY.md` when done.
</output>
