---
phase: 07-data-quality-inference-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analyzer/descriptor_sanitizer.ts
  - src/analyzer/analyze_corpus.ts
  - src/analyzer/cooccurrence.ts
  - src/analyzer/frequency.ts
  - src/inference/noise.ts
  - data/inference/semantic_noise.v1.json
  - src/tests/analysis/descriptor_sanitizer.test.ts
  - src/tests/analysis/orchestration.test.ts
  - src/tests/inference/noise.test.ts
  - src/types/analysis.ts
autonomous: true
requirements: [DQ-01, DQ-02]
must_haves:
  truths:
    - "Hard-excluded technical descriptors do not enter frequency, co-occurrence, alias candidate pools, seed/corpus profiles, or placement evidence."
    - "Generic valid olfactive descriptors remain present for downweighting instead of sanitation removal."
    - "Semantic noise config accepts categorized v2 data and legacy v1 flat data with deterministic weights."
  artifacts:
    - path: "src/analyzer/descriptor_sanitizer.ts"
      provides: "Pure descriptor sanitation and audit result union"
      exports: ["sanitizeDescriptor", "DEFAULT_DESCRIPTOR_SANITIZER_RULES"]
    - path: "src/inference/noise.ts"
      provides: "Semantic noise v1/v2 normalization and scoring"
      exports: ["normalizeSemanticNoiseConfig", "scoreSemanticNoise"]
    - path: "data/inference/semantic_noise.v1.json"
      provides: "Categorized semantic noise config at the existing CLI default path"
  key_links:
    - from: "src/analyzer/analyze_corpus.ts"
      to: "src/analyzer/descriptor_sanitizer.ts"
      via: "analyzeCorpus options passed into computeFrequencyAndCoOccurrence"
      pattern: "sanitizeDescriptor"
    - from: "src/inference/noise.ts"
      to: "data/inference/semantic_noise.v1.json"
      via: "normalizeSemanticNoiseConfig accepts hard_exclude, pattern_exclude, downweight, default_downweight"
      pattern: "hard_exclude|pattern_exclude|downweight"
---

<objective>
Create the pre-analysis sanitation boundary and categorized semantic-noise compatibility layer required before later alias, placement, review, and quality-gate work.

Purpose: Prevent obvious technical/noisy descriptor data from contaminating analysis maps while preserving valid generic olfactive descriptors for semantic downweighting.
Output: Pure sanitizer module, analyzer integration, v2-compatible semantic noise scoring, tests, and updated default noise data.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md
@.planning/phases/07-data-quality-inference-hardening/07-RESEARCH.md
@.planning/phases/07-data-quality-inference-hardening/07-PATTERNS.md
@.planning/phases/07-data-quality-inference-hardening/07-VALIDATION.md
@src/normalizer/normalize_descriptor.ts
@src/analyzer/analyze_corpus.ts
@src/analyzer/cooccurrence.ts
@src/analyzer/frequency.ts
@src/inference/noise.ts
@src/types/analysis.ts
@src/types/inference.ts

<interfaces>
Existing contracts to preserve and extend:
- analyzeCorpus(corpus: readonly AnalysisMaterial[], options?: AnalyzeCorpusOptions): CorpusAnalysis in src/analyzer/analyze_corpus.ts.
- computeFrequencyAndCoOccurrence(corpus: readonly AnalysisMaterial[]): { frequency: FrequencyMap; cooccurrence: CoOccurrenceMap } in src/analyzer/cooccurrence.ts.
- scoreSemanticNoise(descriptor: string, options?: SemanticNoiseOptions): NoiseDecision in src/inference/noise.ts.
- CorpusAnalysis currently has frequency, cooccurrence, aliasCandidates; extend with sanitation audit signals (not final review_queue items) and keep existing callers source-compatible where possible.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure descriptor sanitizer and pre-analysis tests</name>
  <files>src/analyzer/descriptor_sanitizer.ts, src/tests/analysis/descriptor_sanitizer.test.ts</files>
  <read_first>
    - .planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md lines 20-27 for DQ-D-01 through DQ-D-06.
    - .planning/phases/07-data-quality-inference-hardening/07-PATTERNS.md sections for src/analyzer/descriptor_sanitizer.ts.
    - src/inference/noise.ts for pure transform and audit shape patterns.
    - src/normalizer/normalize_descriptor.ts for canonical descriptor normalization contract.
    - src/analyzer/descriptor_sanitizer.ts if it exists before editing.
    - src/tests/analysis/descriptor_sanitizer.test.ts if it exists before editing.
  </read_first>
  <behavior>
    - Test 1: sanitizeDescriptor({ raw: 'at', normalized: 'at', material_id: 'm1', source: 'olfactory.descriptors' }) returns keep false with audit.raw 'at', audit.normalized 'at', audit.reason 'hard_exclude', audit.matched_rule 'at', audit.material_id 'm1', audit.source 'olfactory.descriptors'.
    - Test 2: normalized values 'substantivity_232', 'general_comment_at_100_00_lime', 'odor_strength_5', and 'recommend_smelling_strip' return keep false with reason 'pattern_exclude' and matched_rule equal to the pattern string that matched per DQ-D-04.
    - Test 3: normalized values 'fruity', 'green', 'floral', 'sweet', 'herbal', 'spicy', 'fresh', 'woody', 'balsamic', 'fatty', and 'waxy' return keep true per DQ-D-02.
    - Test 4: the exported sanitizer performs no file, process, Date, random, or package I/O and returns deterministic results for identical inputs.
  </behavior>
  <action>Create src/analyzer/descriptor_sanitizer.ts with readonly exported types DescriptorSanitizerInput, DescriptorSanitizerAuditEntry, DescriptorSanitizerRules, DescriptorSanitizerResult, DEFAULT_DESCRIPTOR_SANITIZER_RULES, and sanitizeDescriptor(input, rules = DEFAULT_DESCRIPTOR_SANITIZER_RULES). Implement locked hard_exclude terms exactly as at, in, de, hour_s, dipropylene, glycol per DQ-D-03. Implement locked pattern_exclude rules exactly as ^substantivity_\d+$, ^general_comment_, ^odor_strength_, ^recommend_smelling_, and .*_at_\d+_\d+.* per DQ-D-04. Return keep true with descriptor for retained descriptors and keep false with deterministic audit entries containing raw, normalized, reason, matched_rule, material_id when provided, and source when provided per DQ-D-05. Keep the module pure with no I/O and no runtime dependencies per DQ-D-06. Do not hard-exclude valid generic olfactive descriptors per DQ-D-02.</action>
  <acceptance_criteria>
    - Source assertion: src/analyzer/descriptor_sanitizer.ts exports sanitizeDescriptor and DEFAULT_DESCRIPTOR_SANITIZER_RULES.
    - Behavior assertion: hard-exclude and pattern-exclude descriptors listed in DQ-D-03 and DQ-D-04 are rejected with audit entries containing raw, normalized, reason, matched_rule, and optional material_id/source.
    - Behavior assertion: generic valid olfactive descriptors listed in this task are retained.
    - Source assertion: src/analyzer/descriptor_sanitizer.ts contains no imports from node:fs, node:path, process, Date, Math.random, or external packages.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm test -- tests/analysis/descriptor_sanitizer.test.ts</automated>
  </verify>
  <done>Descriptor sanitizer exists as a pure audited transform and targeted sanitizer tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire sanitation before frequency and co-occurrence</name>
  <files>src/analyzer/analyze_corpus.ts, src/analyzer/cooccurrence.ts, src/analyzer/frequency.ts, src/tests/analysis/orchestration.test.ts</files>
  <read_first>
    - .planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md lines 20-27 and 95-135.
    - .planning/phases/07-data-quality-inference-hardening/07-PATTERNS.md sections for analyze_corpus/cooccurrence/frequency.
    - src/analyzer/analyze_corpus.ts.
    - src/analyzer/cooccurrence.ts.
    - src/analyzer/frequency.ts.
    - src/types/analysis.ts.
    - src/tests/analysis/cooccurrence.test.ts if present for existing fixture style.
    - src/tests/analysis/orchestration.test.ts if it exists before editing.
  </read_first>
  <behavior>
    - Test 1: analyzeCorpus over materials containing ['Lemon', 'at', 'Substantivity 232'] counts lemon but not at and not substantivity_232 in frequency.
    - Test 2: co-occurrence pairs are computed only from retained descriptors; a material containing lemon, bergamot, and at produces lemon|bergamot only, not at|lemon or at|bergamot.
    - Test 3: sanitizer audit entries preserve raw descriptors and material id/source when input material has id and descriptors came from olfactory.descriptors.
    - Test 4: computeDescriptorFrequency and computeFrequencyAndCoOccurrence produce aligned frequency counts for sanitized inputs.
  </behavior>
  <action>Refactor the analysis boundary so descriptor preprocessing is normalizeDescriptor(raw) then sanitizeDescriptor({ raw, normalized, material_id, source }) before any map update per DQ-D-01 and DQ-D-05. Extend the local AnalysisMaterial shape in analyzer modules to accept optional readonly id and preserve source as the literal string olfactory.descriptors. Keep computeFrequencyAndCoOccurrence deterministic by deduping sanitized descriptors per material with Set and sorting lexically before map updates. Keep computeDescriptorFrequency aligned with the same sanitation behavior instead of maintaining a divergent normalization-only path.

Return analysis-side sanitation signals from analyzeCorpus (for example sanitationAuditEntries and optional sanitationSignalHints) so downstream compiler/review modules can transform these signals into final ReviewQueueItem entries. Do not emit final similarity review_queue items from analyzer code in this plan. If technical-token or ingestion-suspicion heuristics are added here, emit them as audit/signal payloads only, not final ReviewQueueItem objects.</action>
  <acceptance_criteria>
    - Behavior assertion: hard-excluded terms never appear as keys in analysis.frequency or pair components in analysis.cooccurrence.
    - Behavior assertion: generic descriptors remain available in analysis.frequency.
    - Source assertion: cooccurrence and frequency paths use the sanitizer or a shared sanitized descriptor helper, not direct normalizeDescriptor-only insertion.
    - Compatibility assertion: existing callers that read analysis.frequency, analysis.cooccurrence, and analysis.aliasCandidates still compile.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run typecheck && npm test -- tests/analysis/orchestration.test.ts tests/analysis/descriptor_sanitizer.test.ts</automated>
  </verify>
  <done>Corpus analysis computes frequency and co-occurrence from sanitized descriptors before any downstream statistics are built.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add semantic noise v2 compatibility at existing default path</name>
  <files>src/inference/noise.ts, data/inference/semantic_noise.v1.json, src/tests/inference/noise.test.ts</files>
  <read_first>
    - .planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md lines 28-35 and 178-203.
    - .planning/phases/07-data-quality-inference-hardening/07-RESEARCH.md Semantic Noise Schema v2 and Open Questions sections.
    - src/inference/noise.ts.
    - src/tests/inference/noise.test.ts.
    - data/inference/semantic_noise.v1.json.
  </read_first>
  <behavior>
    - Test 1: normalizeSemanticNoiseConfig({ version: '1.0.0', noise_descriptors: ['note'], downweight_value: 0.35 }) returns downweight note = 0.35, default_downweight = 0.35, hard_exclude = [], pattern_exclude = [].
    - Test 2: normalizeSemanticNoiseConfig over categorized input with default_downweight 0.35 and downweight { sweet: 0.25 } returns sweet weight 0.25 and generic legacy-compatible defaults.
    - Test 3: scoreSemanticNoise('sweet', { normalizedConfig, seedDescriptors: [] }) returns weight 0.25, downweighted true, and reason curated_semantic_noise_downweight.
    - Test 4: scoreSemanticNoise('sweet', { normalizedConfig, seedDescriptors: ['sweet'] }) returns weight 1, seed_exception true, and a review-capable evidence object per DQ-D-09.
  </behavior>
  <action>Extend src/inference/noise.ts with exported SemanticNoiseInput and NormalizedSemanticNoiseConfig types plus normalizeSemanticNoiseConfig(input). Accept both legacy v1 fields noise_descriptors/downweight_value and v2 fields hard_exclude, pattern_exclude, downweight, default_downweight per DQ-D-07 and DQ-D-08. Preserve scoreSemanticNoise compatibility by allowing existing SemanticNoiseOptions and adding an option such as normalizedConfig; seed descriptors must keep weight 1 and emit seed_exception evidence per DQ-D-09. Keep corpus suggestions review-only and auto_applied false per DQ-D-10. Update data/inference/semantic_noise.v1.json at the existing CLI default path to categorized content with version 2.0.0, default_downweight 0.35, hard_exclude [at, in, de, hour_s, dipropylene, glycol], pattern_exclude [^substantivity_\\d+$, ^general_comment_, ^odor_strength_, ^recommend_smelling_, .*_at_\\d+_\\d+.*], and downweight entries note, nuance, effect, type, quality at 0.35 plus sweet, fresh, green, floral, fruity, woody, spicy, herbal, natural, clean at 0.25 per DQ-D-07 through DQ-D-12.</action>
  <acceptance_criteria>
    - Source assertion: semantic_noise.v1.json remains the CLI default filename and contains version 2.0.0 with hard_exclude, pattern_exclude, downweight, and default_downweight.
    - Behavior assertion: legacy v1 flat noise config still scores note as downweighted at the supplied default value.
    - Behavior assertion: seed descriptors matching downweight rules keep weight 1 and emit seed_exception true.
    - Behavior assertion: corpus-derived noise suggestions remain review_only true and auto_applied false.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run typecheck && npm test -- tests/inference/noise.test.ts</automated>
  </verify>
  <done>Semantic noise scoring supports categorized v2 and legacy v1 input without changing the existing default file path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus JSON -> analyzer | Untrusted descriptor strings become analysis maps used by all downstream artifacts. |
| inference JSON -> noise scoring | Local config controls downweighting and hard/pattern exclusion semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | src/analyzer/descriptor_sanitizer.ts | mitigate | Reject locked hard/pattern technical descriptors before frequency/co-occurrence and preserve audit evidence with raw, normalized, matched_rule, material_id, and source. |
| T-07-02 | Tampering | src/inference/noise.ts | mitigate | Normalize semantic noise config into a typed shape, preserve v1 compatibility, protect seed descriptors from automatic downweighting, and keep corpus suggestions review-only. |
| T-07-03 | Repudiation | analyzer audit side-channel | mitigate | Sort sanitized descriptor sets and audit outputs deterministically so repeated runs with fixed inputs produce identical maps and review evidence. |
| T-07-SC | Tampering | npm installs | mitigate | No package-manager install tasks are present; preserve zero-runtime-dependency implementation. |
</threat_model>

<verification>
Run from src: npm run typecheck && npm test -- tests/analysis/descriptor_sanitizer.test.ts tests/analysis/orchestration.test.ts tests/inference/noise.test.ts
</verification>

<success_criteria>
- DQ-01 is satisfied by pre-analysis sanitation before map updates.
- DQ-02 is satisfied by categorized semantic noise v2 with v1 compatibility and seed exception behavior.
- No runtime dependencies are added.
- Hard-excluded terms listed in DQ-D-03 and DQ-D-04 are absent from analysis maps while generic olfactive descriptors remain present.
</success_criteria>

<output>
Create `.planning/phases/07-data-quality-inference-hardening/07-01-SUMMARY.md` when done.
</output>
