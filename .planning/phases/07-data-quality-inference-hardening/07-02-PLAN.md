---
phase: 07-data-quality-inference-hardening
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/analyzer/alias_canonicalization.ts
  - src/analyzer/analyze_corpus.ts
  - src/analyzer/cooccurrence.ts
  - src/analyzer/alias_candidates.ts
  - src/cli/compile.ts
  - src/compiler/compile_all.ts
  - src/tests/analysis/alias_canonicalization.test.ts
  - src/tests/analysis/orchestration.test.ts
  - src/tests/cli/compile.test.ts
  - src/types/analysis.ts
autonomous: true
requirements: [DQ-03]
must_haves:
  truths:
    - "Curated descriptor aliases are applied before frequency and co-occurrence statistics."
    - "Alias seed keys and values are normalized before lookup so raw aliases like orange flower canonicalize to orange_blossom."
    - "Alias candidates remain weak evidence and never become authoritative canonicalization input."
  artifacts:
    - path: "src/analyzer/alias_canonicalization.ts"
      provides: "Pure curated alias canonicalization with audit entries"
      exports: ["canonicalizeDescriptor", "buildCuratedAliasMap"]
    - path: "src/analyzer/analyze_corpus.ts"
      provides: "Analysis options that accept curated alias seed input"
    - path: "src/cli/compile.ts"
      provides: "CLI wiring that passes loaded descriptor_aliases.seed.json into analysis"
  key_links:
    - from: "src/cli/compile.ts"
      to: "src/analyzer/analyze_corpus.ts"
      via: "analyzeCorpus(corpus, { curatedAliases: aliasSeed })"
      pattern: "curatedAliases"
    - from: "src/analyzer/cooccurrence.ts"
      to: "src/analyzer/alias_canonicalization.ts"
      via: "canonicalizeDescriptor after sanitizeDescriptor and before Set insertion"
      pattern: "canonicalizeDescriptor"
---

<objective>
Make corpus analysis curated-alias-aware before statistics are computed.

Purpose: Aggregate evidence under canonical descriptor IDs while preserving the rule that Phase 4 alias candidates are weak evidence only.
Output: Curated alias canonicalizer, analyzer/CLI integration, deterministic alias audit, and tests for pre-analysis canonical statistics.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md
@.planning/phases/07-data-quality-inference-hardening/07-RESEARCH.md
@.planning/phases/07-data-quality-inference-hardening/07-PATTERNS.md
@.planning/phases/07-data-quality-inference-hardening/07-VALIDATION.md
@src/analyzer/analyze_corpus.ts
@src/analyzer/cooccurrence.ts
@src/analyzer/alias_candidates.ts
@src/analyzer/descriptor_sanitizer.ts
@src/cli/compile.ts
@src/compiler/compile_all.ts
@src/types/alias.ts
@src/types/analysis.ts
@data/taxonomy/descriptor_aliases.seed.json

<interfaces>
Use contracts created in 07-01:
- sanitizeDescriptor(input) returns keep true/false and must run before canonicalization.
Existing alias seed contract:
- DescriptorAliasSeed is a Record-like mapping loaded by loadAliasSeed and compiled by compileAliases.
Required pipeline:
- normalizeDescriptor(raw) -> sanitizeDescriptor({ raw, normalized, material_id, source }) -> canonicalizeDescriptor(normalized, aliasSeed) -> frequency/co-occurrence/alias pool/seed profiles.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add curated alias canonicalizer with audit entries</name>
  <files>src/analyzer/alias_canonicalization.ts, src/tests/analysis/alias_canonicalization.test.ts</files>
  <read_first>
    - .planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md lines 36-42 for DQ-D-13 through DQ-D-17.
    - .planning/phases/07-data-quality-inference-hardening/07-PATTERNS.md section for src/analyzer/alias_canonicalization.ts.
    - src/analyzer/alias_candidates.ts.
    - src/normalizer/normalize_descriptor.ts.
    - src/types/alias.ts.
    - data/taxonomy/descriptor_aliases.seed.json.
    - src/analyzer/alias_canonicalization.ts if it exists before editing.
    - src/tests/analysis/alias_canonicalization.test.ts if it exists before editing.
  </read_first>
  <behavior>
    - Test 1: canonicalizeDescriptor('orange_flower', { 'orange flower': 'orange_blossom' }) returns canonical 'orange_blossom' with audit.raw when provided, normalized 'orange_flower', canonical 'orange_blossom', alias_source 'curated_seed'.
    - Test 2: buildCuratedAliasMap normalizes keys and values for oak moss -> oakmoss, patchouly -> patchouli, cedar wood -> cedarwood, and sandal wood -> sandalwood.
    - Test 3: canonicalizeDescriptor('lemon', aliasSeed) returns canonical 'lemon' and no alias audit when no curated alias exists.
    - Test 4: no API accepts AliasCandidate[] as canonicalization input.
  </behavior>
  <action>Create src/analyzer/alias_canonicalization.ts with exported types CuratedAliasAuditEntry, CuratedAliasCanonicalizationResult, buildCuratedAliasMap(aliasSeed), and canonicalizeDescriptor(input, aliasSeedOrMap). Normalize alias seed keys and values with normalizeDescriptor before comparison per DQ-D-14. Preserve audit entries with raw, normalized, canonical, and alias_source: curated_seed per DQ-D-17. Do not import or consume alias candidates from src/analyzer/alias_candidates.ts per DQ-D-15. Keep deterministic behavior by sorting or stable-map-building when audit arrays are emitted.</action>
  <acceptance_criteria>
    - Behavior assertion: curated aliases listed in this task canonicalize after key/value normalization.
    - Behavior assertion: descriptors without curated alias mappings are unchanged.
    - Source assertion: alias_canonicalization.ts has no dependency on AliasCandidate or analysis.aliasCandidates.
    - Source assertion: audit entries include raw, normalized, canonical, and alias_source exactly set to curated_seed.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm test -- tests/analysis/alias_canonicalization.test.ts</automated>
  </verify>
  <done>Curated alias canonicalizer is pure, deterministic, normalized on both sides, and tested independently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply curated aliases before statistics and alias candidate discovery</name>
  <files>src/analyzer/analyze_corpus.ts, src/analyzer/cooccurrence.ts, src/analyzer/alias_candidates.ts, src/tests/analysis/orchestration.test.ts</files>
  <read_first>
    - .planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md lines 36-42 and 113-124.
    - src/analyzer/analyze_corpus.ts.
    - src/analyzer/cooccurrence.ts.
    - src/analyzer/alias_candidates.ts.
    - src/analyzer/descriptor_sanitizer.ts.
    - src/analyzer/alias_canonicalization.ts.
    - src/types/analysis.ts.
    - src/tests/analysis/orchestration.test.ts.
  </read_first>
  <behavior>
    - Test 1: analyzeCorpus over descriptors ['orange flower', 'orange_blossom'] with alias seed { 'orange flower': 'orange_blossom' } produces frequency orange_blossom = 2 and no frequency orange_flower.
    - Test 2: co-occurrence uses canonical descriptors, so ['orange flower', 'jasmine'] records jasmine|orange_blossom, not jasmine|orange_flower.
    - Test 3: findAliasCandidates receives canonicalized frequency data and does not suggest curated seed pairs already canonicalized.
    - Test 4: sanitation still runs before canonicalization; hard-excluded raw descriptors do not create alias audit entries or frequency entries.
  </behavior>
  <action>Extend AnalyzeCorpusOptions with a curatedAliases option using the DescriptorAliasSeed type. Pass curated aliases into the shared preprocessing path so normalizeDescriptor and sanitizeDescriptor run before canonicalizeDescriptor per DQ-D-13. Ensure frequency, cooccurrence, alias candidate pool, seed profiles, and placement evidence receive curated-alias-canonicalized descriptor IDs per DQ-D-16 by updating analysis maps at this boundary.

Make alias-seed exclusion explicit in alias candidate discovery: analyzeCorpus(..., { curatedAliases }) must pass aliasSeed (or the normalized curated alias map) to findAliasCandidates when alias candidate generation is enabled, so curated aliases already applied by canonicalization cannot reappear as alias candidates. Preserve alias candidate generation as weak evidence only and keep any alias canonicalization audit as analysis-side audit payload (for example aliasCanonicalizationAudit), not final ReviewQueueItem output in this plan.</action>
  <acceptance_criteria>
    - Behavior assertion: frequency and cooccurrence maps aggregate curated alias evidence under canonical IDs.
    - Behavior assertion: aliasCandidates does not produce authoritative merges and does not feed canonicalization.
    - Source assertion: analyzeCorpus exposes a curatedAliases option and passes it before map construction.
    - Source assertion: preprocessing order in analysis code is normalize, sanitize, canonicalize, then map update.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run typecheck && npm test -- tests/analysis/alias_canonicalization.test.ts tests/analysis/orchestration.test.ts</automated>
  </verify>
  <done>Analysis statistics and alias candidate inputs are curated-alias-canonicalized before downstream inference uses them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire CLI-loaded alias seed into analysis</name>
  <files>src/cli/compile.ts, src/compiler/compile_all.ts, src/tests/cli/compile.test.ts</files>
  <read_first>
    - .planning/phases/07-data-quality-inference-hardening/07-CONTEXT.md lines 36-42 and 126-134.
    - src/cli/compile.ts.
    - src/compiler/compile_all.ts.
    - src/loader/alias_loader.ts.
    - src/tests/cli/compile.test.ts.
    - data/taxonomy/descriptor_aliases.seed.json.
  </read_first>
  <behavior>
    - Test 1: CLI fixture with aliases { lemony: 'lemon' } and corpus descriptors ['lemony', 'lemon'] writes taxonomy output where lemon has aggregated evidence and lemony is not emitted as a separate corpus descriptor.
    - Test 2: CLI still compiles descriptor_aliases.json from the curated alias seed and does not include alias candidates.
    - Test 3: console analysis summary remains deterministic and reports unique descriptor count after sanitation/canonicalization.
  </behavior>
  <action>Update runCompileCli to call analyzeCorpus(corpus, { curatedAliases: aliasSeed }) after loading descriptor_aliases.seed.json per DQ-D-13 and DQ-D-14. Keep compileAll receiving aliasSeed for compileAliases so authoritative descriptor_aliases.json remains seed-derived only per DQ-D-15. If compile_all types need adjustment for normalized noise from 07-01, keep the public CompileAllInputs explicit and typed; do not move filesystem reads into compiler code.</action>
  <acceptance_criteria>
    - Source assertion: src/cli/compile.ts passes aliasSeed into analyzeCorpus options.
    - Behavior assertion: CLI fixture canonicalizes curated aliases before generated taxonomy evidence.
    - Behavior assertion: descriptor_aliases.json remains based on curated aliases and does not include Phase 4 alias candidates.
    - CLI assertion: runCompileCli returns 0 for the updated fixture and writes all three output files.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run typecheck && npm test -- tests/cli/compile.test.ts tests/analysis/orchestration.test.ts</automated>
  </verify>
  <done>Default compile CLI uses curated alias seed for pre-analysis canonicalization while preserving final alias artifact boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alias seed JSON -> analyzer | Curated aliases alter descriptor identity before statistics. |
| alias candidates -> graph evidence | Weak inferred aliases can support evidence but cannot mutate canonical IDs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | src/analyzer/alias_canonicalization.ts | mitigate | Accept only curated descriptor_aliases.seed.json mappings and normalize both keys and values before lookup. |
| T-07-05 | Elevation of Privilege | src/analyzer/alias_candidates.ts | mitigate | Prevent AliasCandidate[] from becoming canonicalization input; candidates remain weak review evidence only. |
| T-07-06 | Repudiation | alias audit entries | mitigate | Emit deterministic audit entries with raw, normalized, canonical, and alias_source curated_seed. |
| T-07-SC | Tampering | npm installs | mitigate | No package-manager install tasks are present; preserve zero-runtime-dependency implementation. |
</threat_model>

<verification>
Run from src: npm run typecheck && npm test -- tests/analysis/alias_canonicalization.test.ts tests/analysis/orchestration.test.ts tests/cli/compile.test.ts
</verification>

<success_criteria>
- DQ-03 is satisfied by curated-alias canonicalization before frequency, co-occurrence, alias candidate pool, seed profiles, and placement scoring.
- Curated aliases such as orange flower, oak moss, patchouly, cedar wood, and sandal wood normalize and merge to canonical IDs.
- Alias candidates remain weak evidence and do not write authoritative aliases.
</success_criteria>

<output>
Create `.planning/phases/07-data-quality-inference-hardening/07-02-SUMMARY.md` when done.
</output>
