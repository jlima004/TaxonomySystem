---
status: execution_in_progress
non_authorizing: false
phase: 23
slug: v2-2-microcuration-candidate-selection
created: 2026-05-27
protected_paths_touched: data/taxonomy/taxonomy-seed.v2.json
execution_readiness: ready_for_execution
candidate: lemon_peel
action: add_target
family: citrus
subfamily: citrus_fresh
evidence_tier: MEDIUM
corpus_count: 24
pattern: valid_variant
predecessor_phase: 20
---

# Phase 23 — Plan 01: `lemon_peel add_target` em `citrus/citrus_fresh`

> **Status:** Plano completo. Aguardando persisted approval explícito do usuário para execução.  
> **Escopo de execução:** Este documento **autoriza** a execução quando o gate de persisted approval for satisfeito.  
> **Hard boundary:** Não executar nenhum passo de execução antes de obter o persisted approval da Seção `PLAN-04`.

---

## PLAN-01: Contexto e Rastreabilidade

| Campo | Valor |
|-------|-------|
| **Candidato** | `lemon_peel` |
| **Operação** | `add_target` |
| **Origem** | `seed_corpus_conflict` (review_queue) |
| **Pattern Phase 22** | `valid_variant` |
| **Evidence** | MEDIUM (corpus_count = 24) |
| **Seed anchor atual** | `lemon` (freq=129, curated) |
| **Família alvo** | `citrus` |
| **Subfamília alvo** | `citrus_fresh` |
| **Fase predecessora** | Phase 20 (`petitgrain` add_target — mesmo padrão, mesmo cluster) |
| **Policies aplicáveis** | P-05, P-06, P-08 (Phase 22) |
| **Research ref** | `23-RESEARCH.md` |
| **Patterns ref** | `23-PATTERNS.md` |
| **Validation ref** | `23-VALIDATION.md` |

### Distinção crítica: `lemon_peel` vs. `lemongrass`

Ambos têm corpus_count=24 e seed_anchor=lemon, mas **são candidatos distintos**:
- `lemon_peel`: valid_variant (casca de limão — Citrus limon). Candidato aprovado para add_target.
- `lemongrass`: planta diferente (*Cymbopogon citratus*). **Não co-adicionado neste plano.**

---

## PLAN-02: Estado Baseline Confirmado

### `taxonomy-seed.v2.json` — citrus/citrus_fresh (pré-execução)

```json
{
  "id": "citrus_fresh",
  "name": "Fresh Citrus",
  "descriptors": ["lemon", "bergamot", "sweet_orange", "grapefruit", "petitgrain"]
}
```

**Count atual:** 5 seeds

### `taxonomy.json` — citrus_fresh seeds (pré-execução)

| Descriptor | Source | Status | Frequency |
|-----------|--------|--------|-----------|
| `lemon` | seed | curated | 129 |
| `grapefruit` | seed | curated | 80 |
| `bergamot` | seed | curated | 53 |
| `petitgrain` | seed | curated | 52 |
| `sweet_orange` | seed | curated | 0 |
| **`lemon_peel`** | **corpus** | **candidate** | **24** |

### `similarity_matrix.json` — review_queue (pré-execução)

| Tipo | Count |
|------|-------|
| seed_corpus_conflict | 34 |
| corpus_candidate_low_support | 282 |
| **Total** | **316** |

`lemon_peel` presente como seed_corpus_conflict com severity=medium.

---

## PLAN-03: Allowlist de Modificações Autorizadas

### Arquivo único autorizado para mutação

| Arquivo | Operação | Modificação |
|---------|---------|------------|
| `data/taxonomy/taxonomy-seed.v2.json` | ✅ WRITE | Adicionar `"lemon_peel"` à lista `descriptors` de `citrus/citrus_fresh` |

### Todos os outros arquivos: READ-ONLY

| Arquivo | Status |
|---------|--------|
| `data/compiled/v2/*` | 🔒 Read-only (gerado pelo compile — não editar manualmente) |
| `data/inference/*` | 🔒 Read-only |
| `data/taxonomy/*.json` (exceto taxonomy-seed.v2.json) | 🔒 Read-only |
| `src/cli/parse_args.ts` | 🔒 Read-only |
| `scripts/check-safety-guards.sh` | 🔒 Read-only |
| `src/package.json` | 🔒 Read-only |
| `graphify-out/*` | 🔒 Read-only |

---

## PLAN-04: Persisted Approval Gate

> ⚠️ **GATE DE EXECUÇÃO — NÃO EXECUTAR SEM APROVAÇÃO EXPLÍCITA**

Este plano requer aprovação explícita do usuário antes de qualquer modificação em `data/taxonomy/taxonomy-seed.v2.json`.

### Declaração de Aprovação (Assinada)

```
PERSISTED APPROVAL — Phase 23, Plan 01

Candidato:          lemon_peel
Operação:           add_target
Família:            citrus
Subfamília:         citrus_fresh
Evidence:           MEDIUM (corpus_count=24)
Pattern:            valid_variant
Arquivo mutado:     data/taxonomy/taxonomy-seed.v2.json

Aprovado por:       Jefferson Dcher
Data:               2026-05-27
Sessão:             Phase 23 approval

Confirmação: Estou ciente de que esta operação adicionará lemon_peel como seed
descriptor em citrus/citrus_fresh e que o compile oficial será executado apenas
após validação de todos os 7 invariantes em /tmp.
```

**Status atual:** ✅ Aprovado e pronto para execução

---

## PLAN-05: Sequência de Execução

> **PRÉ-REQUISITO:** Persisted approval obtido (PLAN-04 preenchido).

### Wave 1 — Modificação do Seed File

**Ação:** Editar `data/taxonomy/taxonomy-seed.v2.json`

```diff
 {
   "id": "citrus_fresh",
   "name": "Fresh Citrus",
-  "descriptors": ["lemon", "bergamot", "sweet_orange", "grapefruit", "petitgrain"]
+  "descriptors": ["lemon", "bergamot", "sweet_orange", "grapefruit", "petitgrain", "lemon_peel"]
 }
```

**Verificação imediata:** Confirmar que apenas 1 linha foi alterada, apenas em `citrus_fresh`, apenas no arquivo autorizado.

---

### Wave 2 — Compile /tmp (Pré-validação)

**Ação:** Executar compile em modo temporário (não publica em `data/compiled/v2/`)

```bash
# Comando a ser confirmado com a CLI do projeto
# Referência Phase 20: compile --target /tmp --version v2
```

> **Nota:** O comando exato deve ser verificado no momento da execução com referência ao `src/cli/parse_args.ts` ou documentação da CLI. O padrão é compile isolado em /tmp.

**Saída esperada:**
- `taxonomy.json` em `/tmp/` com `lemon_peel` como `source: seed, status: curated`
- `similarity_matrix.json` em `/tmp/` sem `lemon_peel` no review_queue

---

### Wave 3 — Validação dos Invariantes

**Ação:** Inspecionar os outputs do compile /tmp e verificar cada invariante.

| Invariante | Verificação | Critério |
|-----------|------------|---------|
| INV-1 | `lemon_peel.source == "seed"` e `status == "curated"` em taxonomy.json | Bloqueante |
| INV-2 | `lemon_peel` ausente do review_queue em similarity_matrix.json | Bloqueante |
| INV-3 | `lemon` permanece seed curated em taxonomy.json | Bloqueante |
| INV-4 | 6 seeds em citrus_fresh (era 5) | Bloqueante |
| INV-5 | 5 seeds pré-existentes intactos em frequência/status | Bloqueante |
| INV-6 | ≤ 2 novos seed_corpus_conflicts com seed_descriptor=lemon_peel | Bloqueante se ≥3 |
| INV-7 | `lemongrass` permanece no review_queue como seed_corpus_conflict | Bloqueante |

**Caso especial INV-6:** Se `peel` (freq=70) aparecer como novo seed_corpus_conflict:
- Documentar no log como `WARN-PAT03`: "peel conflict expected per 23-RESEARCH.md"
- Não bloquear se for o único novo conflict (count=1 ≤ threshold=2)

---

### Wave 4 — Official Compile (Condicional)

**Condição:** TODOS os invariantes bloqueantes em estado PASS.

**Ação:** Executar compile oficial para `data/compiled/v2/`

```bash
# Comando de compile oficial — referência à CLI do projeto
# Gera: data/compiled/v2/taxonomy.json + similarity_matrix.json
```

**Verificação pós-official:** Repetir inspeção de INV-1 e INV-2 nos arquivos em `data/compiled/v2/` (não em /tmp).

---

### Wave 5 — Post-add Conflict Triage

**Ação:** Verificar novos itens no review_queue de `similarity_matrix.json` oficial com `seed_descriptor: lemon_peel`.

```python
# Script de inspeção (read-only)
new_conflicts = [
    item for item in review_queue 
    if item['evidence']['seed_descriptor'] == 'lemon_peel'
]
print(f"Novos conflicts pós-add: {len(new_conflicts)}")
for item in new_conflicts:
    print(item)
```

**Resultado esperado:** 0 novos conflicts (ou 1 se `peel` triggar lógica bidirecional).

**Documentar:** Registrar no `23-DISCUSSION-LOG.md` com o resultado real.

---

### Wave 6 — Commit

**Mensagem de commit:**

```
feat(taxonomy): add lemon_peel as seed descriptor in citrus/citrus_fresh [Phase 23]

- Promoted lemon_peel from corpus candidate to seed descriptor
- corpus_count: 24 (MEDIUM evidence, valid_variant pattern)
- Subfamily: citrus/citrus_fresh (anchor: lemon)
- Predecessor: Phase 20 (petitgrain add_target precedent)
- Persisted approval: [DATA] — Phase 23, Plan 01
- All 7 validation invariants PASS
- Review queue: lemon_peel removed from seed_corpus_conflicts (34→33)
- Post-add conflict triage: [N] new conflicts (expected: 0-1)

Phase-ref: 23-01-PLAN.md
Research-ref: 23-RESEARCH.md
Validation-ref: 23-VALIDATION.md
```

---

## PLAN-06: Rollback

Se qualquer invariante bloqueante falhar em Wave 3 ou Wave 4:

```
1. NÃO executar official compile (se ainda não executado)
2. Reverter taxonomy-seed.v2.json:
   - Remover "lemon_peel" da lista de descriptors de citrus_fresh
   - Confirmar que lista retornou a: ["lemon", "bergamot", "sweet_orange", "grapefruit", "petitgrain"]
3. NÃO commitar nenhuma alteração
4. Documentar falha no 23-DISCUSSION-LOG.md com:
   - Invariante que falhou
   - Output real vs esperado
   - Próximo passo sugerido
```

---

## PLAN-07: Estimativas de Impacto

| Métrica | Antes | Depois | Delta |
|--------|-------|--------|-------|
| Seeds em `citrus_fresh` | 5 | 6 | +1 |
| seed_corpus_conflicts | 34 | 33 (ou 34 se peel) | -1 (ou 0) |
| Total review_queue | 316 | 315 (ou 316) | -1 (ou 0) |
| Descritores seed totais | N | N+1 | +1 |
| Arquivo modificado | — | taxonomy-seed.v2.json | 1 arquivo |

---

## PLAN-08: Status do Plano

| Gate | Status |
|------|--------|
| Research (GR-01 a GR-05) | ✅ Completo (23-RESEARCH.md) |
| Patterns identificados | ✅ Completo (23-PATTERNS.md) |
| Validation contract | ✅ Completo (23-VALIDATION.md) |
| Plano de execução | ✅ Este documento |
| Persisted approval | ✅ Completo |
| Execução | 🔓 Liberada (em progresso) |
