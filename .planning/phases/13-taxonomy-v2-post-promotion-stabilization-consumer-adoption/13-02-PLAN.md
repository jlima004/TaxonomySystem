---
phase: 13-taxonomy-v2-post-promotion-stabilization-consumer-adoption
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md
  - .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SUMMARY.md
autonomous: true
requirements: [POST-02, POST-03, POST-04, POST-06]
user_setup: []
execution_readiness: ready_for_stabilization_only_execution
must_haves:
  truths:
    - "Default CLI/compiler behavior points coherently to v2 and can compile to `/tmp/opencode/taxonomy-phase13-smoke/default-v2` with fixed generated_at per STAB-D-05 through STAB-D-08."
    - "Explicit v1 fallback uses complete flags, does not depend on DEFAULT_PATHS and writes only to `/tmp/opencode/taxonomy-phase13-smoke/explicit-v1-fallback` per STAB-D-09 through STAB-D-13."
    - "Official `data/compiled/v1` and `data/compiled/v2` artifacts are present, versioned and diff-clean after smoke validation per POST-D-06, POST-D-09 and STAB-D-24 through STAB-D-27."
    - "Planned execution includes `cd src && npm run typecheck && npm test && npm run build` before smoke closure per STAB-D-25."
    - "`src/cli/parse_args.ts` remains unchanged while DEFAULT_PATHS are asserted as v2 per Phase 12 baseline and POST-D-03."
  artifacts:
    - path: ".planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md"
      provides: "Default-v2 and explicit-v1 smoke validation evidence"
      contains: "/tmp/opencode/taxonomy-phase13-smoke/default-v2"
  key_links:
    - from: "src/cli/parse_args.ts DEFAULT_PATHS"
      to: "/tmp/opencode/taxonomy-phase13-smoke/default-v2"
      via: "npm run compile -- --out ... --generated-at ..."
      pattern: "version.*2\\.0\\.0"
    - from: "explicit v1 flags"
      to: "/tmp/opencode/taxonomy-phase13-smoke/explicit-v1-fallback"
      via: "npm run compile -- --seed ...v1 --relations ...v1 --accords ...v1 --version 1.0.0"
      pattern: "version.*1\\.0\\.0"
---

<objective>
Validate safe runtime smoke behavior for the post-promotion v2 default and explicit v1 fallback using only temporary `/tmp` outputs, while proving official v1/v2 artifacts and protected source inputs remain unchanged.

Purpose: provide direct CLI/compiler evidence beyond tests-only coverage without overwriting `data/compiled/v1`, `data/compiled/v2`, taxonomy inputs or `src/cli/parse_args.ts`.
Output: `.planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md`.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-CONTEXT.md
@.planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-PREFLIGHT.md
@.planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-RESEARCH.md
@.planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-PATTERNS.md
@.planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-VALIDATION.md
@.planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-01-CONSUMER-INVENTORY.md
@src/package.json
@src/cli/parse_args.ts
@src/cli/compile.ts
@src/tests/curation/v1_v2_comparison.test.ts
@src/tests/cli/compile.test.ts
@data/compiled/v1/taxonomy.json
@data/compiled/v2/taxonomy.json
</context>

<preconditions>
- Stop if Plan 13-01 evidence is missing or reports blockers that make smoke validation misleading.
- Never run a bare `npm run compile` without `--out /tmp/...` in this plan.
- Do not edit `src/cli/parse_args.ts`, taxonomy inputs, `data/compiled/v1`, `data/compiled/v2`, docs or `graphify-out/*`.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Run bounded validation suite before smoke closure</name>
  <read_first>
    - .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md if it already exists, to preserve previous command evidence.
    - .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-CONTEXT.md STAB-D-24 through STAB-D-27.
    - .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-VALIDATION.md Test Infrastructure, Sampling Rate and Gate 2.
    - src/package.json scripts for `typecheck`, `test` and `build`.
    - src/vitest.config.ts test include configuration.
  </read_first>
  <files>.planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md</files>
  <action>Create or append a `Validation Suite` section in `13-02-SMOKE-VALIDATION.md`, then from `src/` run exactly `npm run typecheck`, `npm test`, and `npm run build` in that order. Record each command, exit code, concise stdout/stderr summary and timestamp. After the three commands, run and record the protected diff gate `git diff --exit-code -- data/compiled/v1 data/compiled/v2 data/taxonomy/taxonomy-seed.v1.json data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/inference/curated_relations.v1.json data/inference/curated_relations.v2.json data/inference/accord_map.v1.json data/inference/accord_map.v2.json src/cli/parse_args.ts`. Do not install packages, do not use watch-mode commands, do not run curation and do not write smoke outputs in this task.</action>
  <acceptance_criteria>
    - Command assertion: `cd src && npm run typecheck` exits 0 and its result is recorded in `13-02-SMOKE-VALIDATION.md`.
    - Command assertion: `cd src && npm test` exits 0 and its result is recorded in `13-02-SMOKE-VALIDATION.md`.
    - Command assertion: `cd src && npm run build` exits 0 and its result is recorded in `13-02-SMOKE-VALIDATION.md`.
    - Protected assertion: the protected diff gate exits 0 after typecheck, tests and build.
    - Source assertion: no package install command, watch-mode command, curation command, `graphify-out/*` mutation or official compiled artifact write is recorded for this task.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Validation Suite" .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md && grep -q "npm run typecheck" .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md && grep -q "npm test" .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md && grep -q "npm run build" .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md && git diff --exit-code -- data/compiled/v1 data/compiled/v2 data/taxonomy/taxonomy-seed.v1.json data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/inference/curated_relations.v1.json data/inference/curated_relations.v2.json data/inference/accord_map.v1.json data/inference/accord_map.v2.json src/cli/parse_args.ts</automated>
  </verify>
  <done>`13-02-SMOKE-VALIDATION.md` records passing typecheck, tests, build and a clean protected diff before smoke closure.</done>
</task>

<task type="auto">
  <name>Task 2: Run protected preflight and default-v2 `/tmp` smoke validation</name>
  <read_first>
    - .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md if it already exists, to preserve previous command evidence.
    - .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-CONTEXT.md STAB-D-05 through STAB-D-08 and STAB-D-24 through STAB-D-27.
    - .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-VALIDATION.md Gate 2 and task 13-02-01.
    - src/package.json scripts and src/cli/parse_args.ts DEFAULT_PATHS.
    - src/cli/compile.ts output resolution behavior.
  </read_first>
  <files>.planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md</files>
  <action>Create `13-02-SMOKE-VALIDATION.md` with a protected preflight section, then run and record the protected diff gate `git diff --exit-code -- data/compiled/v1 data/compiled/v2 data/taxonomy/taxonomy-seed.v1.json data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/inference/curated_relations.v1.json data/inference/curated_relations.v2.json data/inference/accord_map.v1.json data/inference/accord_map.v2.json src/cli/parse_args.ts`. From `src/`, assert v2 defaults by running `npm run build` if `dist/cli/parse_args.js` is stale or missing, then `node -e "import('./dist/cli/parse_args.js').then(({DEFAULT_PATHS})=>{const expected={seedPath:'data/taxonomy/taxonomy-seed.v2.json',relationsPath:'data/inference/curated_relations.v2.json',accordsPath:'data/inference/accord_map.v2.json',outputDir:'data/compiled/v2',version:'2.0.0'}; for (const [k,v] of Object.entries(expected)) if (DEFAULT_PATHS[k]!==v) throw new Error(k+'='+DEFAULT_PATHS[k]); console.log('defaults v2 ok')})"`. Run default-v2 smoke only as `npm run compile -- --out /tmp/opencode/taxonomy-phase13-smoke/default-v2 --generated-at 2026-01-01T00:00:00.000Z`. Record stdout/stderr summary, exit code, output directory and validation/quality status. Do not write to `data/compiled/v2`.</action>
  <acceptance_criteria>
    - Command assertion: protected preflight diff exits 0 before the smoke command.
    - Default assertion: DEFAULT_PATHS seedPath, relationsPath, accordsPath, outputDir and version equal the v2 values from STAB-D-06.
    - Smoke assertion: default-v2 command exits 0 and writes only under `/tmp/opencode/taxonomy-phase13-smoke/default-v2`.
    - Output assertion: temp output contains `taxonomy.json`, `descriptor_aliases.json` and `similarity_matrix.json`; `taxonomy.json.version` is `2.0.0`; validation status is ok and quality gate passes if printed by the CLI.
    - Protected assertion: no official compiled artifacts or protected inputs are changed by this task.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md && test -f /tmp/opencode/taxonomy-phase13-smoke/default-v2/taxonomy.json && test -f /tmp/opencode/taxonomy-phase13-smoke/default-v2/descriptor_aliases.json && test -f /tmp/opencode/taxonomy-phase13-smoke/default-v2/similarity_matrix.json && node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('/tmp/opencode/taxonomy-phase13-smoke/default-v2/taxonomy.json','utf8')); if(j.version!=='2.0.0') throw new Error(j.version)" && git diff --exit-code -- data/compiled/v1 data/compiled/v2 data/taxonomy/taxonomy-seed.v1.json data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/inference/curated_relations.v1.json data/inference/curated_relations.v2.json data/inference/accord_map.v1.json data/inference/accord_map.v2.json src/cli/parse_args.ts</automated>
  </verify>
  <done>`13-02-SMOKE-VALIDATION.md` records a passing default-v2 `/tmp` smoke compile and protected paths remain diff-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Run explicit-v1 fallback `/tmp` smoke validation and post-diff gate</name>
  <read_first>
    - .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md from Task 1.
    - .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-CONTEXT.md STAB-D-09 through STAB-D-13.
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-RELEASE-MIGRATION-NOTES.md explicit v1 fallback command.
    - data/compiled/v1/taxonomy.json and data/compiled/v2/taxonomy.json for official version expectations.
  </read_first>
  <files>.planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md</files>
  <action>Append explicit-v1 fallback evidence to `13-02-SMOKE-VALIDATION.md` per POST-03, POST-04, POST-06 and STAB-D-09 through STAB-D-13. From `src/`, run exactly `npm run compile -- --seed ../data/taxonomy/taxonomy-seed.v1.json --aliases ../data/taxonomy/descriptor_aliases.seed.json --relations ../data/inference/curated_relations.v1.json --accords ../data/inference/accord_map.v1.json --out /tmp/opencode/taxonomy-phase13-smoke/explicit-v1-fallback --version 1.0.0 --generated-at 2026-01-01T00:00:00.000Z`. Record exit code, stdout/stderr summary, temp output files and version checks. Confirm this fallback does not depend on DEFAULT_PATHS by documenting all explicit flags and by reasserting DEFAULT_PATHS remain v2. Finish with the full protected diff gate and record the result.</action>
  <acceptance_criteria>
    - Fallback command assertion: explicit-v1 compile exits 0 and uses all required flags: `--seed`, `--aliases`, `--relations`, `--accords`, `--out`, `--version`, `--generated-at`.
    - Output assertion: `/tmp/opencode/taxonomy-phase13-smoke/explicit-v1-fallback` contains `taxonomy.json`, `descriptor_aliases.json` and `similarity_matrix.json`; `taxonomy.json.version` is `1.0.0`.
    - Independence assertion: artifact states fallback used complete explicit v1 paths and did not edit or rely on changing DEFAULT_PATHS.
    - Coherence assertion: official `data/compiled/v1` and `data/compiled/v2` remain present, discoverable and diff-clean.
    - Protected assertion: `src/cli/parse_args.ts`, seed files, relation/accord files, alias seed and official compiled dirs have no diff.
  </acceptance_criteria>
  <verify>
    <automated>test -f /tmp/opencode/taxonomy-phase13-smoke/explicit-v1-fallback/taxonomy.json && test -f /tmp/opencode/taxonomy-phase13-smoke/explicit-v1-fallback/descriptor_aliases.json && test -f /tmp/opencode/taxonomy-phase13-smoke/explicit-v1-fallback/similarity_matrix.json && node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('/tmp/opencode/taxonomy-phase13-smoke/explicit-v1-fallback/taxonomy.json','utf8')); if(j.version!=='1.0.0') throw new Error(j.version)" && grep -q "explicit-v1-fallback" .planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SMOKE-VALIDATION.md && git diff --exit-code -- data/compiled/v1 data/compiled/v2 data/taxonomy/taxonomy-seed.v1.json data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/inference/curated_relations.v1.json data/inference/curated_relations.v2.json data/inference/accord_map.v1.json data/inference/accord_map.v2.json src/cli/parse_args.ts</automated>
  </verify>
  <done>`13-02-SMOKE-VALIDATION.md` records both safe smoke validations, temp output assertions, default v2 assertions and a clean protected post-diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI defaults → filesystem outputs | A bare compile can write to official `data/compiled/v2` unless `/tmp` output is forced. |
| explicit fallback flags → v1 temp output | Fallback must use explicit v1 inputs and not mutate defaults or official artifacts. |
| temp smoke outputs → release evidence | Generated `/tmp` outputs must prove behavior without becoming official artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-default-overwrite | Tampering | `data/compiled/v2` | mitigate | Default-v2 smoke command always includes `--out /tmp/opencode/taxonomy-phase13-smoke/default-v2` and protected pre/post diff gates. |
| T-13-02-fallback-false-positive | Repudiation | explicit v1 fallback evidence | mitigate | Use complete explicit v1 flags and assert `taxonomy.json.version = 1.0.0` in temp output. |
| T-13-02-default-drift | Tampering | `src/cli/parse_args.ts` | mitigate | Assert DEFAULT_PATHS equal v2 values and run protected diff gate. |
| T-13-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; existing npm scripts only. |
</threat_model>

<verification>
Overall checks: `cd src && npm run typecheck`, `cd src && npm test` and `cd src && npm run build` pass and are recorded; default-v2 smoke command writes to `/tmp/opencode/taxonomy-phase13-smoke/default-v2`; explicit-v1 fallback writes to `/tmp/opencode/taxonomy-phase13-smoke/explicit-v1-fallback`; both outputs contain the three JSON artifacts with expected versions; protected diff gate exits 0 after all commands.
</verification>

<success_criteria>
Plan 13-02 succeeds when runtime CLI smoke evidence proves v2 default and explicit v1 fallback are usable without changing official artifacts, taxonomy inputs, `src/cli/parse_args.ts`, docs or Graphify outputs.
</success_criteria>

<output>
Create `.planning/phases/13-taxonomy-v2-post-promotion-stabilization-consumer-adoption/13-02-SUMMARY.md` when done.
</output>
