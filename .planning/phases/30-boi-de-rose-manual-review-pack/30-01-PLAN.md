# Phase 30 — Boi de Rose Manual Review Pack : Plan 01 (Research)

## Tarefas da Pesquisa

1. **Busca por `boi_de_rose`**:
   - Procurar no `data/compiled/v2/taxonomy.json`
   - Procurar no `data/compiled/v2/similarity_matrix.json`
   - Checar se é candidate, seed, alias ou review item nas bases (ex: `data/taxonomy/descriptor_aliases.seed.json`, `data/taxonomy/taxonomy-seed.v2.json`, `data/inference/review_queue.v2.json`).

2. **Busca por Termos Correlatos**:
   - Procurar por `bois_de_rose`
   - Procurar por `rosewood`
   - Procurar por `pau_rosa`

3. **Análise de Contexto**:
   - Determinar a qual categoria/cluster o termo (ou os termos correlatos) pertencem atualmente (ex: `floral_rose`, `woody`, `amber_resinous`).
   - Se `boi_de_rose` estiver em `floral_rose`, verificar se isso é um erro de clusterização causado pelo sufixo "rose".

4. **Elaboração da Disposition**:
   - Com base nos achados, redigir o plano de mutação/correção (e.g., adicionar como alias para `rosewood`, ou defer/manual review) no fechamento desta fase.

## Notas de Execução
- Utilizar apenas consultas read-only (grep/search) na base de dados. Nenhuma alteração é permitida neste step.
