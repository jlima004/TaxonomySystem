---
phase: 61-fail-closed-query-consumption
plan: 02
type: execute
wave: 1
depends_on: [61-01]
files_modified:
  - src/tests/graph_read_model/query_consumer.test.ts
  - src/tests/graph_read_model/query_live_baseline.test.ts
autonomous: true
requirements: [GVAL-07, GQRY-06, GQRY-08]
must_haves:
  truths:
    - "GQRY-06: all eight validated consumer methods preserve the existing proof envelope `{ query_kind, params, result, path }` per D-17."
    - "D-18: missing family, subfamily, alias, descriptor, and similarity targets remain empty/null structured proofs, not errors."
    - "D-10, D-16 and D-19 are enforced as scope fences: no raw-graph shortcut, no throw helper path, no runtime/API/DB/Graphify/publication/proof persistence."
  artifacts:
    - "src/tests/graph_read_model/query_consumer.test.ts contains exhaustive consumer-vs-direct query proof comparisons and missing-target compatibility checks."
    - "src/tests/graph_read_model/query_live_baseline.test.ts proves the sanctioned live v2 graph can be consumed through `asValidatedGraph` and `createValidatedQueryConsumer`."
  key_links:
    - "query_consumer.test.ts -> query_consumer.ts verifies all eight method names and output equality."
    - "query_live_baseline.test.ts -> live compiled v2 artifacts keeps sanctioned baseline coverage under existing local file reads."
    - "query_consumer.test.ts -> query_graph.ts protects low-level proof semantics from Phase 61 guard leakage."
---

<objective>
Lock proof-envelope compatibility and live baseline consumption for the Phase 61 consumer boundary.

Purpose: prove that fail-closed graph admission does not alter successful query proof objects, missing-target semantics, or the sanctioned live v2 baseline.
Output: expanded consumer tests and live regression coverage for GQRY-06, GVAL-07 and GQRY-08.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-fail-closed-query-consumption/61-CONTEXT.md
@.planning/phases/61-fail-closed-query-consumption/61-RESEARCH.md
@.planning/phases/61-fail-closed-query-consumption/61-VALIDATION.md
@.planning/phases/61-fail-closed-query-consumption/61-PATTERNS.md
@.planning/phases/61-fail-closed-query-consumption/61-01-SUMMARY.md
@src/graph_read_model/types.ts
@src/graph_read_model/query_graph.ts
@src/graph_read_model/query_consumer.ts
@src/graph_read_model/validate_graph.ts
@src/tests/graph_read_model/query_graph.test.ts
@src/tests/graph_read_model/query_consumer.test.ts
@src/tests/graph_read_model/query_live_baseline.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove all eight consumer methods preserve direct proof envelopes</name>
  <files>src/tests/graph_read_model/query_consumer.test.ts</files>
  <read_first>
    - .planning/phases/61-fail-closed-query-consumption/61-CONTEXT.md
    - .planning/phases/61-fail-closed-query-consumption/61-PATTERNS.md
    - src/graph_read_model/query_consumer.ts
    - src/graph_read_model/query_graph.ts
    - src/graph_read_model/types.ts
    - src/tests/graph_read_model/query_consumer.test.ts
    - src/tests/graph_read_model/query_graph.test.ts
  </read_first>
  <behavior>
    - The consumer exposes methods corresponding to all eight current query proofs per D-07.
    - Each consumer method returns the same direct proof object as the matching low-level function for the same validated graph per D-17.
    - Successful proof objects do not gain `ok`, `error`, `errors`, `validated`, or any fail-closed metadata fields.
  </behavior>
  <action>Expand `query_consumer.test.ts` with an exhaustive comparison test that builds a sanctioned valid graph, obtains `result = asValidatedGraph(graph)`, obtains `consumerResult = createValidatedQueryConsumer(result.graph)`, and compares all eight consumer method outputs to the matching direct `query_graph.ts` outputs. Use the exact calls `getDescriptorsByFamily('woody')`, `getDescriptorsBySubfamily('woody_dry')`, `resolveAliasPath('cedar')`, `getDescriptorToFamilyPath('cedarwood')`, `getRelatedDescriptors('cedarwood')`, `getSimilarityNeighborhood('floral_rose')`, `getCrossFamilyBridges()`, and `getSimilarityHub()`. Add an `expectExactProofKeys` helper in this test file and assert success keys are only `params`, `query_kind`, `result`, and optional `path`. Do not change `src/graph_read_model/query_graph.ts` or `src/graph_read_model/types.ts`; GQRY-06 is proven by tests, not by changing the envelope.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/query_consumer.test.ts tests/graph_read_model/query_graph.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: all eight consumer outputs are `toEqual` the matching direct `query_graph.ts` outputs for the same graph and inputs.
    - Source assertion: `query_consumer.test.ts` contains all eight method names: `getDescriptorsByFamily`, `getDescriptorsBySubfamily`, `resolveAliasPath`, `getDescriptorToFamilyPath`, `getRelatedDescriptors`, `getSimilarityNeighborhood`, `getCrossFamilyBridges`, and `getSimilarityHub`.
    - Behavior assertion: key checks fail if a success proof includes `ok`, `error`, `errors`, `validated`, or any field outside `query_kind`, `params`, `result`, and optional `path`.
    - Command assertion: `env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/query_consumer.test.ts tests/graph_read_model/query_graph.test.ts` passes.
  </acceptance_criteria>
  <done>GQRY-06 is locked for every current query method through direct consumer-vs-low-level proof equality.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve missing-target empty proof semantics through the consumer</name>
  <files>src/tests/graph_read_model/query_consumer.test.ts</files>
  <read_first>
    - .planning/phases/61-fail-closed-query-consumption/61-CONTEXT.md
    - src/graph_read_model/query_consumer.ts
    - src/graph_read_model/query_graph.ts
    - src/tests/graph_read_model/query_consumer.test.ts
    - src/tests/graph_read_model/query_graph.test.ts
  </read_first>
  <behavior>
    - Missing family and subfamily queries return `descriptors: []`.
    - Unknown alias returns `target_descriptor_id: null` and no `path`.
    - Missing descriptor path returns `{ family_id: null, subfamily_id: null }`; related descriptors return `descriptors: []`; unknown similarity subfamily returns `neighbors: []`.
  </behavior>
  <action>Add a consumer missing-target compatibility test per D-18. Through `consumer`, assert exact proof objects for `getDescriptorsByFamily('missing_family')`, `getDescriptorsBySubfamily('missing_subfamily')`, `resolveAliasPath('unknown_alias')`, `getDescriptorToFamilyPath('missing_descriptor')`, `getRelatedDescriptors('missing_descriptor')`, and `getSimilarityNeighborhood('unknown_subfamily')`. Each assertion must match the existing structured empty/null proof semantics from `query_graph.ts`; none may expect `graph_not_validated`, `error`, a thrown exception, or any `ok` wrapper. Keep fail-closed behavior limited to invalid or unvalidated graph inputs per D-18.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/query_consumer.test.ts tests/graph_read_model/query_graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `consumer.resolveAliasPath('unknown_alias')` equals `{ query_kind: 'alias_resolution_path', params: { alias: 'unknown_alias' }, result: { target_descriptor_id: null } }` and `path` is `undefined`.
    - Behavior assertion: `consumer.getDescriptorToFamilyPath('missing_descriptor')` equals `{ query_kind: 'descriptor_to_family_path', params: { descriptor_id: 'missing_descriptor' }, result: { family_id: null, subfamily_id: null } }`.
    - Behavior assertion: `consumer.getRelatedDescriptors('missing_descriptor')` returns `result: { descriptors: [] }`.
    - Behavior assertion: `consumer.getSimilarityNeighborhood('unknown_subfamily')` returns `result: { neighbors: [] }`.
    - Negative assertion: no missing-target assertion expects `graph_not_validated`, `error`, thrown behavior, or persisted proof output.
  </acceptance_criteria>
  <done>Missing query targets remain structured empty/null proofs through the validated consumer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add live baseline consumer proof and source fences</name>
  <files>src/tests/graph_read_model/query_consumer.test.ts, src/tests/graph_read_model/query_live_baseline.test.ts</files>
  <read_first>
    - .planning/phases/61-fail-closed-query-consumption/61-CONTEXT.md
    - .planning/phases/61-fail-closed-query-consumption/61-VALIDATION.md
    - src/graph_read_model/query_consumer.ts
    - src/graph_read_model/query_graph.ts
    - src/graph_read_model/types.ts
    - src/tests/graph_read_model/query_consumer.test.ts
    - src/tests/graph_read_model/query_live_baseline.test.ts
  </read_first>
  <behavior>
    - The sanctioned live v2 graph validates into `ValidatedGraph` and is consumable through the validated consumer.
    - Source fences prevent accidental raw-graph shortcuts, throw-helper substitution, low-level query hardening, and runtime/API/DB/Graphify scope creep.
    - Full phase validation command from `61-VALIDATION.md` passes.
  </behavior>
  <action>In `query_live_baseline.test.ts`, import `asValidatedGraph` and `createValidatedQueryConsumer`. Add a focused live regression that builds the graph from existing `data/compiled/v2` local files, asserts `asValidatedGraph(graph)` returns `ok: true`, asserts `createValidatedQueryConsumer(result.graph)` returns `ok: true`, and routes representative existing live assertions through the consumer: all 10 families with `getDescriptorsByFamily`, all 18 aliases with `resolveAliasPath`, `getSimilarityNeighborhood` for subfamilies present in similarity edges, `getCrossFamilyBridges()` length 5, `getSimilarityHub()` equal to floral_rose degree 3, `getDescriptorsBySubfamily('woody_dry')`, `getDescriptorToFamilyPath('cedarwood')`, and `getRelatedDescriptors('cedarwood')`. In `query_consumer.test.ts`, add source-level assertions using `readFile` that `query_consumer.ts` does not contain `createValidatedQueryConsumerFromGraph`, `assertValidatedGraph`, `createValidatedQueryConsumerOrThrow`, `fromGraph(`, `node:fs`, `graphify-out`, `neo4j`, `api/`, or `data/read-models`; add a source assertion that `query_graph.ts` does not contain `ValidatedGraph`, `graph_not_validated`, or `validateSanctionedV211Graph`. This covers D-10, D-16, and D-19 without adding optional helper paths.</action>
  <verify>
    <automated>npm --prefix src run typecheck</automated>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/query_consumer.test.ts tests/graph_read_model/query_graph.test.ts tests/graph_read_model/query_live_baseline.test.ts tests/graph_read_model/validate_graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Live assertion: `asValidatedGraph(graph)` returns `ok: true` for the current compiled v2 graph and the graph stats remain `{ families: 10, subfamilies: 18, descriptors: 341, aliases: 18, subfamily_similarity_edges: 13 }`.
    - Live assertion: consumer-routed `getCrossFamilyBridges()` has 5 bridges and `getSimilarityHub()` returns `subfamily_id: 'floral_rose'`, `graph_id: 'subfamily:floral_rose'`, `family_id: 'floral'`, and `degree: 3`.
    - Source assertion: `query_consumer.ts` contains no raw graph shortcut exports and no throw helper exports named in D-10 or D-16.
    - Source assertion: `query_graph.ts` contains no `ValidatedGraph`, no `graph_not_validated`, and no `validateSanctionedV211Graph`.
    - Scope assertion: no Phase 61 source/test change reads from or writes to `graphify-out/**`, mutates `data/taxonomy/**`, mutates `data/compiled/v2/**`, writes `data/read-models/**`, adds API/runtime/database/Neo4J imports, installs packages, or persists query proofs.
    - Command assertion: the full phase gate from `61-VALIDATION.md` passes.
  </acceptance_criteria>
  <done>Live baseline and source fences prove the validated consumer is safe, compatible, and scoped to the Phase 61 boundary.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Reject invalid or unvalidated graphs before proof generation | 61-01, 61-02 | COVERED | Boundary tests, implementation and live regression cover the goal.
REQ | GVAL-07 | Prevent query-proof generation from invalid or unvalidated graphs | 61-01, 61-02 | COVERED | Invalid inputs fail before consumer methods; live graph consumes only after validation.
REQ | GQRY-06 | Preserve proof envelope `{ query_kind, params, result, path }` | 61-02 | COVERED | All eight consumer methods are compared with direct query proofs.
REQ | GQRY-08 | Deterministic typed invalid-graph errors | 61-01, 61-02 | COVERED | Validation errors and `graph_not_validated` are separated.
RESEARCH | Branded `ValidatedGraph` handle | 61-01 | COVERED | Created as explicit validated handle.
RESEARCH | Deterministic ok/error validation result | 61-01 | COVERED | `asValidatedGraph` returns success or preserved errors.
RESEARCH | Thin consumer object delegating to eight query functions | 61-01, 61-02 | COVERED | Consumer delegates without changing query semantics.
RESEARCH | Missing target is not an error | 61-02 | COVERED | Exact empty/null proof assertions cover target absence.
RESEARCH | No new packages, runtime, persistence, API, DB or Graphify | 61-01, 61-02 | COVERED | Source fences and scope assertions enforce it.
CONTEXT | D-01 | `ValidatedGraph` proves sanctioned validation | 61-01 | COVERED | Handle is created after `validateSanctionedV211Graph`.
CONTEXT | D-02 | `query_graph.ts` remains pure low-level layer | 61-01, 61-02 | COVERED | Source fence prevents validation imports in query module.
CONTEXT | D-03 | `asValidatedGraph(graph)` is official raw graph validation entrypoint | 61-01 | COVERED | Export and tests require it.
CONTEXT | D-04 | `ValidatedGraph` is explicit branded/opaque handle | 61-01 | COVERED | Runtime brand and type handle are required.
CONTEXT | D-05 | Reusable validated graph without runtime state/service/cache | 61-01 | COVERED | Handle closes over in-memory graph only.
CONTEXT | D-06 | Main safe consumer is `createValidatedQueryConsumer(validatedGraph)` | 61-01 | COVERED | Raw-graph shortcut is forbidden.
CONTEXT | D-07 | Consumer exposes eight current query proofs | 61-01, 61-02 | COVERED | All eight method names are tested.
CONTEXT | D-08 | Recommended two-step validate then create consumer contract | 61-01, 61-02 | COVERED | Tests use `asValidatedGraph` before `createValidatedQueryConsumer`.
CONTEXT | D-09 | Do not implement `createValidatedQueryConsumer(graph)` as main shortcut | 61-01, 61-02 | COVERED | Source fence forbids shortcut.
CONTEXT | D-10 | Optional raw helper only if clearly secondary | 61-02 | COVERED | No raw helper is introduced.
CONTEXT | D-11 | Official path uses ok/error, no throw | 61-01 | COVERED | Conversion and creation return discriminated unions.
CONTEXT | D-12 | `asValidatedGraph` returns success handle or typed errors | 61-01 | COVERED | Tests require both branches.
CONTEXT | D-13 | Unvalidated consumption produces `graph_not_validated` | 61-01 | COVERED | Forced raw misuse test requires exact factory error.
CONTEXT | D-14 | Validation/profile errors distinct from `graph_not_validated` | 61-01 | COVERED | Profile mismatch test excludes graph_not_validated.
CONTEXT | D-15 | Preserve detailed validation errors | 61-01 | COVERED | Errors are returned without collapsing.
CONTEXT | D-16 | Throw helpers may exist only as controlled convenience | 61-02 | COVERED | No throw helper is introduced in this phase plan.
CONTEXT | D-17 | Success proof envelope remains exact | 61-02 | COVERED | Exhaustive key/equality tests cover all eight proofs.
CONTEXT | D-18 | Missing targets remain empty/null proofs | 61-02 | COVERED | Dedicated missing-target tests cover all relevant query kinds.
CONTEXT | D-19 | No runtime/API/DB/Graphify/publication/persistence/new scope | 61-02 | COVERED | Source and scope assertions enforce this fence.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated consumer -> proof envelope | Future consumers depend on stable proof object shape after fail-closed admission. |
| live compiled artifacts -> validated consumer | Current v2 compiled artifacts prove the sanctioned baseline can flow through the new boundary. |
| source scope -> runtime/database/API surfaces | Phase 61 must not expand a library boundary into runtime or persistence behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04 | Tampering | proof envelope shape | mitigate | Compare all eight consumer outputs to direct `query_graph.ts` outputs and assert exact success keys. |
| T-61-05 | Tampering | missing-target semantics | mitigate | Add exact empty/null proof assertions through the consumer for missing family, subfamily, alias, descriptor and similarity target cases. |
| T-61-06 | Repudiation | sanctioned live profile use | mitigate | Live regression validates through `asValidatedGraph` and records exact baseline stats and representative proof outputs. |
| T-61-07 | Information Disclosure / Elevation of Privilege | runtime/API/DB/Graphify scope creep | mitigate | Source tests reject Graphify, API, DB/Neo4J, read-model write and proof-persistence tokens in Phase 61 files. |
| T-61-SC | Tampering | npm installs | mitigate | No package install tasks; all verification uses existing TypeScript/Vitest. |
</threat_model>

<verification>
Overall phase checks:
- `npm --prefix src run typecheck`
- `env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/query_consumer.test.ts tests/graph_read_model/query_graph.test.ts tests/graph_read_model/query_live_baseline.test.ts tests/graph_read_model/validate_graph.test.ts`
</verification>

<success_criteria>
- Consumer-facing query path fails closed on invalid or unvalidated graph inputs.
- Existing proof envelope shape `{ query_kind, params, result, path }` remains stable across all current query functions.
- Invalid graph attempts produce deterministic typed errors instead of partial or misleading proofs.
- Missing query targets remain empty/null structured proofs.
- No runtime agent, SaaS/API, DB/Neo4J, Graphify, publication, proof persistence, new dependency, or new graph scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/61-fail-closed-query-consumption/61-02-SUMMARY.md` when done.
</output>
