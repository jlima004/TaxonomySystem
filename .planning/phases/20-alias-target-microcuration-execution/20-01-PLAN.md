---
phase: 20-alias-target-microcuration-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md
  - data/taxonomy/taxonomy-seed.v2.json
autonomous: false
requirements:
  - CUR20-01
  - CUR20-02
  - CUR20-03
  - CUR20-04
  - CUR20-05
  - CUR20-06
  - CUR20-07
must_haves:
  truths:
    - "CUR20-01: Future execution is limited to adding descriptor petitgrain under citrus/citrus_fresh in data/taxonomy/taxonomy-seed.v2.json after persisted approval."
    - "CUR20-02: ylang ylang -> ylang_ylang remains accepted_exception_interim; no ylang_ylang add_target occurs in Phase 20."
    - "CUR20-03: Mutation is blocked until .planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md exists with required fields."
    - "CUR20-04: Rollback can exactly restore the pre-mutation taxonomy-seed.v2.json state and leaves descriptor_aliases.seed.json unchanged."
    - "CUR20-05: petitgrain is lower snake-case ASCII, appears exactly once after mutation, seed version remains 2.0.0, and citrus/citrus_fresh remains non-empty."
    - "CUR20-06: Future execution defines safety guard usage and targeted tests without modifying guard/test/package files."
    - "CUR20-07: Any compile validation writes only to /tmp and official data/compiled/v2 publication remains blocked."
  artifacts:
    - path: ".planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md"
      provides: "Future persisted manual approval gate for petitgrain add_target-only execution"
    - path: "data/taxonomy/taxonomy-seed.v2.json"
      provides: "Only future data mutation: petitgrain descriptor in citrus/citrus_fresh"
    - path: "data/taxonomy/descriptor_aliases.seed.json"
      provides: "Unchanged alias map; petit grain -> petitgrain and ylang ylang -> ylang_ylang preserved"
  key_links:
    - from: ".planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md"
      to: "data/taxonomy/taxonomy-seed.v2.json"
      via: "approval fields family_id=citrus, subfamily_id=citrus_fresh, descriptor_id=petitgrain"
      pattern: "approval_id.*phase20-petitgrain-add-target-approval"
    - from: "data/taxonomy/descriptor_aliases.seed.json"
      to: "data/taxonomy/taxonomy-seed.v2.json"
      via: "petit grain -> petitgrain target passes by seed descriptor presence after approved mutation"
      pattern: "petit grain.*petitgrain"
user_setup: []
---

<objective>
Plan and gate a controlled Phase 20 Option 1 microcuration without authorizing mutation by this plan alone: future execution may add `petitgrain` as a curated descriptor under `citrus` / `citrus_fresh` in `data/taxonomy/taxonomy-seed.v2.json` only after persisted explicit approval exists.

Purpose: Resolve the Phase 19 `petit grain -> petitgrain` absent-target case by converting a corpus-backed candidate into seed truth while preserving the lower-confidence `ylang ylang -> ylang_ylang` case as `accepted_exception_interim`.

Output: A future approval gate artifact, a single allowlisted seed mutation after approval, rollback instructions, targeted validation commands, and an explicit no-publication boundary for official compiled artifacts.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-alias-target-microcuration-execution/20-CONTEXT.md
@.planning/phases/20-alias-target-microcuration-execution/20-PREFLIGHT.md
@.planning/phases/20-alias-target-microcuration-execution/20-DISCUSSION-LOG.md
@.planning/phases/20-alias-target-microcuration-execution/20-RESEARCH.md
@.planning/phases/20-alias-target-microcuration-execution/20-PATTERNS.md
@.planning/phases/20-alias-target-microcuration-execution/20-VALIDATION.md
@.planning/phases/19-taxonomy-v2-1-curation-planning/19-CLOSURE.md
@.planning/phases/19-taxonomy-v2-1-curation-planning/19-01-PLAN.md
</context>

<source_audit>
| Source Type | Item | Coverage |
|---|---|---|
| GOAL | Planejar microcuradoria controlada para `petitgrain` in `citrus/citrus_fresh`, preserving `ylang ylang -> ylang_ylang`, with no mutation during planning | Covered by all tasks; mutation is future-gated |
| REQ | CUR20-01 petitgrain add_target scope | Task 2 |
| REQ | CUR20-02 ylang exception boundary | Tasks 1 and 3 |
| REQ | CUR20-03 allowlist and approval | Task 1 |
| REQ | CUR20-04 rollback strategy | Tasks 1 and 3 |
| REQ | CUR20-05 integrity validation | Tasks 2 and 3 |
| REQ | CUR20-06 safety and targeted tests | Tasks 1 and 3 |
| REQ | CUR20-07 compile/publication boundary | Tasks 1 and 3 |
| RESEARCH | `petitgrain` is absent from seed v2 but present in compiled v2 as corpus candidate frequency 52 in `citrus_fresh` | Task 2 approval and mutation criteria |
| RESEARCH | `descriptor_aliases.seed.json` remains unchanged for Option 1 | Tasks 1 and 3 |
| RESEARCH | Safety guard reports `PROTECTED_DIFF` after intentional seed mutation | Tasks 1 and 3 |
| CONTEXT | curation20-D-01: not ready for execution | Task 1 blocking approval gate |
| CONTEXT | curation20-D-02: Option 1 planning approved, execution not authorized | Tasks 1 and 2 |
| CONTEXT | curation20-D-03: ylang remains interim exception | Tasks 1 and 3 |
| CONTEXT | curation20-D-04: no compiled v2 publication | Tasks 1 and 3 |
| CONTEXT | curation20-D-05: protected paths out of scope | Tasks 1 and 3 |
| CONTEXT | curation20-D-06: no compile/smoke/typecheck/tests/build during context gathering | Planning obeyed; future commands only in Task 3 |
</source_audit>

<future_allowlist>
Future execution is not allowed to mutate anything until Task 1 passes. After Task 1 passes, the future allowlist is:

| Path | Future Disposition |
|---|---|
| `.planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md` | Required approval artifact before data mutation; create during execution only after explicit human approval is provided |
| `data/taxonomy/taxonomy-seed.v2.json` | Only allowed data mutation after approval: add descriptor `petitgrain` under family `citrus`, subfamily `citrus_fresh` |
| `data/taxonomy/descriptor_aliases.seed.json` | Explicitly unchanged for Option 1; `petit grain -> petitgrain` and `ylang ylang -> ylang_ylang` remain as-is |
| `data/compiled/v1` | Blocked |
| `data/compiled/v2` | Blocked; no official publication or refresh in Phase 20 |
| `data/inference` | Blocked |
| `src/cli/parse_args.ts` | Blocked |
| `scripts/check-safety-guards.sh` | Blocked |
| `src/package.json` | Blocked |
| `graphify-out/*` | Blocked |
</future_allowlist>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Persist explicit approval and freeze the Option 1 allowlist</name>
  <files>.planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md</files>
  <read_first>
    .planning/phases/20-alias-target-microcuration-execution/20-CONTEXT.md
    .planning/phases/20-alias-target-microcuration-execution/20-PREFLIGHT.md
    .planning/phases/20-alias-target-microcuration-execution/20-RESEARCH.md
    .planning/phases/20-alias-target-microcuration-execution/20-PATTERNS.md
    .planning/phases/20-alias-target-microcuration-execution/20-VALIDATION.md
    .planning/phases/19-taxonomy-v2-1-curation-planning/19-CLOSURE.md
    .planning/phases/19-taxonomy-v2-1-curation-planning/19-01-PLAN.md
  </read_first>
  <decision>Before any mutation, ask the user for explicit execution approval to create `.planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md` and proceed with Option 1 only.</decision>
  <action>Do not mutate `data/taxonomy/taxonomy-seed.v2.json` yet. If the user approves execution, create `20-FINAL-APPROVAL.md` with line-oriented fields: `approval_id: phase20-petitgrain-add-target-approval`, `manual_approval: approved`, `primary_disposition: promote_to_seed`, `family_id: citrus`, `subfamily_id: citrus_fresh`, `descriptor_id: petitgrain`, `alias_preserved: petit grain -> petitgrain`, `ylang_policy: accepted_exception_interim`, `rationale: Resolve corpus-backed absent alias target by adding canonical descriptor to seed truth`, `evidence: compiled v2 candidate frequency 52 in citrus_fresh; absent from seed v2; existing alias mapping already present`, `rollback_plan: pre-mutation snapshot of data/taxonomy/taxonomy-seed.v2.json and exact restore by removing only petitgrain`, and `publication_boundary: no official data/compiled/v2 publication in Phase 20`. Also record the allowlist exactly as: approval artifact plus `data/taxonomy/taxonomy-seed.v2.json` only; `descriptor_aliases.seed.json` unchanged; blocked paths include `data/compiled/v1`, `data/compiled/v2`, `data/inference`, `src/cli/parse_args.ts`, `scripts/check-safety-guards.sh`, `src/package.json`, and `graphify-out/*`. This implements CUR20-02, CUR20-03, CUR20-04, CUR20-06 and CUR20-07 while honoring curation20-D-01 through curation20-D-06.</action>
  <verify>
    <automated>MISSING — approval is intentionally human-gated; after approval, verify by inspecting `.planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md` for all required field names and exact values listed in this task before proceeding.</automated>
    <human-check>Confirm the artifact explicitly approves only `petitgrain` add_target, does not approve `ylang_ylang` add_target, and blocks official `data/compiled/v2` publication.</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md` exists only after explicit human approval.
    - The approval artifact contains `approval_id`, `manual_approval`, `primary_disposition`, `family_id`, `subfamily_id`, `descriptor_id`, `alias_preserved`, `ylang_policy`, `rationale`, `evidence`, `rollback_plan`, and `publication_boundary`.
    - `manual_approval` is exactly `approved`; `family_id` is exactly `citrus`; `subfamily_id` is exactly `citrus_fresh`; `descriptor_id` is exactly `petitgrain`.
    - The allowlist permits only `data/taxonomy/taxonomy-seed.v2.json` as a future data mutation and explicitly keeps `data/taxonomy/descriptor_aliases.seed.json` unchanged.
    - The artifact states that `ylang ylang -> ylang_ylang` remains `accepted_exception_interim` and requires separate approval for any add_target.
  </acceptance_criteria>
  <done>Mutation remains blocked until persisted approval exists with all required fields and the user has approved Option 1 execution only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the approved seed-only petitgrain add_target</name>
  <files>data/taxonomy/taxonomy-seed.v2.json</files>
  <read_first>
    .planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md
    .planning/phases/20-alias-target-microcuration-execution/20-RESEARCH.md
    .planning/phases/20-alias-target-microcuration-execution/20-PATTERNS.md
    data/taxonomy/taxonomy-seed.v2.json
    data/taxonomy/descriptor_aliases.seed.json
  </read_first>
  <behavior>
    - Before mutation, verify the approval artifact exists and contains the exact approval fields from Task 1.
    - Before mutation, capture the pre-mutation content or git blob of `data/taxonomy/taxonomy-seed.v2.json` for rollback.
    - After mutation, `families[id=citrus].subfamilies[id=citrus_fresh].descriptors` contains `petitgrain` exactly once.
    - After mutation, seed `version` remains `2.0.0`, descriptor casing remains lower snake-case ASCII, and the `citrus_fresh` descriptor list remains non-empty.
    - `data/taxonomy/descriptor_aliases.seed.json` has no diff.
  </behavior>
  <action>Only after Task 1 passes, run the future pre-mutation safety guard from `src` with `npm run safety:guard` to prove there is no pre-existing protected-path drift. Capture a rollback snapshot for `data/taxonomy/taxonomy-seed.v2.json` before editing. In `data/taxonomy/taxonomy-seed.v2.json`, find family `id: citrus`, then subfamily `id: citrus_fresh`, and append the descriptor string `petitgrain` to that subfamily's `descriptors` array after `grapefruit` while preserving the existing seed structure and `version: 2.0.0`. Do not edit `data/taxonomy/descriptor_aliases.seed.json`; the existing `petit grain -> petitgrain` alias must become target-valid through seed descriptor presence. Do not add `ylang_ylang` anywhere. Do not edit relation, accord, compiled, CLI, guard, package, test, or Graphify files. After the intentional seed mutation, do not treat `npm run safety:guard` `PROTECTED_DIFF` as an unexpected failure; instead perform an allowlist diff audit that permits only `data/taxonomy/taxonomy-seed.v2.json` plus the already-approved `20-FINAL-APPROVAL.md` planning artifact.</action>
  <verify>
    <automated>Future execution only: `cd src && npm run test -- src/tests/curation/taxonomy_seed_v2.test.ts` after approved mutation.</automated>
    <automated>Future execution only: from repo root, verify changed paths are limited to `.planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md` and `data/taxonomy/taxonomy-seed.v2.json` by inspecting `git diff --name-only`; no `grep -c` gate.</automated>
  </verify>
  <acceptance_criteria>
    - `petitgrain` appears exactly once across seed v2 descriptors and specifically under `citrus/citrus_fresh`.
    - `petitgrain` matches lower snake-case ASCII.
    - Seed `version` remains exactly `2.0.0`.
    - `citrus/citrus_fresh` exists and its descriptor array is not empty.
    - `data/taxonomy/descriptor_aliases.seed.json` has no diff.
    - `data/compiled/v1`, `data/compiled/v2`, `data/inference`, `src/cli/parse_args.ts`, `scripts/check-safety-guards.sh`, `src/package.json`, and `graphify-out/*` have no diff.
  </acceptance_criteria>
  <done>The only future data change is the approved `petitgrain` descriptor addition under `citrus/citrus_fresh`; aliases and all blocked paths remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate alias integrity, rollback path, and no-publication boundary</name>
  <files>data/taxonomy/taxonomy-seed.v2.json</files>
  <read_first>
    .planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md
    .planning/phases/20-alias-target-microcuration-execution/20-VALIDATION.md
    data/taxonomy/taxonomy-seed.v2.json
    data/taxonomy/descriptor_aliases.seed.json
    src/package.json
    src/cli/parse_args.ts
    scripts/check-safety-guards.sh
  </read_first>
  <behavior>
    - `petit grain -> petitgrain` passes alias target integrity through seed descriptor presence after Task 2.
    - `ylang ylang -> ylang_ylang` remains accepted through the existing legacy exception path and is not resolved by a seed mutation.
    - Rollback restores the exact pre-mutation seed state, leaves alias file unchanged, and does not regenerate official compiled artifacts.
    - Optional compile validation uses `/tmp` output only and verifies no official `data/compiled/v2` diff.
  </behavior>
  <action>Run future targeted validation only after Task 2 is complete. From `src`, run the alias integrity test for `petit grain -> petitgrain` and the preserved `ylang ylang -> ylang_ylang` exception, then run the seed integrity test and targeted normalization tests listed in `20-VALIDATION.md`. For rollback validation, restore the pre-mutation snapshot of `data/taxonomy/taxonomy-seed.v2.json` exactly; confirm `petitgrain` is removed from the seed, confirm `descriptor_aliases.seed.json` still contains both `petit grain -> petitgrain` and `ylang ylang -> ylang_ylang`, and run `npm run safety:guard` from `src` after rollback or when no protected diff remains. Do not rollback by regenerating official compiled artifacts. If compile validation is needed, first verify the built CLI path: either run from repo root with `node src/dist/cli/compile.js ... --out /tmp/taxonomy-phase20-v2-compile ...` using repo-root-relative paths, or run from `src` with `node dist/cli/compile.js ... --out /tmp/taxonomy-phase20-v2-compile ...` and adjust all data paths correctly. The compile command must use explicit `--seed`, `--aliases`, `--corpus`, `--relations`, `--accords`, `--noise`, `--out /tmp/taxonomy-phase20-v2-compile`, `--version 2.0.0`, and a fixed `--generated-at`; never use default `npm run compile` because defaults target official `data/compiled/v2`. After optional compile, verify `data/compiled/v2` has no diff and do not commit `/tmp` output.</action>
  <verify>
    <automated>Future execution only: `cd src && npm run test -- src/tests/curation/alias_seed_v2.test.ts src/tests/curation/taxonomy_seed_v2.test.ts src/tests/normalization.test.ts src/tests/normalization/index.test.ts src/tests/normalization/separators.test.ts src/tests/normalization/punctuation.test.ts`.</automated>
    <automated>Future rollback only: `cd src && npm run safety:guard` after exact restore/no-protected-diff state.</automated>
    <automated>Future optional compile only: explicit CLI compile to `/tmp/taxonomy-phase20-v2-compile` after path verification, followed by inspection that `data/compiled/v2` has no diff.</automated>
  </verify>
  <acceptance_criteria>
    - Alias tests pass with `petit grain -> petitgrain` validated by seed descriptor presence and `ylang ylang -> ylang_ylang` preserved as legacy `accepted_exception_interim`.
    - Seed tests pass with seed version `2.0.0`, no duplicate descriptors, lower snake-case ASCII descriptors, and no empty subfamilies.
    - Rollback instructions have been exercised or documented with exact restore of only `data/taxonomy/taxonomy-seed.v2.json`; aliases remain unchanged.
    - Safety guard is used before mutation and after rollback/no-protected-diff state; post-mutation `PROTECTED_DIFF` is handled by allowlist audit.
    - No official `data/compiled/v2` publication, refresh, or diff exists; any compile output is only under `/tmp`.
  </acceptance_criteria>
  <done>Future execution has validated integrity, rollback, and publication boundaries while preserving Option 1 scope and all blocked paths.</done>
</task>

</tasks>

<must_haves>
Goal-backward truths: future execution cannot start mutation without persisted approval; the only seed change is `petitgrain` under `citrus/citrus_fresh`; `ylang_ylang` is not added; aliases remain unchanged; rollback restores the seed exactly; targeted tests and safety guard usage are defined; official compiled artifacts remain unpublished.

Required artifacts: `.planning/phases/20-alias-target-microcuration-execution/20-FINAL-APPROVAL.md` in future execution; `data/taxonomy/taxonomy-seed.v2.json` with exactly one future descriptor addition after approval; no diff in `data/taxonomy/descriptor_aliases.seed.json` for Option 1.

Key links: approval fields bind to the seed mutation; alias target integrity binds `petit grain -> petitgrain` to seed descriptor presence; safety guard results bind to allowlist diff audit after intentional protected-path mutation.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Human approval -> protected data mutation | Manual approval controls whether the executor may edit seed data. |
| Seed data -> compiled artifacts | Seed mutation could affect official compiled artifacts if default compile is used. |
| Alias policy -> curation scope | Legacy alias exception policy could be broadened accidentally to `ylang_ylang` add_target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-20-01 | Tampering | `data/taxonomy/taxonomy-seed.v2.json` | mitigate | Block mutation until `20-FINAL-APPROVAL.md` exists; require pre-mutation snapshot and allowlist audit. |
| T-20-02 | Tampering | `data/compiled/v2` | mitigate | Prohibit default compile and official publication; allow only explicit `/tmp` compile after path verification. |
| T-20-03 | Tampering | `data/taxonomy/descriptor_aliases.seed.json` | mitigate | Option 1 requires zero alias diff; validate `petit grain -> petitgrain` and `ylang ylang -> ylang_ylang` remain unchanged. |
| T-20-04 | Elevation of Scope | `ylang_ylang` curation | mitigate | Preserve as `accepted_exception_interim`; separate explicit approval required for any `ylang_ylang` add_target. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installation is planned or allowed for Phase 20. |
</threat_model>

<verification>
Planning-time verification for this plan: confirm frontmatter includes all CUR20-01 through CUR20-07 requirements; confirm every task has `<read_first>` and `<acceptance_criteria>`; confirm this plan does not itself authorize mutation; confirm allowlist, approval, rollback, validations, and no-publication boundary are explicit.

Future execution verification only: after explicit approval and mutation, run targeted alias, seed, and normalization tests from `src`; use safety guard before mutation and after rollback/no-protected-diff state; use allowlist diff audit after intentional seed mutation; optionally compile only to `/tmp` after CLI path verification; never publish official `data/compiled/v2` in Phase 20.
</verification>

<success_criteria>
- `20-FINAL-APPROVAL.md` is required as a future gate and is not created by planning alone.
- Future allowed data mutation is exactly `data/taxonomy/taxonomy-seed.v2.json` adding `petitgrain` under `citrus/citrus_fresh` after approval.
- `data/taxonomy/descriptor_aliases.seed.json` remains unchanged for Option 1.
- `data/compiled/v1`, `data/compiled/v2`, `data/inference`, `src/cli/parse_args.ts`, `scripts/check-safety-guards.sh`, `src/package.json`, and `graphify-out/*` remain blocked.
- Rollback can restore the seed exactly, preserve aliases, run safety guard after rollback, and never regenerate official compiled artifacts.
- `petit grain -> petitgrain` becomes target-valid by seed descriptor presence after future mutation; `ylang ylang -> ylang_ylang` remains `accepted_exception_interim`.
</success_criteria>

<output>
Create `.planning/phases/20-alias-target-microcuration-execution/20-01-SUMMARY.md` when future execution completes.
</output>
