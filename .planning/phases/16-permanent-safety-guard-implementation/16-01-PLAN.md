---
phase: 16-permanent-safety-guard-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/check-safety-guards.sh]
autonomous: true
requirements: [GUARD16-01, GUARD16-02, GUARD16-03, GUARD16-04]
user_setup: []

must_haves:
  truths:
    - "Running ./scripts/check-safety-guards.sh prints PASS and exits 0 when no staged Graphify or protected-path violations exist."
    - "Staged graphify-out paths are blocked with GRAPHIFY_STAGED label while dirty working-tree graphify-out remains allowed."
    - "Staged protected paths and protected working-tree diffs are blocked with PROTECTED_PATH_STAGED and PROTECTED_DIFF labels."
    - "All violations are reported in one run (report_all) before exiting 1."
    - "The guard is non-mutating (no changes to repo state)."
  artifacts:
    - path: "scripts/check-safety-guards.sh"
      provides: "Non-mutating local guard script for staged Graphify and protected paths"
      contains: "graphify-out"
  key_links:
    - from: "scripts/check-safety-guards.sh"
      to: "git diff --cached --name-only -- graphify-out"
      via: "Graphify staged guard command"
      pattern: "git diff --cached --name-only -- graphify-out"
    - from: "scripts/check-safety-guards.sh"
      to: "git diff --cached --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts"
      via: "Protected paths staged guard command"
      pattern: "git diff --cached --name-only -- data/taxonomy"
    - from: "scripts/check-safety-guards.sh"
      to: "git diff --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts"
      via: "Protected diff guard command"
      pattern: "git diff --name-only -- data/taxonomy"
---

<objective>
Implement a permanent non-mutating local guard script that blocks staged Graphify and protected-path changes while allowing dirty Graphify in the working tree, then validate PASS and failure scenarios per Phase 16 decisions (GUARD16-D-01..D-33).

Purpose: Convert Phase 15 proof-only guards into a durable local script without hooks, package scripts, or CI.
Output: scripts/check-safety-guards.sh with report_all behavior and validated local proof evidence.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/16-permanent-safety-guard-implementation/16-CONTEXT.md
@.planning/phases/16-permanent-safety-guard-implementation/16-RESEARCH.md
@.planning/phases/16-permanent-safety-guard-implementation/16-PATTERNS.md
@.planning/phases/16-permanent-safety-guard-implementation/16-VALIDATION.md

@.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-VALIDATION.md
@.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-01-SUMMARY.md
@.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement non-mutating guard script with report_all output</name>
  <files>scripts/check-safety-guards.sh</files>
  <read_first>
    - .planning/phases/16-permanent-safety-guard-implementation/16-CONTEXT.md
    - .planning/phases/16-permanent-safety-guard-implementation/16-RESEARCH.md
    - .planning/phases/16-permanent-safety-guard-implementation/16-PATTERNS.md
    - .agents/hooks/gsd-phase-boundary.sh
    - .agents/hooks/gsd-validate-commit.sh
    - scripts/check-safety-guards.sh
  </read_first>
  <action>
    Create scripts/check-safety-guards.sh as a bash script per GUARD16-D-01..D-27 and GUARD16-D-33: include shebang #!/usr/bin/env bash, resolve repo root via `git rev-parse --show-toplevel` and cd to it (if that command fails, print NOT_GIT_REPO to stderr and exit 1 per GUARD16-D-22), then run all three git commands exactly as specified with explicit paths and `--` (no arrays). Implement report_all aggregation: collect outputs for (1) `git diff --cached --name-only -- graphify-out`, (2) `git diff --cached --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts`, and (3) `git diff --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts`, and only exit 1 after running all checks if any outputs are non-empty (GUARD16-D-17). For each non-empty output, print label + path lines to stderr using required labels GRAPHIFY_STAGED, PROTECTED_PATH_STAGED, PROTECTED_DIFF (GUARD16-D-16, D-19) and add a single short policy line to stderr: "Policy: non-mutating guard; do not clean, revert, regenerate, stage, unstage, or commit protected paths or graphify-out/* in this plan." Ensure PASS is printed to stdout on success (GUARD16-D-18) and exit codes are 0/1 only (GUARD16-D-15). Do not mutate any repo files, do not add package scripts, hooks, unit tests, or CI integration (GUARD16-D-04..D-05, D-09, D-33). Mark the script executable (chmod +x) but retain bash invocation support (GUARD16-D-06..D-08). Use graphify-out pathspec exactly (no glob) and do not check working-tree graphify-out dirtiness (GUARD16-D-13..D-14, D-26).
  </action>
  <acceptance_criteria>
    - scripts/check-safety-guards.sh exists and starts with `#!/usr/bin/env bash`.
    - scripts/check-safety-guards.sh contains `git rev-parse --show-toplevel` and exits with NOT_GIT_REPO on stderr when not in a git repo.
    - scripts/check-safety-guards.sh contains the exact commands:
      - `git diff --cached --name-only -- graphify-out`
      - `git diff --cached --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts`
      - `git diff --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts`
    - On a clean boundary run, stdout includes `PASS` and exit code is 0.
    - On violations, stderr includes label lines with GRAPHIFY_STAGED, PROTECTED_PATH_STAGED, or PROTECTED_DIFF and the policy line exactly: "Policy: non-mutating guard; do not clean, revert, regenerate, stage, unstage, or commit protected paths or graphify-out/* in this plan."
    - Script does not include commands that mutate git state (no git add, reset, checkout, clean, or rm).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "test -f scripts/check-safety-guards.sh && grep -n 'git diff --cached --name-only -- graphify-out' scripts/check-safety-guards.sh && grep -n 'git diff --cached --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts' scripts/check-safety-guards.sh && grep -n 'git diff --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts' scripts/check-safety-guards.sh"</automated>
  </verify>
  <done>Guard script exists at scripts/check-safety-guards.sh with required commands, labels, PASS output, report_all behavior, and non-mutating policy.</done>
</task>

<task type="auto">
  <name>Task 2: Validate PASS, non-mutation, and /tmp failure simulations</name>
  <files>scripts/check-safety-guards.sh</files>
  <read_first>
    - .planning/phases/16-permanent-safety-guard-implementation/16-VALIDATION.md
    - .planning/phases/16-permanent-safety-guard-implementation/16-CONTEXT.md
    - scripts/check-safety-guards.sh
  </read_first>
  <action>
    Run the real-repo proof required by GUARD16-D-28..D-30: capture `git status --short` before and after running `./scripts/check-safety-guards.sh` and `bash scripts/check-safety-guards.sh`, storing the before/after outputs under /tmp/opencode/ for comparison. Confirm both invocations print PASS on stdout and exit 0, and confirm the before/after status outputs are identical to prove non-mutation. Then create a disposable /tmp git repo and copy scripts/check-safety-guards.sh into it (GUARD16-D-31). In that repo, simulate and run the script for: (1) GRAPHIFY_STAGED by staging a file under graphify-out/, (2) PROTECTED_PATH_STAGED by staging a file under data/taxonomy/ or data/inference/, (3) PROTECTED_DIFF by modifying a protected path without staging it, and (4) a report_all case where graphify-out is staged, a protected path is staged, and a separate protected path has an unstaged diff, confirming all three labels appear in stderr from one run (GUARD16-D-32). For each case, capture stderr and exit code to confirm failures exit 1 and include the required labels and policy line. Keep the script non-mutating in the real repo; /tmp repo resets for simulation are allowed.
  </action>
  <acceptance_criteria>
    - Both `./scripts/check-safety-guards.sh` and `bash scripts/check-safety-guards.sh` exit 0.
    - stdout from each invocation includes `PASS` and stderr is empty in the real repo baseline.
    - `git status --short` before and after the two runs is identical (no new changes).
    - GRAPHIFY_STAGED simulation exits 1 and stderr contains `GRAPHIFY_STAGED` with the staged graphify-out path and the policy line.
    - PROTECTED_PATH_STAGED simulation exits 1 and stderr contains `PROTECTED_PATH_STAGED` with the staged protected path and the policy line.
    - PROTECTED_DIFF simulation exits 1 and stderr contains `PROTECTED_DIFF` with the modified protected path and the policy line.
    - report_all simulation exits 1 and stderr contains all three labels (GRAPHIFY_STAGED, PROTECTED_PATH_STAGED, PROTECTED_DIFF) in a single run.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "set -e; mkdir -p /tmp/opencode; git status --short > /tmp/opencode/guard-status-before.txt; ./scripts/check-safety-guards.sh; bash scripts/check-safety-guards.sh; git status --short > /tmp/opencode/guard-status-after.txt; diff -u /tmp/opencode/guard-status-before.txt /tmp/opencode/guard-status-after.txt; TMP_DIR=$(mktemp -d /tmp/opencode/guard-sim.XXXX); cd \"$TMP_DIR\"; git init -q; mkdir -p scripts graphify-out data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli; cp /home/jlima/Projetos/TaxonomySystem/scripts/check-safety-guards.sh scripts/; git add scripts/check-safety-guards.sh && git commit -q -m 'init'; touch graphify-out/test.txt; git add graphify-out/test.txt; ./scripts/check-safety-guards.sh 1>/tmp/opencode/guard-sim-graphify.out 2>/tmp/opencode/guard-sim-graphify.err || true; git reset -q; rm -f graphify-out/test.txt; touch data/taxonomy/test.json; git add data/taxonomy/test.json; ./scripts/check-safety-guards.sh 1>/tmp/opencode/guard-sim-protected-staged.out 2>/tmp/opencode/guard-sim-protected-staged.err || true; git reset -q; rm -f data/taxonomy/test.json; touch data/inference/unstaged.json; ./scripts/check-safety-guards.sh 1>/tmp/opencode/guard-sim-protected-diff.out 2>/tmp/opencode/guard-sim-protected-diff.err || true; git reset -q; rm -f data/inference/unstaged.json; touch data/taxonomy/staged.json; git add data/taxonomy/staged.json; touch data/inference/unstaged.json; touch graphify-out/staged.txt; git add graphify-out/staged.txt; ./scripts/check-safety-guards.sh 1>/tmp/opencode/guard-sim-report-all.out 2>/tmp/opencode/guard-sim-report-all.err || true; grep -q 'GRAPHIFY_STAGED' /tmp/opencode/guard-sim-graphify.err; grep -q 'PROTECTED_PATH_STAGED' /tmp/opencode/guard-sim-protected-staged.err; grep -q 'PROTECTED_DIFF' /tmp/opencode/guard-sim-protected-diff.err; grep -q 'GRAPHIFY_STAGED' /tmp/opencode/guard-sim-report-all.err && grep -q 'PROTECTED_PATH_STAGED' /tmp/opencode/guard-sim-report-all.err && grep -q 'PROTECTED_DIFF' /tmp/opencode/guard-sim-report-all.err"</automated>
  </verify>
  <done>Real repo PASS + non-mutation proof and /tmp simulations cover GRAPHIFY_STAGED, PROTECTED_PATH_STAGED, PROTECTED_DIFF, and report_all output per GUARD16-D-28..D-32.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → git repo | Untrusted local state crosses into guard decisions via git diff output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | scripts/check-safety-guards.sh | mitigate | Block staged graphify-out and protected paths using git diff --cached --name-only per GUARD16-D-10..D-12. |
| T-16-02 | Repudiation | report_all output | mitigate | Report all violations in one run and emit labels + paths to stderr (GUARD16-D-16..D-19). |
| T-16-03 | Tampering | protected working-tree diff | mitigate | Block any working-tree diff under protected paths using git diff --name-only (GUARD16-D-12, D-25). |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `./scripts/check-safety-guards.sh` and `bash scripts/check-safety-guards.sh` both exit 0 and print PASS in the real repo.
- /tmp simulation stderr includes GRAPHIFY_STAGED, PROTECTED_PATH_STAGED, PROTECTED_DIFF labels and policy line; report_all case prints all three labels.
- `git status --short` is identical before and after guard runs (non-mutation proof).
</verification>

<success_criteria>
- scripts/check-safety-guards.sh implements the exact guard commands and labels from GUARD16-D-01..D-27 without mutation.
- Staged Graphify paths and protected paths are blocked, while dirty working-tree graphify-out remains allowed.
- Validation proof covers real repo PASS, non-mutation, and /tmp failure simulations per GUARD16-D-28..D-32.
</success_criteria>

<output>
Create `.planning/phases/16-permanent-safety-guard-implementation/16-01-SUMMARY.md` when done
</output>
