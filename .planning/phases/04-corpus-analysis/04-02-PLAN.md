---
phase: 04-corpus-analysis
plan_id: 04-02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/analyzer/similarity/levenshtein.ts
  - src/analyzer/similarity/token_overlap.ts
  - src/analyzer/alias_candidates.ts
  - src/analyzer/analyze_corpus.ts
  - src/analyzer/export.ts
  - src/analyzer/index.ts
  - src/tests/analysis/similarity.test.ts
  - src/tests/analysis/alias_candidates.test.ts
  - src/tests/fixtures/analysis/camomile_corpus.json
  - src/tests/fixtures/analysis/substring_trap_corpus.json
  - src/tests/fixtures/analysis/seed_excluded_corpus.json
autonomous: true
requirements:
  - ANAL-03
  - ANAL-04
must_haves:
  truths:
    - "`levenshteinDistance(a, b)` retorna a edit-distance clássica e `levenshteinSimilarity(a, b)` retorna `1 - d / max(|a|, |b|)` em `[0, 1]`."
    - "`tokenJaccard(a, b)` retorna `|A ∩ B| / |A ∪ B|` sobre os tokens obtidos por `split('_')`."
    - "`findAliasCandidates(frequency, options)` produz `readonly AliasCandidate[]` ordenado por score desc + lex `(a, b)` como tiebreaker; defaults `minFrequency = 2`, `minScore = 0.90`, `substringRejectScore = 0.97`, `tokenOverlapFloor = 0.5`."
    - "Caso canônico do ROADMAP `camomile↔chamomile` (score ≈ 0.889) é detectado quando o caller passa `{ minScore: 0.85 }`; com defaults conservadores não passa, e isso é testado explicitamente nos dois sentidos."
    - "`rose↔rosewood` é rejeitado pelo gate substring-only quando score < 0.97; `lily_of_valley↔lily_of_the_valley` é admitido pelo gate token-Jaccard ≥ 0.5."
    - "Pares já cobertos por `descriptor_aliases.seed.json` (após normalização) são excluídos do output."
    - "`analyzeCorpus(corpus, { aliasCandidates: { ... } })` agora popula `aliasCandidates` real (não mais placeholder `[]`); `writeAnalysisArtifacts` escreve `alias_candidates.v1.json` byte-determinístico."
  artifacts:
    - path: src/analyzer/similarity/levenshtein.ts
      provides: "levenshteinDistance + levenshteinSimilarity (two-row DP com Uint16Array)"
    - path: src/analyzer/similarity/token_overlap.ts
      provides: "tokenJaccard"
    - path: src/analyzer/alias_candidates.ts
      provides: "findAliasCandidates + AliasCandidateOptions + helpers privados (pickCanonical, buildDescriptorToFamilies, buildSeedPairSet, shouldEmitCandidate, isSubstringOnly) + UPPER_SNAKE_CASE defaults"
    - path: src/analyzer/analyze_corpus.ts
      provides: "AnalyzeCorpusOptions com AliasCandidateOptions real; orquestrador chama findAliasCandidates quando options.aliasCandidates está definido"
    - path: src/analyzer/export.ts
      provides: "exportAliasCandidatesJson + writeAnalysisArtifacts agora inclui o 3º arquivo"
    - path: src/analyzer/index.ts
      provides: "Barrel atualizado com levenshteinDistance, levenshteinSimilarity, tokenJaccard, findAliasCandidates, exportAliasCandidatesJson"
  key_links:
    - from: "src/analyzer/alias_candidates.ts"
      to: "src/analyzer/similarity/levenshtein.ts + src/analyzer/similarity/token_overlap.ts"
      via: "shouldEmitCandidate combina levenshteinSimilarity (primário) com tokenJaccard (gate multi-token) e isSubstringOnly (anti-substring)"
      pattern: "levenshteinSimilarity\\(|tokenJaccard\\(|isSubstringOnly\\("
    - from: "src/analyzer/alias_candidates.ts"
      to: "src/loader/alias_loader.ts (via options injection)"
      via: "buildSeedPairSet normaliza o aliasSeed e exclui pares já cobertos"
      pattern: "buildSeedPairSet\\("
    - from: "src/analyzer/analyze_corpus.ts"
      to: "src/analyzer/alias_candidates.ts"
      via: "options?.aliasCandidates ? findAliasCandidates(frequency, options.aliasCandidates) : []"
      pattern: "findAliasCandidates\\("
    - from: "src/analyzer/export.ts"
      to: "src/types/analysis.ts (AliasCandidate)"
      via: "exportAliasCandidatesJson serializa { version: 1, candidates: AliasCandidate[] }"
      pattern: "exportAliasCandidatesJson"
---

<objective>
Entregar a camada de detecção lexical de alias candidates sobre o `FrequencyMap` produzido em 04-01: primitivos de similaridade (Levenshtein normalizado + token Jaccard), combiner com gates de precisão (substring-only rejection + token-overlap floor), seed-aware exclusion e canonical-direction picker (`seed > frequency > lex`), tudo orquestrado por `findAliasCandidates(frequency, options)` e wired no `analyzeCorpus` orchestrator. Inclui o 3º exporter determinístico (`alias_candidates.v1.json`) e a atualização do barrel.

Esta plan cobre ANAL-03 (string similarity algorithm) e ANAL-04 (alias detector) da REQUIREMENTS.md, satisfaz o success criterion 2 do ROADMAP Phase 4 ("Levenshtein/Jaro-Winkler identifica `camomile↔chamomile`") via threshold configurável, e fecha a phase 4.

**Output:** módulos puros em `src/analyzer/similarity/` e `src/analyzer/alias_candidates.ts`, fixtures cobrindo casos canônicos + substring trap + seed exclusion, e os 2 arquivos de teste restantes do per-task verification map de `04-VALIDATION.md`.
</objective>

<truths>
**Locked decisions consumidas:**

- **ANAL-D-08** — Métrica primária = **normalized Levenshtein similarity** (`1 - distance / max(len)`). Jaro-Winkler NÃO é implementado (RESEARCH §Por que Jaro-Winkler é mencionado…); ROADMAP menciona "Levenshtein OU Jaro-Winkler", ANAL-D-08 trava Levenshtein.
- **ANAL-D-09** — Descriptors multi-token (snake_case com ≥ 2 segmentos) DEVEM combinar dois sinais: whole-string Levenshtein normalizado E token-level Jaccard sobre split em `_`.
- **ANAL-D-10** — Precision over recall. Substring containment standalone NÃO é sinal de similaridade — pares estruturalmente diferentes que apenas compartilham substring são REJEITADOS pelo gate (`isSubstringOnly && score < substringRejectScore`).
- **ANAL-D-11** — Defaults high-precision: `minScore = 0.90`. Thresholds são configuráveis. Output são apenas SUGESTÕES — NUNCA auto-merge no seed.
- **ANAL-D-15** — Sort do output: score DESC + `(a, b)` lex como tiebreaker. Exports byte-determinísticos.
- **ANAL-D-16** — Pool de comparison pairwise: apenas descriptors com `frequency >= options.minFrequency` (default `2`). Aplicado APENAS na suggestion generation, NUNCA na contagem base de frequência/co-ocorrência (que vive em 04-01).
- **ANAL-D-13** — APIs retornam `readonly` em memória. `findAliasCandidates` retorna `readonly AliasCandidate[]`; persistência via `exportAliasCandidatesJson` é OPCIONAL.
- **ANAL-D-14** — `alias_candidates.v1.json` em `data/analysis/` com versão explícita no nome.
- **REQ ANAL-03** — String similarity algorithm implementado (Levenshtein normalizado + token Jaccard).
- **REQ ANAL-04** — Alias detector identifica candidatos usando string similarity (com pool freq ≥ 2, gates de precisão, exclusão de pares no seed, output ordenado).
- **ROADMAP Success Criterion 2** — "Algoritmo de Levenshtein identifica `camomile↔chamomile`" → satisfeito **com threshold customizado** (`minScore: 0.85`); documentado explicitamente como tensão consciente com ANAL-D-11 (defaults conservadores).

**Claude's Discretion resolvida aqui:**
- **Output schema de AliasCandidate** — definido em `src/types/analysis.ts` em 04-01 com os campos da RESEARCH §Output schema: `{ a, b, score, algo, frequencies, token_overlap?, families?, suggested_canonical?, cross_family? }`. Esta plan apenas popula.
- **algo discriminant** — `'lev_norm' | 'lev_norm+tokens'` (curtos, explicitamente nomeiam qual gate ativou).
- **Canonical direction** — hierarquia `seed > frequency > lex` implementada em `pickCanonical(a, b, freqA, freqB, aliasSeed?, taxonomySeedDescriptors?)`.
- **substringRejectScore default** — `0.97` (RESEARCH §Combiner rule; calibrado para deixar passar plurais/derivações triviais e rejeitar substring-only). Configurável.
- **tokenOverlapFloor default** — `0.5` (RESEARCH §Token overlap; aplicado APENAS quando ambos descriptors têm ≥ 2 tokens).
- **Length bucketing pre-filter** — implementado (RESEARCH §Length bucketing); reduz comparações de `O(N²)` brutos para `O(N²)`-com-corte por bucket. Mandatório para `N > ~1500`.
- **Helpers derivados (PMI, NPMI)** — NÃO expostos (deferido para Phase 5).

**Open Questions encaminhadas (RESEARCH §Risks & Open Questions):**
- **OQ1: Camomile↔chamomile default 0.90 vs success criterion** — RESOLVIDO: defaults ficam em 0.90 (ANAL-D-11 explicit); ROADMAP success criterion satisfeito com threshold customizado `{ minScore: 0.85 }`, testado em ambos os sentidos (default não-detecta, custom detecta) e DOCUMENTADO no JSDoc de `findAliasCandidates` como trade-off consciente.
- **OQ2: substringRejectScore 0.97** — RESOLVIDO: default `0.97`, configurável via options. Testes cobrem `rose↔rosewood` (rejeitado: score=0.5 < 0.97 E substring), `oakmoss↔oakmosses` (rejeitado defensivamente: score≈0.875 < 0.97 E substring — comentário no teste explica que Phase 3 singularizer já deveria ter colapsado).
- **OQ3: PMI/NPMI/Jaccard sobre o sparse map** — RESOLVIDO: NÃO expostos na Phase 4. Phase 5 implementa em cima de `FrequencyMap` + `CoOccurrenceMap` sem retrabalho.
</truths>

<threat_model>
Phase 4 não introduz novas trust boundaries. Esta plan opera puramente sobre dados in-memory já validados pelos loaders existentes (Phase 2) e pelo normalizer (Phase 3).

| Threat ID  | Category   | Component                                | Disposition | Mitigation Plan |
|------------|------------|------------------------------------------|-------------|-----------------|
| T-04-03    | DoS        | `findAliasCandidates` em pool freq ≥ 2 muito grande | accept (low) | Length-bucketing pre-filter (RESEARCH §Length bucketing) reduz pairwise comparisons de `O(N²)` brutos para ~10–20% disso para `minScore = 0.90`. Worst case documentado em RESEARCH (`N = 5000` → ~2.5M comparações, ~10s). Stress test do alias step (com 5k materiais sintéticos, perfil realista) detecta regressões. Sem cap defensivo nesta plan; revisitar apenas se profiling do corpus real mostrar abuso. |
| T-04-04    | Tampering  | `exportAliasCandidatesJson` recebendo path malicioso do caller | accept (low) | Mesma postura de T-04-02 (Plan 04-01): caller-side validation, JSDoc documenta path confiado, sem manipulação de `..`. |

**Block-on threshold:** `high`. Nenhum threat high-severity identificado.
</threat_model>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corpus-analysis/04-CONTEXT.md
@.planning/phases/04-corpus-analysis/04-RESEARCH.md
@.planning/phases/04-corpus-analysis/04-PATTERNS.md
@.planning/phases/04-corpus-analysis/04-VALIDATION.md
@.planning/phases/04-corpus-analysis/04-01-PLAN.md
@src/types/analysis.ts
@src/types/seed.ts
@src/types/alias.ts
@src/analyzer/index.ts
@src/analyzer/analyze_corpus.ts
@src/analyzer/frequency.ts
@src/analyzer/export.ts
@src/normalizer/normalize_descriptor.ts
@src/normalizer/collapse_underscores.ts
@src/normalizer/normalize_case.ts
@src/normalizer/singularize.ts
@src/loader/alias_loader.ts
@src/loader/alias_validator.ts
@src/loader/seed_loader.ts
@src/tests/normalization/case.test.ts
@src/tests/normalization/convergence.test.ts
@src/tests/corpus_loader.test.ts
@data/taxonomy/descriptor_aliases.seed.json
</context>

<interfaces>
<!-- Contracts the executor consumes from 04-01 (already shipped by wave 1). -->

From `src/types/analysis.ts` (created in 04-01-01):

```typescript
export type FrequencyMap = ReadonlyMap<string, number>

export type AliasCandidate = {
  readonly a: string
  readonly b: string
  readonly score: number
  readonly algo: 'lev_norm' | 'lev_norm+tokens'
  readonly frequencies: { readonly a: number; readonly b: number }
  readonly token_overlap?: number
  readonly families?: { readonly a: readonly string[]; readonly b: readonly string[] }
  readonly suggested_canonical?: string
  readonly cross_family?: boolean
}

export type CorpusAnalysis = {
  readonly frequency: FrequencyMap
  readonly cooccurrence: CoOccurrenceMap
  readonly aliasCandidates: readonly AliasCandidate[]
}
```

From `src/analyzer/analyze_corpus.ts` (created in 04-01-03):

```typescript
// AnalyzeCorpusOptions currently has placeholder `aliasCandidates?: unknown`
// This plan tightens it to `aliasCandidates?: AliasCandidateOptions`.
export const analyzeCorpus = (corpus, options?) => CorpusAnalysis
```

From `src/loader/alias_loader.ts` (Phase 2):

```typescript
export const loadAliasSeed = (path: string): Promise<DescriptorAliasSeed>
// DescriptorAliasSeed = Record<string, string> (raw → canonical mapping)
```

From `src/loader/seed_loader.ts` (Phase 2):

```typescript
export const loadTaxonomySeed = (path: string): Promise<TaxonomySeed>
// TaxonomySeed has families[].subfamilies[].descriptors[]
```

From `src/normalizer/normalize_descriptor.ts` (Phase 3): idempotent, canonical charset `^[a-z0-9_]+$`, empty input may produce `""`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <id>04-02-01</id>
  <title>Wave 0 — test stubs (similarity, alias_candidates) e 3 fixtures canônicos</title>
  <files>
    src/tests/analysis/similarity.test.ts,
    src/tests/analysis/alias_candidates.test.ts,
    src/tests/fixtures/analysis/camomile_corpus.json,
    src/tests/fixtures/analysis/substring_trap_corpus.json,
    src/tests/fixtures/analysis/seed_excluded_corpus.json
  </files>
  <read_first>
    src/tests/analysis/frequency.test.ts,
    src/tests/analysis/cooccurrence.test.ts,
    src/tests/normalization/case.test.ts,
    src/tests/normalization/convergence.test.ts,
    src/tests/corpus_loader.test.ts,
    src/tests/fixtures/analysis/tiny_corpus.json,
    src/tests/fixtures/corpus_sample.json,
    data/taxonomy/descriptor_aliases.seed.json,
    .planning/phases/04-corpus-analysis/04-PATTERNS.md,
    .planning/phases/04-corpus-analysis/04-VALIDATION.md,
    .planning/phases/04-corpus-analysis/04-RESEARCH.md
  </read_first>
  <behavior>
    - `camomile_corpus.json`: 6–10 materiais estruturalmente mínimos contendo `"camomile"` em ≥ 2 materiais E `"chamomile"` em ≥ 2 materiais (garante ambos passarem o pool `frequency >= 2`); inclui também outros descriptors para reduzir ratio sinal/ruído e validar que o algoritmo encontra o par mesmo entre ruído.
    - `substring_trap_corpus.json`: materiais com `"rose"`, `"rosewood"`, `"rosemary"`, `"sandalwood"`, `"sandal_wood"` repetidos ≥ 2× cada — exercita ANAL-D-10 (substring containment não é sinal).
    - `seed_excluded_corpus.json`: usa pares que JÁ existem em `data/taxonomy/descriptor_aliases.seed.json` (consultar o arquivo atual; após normalizeDescriptor) — pelo menos um par `(raw, canonical)` aparece nesses materiais com frequência ≥ 2 em ambos, e o teste assertará que o par NÃO aparece no output quando `aliasSeed` é injetado.
    - `similarity.test.ts` stub: 3 blocos `describe` (`levenshteinDistance`, `levenshteinSimilarity`, `tokenJaccard`) com `it.todo(...)` ou body trivial; serão preenchidos em 04-02-02.
    - `alias_candidates.test.ts` stub: 1 bloco `describe('findAliasCandidates', ...)` com `it.todo(...)` para cada cenário canônico (camomile detection com `minScore: 0.85`; default `minScore: 0.90` NÃO detecta camomile; substring rejection; seed exclusion; sort order; canonical direction `seed > freq > lex`).
  </behavior>
  <action>
    Criar `src/tests/fixtures/analysis/camomile_corpus.json` hand-authored em 2-space indent, contendo entre 6 e 10 entradas no formato `{ "olfactory": { "descriptors": [...] } }`. Garantir `"camomile"` em ≥ 2 materiais e `"chamomile"` em ≥ 2 materiais distintos para que ambos cleared o filter `frequency >= 2`. Comentário top-of-file (via key `_comment` ignorada pelo runtime) anota o expected behavior: "ao rodar findAliasCandidates com minFrequency=2 e minScore=0.85, o output deve conter o par (camomile, chamomile) com score≈0.889 e algo='lev_norm'".

    Criar `src/tests/fixtures/analysis/substring_trap_corpus.json` contendo `["rose", "rosewood"]`, `["rosemary"]`, `["sandalwood", "wood"]`, `["sandal_wood"]` etc., cada descriptor aparecendo ≥ 2× no corpus. Comentário documenta: "rose↔rosewood deve ser rejeitado pelo gate isSubstringOnly && score < 0.97; sandalwood↔sandal_wood é caso interessante de plural-like que pode passar pelo combiner — anotar no teste o comportamento esperado".

    Criar `src/tests/fixtures/analysis/seed_excluded_corpus.json` usando pares que existem em `data/taxonomy/descriptor_aliases.seed.json` após normalização. Ler o arquivo do seed (apenas 7 entradas hoje, formato raw→canonical) e escolher 1–2 pares cujas formas normalizadas (`normalizeDescriptor(raw)` e `normalizeDescriptor(canonical)`) sejam ambas não-vazias e distintas. Incluir esses descriptors ≥ 2× no corpus.

    Criar `src/tests/analysis/similarity.test.ts` com 3 blocos `describe` vazios (analog: `src/tests/normalization/case.test.ts` line 4 — smallest unit-test scaffold). Cada bloco tem 1–3 `it.todo(...)` listando os casos do per-task verification map (RESEARCH §Validation Architecture):
      - `levenshteinDistance`: identity, empty inputs, classic case `camomile↔chamomile` distance = 1.
      - `levenshteinSimilarity`: bounds [0,1], `(empty, empty) → 1`, `(rose, rose) → 1`, `(rose, rosewood) → 0.5`, `(camomile, chamomile) → ≈0.889`.
      - `tokenJaccard`: `(lily_of_the_valley, lily_of_valley) → 0.75`, `(rose, rosewood) → 0` (single-token sets disjoint).

    Criar `src/tests/analysis/alias_candidates.test.ts` com 1 bloco `describe('findAliasCandidates', ...)` contendo `it.todo(...)` para cada cenário:
      - "detecta camomile↔chamomile com minScore=0.85 e minFrequency=2"
      - "NÃO detecta camomile↔chamomile com defaults (minScore=0.90)" — documenta a tensão com ANAL-D-11
      - "rejeita rose↔rosewood via gate substring-only (score=0.5 < 0.97)"
      - "exclui pares já presentes em descriptor_aliases.seed.json quando aliasSeed injetado"
      - "respeita pool minFrequency=2 (descriptors com freq=1 não entram no pairwise)"
      - "ordena output por score desc + lex (a, b) tiebreaker"
      - "suggested_canonical segue hierarquia seed > frequency > lex quando taxonomySeed/aliasSeed injetados"
      - "AliasCandidate.algo é 'lev_norm+tokens' apenas quando ambos descriptors têm '_'"

    Convenções: `import { describe, expect, it } from 'vitest'`, no semicolons, two-up traversal (`../../analyzer/...`). Path resolution para fixtures usa `fileURLToPath`+`dirname`+`join` (analog: `src/tests/corpus_loader.test.ts` lines 1-9), com `..` extra porque `src/tests/analysis/*.test.ts` está dois níveis abaixo de `src/tests/fixtures/`.
  </action>
  <acceptance_criteria>
    - Files `src/tests/fixtures/analysis/{camomile,substring_trap,seed_excluded}_corpus.json` exist and parse as JSON arrays.
    - `camomile_corpus.json`: when counted, `"camomile"` and `"chamomile"` each appear in ≥ 2 distinct materials.
    - `substring_trap_corpus.json`: `"rose"` and `"rosewood"` each appear in ≥ 2 materials (so both clear pool freq≥2 in 04-02-03 tests).
    - `seed_excluded_corpus.json`: at least one pair `(normalizeDescriptor(raw), normalizeDescriptor(canonical))` from `data/taxonomy/descriptor_aliases.seed.json` has both members at frequency ≥ 2 in the fixture.
    - `src/tests/analysis/similarity.test.ts` exists with 3 `describe` blocks (`levenshteinDistance`, `levenshteinSimilarity`, `tokenJaccard`).
    - `src/tests/analysis/alias_candidates.test.ts` exists with at least 7 `it.todo(...)` or trivially-passing cases covering the scenarios listed above.
    - `npm --prefix src exec vitest run src/tests/analysis/similarity.test.ts src/tests/analysis/alias_candidates.test.ts` exits 0 (stubs).
    - `npm --prefix src run build` exits 0 (no new source files yet — only tests + fixtures).
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix src run build &amp;&amp; npm --prefix src exec vitest run src/tests/analysis/similarity.test.ts src/tests/analysis/alias_candidates.test.ts</automated>
  </verify>
  <done>Wave 0 de 04-02 completa: 3 fixtures cobrindo camomile detection, substring trap, e seed exclusion; 2 arquivos de teste com estrutura `describe`/`it.todo` pronta para receber os bodies reais nas tasks 04-02-02 e 04-02-03.</done>
</task>

<task type="auto" tdd="true">
  <id>04-02-02</id>
  <title>Primitivos de similaridade: Levenshtein two-row DP + token Jaccard, com tests reais</title>
  <files>
    src/analyzer/similarity/levenshtein.ts,
    src/analyzer/similarity/token_overlap.ts,
    src/tests/analysis/similarity.test.ts
  </files>
  <read_first>
    src/normalizer/collapse_underscores.ts,
    src/normalizer/normalize_case.ts,
    src/normalizer/singularize.ts,
    src/tests/normalization/case.test.ts,
    src/tests/normalization/separators.test.ts,
    src/tests/normalization/convergence.test.ts,
    src/analyzer/index.ts,
    .planning/phases/04-corpus-analysis/04-PATTERNS.md,
    .planning/phases/04-corpus-analysis/04-RESEARCH.md
  </read_first>
  <behavior>
    - `levenshteinDistance(a, b)`: classical two-row DP, retorna `number >= 0`. Casos de borda: `a === b → 0`; `a === '' → b.length`; `b === '' → a.length`. Implementação aloca `Uint16Array` por chamada (sem state de módulo — preserva pureza); usa `charCodeAt` no inner loop. `??` defensivo para `noUncheckedIndexedAccess`. Sem semicolons.
    - `levenshteinSimilarity(a, b)`: retorna `1 - distance / max(|a|, |b|)`. Edge case: `max === 0 → 1`. Bounds verificáveis: para `(a, b)` quaisquer, `0 <= sim <= 1`.
    - `tokenJaccard(a, b)`: retorna `|tokens(a) ∩ tokens(b)| / |tokens(a) ∪ tokens(b)|` com `tokens(x) = new Set(x.split('_'))`. Edge case: `union === 0 → 1` (caso degenerado; nunca ocorre em produção devido ao filter de empties em 04-01).
    - Testes de similarity.test.ts populados com os 5+ casos canônicos do RESEARCH §Normalized Levenshtein similarity table + §Token overlap, usando `toBeCloseTo(value, 3)` para floats.
  </behavior>
  <action>
    Criar `src/analyzer/similarity/levenshtein.ts` com dois exports (analog: `src/normalizer/collapse_underscores.ts` — JSDoc + arrow function única; o módulo terá DOIS exports em vez de um). Header JSDoc do módulo cita ANAL-D-08 e referencia Wikipedia Levenshtein como source [CITED]. Implementação per RESEARCH §Levenshtein distance:
      1. `if (a === b) return 0; if (a.length === 0) return b.length; if (b.length === 0) return a.length`.
      2. Trocar para garantir `short` (menor) e `long` (maior) — aloca `min(m, n)` memória.
      3. Dois `Uint16Array(m + 1)` alternados (`prev`, `curr`); inicializar `prev[i] = i`.
      4. Loop externo `j = 1..n`: setar `curr[0] = j`, captar `bj = long.charCodeAt(j - 1)`, loop interno `i = 1..m` com `cost = short.charCodeAt(i - 1) === bj ? 0 : 1` e `curr[i] = Math.min((prev[i] ?? 0) + 1, (curr[i - 1] ?? 0) + 1, (prev[i - 1] ?? 0) + cost)`.
      5. Swap `prev`/`curr` no fim de cada `j`; retornar `prev[m] ?? 0`.

    Segundo export `levenshteinSimilarity` é trivial: `const max = Math.max(a.length, b.length); return max === 0 ? 1 : 1 - levenshteinDistance(a, b) / max`. JSDoc anota explicitamente o edge case `(empty, empty) → 1` (matematicamente correto; nunca ocorre em produção devido ao filter em 04-01).

    Criar `src/analyzer/similarity/token_overlap.ts` espelhando o estilo de `src/normalizer/collapse_underscores.ts` (1 arquivo, 1 export, JSDoc + arrow function). Export `tokenJaccard(a: string, b: string): number`. Implementação per RESEARCH §Token overlap: `const ta = new Set(a.split('_')); const tb = new Set(b.split('_')); let intersection = 0; for (const t of ta) if (tb.has(t)) intersection++; const union = ta.size + tb.size - intersection; return union === 0 ? 1 : intersection / union`.

    Preencher `src/tests/analysis/similarity.test.ts` com os 3 `describe` blocks (1 por primitivo). Casos canônicos:
      - `levenshteinDistance`: `('', '') → 0`; `('rose', '') → 4`; `('', 'rose') → 4`; `('rose', 'rose') → 0`; `('camomile', 'chamomile') → 1`; `('kitten', 'sitting') → 3` (caso clássico bem-conhecido como sanity check).
      - `levenshteinSimilarity`: `('', '')` → `toBeCloseTo(1, 5)`; `('rose', 'rose')` → 1; `('camomile', 'chamomile')` → `toBeCloseTo(0.888, 3)`; `('rose', 'rosewood')` → 0.5; property loop: para um array de pares `INPUTS`, `0 <= sim <= 1`.
      - `tokenJaccard`: `('lily_of_the_valley', 'lily_of_valley')` → `toBeCloseTo(0.75, 3)`; `('rose', 'rosewood')` → 0 (singleton sets disjuntos); `('a_b_c', 'a_b_c')` → 1; `('a_b', 'c_d')` → 0.

    Convenções: PATTERNS.md §Shared Patterns (no semicolons, arrow, `??` defensivo para `Uint16Array` indexing).
  </action>
  <acceptance_criteria>
    - `src/analyzer/similarity/levenshtein.ts` exports `levenshteinDistance` and `levenshteinSimilarity`. Manual assertions hold: `levenshteinDistance('camomile', 'chamomile') === 1`; `levenshteinDistance('kitten', 'sitting') === 3`; `levenshteinSimilarity('camomile', 'chamomile')` returns a value in `[0.888, 0.890]`; `levenshteinSimilarity('', '')` returns `1`.
    - `src/analyzer/similarity/token_overlap.ts` exports `tokenJaccard`. Manual assertions hold: `tokenJaccard('lily_of_the_valley', 'lily_of_valley')` returns a value in `[0.749, 0.751]`; `tokenJaccard('rose', 'rosewood') === 0`.
    - `npm --prefix src exec vitest run src/tests/analysis/similarity.test.ts` exits 0 with all unit assertions and the property loop passing.
    - `npm --prefix src run build` exits 0.
    - Source files use `Uint16Array` (grep `Uint16Array` in `levenshtein.ts`) and contain no semicolons (`rg "^\s*[A-Za-z_].*;\s*$" src/analyzer/similarity/` returns no matches in the new files).
    - Both new files use arrow-function exports (`grep "export const.*=.*=>" src/analyzer/similarity/levenshtein.ts src/analyzer/similarity/token_overlap.ts` returns ≥ 3 matches: 2 in levenshtein, 1 in token_overlap).
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix src run build &amp;&amp; npm --prefix src exec vitest run src/tests/analysis/similarity.test.ts</automated>
  </verify>
  <done>Primitivos de similaridade implementados (Levenshtein two-row DP zero-dep + token Jaccard), com 5+ casos canônicos passando, including o caso `camomile↔chamomile` (distance=1, similarity≈0.889). ANAL-03 satisfeito.</done>
</task>

<task type="auto" tdd="true">
  <id>04-02-03</id>
  <title>findAliasCandidates + wiring no analyzeCorpus + exportAliasCandidatesJson + barrel + alias tests</title>
  <files>
    src/analyzer/alias_candidates.ts,
    src/analyzer/analyze_corpus.ts,
    src/analyzer/export.ts,
    src/analyzer/index.ts,
    src/tests/analysis/alias_candidates.test.ts
  </files>
  <read_first>
    src/analyzer/analyze_corpus.ts,
    src/analyzer/export.ts,
    src/analyzer/index.ts,
    src/analyzer/frequency.ts,
    src/analyzer/similarity/levenshtein.ts,
    src/analyzer/similarity/token_overlap.ts,
    src/normalizer/normalize_descriptor.ts,
    src/normalizer/singularize.ts,
    src/loader/alias_loader.ts,
    src/loader/alias_validator.ts,
    src/loader/seed_loader.ts,
    src/types/analysis.ts,
    src/types/alias.ts,
    src/types/seed.ts,
    src/tests/analysis/alias_candidates.test.ts,
    src/tests/fixtures/analysis/camomile_corpus.json,
    src/tests/fixtures/analysis/substring_trap_corpus.json,
    src/tests/fixtures/analysis/seed_excluded_corpus.json,
    data/taxonomy/descriptor_aliases.seed.json,
    .planning/phases/04-corpus-analysis/04-PATTERNS.md,
    .planning/phases/04-corpus-analysis/04-RESEARCH.md
  </read_first>
  <behavior>
    - `AliasCandidateOptions = { readonly minFrequency?: number; readonly minScore?: number; readonly tokenOverlapFloor?: number; readonly substringRejectScore?: number; readonly aliasSeed?: DescriptorAliasSeed; readonly taxonomySeed?: TaxonomySeed }` exportado em `src/analyzer/alias_candidates.ts`.
    - Defaults UPPER_SNAKE_CASE no topo do módulo: `DEFAULT_MIN_FREQUENCY = 2`, `DEFAULT_MIN_SCORE = 0.90`, `DEFAULT_TOKEN_OVERLAP_FLOOR = 0.5`, `DEFAULT_SUBSTRING_REJECT_SCORE = 0.97`. Cada um documentado com `// ANAL-D-XX` no comment.
    - `findAliasCandidates(frequency, options = {})` retorna `readonly AliasCandidate[]`:
      1. Destruture defaults: `const { minFrequency = DEFAULT_MIN_FREQUENCY, minScore = DEFAULT_MIN_SCORE, tokenOverlapFloor = DEFAULT_TOKEN_OVERLAP_FLOOR, substringRejectScore = DEFAULT_SUBSTRING_REJECT_SCORE, aliasSeed, taxonomySeed } = options`.
      2. Construir pool: `for (const [word, freq] of frequency) if (freq >= minFrequency) pool.push({ word, freq })`.
      3. Pré-computar `seedPairSet` (via `buildSeedPairSet(aliasSeed)`) e `descriptorToFamilies` (via `buildDescriptorToFamilies(taxonomySeed)`) UMA vez.
      4. Length bucketing: agrupar pool por `.word.length` em `Map<number, Entry[]>`, ordenar lengths; loop `i, j` sobre lens com early break quando `lj - li > (1 - minScore) * lj`.
      5. Para cada par dentro de buckets candidatos: ordenar `(a, b)` lex; pular se `a === b` (mesmo bucket, mesmo entry); pular se `${a}|${b}` ∈ `seedPairSet`; chamar `shouldEmitCandidate(a, b, { minScore, substringRejectScore, tokenOverlapFloor })` para decidir emit + capturar `score`, `algo`, `tokenOverlap`.
      6. Quando emitir: construir `AliasCandidate` populando `families` (se `descriptorToFamilies` definido), `cross_family` (derivado), `suggested_canonical` (via `pickCanonical`).
      7. Sort final: `score` desc, depois `(a, b)` lex (ANAL-D-15).
    - Helpers privados no mesmo arquivo (analog: `singularizeToken` em `singularize.ts`):
      - `isSubstringOnly(a, b)`: retorna `false` se `a === b`; senão `long.includes(short)`.
      - `shouldEmitCandidate(a, b, thresholds)`: aplica score gate (`< minScore` → no-emit), substring gate (`isSubstringOnly && score < substringRejectScore` → no-emit), multi-token gate (se ambos têm `_`, `tokenJaccard < tokenOverlapFloor` → no-emit), retorna `{ emit, score, algo, tokenOverlap }`.
      - `pickCanonical(a, b, freqA, freqB, aliasSeed?, taxonomySeedDescriptors?)`: hierarquia `seed > freqValue (aliasSeed.values()) > frequency > lex`. Pré-computar `Set(aliasSeed.values())` no caller (em `findAliasCandidates`), passar para o helper.
      - `buildDescriptorToFamilies(seed)`: normaliza descriptors do seed via `normalizeDescriptor` antes de inserir no map (Phase 3 D-23 idempotency garante safety se já canônico).
      - `buildSeedPairSet(aliasSeed)`: normaliza ambos `raw` e `canonical` via `normalizeDescriptor`, descarta empties e self-pairs, encoda `${lo}|${hi}` (lex) e insere em `Set<string>`.
    - `analyzeCorpus` orchestrator (04-01-03) atualizado: `AnalyzeCorpusOptions.aliasCandidates?: AliasCandidateOptions` (tighten de `unknown`); se definido, chamar `findAliasCandidates(frequency, options.aliasCandidates)`; senão retornar `aliasCandidates: []` (comportamento atual preservado quando sem options).
    - `src/analyzer/export.ts` ganha `exportAliasCandidatesJson(candidates, path)` que escreve `{ version: 1, candidates }` (candidates já vem ordenado por `findAliasCandidates` — ANAL-D-15). `writeAnalysisArtifacts` atualizado para incluir o 3º arquivo `alias_candidates.v1.json` no `Promise.all`.
    - `src/analyzer/index.ts` ganha re-exports: `findAliasCandidates`, `levenshteinDistance`, `levenshteinSimilarity`, `tokenJaccard`, `exportAliasCandidatesJson`.
    - `alias_candidates.test.ts` populado com 7+ assertions reais cobrindo todos os cenários listados em 04-02-01.
  </behavior>
  <action>
    Criar `src/analyzer/alias_candidates.ts` espelhando o estilo de `src/normalizer/normalize_descriptor.ts` (composer com contract documentado em JSDoc) e `src/loader/alias_validator.ts` (validation pattern com options). Topo do arquivo: JSDoc do módulo + UPPER_SNAKE_CASE constants (`DEFAULT_MIN_FREQUENCY = 2`, etc., cada um com comentário `// ANAL-D-XX`). Imports:
      - `import { normalizeDescriptor } from '../normalizer/normalize_descriptor.js'`
      - `import { levenshteinSimilarity } from './similarity/levenshtein.js'`
      - `import { tokenJaccard } from './similarity/token_overlap.js'`
      - `import { encodePairKey } from './pair_key.js'`
      - `import type { FrequencyMap, AliasCandidate } from '../types/analysis.js'`
      - `import type { DescriptorAliasSeed } from '../types/alias.js'`
      - `import type { TaxonomySeed } from '../types/seed.js'`

    Definir e exportar `AliasCandidateOptions`. Implementar `findAliasCandidates` per RESEARCH §Alias Candidate Generation (length bucketing pre-filter mandatório). Helpers privados declarados como `const helperName = (...): T => { ... }` ACIMA do public export (analog: `singularizeToken` em `singularize.ts` line 19). JSDoc do public export documenta:
      - Defaults conservadores (ANAL-D-11) e a tensão com o ROADMAP success criterion (camomile↔chamomile): "para detectar `camomile↔chamomile`, configurar `{ minScore: 0.85 }` — defaults `0.90` priorizam precision over recall (ANAL-D-11)".
      - Pool `frequency >= minFrequency` aplicado APENAS aqui (ANAL-D-16); contagem base permanece total.
      - Substring rejection (ANAL-D-10) é defense in depth contra falsos positivos como `rose↔rosewood`.
      - Output são SUGESTÕES; nunca auto-merge (ANAL-D-11).

    Atualizar `src/analyzer/analyze_corpus.ts`: importar `AliasCandidateOptions` e `findAliasCandidates` de `'./alias_candidates.js'`; tighten `AnalyzeCorpusOptions.aliasCandidates` de `unknown` para `AliasCandidateOptions`; chamar `findAliasCandidates(frequency, options.aliasCandidates)` quando definido (substituir o placeholder `[]` que 04-01-03 deixou). Remover o comentário TODO de 04-01.

    Atualizar `src/analyzer/export.ts`: adicionar `exportAliasCandidatesJson(candidates: readonly AliasCandidate[], path: string): Promise<void>` que serializa `{ version: 1, candidates }` via `writeJsonDeterministic` (helper já existente de 04-01-03). Atualizar `writeAnalysisArtifacts(analysis, baseDir = 'data/analysis')` para incluir `exportAliasCandidatesJson(analysis.aliasCandidates, \`${baseDir}/alias_candidates.v1.json\`)` no `Promise.all`. Importar `AliasCandidate` type. Remover o comentário TODO de 04-01.

    Atualizar `src/analyzer/index.ts`: adicionar `export { levenshteinDistance, levenshteinSimilarity } from './similarity/levenshtein.js'`, `export { tokenJaccard } from './similarity/token_overlap.js'`, `export { findAliasCandidates } from './alias_candidates.js'`, `export { exportAliasCandidatesJson } from './export.js'`.

    Preencher `src/tests/analysis/alias_candidates.test.ts` com os 7+ casos. Para cada fixture, calcular `frequency` via `computeDescriptorFrequency` (importado do barrel) e passar para `findAliasCandidates`. Casos:
      1. `camomile_corpus.json` + `{ minScore: 0.85, minFrequency: 2 }` → output contém um candidato com `a === 'camomile'` (ou `'chamomile'`), `b ===` o outro, `score` em `[0.888, 0.890]`, `algo === 'lev_norm'`.
      2. `camomile_corpus.json` + defaults → output NÃO contém `(camomile, chamomile)` (documenta a tensão com ANAL-D-11).
      3. `substring_trap_corpus.json` + defaults → output NÃO contém `(rose, rosewood)` (rejeitado por gate substring + score abaixo de 0.97).
      4. `seed_excluded_corpus.json` + `{ aliasSeed: loadedSeed, minFrequency: 2, minScore: 0.85 }` → output NÃO contém o par do seed escolhido em 04-02-01.
      5. Pool gate: corpus com descriptor único de freq=1 → não entra no output mesmo com score alto (verificar via fixture inline).
      6. Sort: chamar com 3+ candidatos esperados, assertar `result[0].score >= result[1].score >= ...`; quando empate, `(a, b)` < `(a, b)` lex.
      7. `suggested_canonical`: fixture inline com `taxonomySeed` injetado contendo `camomile` no seed e `chamomile` apenas no corpus → `suggested_canonical === 'camomile'` (seed vence).
      8. `algo === 'lev_norm+tokens'` apenas quando ambos descriptors têm `_`: inline fixture com `lily_of_valley` e `lily_of_the_valley` que disparam o branch combinado.

    Fixtures path: usar `fileURLToPath`+`dirname`+`join` (analog: `src/tests/corpus_loader.test.ts` lines 1-9), com `..` extra (`src/tests/analysis/` é 2 níveis abaixo de `src/tests/fixtures/`).

    Convenções: PATTERNS.md §Shared Patterns aplica (no semicolons, arrow, `import type`, `.js` extension, `??` defensivo).
  </action>
  <acceptance_criteria>
    - `src/analyzer/alias_candidates.ts` exports `findAliasCandidates` and `AliasCandidateOptions`; top of file declares constants `DEFAULT_MIN_FREQUENCY`, `DEFAULT_MIN_SCORE`, `DEFAULT_TOKEN_OVERLAP_FLOOR`, `DEFAULT_SUBSTRING_REJECT_SCORE` (grep with `rg "^const DEFAULT_" src/analyzer/alias_candidates.ts | wc -l` returns `>= 4`).
    - `DEFAULT_MIN_SCORE === 0.90` and `DEFAULT_MIN_FREQUENCY === 2` (grep the literal values).
    - Behavioral assertion 1 — `findAliasCandidates(freqFromCamomileFixture, { minScore: 0.85, minFrequency: 2 })` returns an array containing one entry whose `(a, b)` (lex-sorted) is `('camomile', 'chamomile')` with `score` in `[0.888, 0.890]` and `algo === 'lev_norm'`.
    - Behavioral assertion 2 — same fixture + defaults: the result does NOT contain a `(camomile, chamomile)` pair (default `0.90` filter blocks it).
    - Behavioral assertion 3 — `findAliasCandidates(freqFromSubstringTrap, defaults)` does NOT contain `(rose, rosewood)` (substring-only gate).
    - Behavioral assertion 4 — when `aliasSeed` injected with a pair that matches the seed-excluded fixture, that pair does NOT appear in the output.
    - Behavioral assertion 5 — result is sorted by `score` descending, ties broken by `(a, b)` lex (verify on a small fixture with ≥ 3 candidates).
    - Behavioral assertion 6 — `analyzeCorpus(corpus, { aliasCandidates: { minScore: 0.85, minFrequency: 2 } })` returns `aliasCandidates.length > 0` on the camomile fixture; without options or with `aliasCandidates === undefined`, length is `0`.
    - `src/analyzer/export.ts` exports `exportAliasCandidatesJson`. After `exportAliasCandidatesJson(candidates, path)`, the file parses as `{ version: 1, candidates: [...] }` and re-writing the same input yields byte-identical bytes (byte-determinism — D-15).
    - `writeAnalysisArtifacts(analysis, tmpdir)` creates all three files: `descriptor_frequency.v1.json`, `descriptor_cooccurrence.v1.json`, `alias_candidates.v1.json` (each parses and contains `version: 1`).
    - `src/analyzer/index.ts` re-exports `findAliasCandidates`, `levenshteinDistance`, `levenshteinSimilarity`, `tokenJaccard`, `exportAliasCandidatesJson` (`rg "findAliasCandidates|levenshteinDistance|tokenJaccard|exportAliasCandidatesJson" src/analyzer/index.ts | wc -l >= 4`).
    - Full Phase 4 quick-suite green: `npm --prefix src exec vitest run src/tests/analysis/` exits 0.
    - Full project suite green: `npm --prefix src run test` exits 0 (no regression in Phase 1/2/3).
    - `npm --prefix src run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix src run build &amp;&amp; npm --prefix src exec vitest run src/tests/analysis/ &amp;&amp; npm --prefix src run test</automated>
  </verify>
  <done>`findAliasCandidates` operacional com pool gate (freq ≥ 2), substring-only rejection (gate 0.97), token-overlap floor (0.5 quando ambos multi-token), seed exclusion, canonical-direction picker (`seed > freq > lex`), family annotation opcional, e output ordenado. `analyzeCorpus` wired (alias step opt-in), `writeAnalysisArtifacts` escreve os 3 arquivos byte-determinísticos. ANAL-04 satisfeito; ROADMAP success criterion 2 satisfeito com threshold customizado documentado. Phase 4 concluída.</done>
</task>

</tasks>

<verification>
**Cross-task validation (executado antes de marcar o plan completo):**

1. `npm --prefix src run build` — tsc --noEmit exits 0.
2. `npm --prefix src exec vitest run src/tests/analysis/` — toda a quick-suite de Phase 4 verde (similarity + alias_candidates + os 5 testes de 04-01).
3. `npm --prefix src run test` — suíte completa verde (Phase 4 não regrede Phase 1/2/3).
4. Audit ROADMAP success criterion: rodar manualmente em REPL/scratch um `analyzeCorpus(camomile_corpus, { aliasCandidates: { minScore: 0.85 } })` e confirmar visualmente que o output contém `{ a: 'camomile', b: 'chamomile', score ≈ 0.889, algo: 'lev_norm' }`.
5. Audit byte-determinism: rodar `writeAnalysisArtifacts` 2× contra `tmpdir` e diff os 3 arquivos — todos devem ser byte-iguais.
6. Audit zero-dep: `cat src/package.json | jq '.dependencies'` retorna `{}` ou `null` (PROJECT.md zero-runtime-deps mantido).
7. Convenções: `rg "^\s*[A-Za-z_].*;\s*$" src/analyzer/similarity/ src/analyzer/alias_candidates.ts` (excluir comentários) retorna zero matches.
8. Re-validar ANAL-D-16: confirmar que `computeDescriptorFrequency` (criado em 04-01) e `computeCoOccurrence` NÃO aplicam threshold de frequência; apenas `findAliasCandidates` o faz. Procurar `minFrequency` em `src/analyzer/frequency.ts` e `cooccurrence.ts` — não deve aparecer.
</verification>

<success_criteria>
- ROADMAP Phase 4 success criterion 2 ("Levenshtein identifica `camomile↔chamomile`") satisfeito com threshold customizado `{ minScore: 0.85 }`, testado em ambos os sentidos (default 0.90 NÃO detecta; custom 0.85 detecta) e documentado como trade-off consciente com ANAL-D-11 em JSDoc de `findAliasCandidates`.
- ANAL-03 (string similarity) e ANAL-04 (alias detector) marcados como cobertos no REQUIREMENTS.md traceability table.
- `analyzeCorpus(corpus, { aliasCandidates: { ... } })` produz `aliasCandidates` real (não mais placeholder); `writeAnalysisArtifacts` escreve os 3 arquivos byte-determinísticos.
- Pares já em `descriptor_aliases.seed.json` são excluídos quando o aliasSeed é injetado (auditability e evitar ruído).
- `suggested_canonical` segue hierarquia `seed > frequency > lex` quando seeds são injetados.
- Zero novas dependências runtime (PROJECT.md zero-dep mantido).
- Phase 4 concluída: roadmap pode avançar para Phase 5 (Inference Engine) consumindo `FrequencyMap`, `CoOccurrenceMap`, e `AliasCandidate[]` do barrel `src/analyzer/index.ts`.
</success_criteria>

<output>
Criar `.planning/phases/04-corpus-analysis/04-02-SUMMARY.md` quando o plan estiver concluído, seguindo o template de `03-01-SUMMARY.md` (frontmatter com `requirements-completed: [ANAL-03, ANAL-04]`, `key-files`, decisões tomadas, calibração do `substringRejectScore` e qualquer ajuste no threshold default após inspeção visual dos primeiros candidatos no corpus real).
</output>
