---
phase: 08-taxonomy-seed-expansion-curation
plan: 05
type: execute
wave: 4
depends_on:
  - 08-02
  - 08-03
  - 08-04
files_modified:
  - src/tests/curation/v1_v2_comparison.test.ts
  - .planning/phases/08-taxonomy-seed-expansion-curation/curation/v1-v2-comparison.md
  - .planning/phases/08-taxonomy-seed-expansion-curation/08-VALIDATION.md
autonomous: false
requirements:
  - CUR-07
must_haves:
  truths:
    - "v1 and v2 can be compiled side by side with deterministic timestamps and explicit paths."
    - "Validation separates hard failures from soft curation warnings."
    - "Regressions and soft findings are documented for human review without switching defaults to v2."
  artifacts:
    - path: "src/tests/curation/v1_v2_comparison.test.ts"
      provides: "Automated explicit-path compile comparison guard"
    - path: ".planning/phases/08-taxonomy-seed-expansion-curation/curation/v1-v2-comparison.md"
      provides: "Human-readable v1 vs v2 validation report"
    - path: ".planning/phases/08-taxonomy-seed-expansion-curation/08-VALIDATION.md"
      provides: "Updated Nyquist validation status"
  key_links:
    - from: "data/taxonomy/taxonomy-seed.v2.json"
      to: "/tmp/opencode/taxonomy-phase8-comparison/v2-candidate"
      via: "npm run compile with explicit --seed --relations --accords --out --generated-at"
      pattern: "--seed ../data/taxonomy/taxonomy-seed.v2.json"
---

<objective>
Validate Phase 8 output with deterministic v1-vs-v2 comparison and hard/soft gate separation.

Purpose: Implement CUR-D-65 through CUR-D-75 without making v2 the default or hiding curation diffs behind nondeterministic artifacts.
Output: v1-v2 comparison test, comparison report, and updated validation sign-off.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-taxonomy-seed-expansion-curation/08-CONTEXT.md
@.planning/phases/08-taxonomy-seed-expansion-curation/08-VALIDATION.md
@.planning/phases/08-taxonomy-seed-expansion-curation/08-PATTERNS.md
@.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
@data/taxonomy/taxonomy-seed.v1.json
@data/taxonomy/taxonomy-seed.v2.json
@data/taxonomy/descriptor_aliases.seed.json
@data/inference/curated_relations.v1.json
@data/inference/curated_relations.v2.json
@data/inference/accord_map.v1.json
@data/inference/accord_map.v2.json
@src/cli/compile.ts
@src/cli/parse_args.ts
@src/compiler/quality_gates.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic v1-v2 comparison test</name>
  <files>src/tests/curation/v1_v2_comparison.test.ts</files>
  <read_first>src/tests/cli/compile.test.ts, src/tests/cli/parse_args.test.ts, src/compiler/quality_gates.ts, src/cli/compile.ts, src/cli/parse_args.ts, data/taxonomy/taxonomy-seed.v2.json, data/inference/curated_relations.v2.json, data/inference/accord_map.v2.json, src/tests/curation/v1_v2_comparison.test.ts if it already exists</read_first>
  <behavior>
    - Test compile CLI accepts explicit v2 seed, aliases, relations, accords, output directory, version, and generated-at paths.
    - Test default paths still target v1.
    - Test generated artifacts use deterministic `generated_at` and include taxonomy, descriptor aliases, and similarity matrix.
    - Test hard failures and soft warnings are inspectable separately through existing quality gate behavior.
  </behavior>
  <action>Create `src/tests/curation/v1_v2_comparison.test.ts` following `src/tests/cli/compile.test.ts`. Use `runCompileCli` with explicit flags `--seed`, `--aliases`, `--relations`, `--accords`, `--out`, `--version 2.0.0`, and `--generated-at 2026-01-01T00:00:00.000Z`; use temporary output directories for test isolation. Assert `DEFAULT_PATHS` still reference v1 paths from `src/cli/parse_args.ts`. Assert output files `taxonomy.json`, `descriptor_aliases.json`, and `similarity_matrix.json` exist and contain deterministic generated timestamps. Include quality-gate assertions for separate `errors` and `warnings` arrays per CUR-D-65 through CUR-D-75.</action>
  <verify><automated>cd src &amp;&amp; npm test -- tests/curation/v1_v2_comparison.test.ts tests/cli/compile.test.ts tests/cli/parse_args.test.ts tests/compiler/quality_gates.test.ts</automated></verify>
  <acceptance_criteria>
    - Test-command assertion: focused comparison/CLI/quality tests pass.
    - Source assertion: test uses explicit v2 `--seed`, `--relations`, `--accords`, `--out`, `--version`, and `--generated-at` flags.
    - Source assertion: test asserts `DEFAULT_PATHS.seedPath`, `relationsPath`, `accordsPath`, and `outputDir` remain v1 defaults.
    - Source assertion: test checks hard `errors` and soft `warnings` separately.
  </acceptance_criteria>
  <done>Automated comparison guard proves deterministic explicit-path v2 compilation without default drift.</done>
</task>

<task type="auto">
  <name>Task 2: Produce v1-v2 curation comparison report</name>
  <files>.planning/phases/08-taxonomy-seed-expansion-curation/curation/v1-v2-comparison.md, .planning/phases/08-taxonomy-seed-expansion-curation/08-VALIDATION.md</files>
  <read_first>.planning/phases/08-taxonomy-seed-expansion-curation/08-VALIDATION.md, .planning/phases/08-taxonomy-seed-expansion-curation/08-CONTEXT.md, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, data/compiled/v1/taxonomy.json, data/compiled/v1/descriptor_aliases.json, data/compiled/v1/similarity_matrix.json, data/taxonomy/taxonomy-seed.v2.json, data/inference/curated_relations.v2.json, data/inference/accord_map.v2.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/v1-v2-comparison.md if it already exists</read_first>
  <action>Run deterministic baseline compile and explicit v2 candidate compile from `src` into explicit comparison directories outside tracked artifacts: `npm run compile -- --seed ../data/taxonomy/taxonomy-seed.v1.json --aliases ../data/taxonomy/descriptor_aliases.seed.json --relations ../data/inference/curated_relations.v1.json --accords ../data/inference/accord_map.v1.json --out /tmp/opencode/taxonomy-phase8-comparison/v1-baseline --version 1.0.0 --generated-at 2026-01-01T00:00:00.000Z` and `npm run compile -- --seed ../data/taxonomy/taxonomy-seed.v2.json --aliases ../data/taxonomy/descriptor_aliases.seed.json --relations ../data/inference/curated_relations.v2.json --accords ../data/inference/accord_map.v2.json --out /tmp/opencode/taxonomy-phase8-comparison/v2-candidate --version 2.0.0 --generated-at 2026-01-01T00:00:00.000Z`. Do not run baseline compile without `--out`, because the default writes to `data/compiled/v1` and Phase 8 must preserve v1 artifacts. Create `.planning/phases/08-taxonomy-seed-expansion-curation/curation/v1-v2-comparison.md` with metrics required by CUR-D-66: coverage, generic pressure, zero-frequency seeds, graph coverage, review_queue quality, alias quality, determinism/schema. Record the generated comparison directories `/tmp/opencode/taxonomy-phase8-comparison/v1-baseline` and `/tmp/opencode/taxonomy-phase8-comparison/v2-candidate` in the report and keep them outside tracked artifacts. Separate hard failures from soft curation warnings per CUR-D-69 through CUR-D-75 and document any regressions with rationale per CUR-D-67. Update `08-VALIDATION.md` frontmatter to `nyquist_compliant: true` and `wave_0_complete: true` only after all new curation tests pass; otherwise leave them false and record remaining failures.</action>
  <verify><automated>cd src &amp;&amp; npm run typecheck &amp;&amp; npm test -- tests/curation/taxonomy_seed_v2.test.ts tests/curation/review_dispositions.test.ts tests/curation/alias_seed_v2.test.ts tests/curation/relation_accord_v2.test.ts tests/curation/v1_v2_comparison.test.ts &amp;&amp; npm run build &amp;&amp; npm run compile -- --seed ../data/taxonomy/taxonomy-seed.v1.json --aliases ../data/taxonomy/descriptor_aliases.seed.json --relations ../data/inference/curated_relations.v1.json --accords ../data/inference/accord_map.v1.json --out /tmp/opencode/taxonomy-phase8-comparison/v1-baseline --version 1.0.0 --generated-at 2026-01-01T00:00:00.000Z &amp;&amp; npm run compile -- --seed ../data/taxonomy/taxonomy-seed.v2.json --aliases ../data/taxonomy/descriptor_aliases.seed.json --relations ../data/inference/curated_relations.v2.json --accords ../data/inference/accord_map.v2.json --out /tmp/opencode/taxonomy-phase8-comparison/v2-candidate --version 2.0.0 --generated-at 2026-01-01T00:00:00.000Z</automated></verify>
  <acceptance_criteria>
    - CLI-output assertion: both compile commands exit 0.
    - Source assertion: baseline compile command uses explicit `--out /tmp/opencode/taxonomy-phase8-comparison/v1-baseline` and does not write to default `data/compiled/v1`.
    - Source assertion: report contains headings `Hard failures`, `Soft curation warnings`, `Regressions with rationale`, `Coverage`, `Generic pressure`, `Zero-frequency seeds`, `Graph coverage`, `Review queue quality`, `Alias quality`, and `Determinism/schema`.
    - Source assertion: `08-VALIDATION.md` sets `nyquist_compliant: true` and `wave_0_complete: true` only if all curation tests pass.
    - Source assertion: `src/cli/parse_args.ts` remains unchanged and no default path points to v2.
  </acceptance_criteria>
  <done>Comparison report documents deterministic v1-vs-v2 impact, hard/soft gate separation, and validation status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review v1-v2 soft findings and promotion readiness</name>
  <files>.planning/phases/08-taxonomy-seed-expansion-curation/curation/v1-v2-comparison.md</files>
  <read_first>.planning/phases/08-taxonomy-seed-expansion-curation/curation/v1-v2-comparison.md, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, .planning/phases/08-taxonomy-seed-expansion-curation/08-CONTEXT.md</read_first>
  <action>Pause execution for human verification of the v1-v2 comparison report. The executor must not switch compiler defaults, promote v2, or reinterpret soft warnings as hard failures during this checkpoint; the only acceptable outcome is human approval or specific correction requests.</action>
  <what-built>V2 candidate data and deterministic comparison report were produced without switching defaults.</what-built>
  <how-to-verify>Review the report sections for hard failures, soft curation warnings, regressions, coverage, graph coverage, review queue quality, and alias quality. Confirm hard failures are absent or corrected, soft warnings are acceptable/revisable, and v2 remains a candidate rather than a default.</how-to-verify>
  <verify><automated>test -f .planning/phases/08-taxonomy-seed-expansion-curation/curation/v1-v2-comparison.md &amp;&amp; grep -q 'Hard failures' .planning/phases/08-taxonomy-seed-expansion-curation/curation/v1-v2-comparison.md &amp;&amp; grep -q 'Soft curation warnings' .planning/phases/08-taxonomy-seed-expansion-curation/curation/v1-v2-comparison.md</automated></verify>
  <acceptance_criteria>
    - Human assertion: curator accepts documented soft warnings or requests specific revisions.
    - Human assertion: curator confirms no automatic promotion/default switch occurred.
  </acceptance_criteria>
  <done>Human approval is recorded by the execution workflow, or corrections are returned before Phase 8 is considered complete.</done>
  <resume-signal>Type `approved` to finalize Phase 8 execution, or describe required corrections.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v2 inputs→compiled v2-candidate artifacts | Curated candidate inputs become generated artifacts for validation only. |
| generated artifacts→human review | Metrics and warnings inform curation but do not replace manual approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-19 | Repudiation | generated artifacts | mitigate | Use fixed `--generated-at 2026-01-01T00:00:00.000Z` and explicit output path to make diffs reproducible. |
| T-08-20 | Tampering | compiler defaults | mitigate | Tests assert `DEFAULT_PATHS` remain v1 and no default switch happens. |
| T-08-21 | Tampering | quality gates | mitigate | Report separates hard failures from soft warnings and blocks on schema/nondeterminism/alias contamination/auto-promotion. |
| T-08-22 | Tampering | unreviewed corpus candidate promotion | mitigate | Comparison consumes only curated v2 inputs created by prior approved plans; review_queue remains evidence. |
| T-08-23 | Tampering | external scraping/enrichment | mitigate | No external enrichment is run; report derives from local deterministic compile outputs. |
| T-08-SC | Tampering | npm installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
Run the full automated command in Task 2, then complete the human verification checkpoint.
</verification>

<success_criteria>
All curation tests pass, v1 and v2 compile deterministically through explicit paths, hard/soft findings are documented separately, and human review approves or returns corrections.
</success_criteria>

<output>
Create `.planning/phases/08-taxonomy-seed-expansion-curation/08-05-SUMMARY.md` when done.
</output>
