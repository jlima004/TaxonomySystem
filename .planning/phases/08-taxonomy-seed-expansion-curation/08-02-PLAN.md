---
phase: 08-taxonomy-seed-expansion-curation
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - data/taxonomy/taxonomy-seed.v2.json
  - src/tests/curation/taxonomy_seed_v2.test.ts
autonomous: true
requirements:
  - CUR-01
  - CUR-02
  - CUR-03
must_haves:
  truths:
    - "Expanded curated seed v2 exists without modifying v1."
    - "Seed v2 expands beyond v1 with at least one approved family, subfamily, or descriptor addition."
    - "Seed v2 includes only manually approved in-scope groups/descriptors with lowercase snake_case ASCII IDs."
    - "Compiler defaults still point to v1; v2 is usable only by explicit path."
  artifacts:
    - path: "data/taxonomy/taxonomy-seed.v2.json"
      provides: "Expanded curated taxonomy seed"
      contains: "\"version\": \"2.0.0\""
    - path: "src/tests/curation/taxonomy_seed_v2.test.ts"
      provides: "Automated seed v2 validation and v1 preservation checks"
  key_links:
    - from: "data/taxonomy/taxonomy-seed.v2.json"
      to: "src/loader/seed_validator.ts"
      via: "validateSeed"
      pattern: "validateSeed"
---

<objective>
Create the versioned curated taxonomy seed v2 while preserving the v1 baseline.

Purpose: Implement CUR-D-08 through CUR-D-13 and CUR-D-14 through CUR-D-25 without changing compiler defaults or auto-promoting corpus candidates.
Output: `data/taxonomy/taxonomy-seed.v2.json` plus focused validation tests.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-taxonomy-seed-expansion-curation/08-CONTEXT.md
@.planning/phases/08-taxonomy-seed-expansion-curation/08-PATTERNS.md
@.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
@data/taxonomy/taxonomy-seed.v1.json
@src/loader/seed_validator.ts
@src/cli/parse_args.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add seed v2 validation tests</name>
  <files>src/tests/curation/taxonomy_seed_v2.test.ts</files>
  <read_first>src/tests/seed_validator.test.ts, src/loader/seed_validator.ts, src/cli/parse_args.ts, data/taxonomy/taxonomy-seed.v1.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, src/tests/curation/taxonomy_seed_v2.test.ts if it already exists</read_first>
  <behavior>
    - Test `data/taxonomy/taxonomy-seed.v2.json` validates with `validateSeed` when it exists, while the test file itself passes before data creation by validating reusable fixtures and workbook prerequisites.
    - Test `data/taxonomy/taxonomy-seed.v1.json` still exists and has version `1.0.0`.
    - Test compiler default seed path remains `data/taxonomy/taxonomy-seed.v1.json`.
    - Test v2 contains only in-scope families/groups and no deferred group IDs.
    - Test IDs and descriptors are lowercase snake_case ASCII and globally non-duplicated across v2 descriptors.
    - Test v2 has a descriptor/subfamily/family count greater than v1 and every addition traces to an approved workbook entry with rationale and evidence.
  </behavior>
  <action>Create `src/tests/curation/taxonomy_seed_v2.test.ts` before adding v2 data. Use `node:fs/promises`, `node:path`, and `node:url` to load `../../data/taxonomy/taxonomy-seed.v1.json`, the curation workbook, and `../../data/taxonomy/taxonomy-seed.v2.json` from the repository root only if v2 already exists. Import `validateSeed` from `../../loader/seed_validator.js` and `DEFAULT_PATHS` from `../../cli/parse_args.js`. Assert CUR-D-08 through CUR-D-13, CUR-D-14 through CUR-D-25, and the deferred list from CUR-D-06. Include reusable helper assertions for expansion count, approved-entry traceability, rationale/evidence presence, lowercase snake_case ASCII IDs, no duplicate descriptors, and no deferred IDs. When v2 is absent, the test must still pass by validating the helper contract against an inline fixture and by asserting the workbook contains at least one `manual_approval: approved` `promote_to_seed` entry with family/subfamily/descriptor/rationale/evidence prerequisites; it must not use an intentionally failing red phase because TDD_MODE is false.</action>
  <verify><automated>cd src &amp;&amp; npm test -- tests/curation/taxonomy_seed_v2.test.ts</automated></verify>
  <acceptance_criteria>
    - Test-command assertion: `cd src && npm test -- tests/curation/taxonomy_seed_v2.test.ts` passes at the end of this task without requiring `taxonomy-seed.v2.json` to exist.
    - Source assertion: test checks `DEFAULT_PATHS.seedPath` equals `data/taxonomy/taxonomy-seed.v1.json`.
    - Source assertion: test includes all deferred IDs from CUR-D-06 and asserts they are absent from v2 family/subfamily IDs.
    - Source assertion: test checks global descriptor duplication across v2 families/subfamilies.
    - Source assertion: test checks v2 expansion beyond v1 and traces additions to approved workbook rationale/evidence when v2 exists.
  </acceptance_criteria>
  <done>Seed v2 test contract exists and encodes v1 preservation, explicit v2 path usage, scope, and naming rules.</done>
</task>

<task type="auto">
  <name>Task 2: Create curated taxonomy seed v2</name>
  <files>data/taxonomy/taxonomy-seed.v2.json</files>
  <read_first>data/taxonomy/taxonomy-seed.v1.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, .planning/phases/08-taxonomy-seed-expansion-curation/08-CONTEXT.md, src/tests/curation/taxonomy_seed_v2.test.ts, data/taxonomy/taxonomy-seed.v2.json if it already exists</read_first>
  <action>Create `data/taxonomy/taxonomy-seed.v2.json` by controlled migration from `data/taxonomy/taxonomy-seed.v1.json` per CUR-D-08 through CUR-D-13. Before editing data, confirm the workbook contains at least one explicit approved seed expansion entry with `manual_approval: approved`, `primary_disposition: promote_to_seed`, concrete family/subfamily/descriptor IDs, individual rationale, and evidence; stop if the only approval is generic workbook approval. Preserve existing v1 families/subfamilies/descriptors unless the approved workbook explicitly says otherwise; add only workbook-approved entries for the initial groups `gourmand`, `spicy`, `green`, `fruity`, `amber_resinous`, and `animalic` per CUR-D-04 and CUR-D-05. Ensure v2 expands beyond v1 by adding at least one approved family, subfamily, or descriptor, and ensure every addition can be traced to a workbook-approved rationale/evidence block. Set `version` to `2.0.0`, metadata `created_at` to `2026-05-22T00:00:00Z`, author to `Taxonomy Team`, and description to `Expanded curated olfactory taxonomy seed v2 - Phase 8 manual curation`. Use lowercase snake_case ASCII IDs. Do not edit `data/taxonomy/taxonomy-seed.v1.json`, `src/cli/parse_args.ts`, or compiled artifacts. Do not add deferred groups from CUR-D-06.</action>
  <verify><automated>cd src &amp;&amp; npm test -- tests/curation/taxonomy_seed_v2.test.ts &amp;&amp; npm test -- tests/seed_validator.test.ts</automated></verify>
  <acceptance_criteria>
    - Source assertion: `data/taxonomy/taxonomy-seed.v2.json` contains `"version": "2.0.0"`.
    - Source assertion: v2 descriptor/subfamily/family counts are greater than v1 by at least one approved addition.
    - Source assertion: every v2 addition has an approved workbook block with rationale and evidence.
    - Source assertion: `git diff -- data/taxonomy/taxonomy-seed.v1.json src/cli/parse_args.ts data/compiled/v1` shows no changes.
    - Test-command assertion: `cd src && npm test -- tests/curation/taxonomy_seed_v2.test.ts && npm test -- tests/seed_validator.test.ts` passes.
    - Source assertion: v2 contains no family or subfamily IDs matching `marine_ozonic`, `tobacco`, `powdery`, `aldehydic`, `medicinal_camphoraceous`, `earthy_mineral`, or `smoky`.
  </acceptance_criteria>
  <done>Seed v2 exists, validates, preserves v1, does not change defaults, and contains only approved in-scope curated entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curation workbook→taxonomy-seed.v2.json | Approved manual decisions become curated seed truth. |
| v2 seed→compiler validation | Versioned JSON enters existing seed validation/compile path only by explicit path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | taxonomy-seed.v2.json | mitigate | Tests require approved scope, snake_case ASCII IDs, no deferred groups, and no duplicate descriptors. |
| T-08-06 | Tampering | taxonomy-seed.v1.json | mitigate | Explicit no-edit acceptance gate and default path test preserve v1 baseline. |
| T-08-07 | Tampering | corpus candidates | mitigate | Task reads candidate workbook and may add only manually approved entries; no corpus auto-promotion. |
| T-08-08 | Repudiation | seed migration | mitigate | Workbook rationale/evidence is prerequisite context for each new entry. |
| T-08-09 | Tampering | external scraping/enrichment | mitigate | No external scraping; only approved workbook evidence may inform additions. |
| T-08-SC | Tampering | npm installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
Run `cd src && npm test -- tests/curation/taxonomy_seed_v2.test.ts && npm test -- tests/seed_validator.test.ts`; confirm `git diff -- data/taxonomy/taxonomy-seed.v1.json src/cli/parse_args.ts data/compiled/v1` is empty.
</verification>

<success_criteria>
`taxonomy-seed.v2.json` is a validated manual curated expansion, `taxonomy-seed.v1.json` is untouched, compiler defaults remain v1, and deferred groups are absent.
</success_criteria>

<output>
Create `.planning/phases/08-taxonomy-seed-expansion-curation/08-02-SUMMARY.md` when done.
</output>
