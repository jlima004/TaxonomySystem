---
phase: 08-taxonomy-seed-expansion-curation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
  - src/tests/curation/review_dispositions.test.ts
autonomous: false
requirements:
  - CUR-01
  - CUR-03
  - CUR-04
must_haves:
  truths:
    - "Curator can review Phase 7 corpus candidates and review_queue as evidence only."
    - "Every proposed seed change is represented by a per-entry approval, rationale, evidence, bucket, and disposition before any curated JSON is changed."
    - "Deferred groups remain outside Phase 8 promotion scope."
  artifacts:
    - path: ".planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md"
      provides: "Manual curation workbook and approval ledger"
    - path: "src/tests/curation/review_dispositions.test.ts"
      provides: "Automated guard that review dispositions are evidence-only"
  key_links:
    - from: "data/compiled/v1/similarity_matrix.json"
      to: ".planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md"
      via: "review_queue evidence copied with disposition, not promotion"
      pattern: "review_queue"
---

<objective>
Create the Phase 8 manual curation surface before changing curated inputs.

Purpose: Preserve CUR-D-01 through CUR-D-07 and CUR-D-14 through CUR-D-37 by forcing explicit human approval, rationale, evidence, bucket, and disposition before seed/alias/relation/accord edits.
Output: A curation workbook plus a test that prevents treating review dispositions as automatic mutations.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-taxonomy-seed-expansion-curation/08-CONTEXT.md
@.planning/phases/08-taxonomy-seed-expansion-curation/08-RESEARCH.md
@.planning/phases/08-taxonomy-seed-expansion-curation/08-VALIDATION.md
@.planning/phases/08-taxonomy-seed-expansion-curation/08-PATTERNS.md
@data/compiled/v1/similarity_matrix.json
@data/compiled/v1/taxonomy.json
@src/compiler/review_queue.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create manual candidate review workbook</name>
  <files>.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</files>
  <read_first>.planning/phases/08-taxonomy-seed-expansion-curation/08-CONTEXT.md, .planning/phases/08-taxonomy-seed-expansion-curation/08-RESEARCH.md, .planning/phases/08-taxonomy-seed-expansion-curation/08-PATTERNS.md, data/compiled/v1/similarity_matrix.json, data/compiled/v1/taxonomy.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md if it already exists</read_first>
  <action>Create `.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md` as the authoritative Phase 8 curation workbook per CUR-D-01 through CUR-D-07 and CUR-D-26 through CUR-D-37. Include sections for in-scope groups `gourmand`, `spicy`, `green`, `fruity`, `amber_resinous`, and `animalic`; include an explicit deferred/out-of-scope section listing `marine_ozonic`, `tobacco`, `powdery`, `aldehydic`, `medicinal_camphoraceous`, `earthy_mineral`, and `smoky` per CUR-D-06. Add an evidence-ranked review queue section per CUR-D-30 through CUR-D-33 that is explicitly priority-only, not promotion: each queued item must include `priority_rank`, `frequency_signal`, `review_type`, `conflict_severity`, `placement_ambiguity`, `overloaded_subfamily_impact`, `priority_rationale`, and `promotion_effect: none`. Populate the queue with concrete Phase 7 evidence, not only an empty template: read `data/compiled/v1/similarity_matrix.json` and seed at least the top 20 review_queue/corpus candidate rows after deterministic ranking, preserving review item type/severity/affected/evidence values where present. For any in-scope group with no matching Phase 7 evidence, add an explicit `no_phase7_evidence_found` row with rationale instead of leaving the section blank. Define the ranking procedure as a deterministic sort over all CUR-D-31 factors with no single factor allowed to promote an entry. For each candidate block require fields `canonical_id`, `bucket`, `primary_disposition`, `secondary_hypotheses`, `target_family`, `target_subfamily`, `manual_approval`, `rationale`, `evidence`, `source_review_queue_refs`, `source_corpus_signal`, `aliases_to_review`, `relation_accord_followup`, and `curation_status`. Use only dispositions `promote_to_seed`, `add_alias`, `create_gap`, `relation_gap`, `reject`, and `defer` per CUR-D-35. Mark all unapproved entries as `manual_approval: pending`; do not copy candidates into curated JSON.</action>
  <verify><automated>test -f .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -q 'manual_approval' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -q 'marine_ozonic' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -q 'frequency_signal' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -q 'review_type' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -q 'conflict_severity' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -q 'placement_ambiguity' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -q 'overloaded_subfamily_impact' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -q 'promotion_effect: none' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -Eq 'source_review_queue_refs|source_corpus_signal|no_phase7_evidence_found' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</automated></verify>
  <acceptance_criteria>
    - Source assertion: workbook contains all six in-scope group IDs exactly: `gourmand`, `spicy`, `green`, `fruity`, `amber_resinous`, `animalic`.
    - Source assertion: workbook contains all seven deferred IDs exactly and states they are not Phase 8 promotion targets.
    - Source assertion: evidence-ranked queue records all CUR-D-31 factors: frequency, review type, conflict severity, placement ambiguity, and impact on overloaded subfamilies.
    - Source assertion: workbook is not template-only; it contains concrete Phase 7 evidence rows from `data/compiled/v1/similarity_matrix.json` or explicit `no_phase7_evidence_found` rows for in-scope groups without matching evidence.
    - Behavior assertion: ranking is documented as priority-only and cannot promote candidates without manual approval.
    - Source assertion: every candidate block template includes `manual_approval`, `rationale`, `evidence`, `primary_disposition`, and `relation_accord_followup`.
    - Behavior assertion: no data file under `data/` is modified by this task.
  </acceptance_criteria>
  <done>Candidate review workbook exists and encodes manual approval/rationale/evidence requirements before any curated input change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add review disposition evidence-only tests</name>
  <files>src/tests/curation/review_dispositions.test.ts</files>
  <read_first>src/tests/inference/seed_expansion_review.test.ts, src/compiler/review_queue.ts, src/compiler/compile_all.ts, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, src/tests/curation/review_dispositions.test.ts if it already exists</read_first>
  <behavior>
    - Test that official buckets/dispositions from CUR-D-27 and CUR-D-35 are represented in the workbook template.
    - Test that evidence-ranked queue fields cover frequency, review type, conflict severity, placement ambiguity, and overloaded subfamily impact per CUR-D-31.
    - Test that review queue sorting/building remains deterministic evidence and does not expose any mutation path into seed families/descriptors.
    - Test that high ranking or high frequency is described as prioritization only, not promotion.
  </behavior>
  <action>Create `src/tests/curation/review_dispositions.test.ts` using Vitest ESM style and `.js` relative imports where importing project modules. Read the workbook as text with `node:fs/promises` and assert the allowed bucket/disposition strings exist. Assert the evidence-ranked queue template includes `frequency_signal`, `review_type`, `conflict_severity`, `placement_ambiguity`, `overloaded_subfamily_impact`, `priority_rationale`, and `promotion_effect: none`. Import `sortReviewQueue` and/or `buildSeedGapReviewItems` from `../../compiler/review_queue.js` and assert output items preserve `suggested_action`/evidence fields while no seed object or descriptor list is returned. Implement per CUR-D-26 through CUR-D-37 and CUR-04; do not add dependencies or production mutation code.</action>
  <verify><automated>cd src &amp;&amp; npm test -- tests/curation/review_dispositions.test.ts</automated></verify>
  <acceptance_criteria>
    - Test-command assertion: `cd src && npm test -- tests/curation/review_dispositions.test.ts` passes.
    - Source assertion: test file imports from `vitest` and uses `.js` extensions for project imports.
    - Source assertion: test file checks `promote_to_seed`, `add_alias`, `create_gap`, `relation_gap`, `reject`, and `defer`.
    - Source assertion: test file contains no automatic write to `data/taxonomy/taxonomy-seed.v2.json`, `data/taxonomy/descriptor_aliases.seed.json`, `data/inference/curated_relations.v2.json`, or `data/inference/accord_map.v2.json`.
  </acceptance_criteria>
  <done>Review disposition tests prove Phase 7 review evidence remains review-only until separately approved and edited.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve curation workbook before data edits</name>
  <files>.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</files>
  <read_first>.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, .planning/phases/08-taxonomy-seed-expansion-curation/08-CONTEXT.md</read_first>
  <action>Pause execution for human verification of the curation workbook. The executor must not modify curated data files in this checkpoint. Before dependent plans may edit `taxonomy-seed.v2.json`, the human must explicitly approve at least one concrete seed expansion entry by family, subfamily, and descriptor with individual `manual_approval: approved`, `primary_disposition: promote_to_seed`, `rationale`, and `evidence` written into `.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md` per CUR-D-14 through CUR-D-18. Approval in the chat/transcript alone is not sufficient; the executor must persist the approved entry in the workbook before accepting `approved` as the resume signal. A generic workbook-structure approval is not sufficient for seed data edits; if no entries are approved in the workbook, dependent data-edit plans must stop rather than creating a non-expanded v2.</action>
  <what-built>A curation workbook and automated evidence-only tests were created.</what-built>
  <how-to-verify>Review `.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md`. Confirm every entry intended for later seed/alias/relation/accord changes has `manual_approval`, `rationale`, and `evidence`; confirm at least one seed expansion entry names an approved family, subfamily, and descriptor before v2 seed editing; confirm the approval is persisted in the workbook with `manual_approval: approved` and `primary_disposition: promote_to_seed`; confirm deferred groups remain deferred; confirm review_queue/corpus signals and evidence-ranked queue position are priority-only evidence, not promotion.</how-to-verify>
  <verify><automated>test -f .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -q 'manual_approval: approved' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -q 'primary_disposition: promote_to_seed' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -q 'rationale' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md &amp;&amp; grep -q 'evidence' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</automated></verify>
  <acceptance_criteria>
    - Human assertion: curator approves the workbook structure and explicitly approves at least one concrete family/subfamily/descriptor seed expansion entry before any v2 seed data edit.
    - Source assertion: the approved seed expansion is persisted in `candidate-review.md` with `manual_approval: approved`, `primary_disposition: promote_to_seed`, concrete family/subfamily/descriptor IDs, individual rationale, and evidence before `approved` is accepted as the resume signal.
    - Human assertion: no candidate is promoted solely from frequency, review_queue rank, or corpus evidence.
  </acceptance_criteria>
  <done>Human approval is recorded by the execution workflow, or corrections are returned before any dependent curated data plan runs.</done>
  <resume-signal>First persist at least one approved seed expansion entry in `candidate-review.md`; then type `approved` to continue, or describe required workbook corrections.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus/review_queue→curation workbook | Unreviewed Phase 7 evidence enters manual review, not curated truth. |
| curator workbook→future data edits | Human approval gates later JSON changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | candidate-review.md | mitigate | Require `manual_approval`, `rationale`, `evidence`, and constrained disposition for every candidate per CUR-D-14 through CUR-D-37. |
| T-08-02 | Tampering | review_queue evidence | mitigate | Test that review queue output remains evidence-only and does not write seed/alias/relation/accord files. |
| T-08-03 | Repudiation | curation decisions | mitigate | Workbook records rationale and evidence per entry before downstream JSON edits. |
| T-08-04 | Tampering | external enrichment | mitigate | No external scraping/enrichment; external sources may be cited manually only as evidence text. |
| T-08-SC | Tampering | npm installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
Run `cd src && npm test -- tests/curation/review_dispositions.test.ts` and complete the blocking human verification before executing dependent plans.
</verification>

<success_criteria>
The curation workbook exists, deferred categories are excluded, official buckets/dispositions are enforced by tests, and the human gate approves proceeding to curated JSON edits.
</success_criteria>

<output>
Create `.planning/phases/08-taxonomy-seed-expansion-curation/08-01-SUMMARY.md` when done.
</output>
