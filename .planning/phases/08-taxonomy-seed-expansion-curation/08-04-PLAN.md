---
phase: 08-taxonomy-seed-expansion-curation
plan: 04
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - data/inference/curated_relations.v2.json
  - data/inference/accord_map.v2.json
  - src/tests/curation/relation_accord_v2.test.ts
  - .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
autonomous: true
requirements:
  - CUR-06
must_haves:
  truths:
    - "Every new subfamily has either approved manual relation/accord scores or an explicit relation/accord gap rationale."
    - "Relation and accord scores are manual, normalized in [0,1], and revisable."
    - "Missing relation/accord remains neutral/undefined, not zero."
  artifacts:
    - path: "data/inference/curated_relations.v2.json"
      provides: "Versioned curated relation inputs for v2"
    - path: "data/inference/accord_map.v2.json"
      provides: "Versioned curated accord inputs for v2"
    - path: "src/tests/curation/relation_accord_v2.test.ts"
      provides: "Manual score and neutral-missing validation"
  key_links:
    - from: "data/inference/curated_relations.v2.json"
      to: "src/inference/curated_input_validation.ts"
      via: "validateCuratedRelationsInput"
      pattern: "score.*\[0,1\]"
    - from: "data/inference/accord_map.v2.json"
      to: "src/inference/curated_input_validation.ts"
      via: "validateAccordMapInput"
      pattern: "score.*\[0,1\]"
---

<objective>
Create v2 companion curated relation and accord inputs for the expanded seed.

Purpose: Implement CUR-D-51 through CUR-D-64 by considering relation/accord coverage with every new subfamily while keeping missing entries neutral and manual.
Output: `curated_relations.v2.json`, `accord_map.v2.json`, and focused validation tests.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-taxonomy-seed-expansion-curation/08-CONTEXT.md
@.planning/phases/08-taxonomy-seed-expansion-curation/08-PATTERNS.md
@.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
@data/taxonomy/taxonomy-seed.v2.json
@data/inference/curated_relations.v1.json
@data/inference/accord_map.v1.json
@src/inference/curated_input_validation.ts
@src/inference/tradition_score.ts
@src/inference/accord_compatibility.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add v2 relation and accord validation tests</name>
  <files>src/tests/curation/relation_accord_v2.test.ts</files>
  <read_first>src/tests/inference/tradition_score.test.ts, src/tests/inference/accord_compatibility.test.ts, src/inference/curated_input_validation.ts, data/taxonomy/taxonomy-seed.v2.json, data/inference/curated_relations.v1.json, data/inference/accord_map.v1.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, src/tests/curation/relation_accord_v2.test.ts if it already exists</read_first>
  <behavior>
    - Test v2 relation and accord files validate with existing input validation when they exist, while the test file itself passes before data creation by validating reusable fixtures and workbook gap/approval prerequisites.
    - Test every relation/accord subfamily ID exists in `taxonomy-seed.v2.json`.
    - Test scores are numbers in [0,1].
    - Test missing relation/accord for a v2 subfamily is represented as workbook gap text and not as score 0.
  </behavior>
  <action>Create `src/tests/curation/relation_accord_v2.test.ts` using Vitest ESM style. Load `data/taxonomy/taxonomy-seed.v2.json` and the curation workbook; load `data/inference/curated_relations.v2.json` and `data/inference/accord_map.v2.json` only if those files already exist. Import existing validation/scoring functions from `../../inference/curated_input_validation.js`, `../../inference/tradition_score.js`, and/or `../../inference/accord_compatibility.js` according to actual exports. Assert CUR-D-51 through CUR-D-64 with reusable helpers for manual score bounds, valid subfamily IDs, optional evidence/reference strings, no fake zero entries for missing relations/accords, and workbook traceability for approved records or `relation_gap`/accord-gap rationale. When v2 inference files are absent, the test must still pass by validating inline relation/accord fixtures and workbook prerequisites; it must not use an intentionally failing red phase because TDD_MODE is false.</action>
  <verify><automated>cd src &amp;&amp; npm test -- tests/curation/relation_accord_v2.test.ts tests/inference/tradition_score.test.ts tests/inference/accord_compatibility.test.ts</automated></verify>
  <acceptance_criteria>
    - Test-command assertion: `cd src && npm test -- tests/curation/relation_accord_v2.test.ts tests/inference/tradition_score.test.ts tests/inference/accord_compatibility.test.ts` passes at the end of this task without requiring v2 inference files to exist.
    - Source assertion: tests load both v2 inference JSON files when present, not v1 only.
    - Source assertion: tests check each relation/accord endpoint against v2 subfamily IDs.
    - Source assertion: tests assert missing relation/accord is not encoded as `score: 0`.
  </acceptance_criteria>
  <done>Validation contract exists for v2 relation/accord inputs and neutral missing semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Create v2 curated relation and accord files</name>
  <files>data/inference/curated_relations.v2.json, data/inference/accord_map.v2.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</files>
  <read_first>data/inference/curated_relations.v1.json, data/inference/accord_map.v1.json, data/taxonomy/taxonomy-seed.v2.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, src/tests/curation/relation_accord_v2.test.ts, data/inference/curated_relations.v2.json if it already exists, data/inference/accord_map.v2.json if it already exists</read_first>
  <action>Create `data/inference/curated_relations.v2.json` and `data/inference/accord_map.v2.json` as v2 companions. Start from v1 entries only when still applicable to v2 subfamily IDs, then add only workbook-approved manual relation/accord records per CUR-D-51 through CUR-D-64. Set each file `version` to `2.0.0`. For relations use fields `source_subfamily_id`, `target_subfamily_id`, `relation`, `score`, and optional `evidence`; for accords use `source_subfamily_id`, `target_subfamily_id`, `accord`, `score`, and optional `reference`. Scores must be explicit manual values in [0,1]; do not derive scores from corpus/co-occurrence; do not add score 0 placeholders. When the workbook says no relation/accord is approved, keep it absent and update `.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md` with `primary_disposition: relation_gap` or an explicit accord gap rationale for the affected v2 subfamily.</action>
  <verify><automated>cd src &amp;&amp; npm test -- tests/curation/relation_accord_v2.test.ts tests/inference/tradition_score.test.ts tests/inference/accord_compatibility.test.ts</automated></verify>
  <acceptance_criteria>
    - Source assertion: both v2 inference files contain `"version": "2.0.0"`.
    - Source assertion: no relation or accord record contains `"score": 0` unless an approved workbook block explicitly states a real manual zero score; otherwise missing entries are absent.
    - Source assertion: every new relation/accord has approval/rationale/evidence or reference in `candidate-review.md`.
    - Source assertion: every new v2 subfamily without an approved relation/accord has an explicit `relation_gap` or accord gap rationale in `candidate-review.md`.
    - Test-command assertion: focused relation/accord tests pass.
  </acceptance_criteria>
  <done>V2 relation and accord inputs exist with manual normalized scores or explicit workbook gaps, preserving neutral missing semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curation workbook→v2 inference inputs | Manual relation/accord approvals become graph scoring inputs. |
| v2 inference inputs→similarity graph | Manual scores influence compiled edges when v2 is explicitly compiled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Tampering | curated_relations.v2.json | mitigate | Existing validation plus tests enforce subfamily IDs and scores in [0,1]. |
| T-08-15 | Tampering | accord_map.v2.json | mitigate | Existing validation plus tests enforce subfamily IDs and scores in [0,1]. |
| T-08-16 | Tampering | relation/accord scores | mitigate | Acceptance requires workbook approval/rationale/evidence and forbids corpus-derived scores. |
| T-08-17 | Denial of Service | fake zero placeholders | mitigate | Tests/acceptance require missing relation/accord to remain absent/undefined, not zero. |
| T-08-18 | Tampering | external scraping/enrichment | mitigate | No external scraping; records come from manual workbook approval only. |
| T-08-SC | Tampering | npm installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
Run `cd src && npm test -- tests/curation/relation_accord_v2.test.ts tests/inference/tradition_score.test.ts tests/inference/accord_compatibility.test.ts`.
</verification>

<success_criteria>
V2 relation and accord files validate, use only manual normalized scores, refer to v2 subfamilies, and preserve undefined missing semantics.
</success_criteria>

<output>
Create `.planning/phases/08-taxonomy-seed-expansion-curation/08-04-SUMMARY.md` when done.
</output>
