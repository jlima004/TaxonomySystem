---
phase: 08-taxonomy-seed-expansion-curation
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - data/taxonomy/descriptor_aliases.seed.json
  - src/tests/curation/alias_seed_v2.test.ts
  - .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
autonomous: true
requirements:
  - CUR-05
must_haves:
  truths:
    - "Approved aliases point only to clear canonical seed v2 descriptors."
    - "Aliases are not primary descriptors in seed v2."
    - "Alias candidates and corpus frequency do not promote aliases automatically."
  artifacts:
    - path: "data/taxonomy/descriptor_aliases.seed.json"
      provides: "Curated alias seed map"
    - path: "src/tests/curation/alias_seed_v2.test.ts"
      provides: "Alias contamination guard for v2 canonical descriptors"
  key_links:
    - from: "data/taxonomy/descriptor_aliases.seed.json"
      to: "data/taxonomy/taxonomy-seed.v2.json"
      via: "alias target must exist as canonical descriptor"
      pattern: "alias target in seed descriptors"
---

<objective>
Expand curated aliases for approved v2 descriptors without contaminating primary descriptors.

Purpose: Implement CUR-D-38 through CUR-D-50 by requiring clear canonical ownership, manual approval, rationale, and evidence for every alias.
Output: Updated curated alias seed and focused alias validation tests.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-taxonomy-seed-expansion-curation/08-CONTEXT.md
@.planning/phases/08-taxonomy-seed-expansion-curation/08-PATTERNS.md
@.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
@data/taxonomy/taxonomy-seed.v2.json
@data/taxonomy/descriptor_aliases.seed.json
@src/loader/alias_validator.ts
@src/compiler/compile_aliases.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add v2 alias contamination tests</name>
  <files>src/tests/curation/alias_seed_v2.test.ts</files>
  <read_first>src/tests/compiler/compile_aliases.test.ts, src/loader/alias_validator.ts, src/compiler/compile_aliases.ts, data/taxonomy/taxonomy-seed.v2.json, data/taxonomy/descriptor_aliases.seed.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, src/tests/curation/alias_seed_v2.test.ts if it already exists</read_first>
  <behavior>
    - Test `descriptor_aliases.seed.json` validates with existing alias validator.
    - Test every alias target exists as a descriptor in `taxonomy-seed.v2.json`.
    - Test no alias key is also a primary descriptor in `taxonomy-seed.v2.json`.
    - Test no alias target points to `candidate`, an absent canonical, or an ambiguous/deferred canonical.
  </behavior>
  <action>Create `src/tests/curation/alias_seed_v2.test.ts` using Vitest ESM style. Import `validateAliasSeed` from `../../loader/alias_validator.js` and optionally `compileAliases` from `../../compiler/compile_aliases.js`. Load `data/taxonomy/descriptor_aliases.seed.json` and `data/taxonomy/taxonomy-seed.v2.json`. Build a set of v2 descriptors and assert each alias target is present, each alias key is absent from primary descriptors, and deferred group IDs from CUR-D-06 are not used as targets. Implement CUR-D-38 through CUR-D-50 and CUR-05; do not write production code.</action>
  <verify><automated>cd src &amp;&amp; npm test -- tests/curation/alias_seed_v2.test.ts tests/compiler/compile_aliases.test.ts</automated></verify>
  <acceptance_criteria>
    - Test-command assertion: focused alias tests pass after Task 2.
    - Source assertion: test checks alias targets against descriptors from `taxonomy-seed.v2.json`.
    - Source assertion: test checks alias keys are not primary descriptors.
    - Source assertion: test uses existing `validateAliasSeed`, not a new validation package.
  </acceptance_criteria>
  <done>Alias v2 validation contract prevents alias contamination and absent canonical targets.</done>
</task>

<task type="auto">
  <name>Task 2: Apply approved curated aliases</name>
  <files>data/taxonomy/descriptor_aliases.seed.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</files>
  <read_first>data/taxonomy/descriptor_aliases.seed.json, data/taxonomy/taxonomy-seed.v2.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, .planning/phases/08-taxonomy-seed-expansion-curation/08-CONTEXT.md, src/tests/curation/alias_seed_v2.test.ts</read_first>
  <action>Update `data/taxonomy/descriptor_aliases.seed.json` only with aliases whose candidate-review block has explicit `manual_approval: approved`, `primary_disposition: add_alias`, a clear canonical descriptor present in `taxonomy-seed.v2.json`, a short rationale, and evidence per CUR-D-38 through CUR-D-50. Keep existing aliases such as `jasmin`, `orangeflower`, `orange blossom`, `oak moss`, `ylang ylang`, `petit grain`, `patchouly`, `cedar wood`, and `sandal wood` unless the workbook explicitly approves a migration. Sort keys deterministically in ascending lexical order. Do not add ambiguous aliases; update `.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md` to mark ambiguous alias candidates as `primary_disposition: reject` or `primary_disposition: defer` with rationale/evidence instead of changing the alias map.</action>
  <verify><automated>cd src &amp;&amp; npm test -- tests/curation/alias_seed_v2.test.ts tests/compiler/compile_aliases.test.ts</automated></verify>
  <acceptance_criteria>
    - Source assertion: every new alias key in `descriptor_aliases.seed.json` has an approved `add_alias` block in `candidate-review.md`.
    - Source assertion: every alias value exists as a descriptor in `data/taxonomy/taxonomy-seed.v2.json`.
    - Source assertion: no alias key is also a primary descriptor in `data/taxonomy/taxonomy-seed.v2.json`.
    - Source assertion: any ambiguous alias candidate reviewed during this task is recorded in `candidate-review.md` as `reject` or `defer` with rationale/evidence rather than added to `descriptor_aliases.seed.json`.
    - Test-command assertion: `cd src && npm test -- tests/curation/alias_seed_v2.test.ts tests/compiler/compile_aliases.test.ts` passes.
  </acceptance_criteria>
  <done>Curated alias seed contains only approved aliases with clear canonical v2 targets and no primary-descriptor contamination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curation workbook→alias seed | Manual alias decisions become authoritative alias input. |
| alias seed→compiled aliases | Curated aliases become compiled authoritative aliases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Tampering | descriptor_aliases.seed.json | mitigate | Tests require targets to exist in v2 seed and keys not to be primary descriptors. |
| T-08-11 | Tampering | alias candidates | mitigate | Only workbook-approved `add_alias` entries may be added; frequency and alias candidates cannot promote automatically. |
| T-08-12 | Repudiation | alias rationale | mitigate | Acceptance requires each new alias to map back to approved workbook rationale/evidence. |
| T-08-13 | Tampering | external enrichment | mitigate | No scraping/enrichment; aliases come from manual workbook approval only. |
| T-08-SC | Tampering | npm installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
Run `cd src && npm test -- tests/curation/alias_seed_v2.test.ts tests/compiler/compile_aliases.test.ts`.
</verification>

<success_criteria>
Alias seed updates are deterministic, approved, target existing v2 canonical descriptors, and do not turn aliases into primary descriptors.
</success_criteria>

<output>
Create `.planning/phases/08-taxonomy-seed-expansion-curation/08-03-SUMMARY.md` when done.
</output>
