---
phase: 51-legacy-alias-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-legacy-alias-remediation/51-SAFE-FIT-RATIONALE.md
  - data/taxonomy/taxonomy-seed.v2.json
  - data/compiled/v2/taxonomy.json
  - data/compiled/v2/descriptor_aliases.json
  - data/compiled/v2/similarity_matrix.json
  - src/tests/inventory/alias_target_inventory.test.ts
  - src/tests/cli/alias_integrity.test.ts
  - .planning/phases/51-legacy-alias-remediation/51-01-SUMMARY.md
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "Before mutation, `npm run alias:integrity -- --json` exits 1 with exactly one unresolved target `ylang ylang -> ylang_ylang` per D-51-19 step 1 and D-51-22."
    - "`ylang_ylang` exists as a curated seed descriptor under `floral/floral_white` in `taxonomy-seed.v2.json` and in compiled `data/compiled/v2/taxonomy.json` after v2.9.0 publication per D-51-01, D-51-06, and D-51-20."
    - "The alias map `ylang ylang -> ylang_ylang` is preserved unchanged; `ylang` remains a separate corpus candidate; `alias_target_exceptions.v1.json` stays empty per D-51-02, D-51-05, D-51-03, and D-51-26."
    - "After publication, `npm run alias:integrity -- --json` exits 0 with `compiled_descriptor_count: 341`, `valid_target_count: 18`, `unresolved_target_count: 0` per D-51-19 steps 5 and D-51-22."
    - "Full vitest suite passes after state-locked test expectations are updated per D-51-19 step 6."
  artifacts:
    - path: ".planning/phases/51-legacy-alias-remediation/51-SAFE-FIT-RATIONALE.md"
      provides: "Evidence-backed safe-fit affirmation for ylang_ylang in floral_white before seed mutation"
    - path: "data/taxonomy/taxonomy-seed.v2.json"
      provides: "Curated seed with ylang_ylang appended to floral_white.descriptors"
    - path: "data/compiled/v2/taxonomy.json"
      provides: "Published v2.9.0 compiled taxonomy including id ylang_ylang"
    - path: "src/tests/inventory/alias_target_inventory.test.ts"
      provides: "Post-remediation live-data inventory assertions (341/18/0)"
    - path: "src/tests/cli/alias_integrity.test.ts"
      provides: "Post-remediation live-data gate assertions (exit 0 / PASS)"
  key_links:
    - from: "data/taxonomy/taxonomy-seed.v2.json"
      to: "data/compiled/v2/taxonomy.json"
      via: "npm run compile -- --version 2.9.0"
      pattern: "ylang_ylang"
    - from: "data/compiled/v2/taxonomy.json"
      to: "src/cli/alias_integrity.ts"
      via: "validateAliasTargetIntegrity reads compiled descriptor IDs"
      pattern: "validateAliasTargetIntegrity"
    - from: "data/taxonomy/descriptor_aliases.seed.json"
      to: "npm run alias:integrity"
      via: "unchanged alias ylang ylang -> ylang_ylang resolves when target exists"
      pattern: "ylang ylang"
---

<objective>
Resolve HYG-01 by remediating the single confirmed dangling alias `ylang ylang -> ylang_ylang` via the locked `add_target` path: append `ylang_ylang` to `floral/floral_white`, publish v2.9.0 compiled artifacts, update state-locked tests, and prove the Phase 50 `alias:integrity` gate flips from FAIL to PASS.

Purpose: Close the v2.9 alias-hygiene gap with deterministic before/after proof per D-51-19.
Output: Updated seed + compiled v2 artifacts, safe-fit rationale artifact, updated test expectations, and a green full suite.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.cursor/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/51-legacy-alias-remediation/51-CONTEXT.md
@.planning/phases/51-legacy-alias-remediation/51-RESEARCH.md
@.planning/phases/51-legacy-alias-remediation/51-PATTERNS.md
@.planning/phases/51-legacy-alias-remediation/51-VALIDATION.md
@.planning/phases/49-alias-target-integrity-inventory/49-ALIAS-TARGET-INVENTORY.md
@.planning/phases/50-alias-target-integrity-automation/50-01-SUMMARY.md
@data/taxonomy/taxonomy-seed.v2.json
@data/taxonomy/descriptor_aliases.seed.json
@data/taxonomy/alias_target_exceptions.v1.json
@data/compiled/v2/taxonomy.json
@src/cli/compile.ts
@src/cli/parse_args.ts
@src/cli/alias_integrity.ts
@src/tests/inventory/alias_target_inventory.test.ts
@src/tests/cli/alias_integrity.test.ts
@src/tests/curation/alias_seed_v2.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture before-proof, affirm safe-fit, and append ylang_ylang to seed</name>
  <read_first>
    - .planning/phases/51-legacy-alias-remediation/51-CONTEXT.md
    - .planning/phases/51-legacy-alias-remediation/51-PATTERNS.md
    - data/taxonomy/taxonomy-seed.v2.json
    - data/taxonomy/descriptor_aliases.seed.json
    - data/taxonomy/alias_target_exceptions.v1.json
    - data/compiled/v2/taxonomy.json
    - src/cli/alias_integrity.ts
  </read_first>
  <files>.planning/phases/51-legacy-alias-remediation/51-SAFE-FIT-RATIONALE.md, data/taxonomy/taxonomy-seed.v2.json</files>
  <acceptance_criteria>
    - `cd src && npm run alias:integrity -- --json` exits 1 with `status: FAIL`, `compiled_descriptor_count: 340`, `valid_target_count: 17`, `unresolved_target_count: 1`, and `unresolved[0].alias === "ylang ylang"` with `target === "ylang_ylang"` per D-51-19 step 1 and D-51-22.
    - `.planning/phases/51-legacy-alias-remediation/51-SAFE-FIT-RATIONALE.md` exists before seed mutation and documents: (a) `ylang_ylang` is a legitimate olfactory descriptor (D-51-10), (b) safe semantic fit in `floral/floral_white` alongside peers jasmine/tuberose/gardenia/orange_blossom (D-51-11/D-51-06), (c) no new family/subfamily required (D-51-12), (d) remediation does not use corpus candidate `ylang` (D-51-13), (e) no exception entry (D-51-14) per D-51-15.
    - `data/taxonomy/taxonomy-seed.v2.json` `floral/floral_white.descriptors` gains exactly one new trailing element `"ylang_ylang"` after `"linden_flower"` per D-51-01 and D-51-06; `git diff data/taxonomy/taxonomy-seed.v2.json` shows only that append.
    - `data/taxonomy/descriptor_aliases.seed.json` and `data/taxonomy/alias_target_exceptions.v1.json` are byte-identical to pre-task state per D-51-05 and D-51-03.
    - `grep -c '"ylang_ylang"' data/taxonomy/taxonomy-seed.v2.json` returns 1; `grep '"ylang_ylang"' data/taxonomy/descriptor_aliases.seed.json` still shows only the alias target value, not a new seed descriptor entry elsewhere.
  </acceptance_criteria>
  <action>
    Per D-51-19 step 1, run `cd src && npm run alias:integrity -- --json` and capture the JSON output as before-state evidence (do not mutate data first). If the gate does not fail with exactly `ylang ylang -> ylang_ylang`, halt and report — do not proceed with mutation.

    Per D-51-07/D-51-10/D-51-11/D-51-15, write `.planning/phases/51-legacy-alias-remediation/51-SAFE-FIT-RATIONALE.md` affirming that ylang-ylang (Cananga odorata) is a canonical white-floral material and belongs alongside the existing `floral_white` peers (`jasmine`, `tuberose`, `gardenia`, `orange_blossom`, `lily_of_the_valley`, `freesia`, `osmanthus`, `elderflower`, `linden_flower`). State explicitly that placement does not use corpus candidate `ylang`, does not require a new family/subfamily, and does not rely on an exception. If safe fit cannot be affirmed, halt per D-51-16/D-51-17 and do not mutate seed.

    Per D-51-01/D-51-06, edit `data/taxonomy/taxonomy-seed.v2.json`: in the `floral_white` subfamily block, append the plain string `"ylang_ylang"` as the last element of the `descriptors` array after `"linden_flower"`. Do not modify any other family, subfamily, or descriptor. Do not touch `descriptor_aliases.seed.json`, `alias_target_exceptions.v1.json`, relations, accords, `src/cli/parse_args.ts`, or corpus candidate `ylang` per D-51-23 through D-51-26.
  </action>
  <verify>
    <automated>cd src && npm run alias:integrity -- --json; test $? -eq 1</automated>
  </verify>
  <done>Before-proof captured, safe-fit rationale documented, and seed contains exactly one new `ylang_ylang` curated descriptor under `floral_white` with protected files unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Sandbox-compile and officially publish v2.9.0 artifacts</name>
  <read_first>
    - .planning/phases/51-legacy-alias-remediation/51-PATTERNS.md
    - .planning/phases/48-v2-8-artifact-publication-closure/48-CONTEXT.md
    - src/cli/compile.ts
    - src/cli/parse_args.ts
    - data/taxonomy/taxonomy-seed.v2.json
  </read_first>
  <files>data/compiled/v2/taxonomy.json, data/compiled/v2/descriptor_aliases.json, data/compiled/v2/similarity_matrix.json</files>
  <acceptance_criteria>
    - Sandbox compile succeeds: `cd src && rm -rf /tmp/phase51-compile-validate && npm run compile -- --version 2.9.0 --out /tmp/phase51-compile-validate --generated-at 2026-06-05T00:00:00Z` exits 0 per D-51-19 step 3.
    - Official publish succeeds: `cd src && npm run compile -- --version 2.9.0` exits 0 with no `--out` flag (default resolves to `data/compiled/v2`) per D-51-19 step 4 and D-51-20.
    - All three published artifacts report `version: "2.9.0"`: `data/compiled/v2/taxonomy.json`, `data/compiled/v2/descriptor_aliases.json`, `data/compiled/v2/similarity_matrix.json`.
    - `node -e` check on compiled taxonomy finds a descriptor with `id === "ylang_ylang"` and `source === "seed"` under `floral_white`.
    - `src/cli/parse_args.ts` `DEFAULT_PATHS.version` remains `'2.1.0'` (byte-identical default policy) per D-51-21.
    - `data/compiled/v1/*` and `data/taxonomy/taxonomy-seed.v1.json` are unchanged.
    - Compiled `descriptor_aliases.json` still has exactly 18 alias entries with `"ylang ylang": "ylang_ylang"` unchanged per D-51-05.
  </acceptance_criteria>
  <action>
    Per D-51-19 steps 3-4 and D-51-20, from the `src/` directory run the two-step compile pattern from 51-PATTERNS.md: first sandbox-compile with `--version 2.9.0 --out /tmp/phase51-compile-validate` (use `--out`, never `--output`), then official publish with `npm run compile -- --version 2.9.0` only (no `--out`). Do not hand-edit any file under `data/compiled/v2/`. Do not modify `DEFAULT_PATHS` in `src/cli/parse_args.ts` per D-51-21.

    After publish, verify `ylang_ylang` appears in `data/compiled/v2/taxonomy.json` with `source: "seed"` and that all three artifact `version` fields read `2.9.0`. Confirm `ylang` corpus candidate remains present and separate from `ylang_ylang` per D-51-26.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for (const p of ['data/compiled/v2/taxonomy.json','data/compiled/v2/descriptor_aliases.json','data/compiled/v2/similarity_matrix.json']) { const j=JSON.parse(fs.readFileSync(p,'utf8')); if (j.version !== '2.9.0') throw new Error(p+' version mismatch: '+j.version); } const t=JSON.parse(fs.readFileSync('data/compiled/v2/taxonomy.json','utf8')); let found=false; for (const f of t.families) for (const s of f.subfamilies||[]) for (const d of s.descriptors||[]) if (d.id==='ylang_ylang'&&d.source==='seed') found=true; if (!found) throw new Error('ylang_ylang not in compiled taxonomy');"</automated>
  </verify>
  <done>v2.9.0 artifacts officially published to `data/compiled/v2` with `ylang_ylang` present as a curated seed descriptor; defaults and protected surfaces unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Update state-locked tests and prove gate PASS + full suite green</name>
  <read_first>
    - .planning/phases/51-legacy-alias-remediation/51-PATTERNS.md
    - .planning/phases/51-legacy-alias-remediation/51-VALIDATION.md
    - src/tests/inventory/alias_target_inventory.test.ts
    - src/tests/cli/alias_integrity.test.ts
    - src/tests/curation/alias_seed_v2.test.ts
  </read_first>
  <files>src/tests/inventory/alias_target_inventory.test.ts, src/tests/cli/alias_integrity.test.ts</files>
  <acceptance_criteria>
    - `src/tests/inventory/alias_target_inventory.test.ts` live-audit case expects `descriptorIds.size === 341`, `valid` length 18, `dangling === {}`, and `compiledIds.has('ylang_ylang') === true` plus `seedIds.has('ylang_ylang') === true` per 51-PATTERNS.md; `compiledIds.has('ylang') === true` unchanged.
    - `src/tests/cli/alias_integrity.test.ts` live-data case expects `exitCode === 0`, `jsonOut.status === 'PASS'`, `unresolved_target_count === 0`, and `jsonOut.unresolved` length 0 per 51-PATTERNS.md.
    - `src/tests/cli/alias_integrity.test.ts` npm-wiring `--json` case at lines 150-154 expects `exitCode === 0` (not 1) after remediation.
    - Temp-fixture PASS/FAIL cases, `--help` case, and `alias_seed_v2.test.ts` remain green without modification per D-51-05.
    - `cd src && npm run alias:integrity -- --json` exits 0 with `compiled_descriptor_count: 341`, `valid_target_count: 18`, `unresolved_target_count: 0` per D-51-19 step 5.
    - `npm --prefix src test` exits 0 per D-51-19 step 6.
  </acceptance_criteria>
  <action>
    Per 51-PATTERNS.md and 51-VALIDATION.md Wave 0 gaps, update only the state-locked live-data expectations in `src/tests/inventory/alias_target_inventory.test.ts`: flip counts from 340/17/1-dangling to 341/18/0-dangling, flip `ylang_ylang` absence assertions to presence, update the `it(...)` title accordingly. Preserve inventory-artifact section checks and `ylang` near-match evidence assertions that document Phase 49 inventory (lines 99-130) unchanged.

    Update `src/tests/cli/alias_integrity.test.ts`: flip the primary live-data case (lines 76-91) from FAIL/exit 1 to PASS/exit 0 with zero unresolved targets. Also flip the npm-wiring `--json` case (lines 150-154) from `exitCode === 1` to `exitCode === 0`. Do not modify temp-fixture cases, `--help` case, or shape-only objectContaining fields that remain valid at PASS.

    Run `npm --prefix src test -- --run tests/curation/alias_seed_v2.test.ts` to confirm the unchanged alias map still passes. Then run full `npm --prefix src test` and `cd src && npm run alias:integrity -- --json` as final proof per D-51-19 steps 5-6.
  </action>
  <verify>
    <automated>cd src && npm run alias:integrity -- --json && npm --prefix src test</automated>
  </verify>
  <done>State-locked tests reflect post-remediation truth; `alias:integrity` exits 0; full vitest suite is green; HYG-01 is resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Curated seed → compiled artifacts | Seed mutation must compile to consistent v2.9.0 artifacts; hand-editing compiled JSON would desync the gate |
| Compiled taxonomy → alias:integrity gate | Gate reads compiled descriptor IDs; unpublished seed edits do not satisfy proof |
| Protected surfaces → remediation scope | Relations, accords, alias map, exception policy, scoring, Graphify, UI must not change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | data/compiled/v2/*.json | mitigate | Regenerate via `npm run compile -- --version 2.9.0`; never hand-edit; verify version alignment and `ylang_ylang` presence with node -e check |
| T-51-02 | Tampering | data/taxonomy/taxonomy-seed.v2.json | mitigate | `git diff` must show only one appended `"ylang_ylang"` string; no other families/subfamilies/descriptors modified |
| T-51-03 | Tampering | data/taxonomy/alias_target_exceptions.v1.json | mitigate | Keep `exceptions: []` empty per D-51-03/D-51-14; `validateExceptionPolicy` fails closed on malformed policy |
| T-51-04 | Tampering | data/taxonomy/descriptor_aliases.seed.json | mitigate | Byte-identical preservation per D-51-05; alias resolves when target exists, not by remap |
| T-51-05 | Elevation of privilege / scope creep | relations, accords, DEFAULT_PATHS, corpus ylang | mitigate | Protected-boundary checklist in Task 1/2 acceptance; halt if diff touches forbidden paths |
| T-51-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase; existing toolchain only |
</threat_model>

<verification>
1. `cd src && npm run alias:integrity -- --json` — before Task 1 mutation: exit 1, exactly `ylang ylang -> ylang_ylang`; after Task 3: exit 0, 341/18/0.
2. `git diff data/taxonomy/taxonomy-seed.v2.json` — exactly one `"ylang_ylang"` append in `floral_white`.
3. `node -e` version check — all three `data/compiled/v2/*` artifacts at `2.9.0` with `ylang_ylang` in compiled taxonomy.
4. `npm --prefix src test` — full suite green.
5. Protected surfaces unchanged: `descriptor_aliases.seed.json`, `alias_target_exceptions.v1.json`, `src/cli/parse_args.ts` DEFAULT_PATHS, `data/compiled/v1/*`.
</verification>

<success_criteria>
- HYG-01 resolved: `ylang ylang -> ylang_ylang` dangling target remediated via explicit `add_target` decision (not remap, drop, or exception).
- Phase 50 `alias:integrity` gate passes (exit 0) on live data after v2.9.0 publication.
- All locked decisions D-51-01 through D-51-27 honored; no protected-surface drift.
- Full test suite green with updated state-locked expectations.
</success_criteria>

<output>
Create `.planning/phases/51-legacy-alias-remediation/51-01-SUMMARY.md` when done.
</output>
