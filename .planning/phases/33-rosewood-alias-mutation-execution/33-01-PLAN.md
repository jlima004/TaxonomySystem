# Phase 33: Rosewood High-Confidence Alias Mutation Execution - Plan 01

## Objetivo
Executar as modificações nos arquivos de aliases para mapear os candidatos de alta confiança (`boi_de_rose` e `bois_de_rose`) ao target `rosewood`.

## Passos de Execução
1. **Modificar `data/taxonomy/descriptor_aliases.seed.json`**:
   - Adicionar ou atualizar a entrada `"boi_de_rose": "rosewood"`.
   - Adicionar ou atualizar a entrada `"bois_de_rose": "rosewood"`.
2. **Validar no `/tmp`**:
   - Compilar a taxonomia no ambiente temporário (`/tmp`) para garantir que os candidatos sumiram e os invariantes são mantidos.
3. **Verificar os Itens Excluídos**:
   - Garantir que `boi` permanece intocado (defer/manual_review).
   - Garantir que `pau_rosa` permanece intocado.
4. **Publicação (se aprovado)**:
   - Compilar a versão oficial v2 apenas se a compilação `/tmp` passar (PASS).
   - Atualizar a versão do sistema.
