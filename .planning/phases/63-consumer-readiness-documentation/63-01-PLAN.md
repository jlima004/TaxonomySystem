---
phase: 63-consumer-readiness-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/olfactory_graph_read_model.md
autonomous: true
requirements: [GQRY-07, GDOC-04, GDOC-05]
requirements_addressed: [GQRY-07, GDOC-04, GDOC-05]
must_haves:
  truths:
    - "GDOC-04 / D-01, D-02, D-03, D-04: the guide is reordered into the locked ten-section hierarchy and teaches `graph:build -> graph.json cru -> asValidatedGraph -> createValidatedQueryConsumer -> query proof` before any consumer interpretation."
    - "GQRY-07 / D-05, D-06, D-07, D-08, D-09, D-10: the guide contains a proof-envelope matrix for `query_kind`, `params`, `result`, and `path`, including safe use, forbidden readings, and the rule that future metadata requires explicit contract change."
    - "GQRY-07 / D-11, D-12, D-13, D-14, D-15: examples are labeled `Canonico`, `Ilustrativo`, or `Proibido`; the guide preserves validation errors, `graph_not_validated`, and missing-target success proofs as separate cases; anti-patterns forbid raw graph consumption, CLI queries, mandatory `path`, fabricated `ValidatedGraph`, and direct `query_graph.ts` use for agent/RAG integrations."
    - "GDOC-05 / D-16, D-17, D-18, D-19, D-20: global and local fences state the read model is static, read-only, zero-dependency, and not a runtime, API, service, database, Neo4J, Graphify, publication, persisted-proof, or public query CLI contract."
  artifacts:
    - "`docs/olfactory_graph_read_model.md` exists as the single Phase 63 edit target and contains the locked guide hierarchy."
    - "`docs/olfactory_graph_read_model.md` contains a proof-envelope consumption matrix backed by `src/graph_read_model/types.ts` and query/consumer tests."
    - "`docs/olfactory_graph_read_model.md` ends with a short non-scope checklist matching D-20."
  key_links:
    - "`docs/olfactory_graph_read_model.md` -> `src/cli/graph_read_model.ts` via documented `graph:build` dry-run/non-dry-run, guardrails, and boundary audit behavior."
    - "`docs/olfactory_graph_read_model.md` -> `src/graph_read_model/query_consumer.ts` via `asValidatedGraph(graph)` and `createValidatedQueryConsumer(validatedGraph)` as the only sanctioned consumer path."
    - "`docs/olfactory_graph_read_model.md` -> `src/graph_read_model/types.ts` via exact `{ query_kind, params, result, path? }` proof-envelope field semantics."
    - "`docs/olfactory_graph_read_model.md` -> existing Vitest suites via examples and validation commands copied from Phase 63 validation."
---

<objective>
Criar um plano documental, executor-ready, para transformar `docs/olfactory_graph_read_model.md` no guia de prontidao de consumo da Fase 63.

Purpose: documentar o workflow seguro de build-validacao-consumo e travar a fronteira do proof envelope para consumo futuro Alquem.io agent/RAG sem alterar contratos de codigo.
Output: uma alteracao documental coesa no guia operacional, verificada por checks de conteudo, typecheck e suites Vitest existentes.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/63-consumer-readiness-documentation/63-CONTEXT.md
@.planning/phases/63-consumer-readiness-documentation/63-RESEARCH.md
@.planning/phases/63-consumer-readiness-documentation/63-VALIDATION.md
@.planning/phases/63-consumer-readiness-documentation/63-PATTERNS.md
@.planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
@.planning/phases/61-fail-closed-query-consumption/61-CONTEXT.md
@.planning/phases/62-sanctioned-cli-boundary-proofs/62-CONTEXT.md
@docs/olfactory_graph_read_model.md
@docs/olfactory_graph_contract.md
@src/graph_read_model/types.ts
@src/graph_read_model/query_consumer.ts
@src/graph_read_model/query_graph.ts
@src/graph_read_model/validate_graph.ts
@src/graph_read_model/validation_errors.ts
@src/cli/graph_read_model.ts
@src/cli/sanctioned_graph_workflow.ts
@src/tests/graph_read_model/query_graph.test.ts
@src/tests/graph_read_model/query_consumer.test.ts
@src/tests/graph_read_model/query_live_baseline.test.ts
@src/tests/cli/graph_read_model.test.ts
@src/tests/cli/sanctioned_graph_workflow.test.ts
@src/tests/graph_read_model/write_graph.test.ts

<interfaces>
Preserve these contracts exactly:
- `GraphQueryProof<TKind, TParams, TResult>` remains `{ query_kind, params, result, path? }`.
- `query_kind` is the stable discriminator; `params` is request correlation only; `result` is the authoritative payload; `path` is optional provenance only.
- `asValidatedGraph(graph)` is the only sanctioned origin of a `ValidatedGraph`.
- `createValidatedQueryConsumer(validatedGraph)` is the only documented future agent/RAG consumer entrypoint.
- `query_graph.ts` remains a low-level primitive layer and must not be recommended as the integration surface.
- `graph:build` remains a build/validation/guardrail/write/audit workflow, not a query runtime or public query CLI. For operational order, `src/cli/sanctioned_graph_workflow.ts` and Phase 62 workflow tests are normative; divergent public help text is documentation/technical follow-up only and must not be copied as truth or fixed with code changes in Phase 63.
- Do not edit production code, `docs/olfactory_graph_contract.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, or `graphify-out/**`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder the guide into the locked trust-flow hierarchy</name>
  <files>docs/olfactory_graph_read_model.md</files>
  <read_first>
    - docs/olfactory_graph_read_model.md
    - .planning/phases/63-consumer-readiness-documentation/63-CONTEXT.md
    - .planning/phases/63-consumer-readiness-documentation/63-RESEARCH.md
    - .planning/phases/63-consumer-readiness-documentation/63-PATTERNS.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/62-sanctioned-cli-boundary-proofs/62-CONTEXT.md
    - src/cli/graph_read_model.ts
    - src/cli/sanctioned_graph_workflow.ts
  </read_first>
  <action>Rewrite the top-level structure of `docs/olfactory_graph_read_model.md` to use exactly these H2 sections in this order, per D-01: `## 1. Escopo, audiencia e fences`, `## 2. Mapa do fluxo completo`, `## 3. Workflow operacional graph:build`, `## 4. Validacao sancionada`, `## 5. ValidatedGraph`, `## 6. Consumer fail-closed`, `## 7. Envelope seguro para agent/RAG`, `## 8. Erros e missing targets`, `## 9. Exemplos canonicos e antipatrones`, `## 10. Referencias normativas e provas automatizadas`. Open with the global fence from D-16 and D-17: this is a static, read-only, zero-dependency graph read model guide, not runtime, API, HTTP service, database, Neo4J, Graphify, automatic publication, proof persistence, or public query CLI scope. In section 2, explicitly render the trust chain `graph:build -> graph.json cru -> asValidatedGraph -> createValidatedQueryConsumer -> query proof` and explain that this order teaches artifact lifecycle first, then trust boundary, then consumption per D-02 and D-03. In section 3, document `graph:build --dry-run`, non-dry-run, the normative operational order from `src/cli/sanctioned_graph_workflow.ts` and Phase 62 tests (`validate path -> pre-digests -> load/build/validate -> guardrails -> write -> boundary audit` for non-dry-run), boundary audit, the public JSON keys `ok`, `graph_output`, `boundary_audit`, `guardrails`, and the CLI fence that `graph:build` does not invoke or expose query functions, per D-04 and D-19. If the public help in `src/cli/graph_read_model.ts` still says `build -> validate -> write -> audit -> guardrails`, record that inconsistency as a documentation/technical follow-up inside the guide without copying the obsolete order and without altering code in Phase 63. Move existing historical Neo4J and Phase 59 recap material under no-scope/reference prose rather than leaving it before the workflow; do not turn conceptual Neo4J text into integration instructions.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const text=fs.readFileSync('docs/olfactory_graph_read_model.md','utf8'); const headings=['## 1. Escopo, audiencia e fences','## 2. Mapa do fluxo completo','## 3. Workflow operacional graph:build','## 4. Validacao sancionada','## 5. ValidatedGraph','## 6. Consumer fail-closed','## 7. Envelope seguro para agent/RAG','## 8. Erros e missing targets','## 9. Exemplos canonicos e antipatrones','## 10. Referencias normativas e provas automatizadas']; let last=-1; for (const h of headings){ const count=text.split(h).length-1; if (count !== 1) throw new Error('heading count '+count+' for '+h); const i=text.indexOf(h); if (!(i > last)) throw new Error('heading order broken at '+h); last=i; }"</automated>
    <automated>rg -n "graph:build -> graph.json cru -> asValidatedGraph -> createValidatedQueryConsumer -> query proof|--dry-run|boundary_audit|guardrails|graph:build.*query" docs/olfactory_graph_read_model.md</automated>
  </verify>
  <acceptance_criteria>
    - Heading assertion: the ten H2 headings appear exactly once each and in D-01 order.
    - Flow assertion: section 2 contains the exact chain `graph:build -> graph.json cru -> asValidatedGraph -> createValidatedQueryConsumer -> query proof`.
    - Fence assertion: the opening section names static, read-only, zero-dependency scope and explicitly excludes runtime, API, service, database, Neo4J, Graphify, publication, proof persistence, and public query CLI scope.
    - CLI assertion: section 3 covers `--dry-run`, non-dry-run, guardrails-before-write operational order from `src/cli/sanctioned_graph_workflow.ts`, boundary audit, and the four public JSON keys without describing query execution as a CLI feature.
    - Scope assertion: old Neo4J/recap prose is retained only as conceptual no-scope/reference material or removed; it no longer interrupts the safe workflow before validation and consumer boundaries.
  </acceptance_criteria>
  <done>The guide order and trust-flow map satisfy GDOC-04 and D-01 through D-04 without changing any source contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add the proof-envelope matrix, error taxonomy, and labeled examples</name>
  <files>docs/olfactory_graph_read_model.md</files>
  <read_first>
    - docs/olfactory_graph_read_model.md
    - .planning/phases/63-consumer-readiness-documentation/63-CONTEXT.md
    - .planning/phases/63-consumer-readiness-documentation/63-PATTERNS.md
    - src/graph_read_model/types.ts
    - src/graph_read_model/query_consumer.ts
    - src/graph_read_model/query_graph.ts
    - src/graph_read_model/validate_graph.ts
    - src/graph_read_model/validation_errors.ts
    - src/tests/graph_read_model/query_graph.test.ts
    - src/tests/graph_read_model/query_consumer.test.ts
    - src/tests/graph_read_model/query_live_baseline.test.ts
  </read_first>
  <action>In `docs/olfactory_graph_read_model.md`, add a compact proof-envelope matrix in section 7 with rows for `query_kind`, `params`, `result`, and `path`. Use these concrete classifications from D-06 through D-09: `query_kind` is `seguro e estavel`, `params` is `seguro com cautela`, `result` is `seguro e principal`, and `path` is `seguro condicional de proveniencia`. State per D-05 that consumers must discriminate by `query_kind` first, then interpret `params`, `result`, and optional `path`; state per D-10 that no field may be silently enriched or treated as expandable without explicit contract change. In sections 4 through 6, document the sanctioned path with concrete names: `validateSanctionedV211Graph(graph)`, `asValidatedGraph(graph)`, `ValidatedGraph`, and `createValidatedQueryConsumer(validatedGraph)`. In section 8, preserve the three distinct cases from D-13: validation failure with detailed structural/profile errors, missing validation proof as `graph_not_validated`, and missing target as a successful proof with empty array or `null` result. In section 9, label every example block or anti-pattern group as `Canonico`, `Ilustrativo`, or `Proibido` per D-11. Include the D-12 canonical example sequence in order: `graph:build --dry-run`; non-dry-run `graph:build` with boundary audit reading; reading `graph.json` as raw `OlfactoryGraph`; `asValidatedGraph(graph)` success; `asValidatedGraph(graph)` validation error; `createValidatedQueryConsumer(validatedGraph)`; a happy query interpreted by `query_kind`; bypass attempt producing `graph_not_validated`; missing target returning valid success proof. Add `Proibido` anti-patterns for D-14 and D-15, including raw `graph.json` query, CLI query exposure, mandatory `path`, facts inferred from `path`, empty `result` as failure, runtime/API/DB/Neo4J/Graphify/publication interpretation, fabricated `ValidatedGraph`, and direct `query_graph.ts` use for agent/RAG integrations. Use existing test expectations as examples; if desired prose conflicts with tests or types, record the mismatch as a documentation note/follow-up inside the guide and do not change code.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const text=fs.readFileSync('docs/olfactory_graph_read_model.md','utf8'); const required=['query_kind','seguro e estavel','params','seguro com cautela','result','seguro e principal','path','seguro condicional de proveniencia','graph_not_validated','target_descriptor_id: null','descriptors: []','Canonico','Ilustrativo','Proibido']; for (const token of required){ if (!text.includes(token)) throw new Error('missing token '+token); }"</automated>
    <automated>node -e "const fs=require('node:fs'); const text=fs.readFileSync('docs/olfactory_graph_read_model.md','utf8'); const start=text.indexOf('## 9. Exemplos canonicos e antipatrones'); const end=text.indexOf('## 10. Referencias normativas e provas automatizadas'); if (start===-1 || end===-1 || end <= start) throw new Error('section 9 bounds missing'); const section=text.slice(start,end); const ordered=['graph:build --dry-run','graph:build non-dry-run','graph.json','asValidatedGraph(graph)','erro detalhado de validacao','createValidatedQueryConsumer(validatedGraph)','query_kind','graph_not_validated','missing target']; let last=-1; for (const token of ordered){ const i=section.indexOf(token); if (i===-1) throw new Error('missing ordered token in section 9 '+token); if (!(i > last)) throw new Error('ordered token out of order in section 9 '+token); last=i; }"</automated>
    <automated>TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/query_graph.test.ts tests/graph_read_model/query_consumer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Matrix assertion: the envelope section contains rows for exactly `query_kind`, `params`, `result`, and `path`, with the D-06 through D-09 classifications and rules.
    - Contract assertion: success proofs are described as `{ query_kind, params, result, path? }`, without adding `ok`, `error`, `errors`, `validated`, confidence, ranking, authorization, or persistence metadata to the success envelope.
    - Error assertion: validation failure, `graph_not_validated`, and missing target are three separate documented cases.
    - Missing-target assertion: at least one example shows `target_descriptor_id: null` or empty descriptors as a valid success proof, and states this is not a system failure.
    - Example assertion: each example or anti-pattern group is labeled `Canonico`, `Ilustrativo`, or `Proibido`.
    - Source assertion: examples are sourced from `query_graph.test.ts`, `query_consumer.test.ts`, or `query_live_baseline.test.ts`; no invented helper such as `createValidatedQueryConsumerFromGraph`, `assertValidatedGraph`, or `OrThrow` is recommended.
  </acceptance_criteria>
  <done>The guide satisfies GQRY-07 and D-05 through D-15 with a concrete field matrix, labeled examples, and preserved error semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add final fences, normative references, and validation proof</name>
  <files>docs/olfactory_graph_read_model.md</files>
  <read_first>
    - docs/olfactory_graph_read_model.md
    - docs/olfactory_graph_contract.md
    - .planning/phases/63-consumer-readiness-documentation/63-VALIDATION.md
    - .planning/phases/63-consumer-readiness-documentation/63-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - src/tests/cli/graph_read_model.test.ts
    - src/tests/cli/sanctioned_graph_workflow.test.ts
    - src/tests/graph_read_model/write_graph.test.ts
  </read_first>
  <action>Finish section 10 with normative references to `docs/olfactory_graph_contract.md`, `src/graph_read_model/types.ts`, `src/graph_read_model/query_consumer.ts`, `src/graph_read_model/validate_graph.ts`, `src/graph_read_model/validation_errors.ts`, `src/cli/graph_read_model.ts`, `src/cli/sanctioned_graph_workflow.ts`, and the listed Vitest suites. Add the short final D-20 checklist with these exact no-scope checks as visible checklist items: `Nao expor query pela CLI`, `Nao consultar graph.json cru`, `Nao fabricar ValidatedGraph`, `Nao usar query_graph.ts diretamente em integracoes`, `Nao exigir path`, `Nao tratar missing target como falha`, and `Nao interpretar este guia como runtime/API/DB/Neo4J/Graphify/publicacao`. Review local fences in sections 3, 4, 5, 6, 7, 8, and 9 and make sure they match D-18 and D-19: CLI is not query runtime, raw `graph.json` is not validation proof, `ValidatedGraph` is not manually fabricated, consumer creation requires `createValidatedQueryConsumer(validatedGraph)`, envelope is not an API response/event/database record, and `path` is optional provenance only. Keep edits in `docs/olfactory_graph_read_model.md` only; do not edit production code, `docs/olfactory_graph_contract.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, or `graphify-out/**`.</action>
  <verify>
    <automated>rg -n "Nao expor query pela CLI|Nao consultar graph.json cru|Nao fabricar ValidatedGraph|Nao usar query_graph.ts diretamente em integracoes|Nao exigir path|Nao tratar missing target como falha|Nao interpretar este guia como runtime/API/DB/Neo4J/Graphify/publicacao" docs/olfactory_graph_read_model.md</automated>
    <automated>rg -n "docs/olfactory_graph_contract.md|src/graph_read_model/types.ts|src/graph_read_model/query_consumer.ts|src/graph_read_model/validate_graph.ts|src/graph_read_model/validation_errors.ts|src/cli/graph_read_model.ts|src/cli/sanctioned_graph_workflow.ts" docs/olfactory_graph_read_model.md</automated>
    <automated>TMPDIR=/tmp npm --prefix src run typecheck</automated>
    <automated>TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/query_graph.test.ts tests/graph_read_model/query_consumer.test.ts tests/graph_read_model/query_live_baseline.test.ts tests/cli/graph_read_model.test.ts tests/graph_read_model/write_graph.test.ts</automated>
    <automated>TMPDIR=/tmp npm --prefix src test -- tests/cli/sanctioned_graph_workflow.test.ts</automated>
    <automated>node -e "const {execSync}=require('node:child_process'); const out=execSync('git diff --name-only -- src docs/olfactory_graph_contract.md',{encoding:'utf8'}).trim(); if (out) throw new Error('unexpected non-doc contract/code diff: '+out);"</automated>
  </verify>
  <acceptance_criteria>
    - Checklist assertion: section 10 contains all seven D-20 checklist items exactly as listed in the action.
    - Fence assertion: local fences exist for `graph:build`, `graph.json`, `ValidatedGraph`, consumer, envelope agent/RAG, `path`, and examples.
    - Reference assertion: section 10 names the normative docs, source files, and test suites that back the guide.
    - Scope assertion: final diff touches `docs/olfactory_graph_read_model.md` for implementation; no production code or `docs/olfactory_graph_contract.md` changes are required.
    - Command assertion: typecheck, targeted graph-read-model/CLI suites, and sanctioned workflow suite pass with `TMPDIR=/tmp`.
    - Documentation-first assertion: any mismatch discovered between desired prose and existing contracts is recorded as a note/follow-up, never resolved by changing code contracts in this phase.
  </acceptance_criteria>
  <done>The guide closes GDOC-05 and D-16 through D-20, and final validation proves the phase remains documentation-first with code contracts intact.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Document the safe build-validate-query workflow and lock the proof-envelope boundary for future Alquem.io agent/RAG consumption | 63-01 | COVERED | All tasks edit only the operational guide and preserve code contracts.
REQ | GQRY-07 | Document safe-consumption boundaries for every proof-envelope field | 63-01 Task 2 | COVERED | Envelope matrix covers `query_kind`, `params`, `result`, and `path`.
REQ | GDOC-04 | Reorder operational guide for safe build -> validate -> query workflow | 63-01 Task 1 | COVERED | Locked ten-section hierarchy and trust-flow map.
REQ | GDOC-05 | Consumer-readiness guidance stays read-only and excludes runtime/API/database/export scope | 63-01 Task 3 | COVERED | Global/local fences and final no-scope checklist.
RESEARCH | - | Primary recommendation: one documentation-first plan editing `docs/olfactory_graph_read_model.md` | 63-01 | COVERED | Single coeso plan, no production code edits.
RESEARCH | - | Use types and tests as normative sources | 63-01 Tasks 2-3 | COVERED | `types.ts`, consumer/query modules, and listed tests are read-first and validation anchors.
RESEARCH | - | No new package installation or external service | 63-01 | COVERED | No install tasks, no user setup, threat model records package scope.
RESEARCH | - | Use `TMPDIR=/tmp` validation commands | 63-01 Task 3 | COVERED | Final verification uses Phase 63 validation commands.
CONTEXT | D-01 | Ten-section canonical guide order | 63-01 Task 1 | COVERED | Exact headings asserted by Node check.
CONTEXT | D-02 | Teach artifact lifecycle before trust boundary and consumption | 63-01 Task 1 | COVERED | Trust-flow map precedes validation and consumer sections.
CONTEXT | D-03 | Explicit full-flow chain | 63-01 Task 1 | COVERED | Exact chain is required.
CONTEXT | D-04 | `graph:build` covers dry-run, non-dry-run, guardrails, boundary audit, and CLI non-query fence | 63-01 Task 1 | COVERED | CLI section action and acceptance criteria require it.
CONTEXT | D-05 | Discriminate first by `query_kind` | 63-01 Task 2 | COVERED | Envelope action mandates this rule.
CONTEXT | D-06 | `query_kind` safe and stable | 63-01 Task 2 | COVERED | Matrix row required.
CONTEXT | D-07 | `params` safe with caution | 63-01 Task 2 | COVERED | Matrix row required.
CONTEXT | D-08 | `result` safe and primary | 63-01 Task 2 | COVERED | Matrix row required.
CONTEXT | D-09 | `path` conditional provenance only | 63-01 Tasks 2-3 | COVERED | Matrix and local fence required.
CONTEXT | D-10 | No silent enrichment or expansion of envelope fields | 63-01 Task 2 | COVERED | Acceptance forbids extra success metadata.
CONTEXT | D-11 | Examples labeled `Canonico` / `Ilustrativo` / `Proibido` | 63-01 Task 2 | COVERED | Label checks required.
CONTEXT | D-12 | Canonical example sequence | 63-01 Task 2 | COVERED | Ordered-token check required.
CONTEXT | D-13 | Preserve three error/missing-target cases | 63-01 Task 2 | COVERED | Error taxonomy acceptance.
CONTEXT | D-14 | Required anti-patterns | 63-01 Task 2 | COVERED | Proibido group required.
CONTEXT | D-15 | No fabricated `ValidatedGraph`; no direct `query_graph.ts` agent/RAG integration | 63-01 Task 2 | COVERED | Proibido group and source assertion required.
CONTEXT | D-16 | Global static/read-only/no-runtime fence | 63-01 Task 1 | COVERED | Opening fence required.
CONTEXT | D-17 | Global raw graph and existing-surface clarification | 63-01 Task 1 | COVERED | Opening fence and trust-flow map required.
CONTEXT | D-18 | Local fences in high-risk sections | 63-01 Task 3 | COVERED | Local fence assertion required.
CONTEXT | D-19 | Concrete local fence wording by section | 63-01 Task 3 | COVERED | Action lists each section-specific fence.
CONTEXT | D-20 | Final no-scope checklist | 63-01 Task 3 | COVERED | Exact final checklist strings required.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> future consumers | Maintainers and future agent/RAG implementers may treat prose as contract; prose must not broaden implemented surfaces. |
| Raw artifact -> validated handle | Re-read `graph.json` is raw `OlfactoryGraph` until `asValidatedGraph(graph)` succeeds. |
| Proof envelope -> consumer interpretation | Consumers must route by `query_kind` and must not overread `params` or `path`. |
| CLI workflow -> query consumption | `graph:build` produces/audits artifacts; it is not a query runtime or public query command surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Spoofing / Tampering | `graph.json` consumption prose | mitigate | Require the trust chain `graph.json cru -> asValidatedGraph(graph) -> createValidatedQueryConsumer(validatedGraph)` before any query proof example. |
| T-63-02 | Elevation of Privilege | guide scope and CLI interpretation | mitigate | Global and local fences explicitly exclude runtime, API, service, DB, Neo4J, Graphify, publication, proof persistence, and public query CLI scope. |
| T-63-03 | Tampering | proof-envelope interpretation | mitigate | Matrix classifies `query_kind`, `params`, `result`, and `path`; forbids extra fields and silent enrichment. |
| T-63-04 | Repudiation | error and missing-target taxonomy | mitigate | Document validation failure, `graph_not_validated`, and valid empty/null proof as separate cases backed by tests. |
| T-63-05 | Information Disclosure | `path` provenance | mitigate | State `path` is optional provenance/explanation only and cannot define confidence, ranking, authorization, or completeness. |
| T-63-SC | Tampering | package/runtime scope creep | mitigate | No package installs, no services, no production code edits, and no `graphify-out/**` work are planned. |
</threat_model>

<verification>
Overall phase checks:
- `TMPDIR=/tmp npm --prefix src run typecheck`
- `TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/query_graph.test.ts tests/graph_read_model/query_consumer.test.ts tests/graph_read_model/query_live_baseline.test.ts tests/cli/graph_read_model.test.ts tests/graph_read_model/write_graph.test.ts`
- `TMPDIR=/tmp npm --prefix src test -- tests/cli/sanctioned_graph_workflow.test.ts`
- `rg -n "graph:build|asValidatedGraph|createValidatedQueryConsumer|graph_not_validated|query_kind|params|result|path|Canonico|Ilustrativo|Proibido" docs/olfactory_graph_read_model.md`
- `rg -n "runtime|API|Neo4J|Graphify|database|banco|publicacao|persistencia" docs/olfactory_graph_read_model.md`
- `rg -n "createValidatedQueryConsumerFromGraph|assertValidatedGraph|OrThrow|--out" docs/olfactory_graph_read_model.md` reviewed so any occurrence is marked as forbidden or no-scope, never recommended.
- `node -e "const {execSync}=require('node:child_process'); const out=execSync('git diff --name-only -- src docs/olfactory_graph_contract.md',{encoding:'utf8'}).trim(); if (out) throw new Error('unexpected non-doc contract/code diff: '+out);"`
</verification>

<success_criteria>
- Maintainers can read the guide top-to-bottom without jumping across historical sections to understand build, validation, consumer boundary, envelope semantics, and non-scope fences.
- Future Alquem.io agent/RAG consumers can identify which proof-envelope fields are safe to consume and which readings are forbidden.
- The guide preserves the implemented fail-closed boundary and missing-target semantics without changing production code.
- Phase 63 remains documentation-first: implementation target is `docs/olfactory_graph_read_model.md`, code contracts stay intact, and no `graphify-out/**` work is planned.
</success_criteria>

<output>
Create `.planning/phases/63-consumer-readiness-documentation/63-01-SUMMARY.md` when done.
</output>
