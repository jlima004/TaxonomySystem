# Phase 28 Plan — Ambergri Alias Mutation

## Escopo
1. Efetuar a adição do alias `ambergri → ambergris` em `descriptor_aliases.seed.json`.
2. Validar compilando temporariamente em `/tmp`.
3. Validar testes de unidade.
4. Efetuar compilação definitiva em `data/compiled/v2` (version 2.4.0).

## Passos de Execução
- [ ] 1. Alterar `data/taxonomy/descriptor_aliases.seed.json` inserindo a chave `"ambergri": "ambergris"`.
- [ ] 2. Executar a suite de testes locais `npm run check:types` e `npm test`.
- [ ] 3. Compilar em `/tmp` utilizando somente flags e comandos oficiais do compilador, assegurando saída em `/tmp`, `version=2.4.0` e data fixada em `2026-05-27T00:00:00.000Z`. Não alterar scripts nem manipular output.
- [ ] 4. Rodar validação programática (ou inspeção baseada em `jq`/grep) para confirmar INV-4, INV-5, INV-6, INV-7 nos arquivos do `/tmp`.
- [ ] 5. Obter **persisted approval** para a publicação dos artifacts.
- [ ] 6. Rodar o build de publicação final (garantindo versionamento `2.4.0` e data `2026-05-27T00:00:00.000Z`) direcionado para `data/compiled/v2`.
- [ ] 7. Gerar `28-CLOSURE.md`.

*Status: aguardando permissão do usuário (persisted approval) para iniciar a execução de qualquer mutação.*
