---
phase: 44-remaining-low-support-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/44-remaining-low-support-inventory/44-LOW-SUPPORT-INVENTORY.md
autonomous: true
requirements: [INV-01, INV-02]

must_haves:
  truths:
    - "44-LOW-SUPPORT-INVENTORY.md exists in the phase directory"
    - "The inventory lists exactly 259 corpus_candidate_low_support items extracted from the v2.7 compiled artifact"
    - "All 10 seed_corpus_conflict rows are excluded from the inventory"
    - "The six v2.7 promoted descriptors (peppermint, rosemary, cumin, spearmint, caraway, opoponax) are proven absent from the current low_support queue"
    - "No Batch 2 selection is made — readiness groups are advisory metadata only"
    - "Zero mutations are confirmed — no taxonomy, alias, compiled artifact, or code changes"
  artifacts:
    - path: ".planning/phases/44-remaining-low-support-inventory/44-LOW-SUPPORT-INVENTORY.md"
      provides: "Complete low-support candidate inventory for downstream Phase 45 Batch 2 selection"
      contains: "corpus_candidate_low_support"
  key_links:
    - from: "44-LOW-SUPPORT-INVENTORY.md"
      to: "data/compiled/v2/similarity_matrix.json"
      via: "review_queue extraction"
      pattern: "corpus_candidate_low_support"
---

<objective>
Validate and finalize the Phase 44 low-support inventory artifact against the v2.7 compiled source of truth.

Purpose: Confirm the existing 44-LOW-SUPPORT-INVENTORY.md meets all acceptance criteria — correct row count (259), exclusion proof (10 seed_corpus_conflict excluded, 6 v2.7 promoted absent), deterministic ordering, no Batch 2 selection, and zero mutations — so Phase 45 can consume it as the only eligible source for Batch 2 selection.

Output: Validated 44-LOW-SUPPORT-INVENTORY.md artifact with all acceptance criteria proven.
</objective>

<execution_context>
@.agents/get-shit-done/workflows/execute-plan.md
@.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-remaining-low-support-inventory/44-CONTEXT.md
@data/compiled/v2/similarity_matrix.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate inventory completeness and correctness</name>
  <files>.planning/phases/44-remaining-low-support-inventory/44-LOW-SUPPORT-INVENTORY.md</files>

  <read_first>
    - .planning/phases/44-remaining-low-support-inventory/44-LOW-SUPPORT-INVENTORY.md
    - .planning/phases/44-remaining-low-support-inventory/44-CONTEXT.md
    - data/compiled/v2/similarity_matrix.json
  </read_first>

  <action>
  Run a deterministic validation script against the v2.7 compiled artifact to confirm:

  1. **Row count**: Extract all `corpus_candidate_low_support` items from `data/compiled/v2/similarity_matrix.json` review_queue. Count must equal exactly 259. Cross-check with the inventory table row count in `44-LOW-SUPPORT-INVENTORY.md`.

  2. **Exclusion proof**: Extract all `seed_corpus_conflict` items. Count must equal exactly 10. Confirm none appear in the inventory table.

  3. **v2.7 promoted absence**: Check descriptors `peppermint`, `rosemary`, `cumin`, `spearmint`, `caraway`, `opoponax` against the current `corpus_candidate_low_support` list. All six must be absent. Confirm the exclusion proof table in the inventory records this.

  4. **No Batch 2 selection**: Confirm the inventory uses non-selecting readiness groups only (per D-44-04, D-44-05). No candidate has a disposition of promote, reject, alias, defer, or manual_review.

  5. **Zero mutations**: Confirm no taxonomy seed, alias, compiled artifact, or source code files were modified by this phase. Per D-44-02, the zero-mutation confirmation section must exist in the inventory.

  6. **Ordering**: Verify the inventory is sorted by `candidate_frequency` descending, then `placement_score` descending, then candidate ascending (per D-44-09).

  If any criterion fails, fix the inventory artifact to match the source of truth. If all pass, no changes are needed.
  </action>

  <acceptance_criteria>
    - `node -e "const d=require('./data/compiled/v2/similarity_matrix.json'); const low=d.review_queue.filter(i=>i.type==='corpus_candidate_low_support'); console.log(low.length)"` outputs `259`
    - `node -e "const d=require('./data/compiled/v2/similarity_matrix.json'); const conf=d.review_queue.filter(i=>i.type==='seed_corpus_conflict'); console.log(conf.length)"` outputs `10`
    - `node -e "const d=require('./data/compiled/v2/similarity_matrix.json'); const low=d.review_queue.filter(i=>i.type==='corpus_candidate_low_support'); const promoted=['peppermint','rosemary','cumin','spearmint','caraway','opoponax']; const found=promoted.filter(p=>low.some(i=>i.affected.descriptor===p)); console.log(found.length)"` outputs `0`
    - `grep -c '| \`' .planning/phases/44-remaining-low-support-inventory/44-LOW-SUPPORT-INVENTORY.md` row count matches 259 data rows (excluding headers)
    - The inventory contains "Zero-Mutation Confirmation" section
    - The inventory contains the v2.7 promoted descriptor exclusion proof table with all six showing "no"
    - No readiness group uses selecting language (promote, reject, alias, defer, manual_review)
  </acceptance_criteria>

  <verify>
    <automated>node -e "const d=require('./data/compiled/v2/similarity_matrix.json'); const low=d.review_queue.filter(i=>i.type==='corpus_candidate_low_support'); const conf=d.review_queue.filter(i=>i.type==='seed_corpus_conflict'); const promoted=['peppermint','rosemary','cumin','spearmint','caraway','opoponax']; const found=promoted.filter(p=>low.some(i=>i.affected.descriptor===p)); console.log('low_support:', low.length, '(expect 259)'); console.log('conflict:', conf.length, '(expect 10)'); console.log('promoted_in_low:', found.length, '(expect 0)'); if(low.length!==259||conf.length!==10||found.length!==0) process.exit(1)"</automated>
  </verify>

  <done>
  44-LOW-SUPPORT-INVENTORY.md exists, inventories exactly 259 corpus_candidate_low_support rows, excludes all 10 seed_corpus_conflict rows, proves the six v2.7 promoted descriptors are absent from current low_support, does not select Batch 2, and confirms zero mutations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This is a read-only inventory phase producing a documentation artifact. No user input, API, network, auth, or data mutation boundaries exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | 44-LOW-SUPPORT-INVENTORY.md | accept | Read-only documentation artifact; git history provides integrity. No runtime code, no user-facing surface. |
| T-44-02 | Information Disclosure | similarity_matrix.json | accept | Already a published compiled artifact in the repository. No secrets or PII. |
</threat_model>

<verification>
- The v2.7 compiled artifact (`data/compiled/v2/similarity_matrix.json`) contains exactly 259 `corpus_candidate_low_support` and 10 `seed_corpus_conflict` items
- The inventory artifact row count matches 259
- The six v2.7 promoted descriptors do not appear in the current low_support queue
- No files outside `.planning/phases/44-remaining-low-support-inventory/` were modified
- No taxonomy, alias, compiled artifact, inference, or code mutations occurred
</verification>

<success_criteria>
- 44-LOW-SUPPORT-INVENTORY.md exists in the phase directory
- Inventory contains exactly 259 corpus_candidate_low_support rows
- All 10 seed_corpus_conflict rows are excluded
- Six v2.7 promoted descriptors proven absent
- No Batch 2 selection was made
- Zero mutations confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/44-remaining-low-support-inventory/44-01-SUMMARY.md`
</output>
