---
phase: 47-controlled-curation-mutation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/taxonomy/taxonomy-seed.v2.json
  - .planning/phases/47-controlled-curation-mutation/47-VERIFICATION.md
  - .planning/phases/47-controlled-curation-mutation/47-01-SUMMARY.md
autonomous: true
requirements:
  - CUR-01
  - CUR-02
  - CUR-03
requirements_addressed:
  - CUR-01
  - CUR-02
  - CUR-03
user_setup: []
must_haves:
  truths:
    - "CUR-01: Only explicitly approved safe seed additions from the decision matrix were applied — exactly 12 promote_to_seed rows, zero add_alias, zero reject/defer/manual_review promoted."
    - "CUR-02: Rejects, defers, and manual_review outcomes remain non-promoted decisions; their descriptors are absent from taxonomy-seed.v2.json after mutation."
    - "CUR-03: Protected boundaries remain unchanged — data/compiled/*, data/taxonomy/taxonomy-seed.v1.json, descriptor_aliases.seed.json, data/inference/*, src/**, graphify-out/*, package.json are byte-identical to pre-mutation state."
    - "D-47-04: No compiled artifacts in data/compiled/* were created or modified; sandbox compile output was captured under /tmp only."
    - "D-47-08: Parser assertion proves exactly 12 new descriptors exist at the approved target paths, zero outside, zero removed, zero other JSON fields changed."
    - "D-47-20: scripts/check-safety-guards.sh passes before and after mutation with exit code 0."
    - "D-47-21: git diff --name-only at end of plan contains only allow-listed paths from D-47-18."
    - "D-47-24: The 10-step locked flow was executed in order without skipping steps."
  artifacts:
    - path: data/taxonomy/taxonomy-seed.v2.json
      provides: "Canonical v2 seed taxonomy with 12 new curated seed descriptors (49 → 61 total)"
      contains: "carrot_seed, freesia, cardamom, tangerine, saffron, osmanthus, cubeb, elderflower, mace, linden_flower, agarwood, tolu"
    - path: .planning/phases/47-controlled-curation-mutation/47-VERIFICATION.md
      provides: "Parser assertion results, safety-guard results, diff assertion, sandbox compile metrics, zero-hard-failures assertion"
      contains: "## Verification"
    - path: .planning/phases/47-controlled-curation-mutation/47-01-SUMMARY.md
      provides: "Per-row status table, validation summary, Phase 48 handoff note"
      contains: "## Summary"
  key_links:
    - from: .planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md
      to: data/taxonomy/taxonomy-seed.v2.json
      via: "12 mutation_allowed=true rows with mechanical phase47_instruction values"
      pattern: "add_seed target_family="
    - from: data/taxonomy/taxonomy-seed.v2.json
      to: .planning/phases/48-v2-8-artifact-publication-closure
      via: "Phase 48 reads mutated seed for official compile and publication"
      pattern: "taxonomy-seed.v2.json"
---

<objective>
Apply exactly 12 approved promote_to_seed mutations from the Phase 46 decision matrix to `data/taxonomy/taxonomy-seed.v2.json`, then validate via parser assertion, safety guards, typecheck, test suite, and sandbox compile. This plan implements CUR-01, CUR-02, CUR-03 and locked context decisions D-47-01 through D-47-33.

Purpose: Execute the user-locked 10-step flow (D-47-24) to transform decision-matrix authorization into seed-truth mutation with full sandbox validation, while keeping compiled artifact publication for Phase 48.

Output:
- `data/taxonomy/taxonomy-seed.v2.json` with 12 new seed descriptors (49 → 61 total)
- `.planning/phases/47-controlled-curation-mutation/47-VERIFICATION.md`
- `.planning/phases/47-controlled-curation-mutation/47-01-SUMMARY.md`
</objective>

<execution_context>
@.agents/get-shit-done/workflows/execute-plan.md
@.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-controlled-curation-mutation/47-CONTEXT.md
@.planning/phases/47-controlled-curation-mutation/47-RESEARCH.md
@.planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md
@.planning/phases/46-batch-2-decision-matrix/46-CONTEXT.md
@.planning/phases/46-batch-2-decision-matrix/46-PATTERNS.md
@data/taxonomy/taxonomy-seed.v2.json
@scripts/check-safety-guards.sh
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Approved Batch 2 decisions are applied safely without automatic promotion or protected-boundary drift | Covered by Tasks 1-5 full locked flow |
| REQ | CUR-01 apply only explicitly approved safe seed additions or aliases | Covered by Task 1 matrix parsing + Task 2 exact 12 edits + Task 3 parser assertion |
| REQ | CUR-02 preserve rejects, defers, manual_review as non-promoted | Covered by Task 1 ignoring 28 non-executable rows + Task 3 proving zero outside additions |
| REQ | CUR-03 verify protected boundaries unchanged | Covered by Task 4 safety-guard + diff assertion + Task 5 verification artifacts |
| RESEARCH | 49 baseline seed descriptors, 12 target paths validated, all family/subfamily nodes exist | Covered by Task 2 pre-edit assertions + Task 3 post-edit assertions |
| RESEARCH | Sandbox compile command with --version 2.8.0 to /tmp | Covered by Task 5 sandbox compile step |
| RESEARCH | scripts/check-safety-guards.sh before/after | Covered by Task 4 |
| CONTEXT | D-47-01 through D-47-33 locked decisions | Covered by all tasks following locked 10-step flow |
| CONTEXT | D-47-07 direct JSON Edit pattern, one per add_seed | Covered by Task 2 |
| CONTEXT | D-47-10 exact 12-row set with id ↔ target path mapping | Covered by Task 1 parse + Task 2 apply + Task 3 verify |
| CONTEXT | D-47-25 atomic instruction-level edits | Covered by Task 2 sequential application |

No source items are missing. Deferred ideas: none.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Parse decision matrix and extract 12 executable rows</name>
  <files>No files modified — read-only parsing step</files>
  <read_first>
    .planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md;
    .planning/phases/47-controlled-curation-mutation/47-CONTEXT.md;
    data/taxonomy/taxonomy-seed.v2.json
  </read_first>
  <action>Parse `46-DECISION-MATRIX.md` and extract only rows where `mutation_allowed=true`. This implements D-47-24 steps 1–3. Confirm exactly 12 rows are extracted and all 12 have `disposition == promote_to_seed`. Confirm zero `add_alias` rows exist. Confirm the 12 extracted rows match exactly the D-47-10 set: id 05 → `spicy/warm_spice/carrot_seed`, id 10 → `floral/floral_white/freesia`, id 11 → `spicy/warm_spice/cardamom`, id 14 → `citrus/citrus_fresh/tangerine`, id 15 → `spicy/warm_spice/saffron`, id 20 → `floral/floral_white/osmanthus`, id 21 → `spicy/warm_spice/cubeb`, id 22 → `floral/floral_white/elderflower`, id 29 → `spicy/warm_spice/mace`, id 30 → `floral/floral_white/linden_flower`, id 32 → `woody/woody_dry/agarwood`, id 37 → `amber_resinous/balsamic_resin/tolu`. Also verify each target family and subfamily node already exists in `data/taxonomy/taxonomy-seed.v2.json` and that none of the 12 descriptor names are already present as seeds. Parse the `phase47_instruction` literal for each row (format: `add_seed target_family=<family> target_subfamily=<subfamily> descriptor=<descriptor>`) and pass verbatim to Task 2 — do not infer targets from any other field (D-47-09, D-47-29).</action>
  <acceptance_criteria>
    - Exactly 12 rows extracted with `mutation_allowed=true` and `disposition=promote_to_seed`.
    - Zero `add_alias` rows found (count is 0).
    - All 12 `phase47_instruction` literals parsed successfully into target_family, target_subfamily, descriptor triples.
    - All 12 target family nodes exist in `taxonomy-seed.v2.json`.
    - All 12 target subfamily nodes exist under the correct family in `taxonomy-seed.v2.json`.
    - None of the 12 descriptor names appear in any subfamily's `descriptors` array pre-mutation.
    - 28 non-executable rows (ids 01-04, 06-09, 12-13, 16-19, 23-28, 31, 33-36, 38-40) are ignored.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json, re
from pathlib import Path

matrix = Path('.planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md').read_text()
seed = json.loads(Path('data/taxonomy/taxonomy-seed.v2.json').read_text())

rows = []
for line in matrix.splitlines():
    if re.match(r'\|\s*\d{2}\s*\|', line):
        cols = [c.strip() for c in line.strip().strip('|').split('|')]
        rows.append(cols)

executable = [r for r in rows if r[5] == 'true']
assert len(executable) == 12, f'Expected 12, got {len(executable)}'
assert all(r[4] == 'promote_to_seed' for r in executable), 'Non-promote_to_seed found'
assert not any(r[4] == 'add_alias' and r[5] == 'true' for r in rows), 'add_alias found'

expected_ids = {'05','10','11','14','15','20','21','22','29','30','32','37'}
actual_ids = {r[0] for r in executable}
assert actual_ids == expected_ids, f'ID mismatch: {actual_ids}'

# Verify targets exist in seed
fam_sub_map = {}
for fam in seed['families']:
    for sub in fam.get('subfamilies', []):
        fam_sub_map[(fam['id'], sub['id'])] = sub.get('descriptors', [])

for r in executable:
    m = re.fullmatch(r'add_seed target_family=(\w+) target_subfamily=(\w+) descriptor=(\w+)', r[14])
    assert m, f'Bad instruction: {r[14]}'
    key = (m.group(1), m.group(2))
    assert key in fam_sub_map, f'Missing target: {key}'
    assert m.group(3) not in fam_sub_map[key], f'Already present: {m.group(3)}'

print('Task 1 assertions PASS: 12 rows, 0 aliases, all targets valid')
"</automated>
  </verify>
  <done>12 executable rows parsed and validated from decision matrix; all target paths confirmed in seed taxonomy.</done>
</task>

<task type="auto">
  <name>Task 2: Apply 12 atomic seed additions to taxonomy-seed.v2.json</name>
  <files>data/taxonomy/taxonomy-seed.v2.json</files>
  <read_first>
    data/taxonomy/taxonomy-seed.v2.json;
    .planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md;
    .planning/phases/47-controlled-curation-mutation/47-CONTEXT.md;
    .planning/phases/46-batch-2-decision-matrix/46-PATTERNS.md
  </read_first>
  <action>Apply 12 direct JSON Edit operations to `data/taxonomy/taxonomy-seed.v2.json` following D-47-07 and D-47-09. For each of the 12 approved rows (parsed in Task 1), append the descriptor string to the end of the existing `descriptors` array of the matching subfamily. Apply in this order by subfamily grouping to minimize edit conflicts: (1) warm_spice: carrot_seed, cardamom, saffron, cubeb, mace; (2) floral_white: freesia, osmanthus, elderflower, linden_flower; (3) citrus_fresh: tangerine; (4) woody_dry: agarwood; (5) balsamic_resin: tolu. Do NOT change any other JSON field: no `version` bump, no `metadata.created_at` change, no `metadata.author` change, no `metadata.description` change, no key additions, no subfamily reordering, no descriptor reordering within existing arrays. Only append to the end of `descriptors` arrays. Follow Phase 23 (lemon_peel) and Phase 42 (peppermint/rosemary/cumin/spearmint/caraway/opoponax) precedent exactly.</action>
  <acceptance_criteria>
    - `taxonomy-seed.v2.json` is valid JSON after all 12 edits.
    - `version` field remains `"2.0.0"`.
    - `metadata` block is byte-identical to pre-mutation state.
    - `warm_spice.descriptors` ends with `carrot_seed, cardamom, saffron, cubeb, mace` (5 new, appended after existing caraway).
    - `floral_white.descriptors` ends with `freesia, osmanthus, elderflower, linden_flower` (4 new, appended after existing lily_of_the_valley).
    - `citrus_fresh.descriptors` ends with `tangerine` (1 new, appended after existing lemon_peel).
    - `woody_dry.descriptors` ends with `agarwood` (1 new, appended after existing rosewood).
    - `balsamic_resin.descriptors` ends with `tolu` (1 new, appended after existing opoponax).
    - Total seed descriptor count is 61 (49 baseline + 12 new).
    - No other subfamily's `descriptors` array changed.
    - No families or subfamilies were added or removed.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json
from pathlib import Path

seed = json.loads(Path('data/taxonomy/taxonomy-seed.v2.json').read_text())
assert seed['version'] == '2.0.0', f'Version changed: {seed[\"version\"]}'
assert seed['metadata']['created_at'] == '2026-05-23T00:00:00Z', 'metadata.created_at changed'
assert seed['metadata']['author'] == 'Taxonomy Team', 'metadata.author changed'

expected = {
    'warm_spice': ['cinnamon','clove','allspice','cumin','caraway','carrot_seed','cardamom','saffron','cubeb','mace'],
    'floral_white': ['jasmine','tuberose','gardenia','orange_blossom','lily_of_the_valley','freesia','osmanthus','elderflower','linden_flower'],
    'citrus_fresh': ['lemon','bergamot','sweet_orange','grapefruit','petitgrain','lemon_peel','tangerine'],
    'woody_dry': ['cedarwood','vetiver','patchouli','sandalwood','rosewood','agarwood'],
    'balsamic_resin': ['labdanum','benzoin','opoponax','tolu'],
}
unchanged = {
    'floral_rose': ['rose','geranium','peony'],
    'woody_mossy': ['oakmoss','tree_moss'],
    'citrus_bitter': ['bitter_orange','yuzu','lime'],
    'vanilla': ['vanilla'],
    'herbal_green': ['basil','rosemary'],
    'leafy_green': ['tomato_leaf'],
    'tropical_fruit': ['pineapple','banana'],
    'red_fruit': ['strawberry','blackberry'],
    'orchard_fruit': ['melon'],
    'amber': ['amber','ambergris'],
    'musky': ['musk','ambrette'],
    'leathery': ['leathery'],
    'fresh_spice': ['anise','peppermint','spearmint'],
}

total = 0
for fam in seed['families']:
    for sub in fam.get('subfamilies', []):
        descs = sub['descriptors']
        total += len(descs)
        if sub['id'] in expected:
            assert descs == expected[sub['id']], f'{sub[\"id\"]}: {descs} != {expected[sub[\"id\"]]}'
        elif sub['id'] in unchanged:
            assert descs == unchanged[sub['id']], f'{sub[\"id\"]} changed: {descs}'
        else:
            assert False, f'Unknown subfamily: {sub[\"id\"]}'

assert total == 61, f'Expected 61 total, got {total}'
assert len(seed['families']) == 10, f'Family count changed: {len(seed[\"families\"])}'

subfamily_count = sum(len(f.get('subfamilies',[])) for f in seed['families'])
assert subfamily_count == 18, f'Subfamily count changed: {subfamily_count}'

print(f'Task 2 assertions PASS: {total} descriptors, 10 families, 18 subfamilies')
"</automated>
  </verify>
  <done>12 seed descriptors appended to 5 subfamilies in taxonomy-seed.v2.json; no metadata or structural changes.</done>
</task>

<task type="auto">
  <name>Task 3: Parser assertion — prove exactly 12 additions at approved paths</name>
  <files>No files modified — verification-only step</files>
  <read_first>
    data/taxonomy/taxonomy-seed.v2.json;
    .planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md;
    .planning/phases/47-controlled-curation-mutation/47-CONTEXT.md
  </read_first>
  <action>Run the inline parser assertion from D-47-08. Prove: (a) exactly 12 new descriptors exist at the approved `target_family/target_subfamily/target_descriptor` paths from D-47-10; (b) no descriptor was added outside the approved list; (c) no descriptor was removed from any subfamily; (d) no other JSON field changed (top-level keys, version, metadata block, family/subfamily structure, all other subfamily descriptor arrays). Also confirm zero alias mutation: `descriptor_aliases.seed.json` must be byte-identical to its pre-mutation state (D-47-06). Use a hardcoded baseline of the pre-mutation descriptor arrays for comparison (from 47-RESEARCH.md: 49 baseline descriptors across 18 subfamilies).</action>
  <acceptance_criteria>
    - Parser exits 0.
    - Exactly 12 new descriptors found at approved paths.
    - Zero descriptors found outside the 12 approved additions.
    - Zero descriptors removed from any subfamily.
    - `version`, `metadata`, family count, subfamily count, and non-target subfamily descriptor arrays are unchanged.
    - `descriptor_aliases.seed.json` is byte-identical (no alias mutation).
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json, hashlib
from pathlib import Path

seed = json.loads(Path('data/taxonomy/taxonomy-seed.v2.json').read_text())

# Pre-mutation baseline (from 47-RESEARCH.md)
baseline = {
    'floral_white': ['jasmine','tuberose','gardenia','orange_blossom','lily_of_the_valley'],
    'floral_rose': ['rose','geranium','peony'],
    'woody_dry': ['cedarwood','vetiver','patchouli','sandalwood','rosewood'],
    'woody_mossy': ['oakmoss','tree_moss'],
    'citrus_fresh': ['lemon','bergamot','sweet_orange','grapefruit','petitgrain','lemon_peel'],
    'citrus_bitter': ['bitter_orange','yuzu','lime'],
    'vanilla': ['vanilla'],
    'herbal_green': ['basil','rosemary'],
    'leafy_green': ['tomato_leaf'],
    'tropical_fruit': ['pineapple','banana'],
    'red_fruit': ['strawberry','blackberry'],
    'orchard_fruit': ['melon'],
    'warm_spice': ['cinnamon','clove','allspice','cumin','caraway'],
    'amber': ['amber','ambergris'],
    'balsamic_resin': ['labdanum','benzoin','opoponax'],
    'musky': ['musk','ambrette'],
    'leathery': ['leathery'],
    'fresh_spice': ['anise','peppermint','spearmint'],
}

approved_additions = {
    'warm_spice': ['carrot_seed','cardamom','saffron','cubeb','mace'],
    'floral_white': ['freesia','osmanthus','elderflower','linden_flower'],
    'citrus_fresh': ['tangerine'],
    'woody_dry': ['agarwood'],
    'balsamic_resin': ['tolu'],
}

actual = {}
for fam in seed['families']:
    for sub in fam.get('subfamilies', []):
        actual[sub['id']] = sub['descriptors']

# Verify structure
assert set(actual.keys()) == set(baseline.keys()), f'Subfamily set changed: {set(actual.keys()) - set(baseline.keys())}'
assert seed['version'] == '2.0.0'

additions_found = 0
for sub_id, base_descs in baseline.items():
    current = actual[sub_id]
    if sub_id in approved_additions:
        expected = base_descs + approved_additions[sub_id]
        assert current == expected, f'{sub_id}: {current} != {expected}'
        additions_found += len(approved_additions[sub_id])
    else:
        assert current == base_descs, f'{sub_id} changed unexpectedly: {current}'

assert additions_found == 12, f'Expected 12 additions, found {additions_found}'

# Alias file unchanged
alias_content = Path('data/taxonomy/descriptor_aliases.seed.json').read_bytes()
alias_hash = hashlib.sha256(alias_content).hexdigest()
print(f'descriptor_aliases.seed.json SHA-256: {alias_hash}')
# Must be same as pre-mutation (no alias changes)
print(f'Task 3 assertions PASS: 12 additions verified, 0 outside, 0 removed, aliases unchanged')
"</automated>
  </verify>
  <done>Parser proves exactly 12 additions at approved paths, zero outside, zero removed, aliases unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Safety guard and protected-boundary diff assertion</name>
  <files>No files modified — guard and diff check only</files>
  <read_first>
    scripts/check-safety-guards.sh;
    .planning/phases/47-controlled-curation-mutation/47-CONTEXT.md
  </read_first>
  <action>Implement D-47-20 and D-47-21. Run `bash scripts/check-safety-guards.sh` and assert exit code 0 (no staged protected-path, staged graphify-out/*, or protected-diff violations). Then run `git diff --name-only` and assert every changed path is in the D-47-18 allow-list: `data/taxonomy/taxonomy-seed.v2.json`, `.planning/phases/47-controlled-curation-mutation/*`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`. Any path outside the allow-list is an immediate halt condition per D-47-21. Note: dirty `graphify-out/*` in the working tree is `accepted_with_policy` per D-47-22 and must not cause a failure. The safety-guard script only fails on staged violations, which is the correct behavior for Phase 47.</action>
  <acceptance_criteria>
    - `scripts/check-safety-guards.sh` exits with code 0.
    - `git diff --name-only` output contains no path outside D-47-18 allow-list.
    - Specifically absent from diff: `data/compiled/*`, `data/taxonomy/taxonomy-seed.v1.json`, `data/taxonomy/descriptor_aliases.seed.json`, `data/inference/*`, `src/**`, `graphify-out/*`, `package.json`, `package-lock.json`.
    - Dirty `graphify-out/*` in working tree does not trigger failure.
  </acceptance_criteria>
  <verify>
    <automated>bash -c "
set -e
echo '=== Safety Guard ==='
bash scripts/check-safety-guards.sh
echo 'Safety guard PASS (exit 0)'

echo '=== Diff Allow-List Check ==='
CHANGED=\$(git diff --name-only 2>/dev/null || true)
echo \"Changed files: \$CHANGED\"
while IFS= read -r path; do
  [ -z \"\$path\" ] && continue
  case \"\$path\" in
    data/taxonomy/taxonomy-seed.v2.json) ;;
    .planning/phases/47-controlled-curation-mutation/*) ;;
    .planning/ROADMAP.md) ;;
    .planning/STATE.md) ;;
    .planning/REQUIREMENTS.md) ;;
    *) echo \"HALT: path outside allow-list: \$path\"; exit 1 ;;
  esac
done <<< \"\$CHANGED\"
echo 'Diff allow-list PASS'
"</automated>
  </verify>
  <done>Safety guard and diff allow-list both pass; no protected-boundary drift detected.</done>
</task>

<task type="auto">
  <name>Task 5: Typecheck, test suite, sandbox compile, verification artifacts, and summary</name>
  <files>.planning/phases/47-controlled-curation-mutation/47-VERIFICATION.md, .planning/phases/47-controlled-curation-mutation/47-01-SUMMARY.md</files>
  <read_first>
    data/taxonomy/taxonomy-seed.v2.json;
    .planning/phases/47-controlled-curation-mutation/47-CONTEXT.md;
    .planning/phases/47-controlled-curation-mutation/47-RESEARCH.md;
    src/package.json
  </read_first>
  <action>Implement D-47-24 steps 8–10. First, run `cd src && npx tsc --noEmit` — if tsc is unavailable, document the skip in 47-VERIFICATION.md per D-47-13. Second, run `cd src && npx vitest run` (full suite per D-47-14) — capture exit code and output. Third, run the sandbox compile (D-47-15): `cd src && npm run build && node dist/cli/compile.js --seed ../data/taxonomy/taxonomy-seed.v2.json --aliases ../data/taxonomy/descriptor_aliases.seed.json --corpus ../data/corpus --relations ../data/inference/curated_relations.v2.json --accords ../data/inference/curated_accords.v2.json --output /tmp/compile-2.8-validate/ --version 2.8.0` — capture stderr/stdout. Fourth, from the `/tmp/compile-2.8-validate/` output JSON artifacts, extract: family count, subfamily count, seed descriptor count, total compiled descriptor count, review_queue count, graph edge count. Assert zero hard failures per D-47-16. Fifth, create `47-VERIFICATION.md` in the phase directory with: date, parser assertion results (from Task 3), safety-guard results (from Task 4), diff assertion results (from Task 4), tsc result, vitest result, sandbox compile metrics, zero-hard-failures assertion, and a note that /tmp artifacts are discarded (not copied to data/compiled/v2). Sixth, create `47-01-SUMMARY.md` in the phase directory with: per-row status table (12 rows applied, 28 ignored), validation summary, final seed descriptor count, and a clear Phase 48 handoff note per D-47-30 and D-47-33. Include expected_effect and notes columns from the decision matrix per D-47-30.</action>
  <acceptance_criteria>
    - `tsc --noEmit` exits 0 (or skip is documented).
    - `vitest run` exits 0 with full suite passing.
    - Sandbox compile to `/tmp/compile-2.8-validate/` exits 0 and produces JSON artifacts.
    - Zero hard failures in sandbox compile output.
    - `/tmp` artifacts are NOT copied to `data/compiled/v2/`.
    - `47-VERIFICATION.md` exists and contains parser, safety-guard, diff, tsc, vitest, and compile results.
    - `47-01-SUMMARY.md` exists and contains per-row status (12 applied, 28 ignored), validation metrics, and Phase 48 handoff note.
    - Sandbox compile metrics captured: family count, subfamily count, seed descriptor count ≥ 61, total compiled descriptor count, review_queue count, graph edge count.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
from pathlib import Path
v = Path('.planning/phases/47-controlled-curation-mutation/47-VERIFICATION.md')
s = Path('.planning/phases/47-controlled-curation-mutation/47-01-SUMMARY.md')
assert v.exists(), '47-VERIFICATION.md missing'
assert s.exists(), '47-01-SUMMARY.md missing'
vc = v.read_text()
sc = s.read_text()
assert 'zero hard failures' in vc.lower() or 'hard failure' in vc.lower(), 'Missing hard-failures assertion'
assert 'phase 48' in sc.lower(), 'Missing Phase 48 handoff note'
assert '12' in sc, 'Missing 12-row count'
assert '28' in sc, 'Missing 28-row count'
print('Task 5 assertions PASS: verification and summary artifacts exist with required content')
"</automated>
    <automated>bash -c "
# Verify /tmp artifacts were NOT copied to data/compiled/v2
COMPILE_TIMESTAMP_FILE='/tmp/compile-2.8-validate/taxonomy.json'
if [ -f \"\$COMPILE_TIMESTAMP_FILE\" ]; then
  echo '/tmp sandbox compile artifacts exist (expected)'
else
  echo 'WARNING: /tmp sandbox compile artifacts not found — may have been cleaned'
fi
# Verify data/compiled/v2 was not modified
COMPILED_DIFF=\$(git diff --name-only -- data/compiled/ 2>/dev/null || true)
if [ -z \"\$COMPILED_DIFF\" ]; then
  echo 'data/compiled/ unchanged — PASS'
else
  echo \"HALT: data/compiled/ was modified: \$COMPILED_DIFF\"
  exit 1
fi
"</automated>
  </verify>
  <done>Typecheck, tests, and sandbox compile pass with zero hard failures; verification and summary artifacts written; Phase 48 handoff documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 46 decision matrix → Phase 47 executor | `mutation_allowed` and `phase47_instruction` are the sole authorization layer for seed mutation. |
| Phase 47 executor → taxonomy-seed.v2.json | Direct JSON edits could corrupt seed structure if applied incorrectly. |
| Phase 47 executor → repository working tree | Mutation could accidentally touch protected paths outside the D-47-18 allow-list. |
| Phase 47 sandbox compile → /tmp | Compile output must stay in /tmp and not leak to data/compiled/v2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | taxonomy-seed.v2.json structure | mitigate | Parser assertion (Task 3) verifies exactly 12 additions at approved paths, zero outside, zero removed, version/metadata unchanged. |
| T-47-02 | Tampering | protected repository paths | mitigate | Safety guard (Task 4) + git diff allow-list assertion blocks any path outside D-47-18. |
| T-47-03 | Elevation of Privilege | non-executable rows promoted | mitigate | Task 1 parser extracts only `mutation_allowed=true` rows; Task 3 proves count is exactly 12. |
| T-47-04 | Tampering | alias file | mitigate | Task 3 verifies `descriptor_aliases.seed.json` is byte-identical to pre-mutation state. |
| T-47-05 | Information Disclosure | /tmp compile artifacts | mitigate | Task 5 verify block confirms `data/compiled/v2` was not modified; /tmp artifacts are ephemeral. |
| T-47-SC | Tampering | package installs | accept | No npm/pip/cargo install tasks exist; package legitimacy gate is not applicable. |
</threat_model>

<verification>
Overall phase checks:
- Task 1 parser proves exactly 12 executable rows from decision matrix, zero aliases, all target paths valid.
- Task 2 applies 12 atomic edits and proves seed file remains valid JSON with correct structure.
- Task 3 parser proves exactly 12 additions at approved paths, zero outside, zero removed, aliases unchanged.
- Task 4 safety guard + diff assertion prove no protected-boundary drift.
- Task 5 typecheck + test suite + sandbox compile prove zero hard failures; verification and summary artifacts created.
</verification>

<success_criteria>
- `taxonomy-seed.v2.json` contains exactly 61 seed descriptors (49 baseline + 12 new).
- All 12 new descriptors are at their approved target paths from D-47-10.
- Zero descriptors added outside the approved 12.
- Zero descriptors removed.
- `version` and `metadata` unchanged.
- `descriptor_aliases.seed.json` unchanged.
- `data/compiled/*` unchanged.
- All protected paths unchanged.
- `tsc --noEmit` passes (or skip documented).
- `vitest run` passes.
- Sandbox compile produces zero hard failures.
- `47-VERIFICATION.md` and `47-01-SUMMARY.md` created with required content.
- Phase 48 handoff is clearly documented.
</success_criteria>

<output>
Create `47-01-SUMMARY.md` and `47-VERIFICATION.md` when done.
</output>
