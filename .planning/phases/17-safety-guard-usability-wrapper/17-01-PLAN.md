---
phase: 17-safety-guard-usability-wrapper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/package.json]
autonomous: true
requirements: [WRAPPER17-01, WRAPPER17-02]
user_setup: []

must_haves:
  truths:
    - "Running npm run safety:guard from src/ directory executes bash ../scripts/check-safety-guards.sh and returns PASS with exit code 0 when no violations exist."
    - "The wrapper preserves the original script's exit code, exiting 1 on safety-guard violation scenarios."
    - "The execution does not modify local Git hooks, CI configurations, Graphify outputs, or protected paths."
  artifacts:
    - path: "src/package.json"
      provides: "Add safety:guard script mapping to bash ../scripts/check-safety-guards.sh"
      contains: "safety:guard"
---

<objective>
Implement a package script wrapper `safety:guard` in `src/package.json` that delegates execution to `bash ../scripts/check-safety-guards.sh`, ensuring usability in local development and preserving the exact exit codes of the safety guard script without modifying hooks, CI, data, compiled artifacts or Graphify.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-safety-guard-usability-wrapper/17-CONTEXT.md
@.planning/phases/17-safety-guard-usability-wrapper/17-DISCUSSION-LOG.md
@.planning/phases/17-safety-guard-usability-wrapper/17-PREFLIGHT.md
@scripts/check-safety-guards.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add safety:guard package script in src/package.json</name>
  <files>src/package.json</files>
  <read_first>
    - src/package.json
  </read_first>
  <action>
    Modify src/package.json to add a new script entry under the "scripts" section:
    "safety:guard": "bash ../scripts/check-safety-guards.sh"
    Ensure formatting is preserved and other scripts remain untouched.
  </action>
  <acceptance_criteria>
    - src/package.json contains "safety:guard" mapped to "bash ../scripts/check-safety-guards.sh".
    - src/package.json remains valid JSON and compiles without issues.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "test -f src/package.json && grep -n '\"safety:guard\":' src/package.json"</automated>
  </verify>
  <done>Package script safety:guard added to src/package.json pointing to bash ../scripts/check-safety-guards.sh.</done>
</task>

<task type="auto">
  <name>Task 2: Validate usability wrapper execution and exit code preservation</name>
  <files>src/package.json</files>
  <read_first>
    - src/package.json
    - scripts/check-safety-guards.sh
  </read_first>
  <action>
    Validate the usability wrapper:
    1. Run `npm run safety:guard` from inside the `src/` directory. Ensure it prints `PASS` on stdout, has no output on stderr, and exits with 0.
    2. Check git status to ensure there are no unintended changes or new untracked files.
    3. Simulate a protected-diff violation by making a minor temporary modification (e.g. adding a comment) to `src/cli/parse_args.ts` (a protected path). Run `npm run safety:guard` from `src/` and verify that it prints `PROTECTED_DIFF` to stderr, the policy message, and exits with code 1.
    4. Revert the modification and confirm that `npm run safety:guard` returns to exit code 0.
  </action>
  <acceptance_criteria>
    - npm run safety:guard exits 0 and prints PASS when no violations exist.
    - npm run safety:guard exits 1 and prints PROTECTED_DIFF to stderr when a protected path has unstaged changes.
    - No git hooks, CI, data, compiled files, or Graphify outputs are altered.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd src && npm run safety:guard && echo '// temp change' >> cli/parse_args.ts && (npm run safety:guard && echo 'FAIL: should have exited 1' && exit 1 || true) && git checkout -- cli/parse_args.ts && npm run safety:guard"</automated>
  </verify>
  <done>Usability wrapper validated for both PASS and failure simulation scenarios from inside the src/ directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → package.json wrapper | Shell execution context for package manager scripts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | src/package.json | mitigate | Ensure the command strictly executes `bash ../scripts/check-safety-guards.sh` and does not run arbitrary shell scripts or accept unsafe input parameters. |
| T-17-02 | Integrity | Exit Code Preservation | mitigate | Verify that the shell delegates the exit status directly, ensuring exit 1 on script failure. |
</threat_model>

<verification>
- Running `npm run safety:guard` from the `src` directory exits 0 and prints PASS.
- Simulating a change in `src/cli/parse_args.ts` causes `npm run safety:guard` from the `src` directory to exit 1.
</verification>

<success_criteria>
- src/package.json contains "safety:guard": "bash ../scripts/check-safety-guards.sh".
- Usability wrapper correctly executes the underlying script and propagates the exit code.
- No protected paths, CI, git hooks, or Graphify outputs are mutated.
</success_criteria>

<output>
Create `.planning/phases/17-safety-guard-usability-wrapper/17-01-SUMMARY.md` when done
</output>
