---
phase: 15-post-triage-safety-guards-current-state-docs-cleanup
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-02-PLAN.md
autonomous: false
execution_readiness: requires_explicit_approval
validation_mode: local_proof_only
non_mutating: true
requirements: [SAFETY-01, SAFETY-03]
must_haves:
  truths:
    - "SAFE-D-22: The preexisting dirty state of graphify-out/* is a known issue accepted with policy."
    - "SAFE-D-23: Dirty Graphify remains a commit-contamination risk, not taxonomy evidence."
    - "SAFE-D-24: 15-02 must not require graphify-out/* to be clean in the working tree as a commit condition."
    - "SAFE-D-25: 15-02 must require graphify-out/* is not staged and not included in the commit."
    - "SAFE-D-26: Any cleanup, revert, regeneration, staging or commit of graphify-out/* requires a separate generated-artifacts plan with allowlist and diff policy."
    - "SAFE-D-27: 15-02 uses commit/staging guards, not a working-tree-clean guard, for Graphify."
  artifacts:
    - path: ".planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-02-PLAN.md"
      provides: "Approved local-proof-only plan contract for staged commit safety guards"
      contains: "execution_readiness: requires_explicit_approval"
  key_links:
    - from: "Graphify staged guard"
      to: "Graphify accepted-with-policy working-tree dirtiness"
      via: "git diff --cached --name-only -- graphify-out"
      pattern: "empty output passes; any graphify-out/* staged path fails"
    - from: "Protected paths staged guard"
      to: "Phase 14 protected boundary"
      via: "git diff --cached --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts"
      pattern: "empty output passes; any protected staged path fails"
---

<objective>
Create a manually approved, local-proof-only execution contract for a staged commit safety guard.

Purpose: protect the commit/staging boundary after `15-01` proved the protected taxonomy/default/artifact boundary clean and recorded preexisting `graphify-out/*` dirtiness as known issue `accepted_with_policy`.

Output: a non-mutating plan that can only be executed after explicit validation/approval and that records exact staged guard commands, exit codes, stdout, stderr, timestamp, interpretation, accepted-with-policy Graphify working-tree handling, and no-mutation assertions.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-CONTEXT.md
@.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-PREFLIGHT.md
@.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-DISCUSSION-LOG.md
@.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-01-SUMMARY.md
@.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-VALIDATION.md
@.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-RESEARCH.md
@.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-PATTERNS.md
</context>

<preconditions>
- Do not execute this plan until explicit human validation/approval is given; execution_readiness is requires_explicit_approval.
- Only local_proof_only validation is allowed for 15-02.
- Do not require `graphify-out/*` to be clean in the working tree; the preexisting dirty state is known issue `accepted_with_policy`.
- Do not create scripts, package scripts, hooks, CI jobs, permanent automation, tests or build artifacts.
- Do not run compile, smoke, typecheck, unit tests, build, npm scripts, artifact refresh, Graphify regeneration, cleanup, revert, staging, unstaging or commit.
- Do not execute curation, descriptor promotion, alias add/remove/remap, relation/accord edits, docs/help fixes or `DEFAULT_PATHS` changes.
- Do not modify source, data, docs/help, `graphify-out/*`, compiled artifacts, hooks, CI, package scripts, git state, `STATE.md` or `ROADMAP.md`.
- If either staged guard emits output, record it as blocking commit contamination and stop without remediation.
</preconditions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve non-mutating staged commit proof before execution</name>
  <read_first>
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-CONTEXT.md SAFE-D-22 through SAFE-D-27.
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-PREFLIGHT.md Post-15-01 Policy Update and Next Suggested Plan.
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-DISCUSSION-LOG.md Post-15-01 Graphify Policy.
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-01-SUMMARY.md Interpretation and No-Mutation Assertions.
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-VALIDATION.md rows 15-02-01 through 15-02-03.
  </read_first>
  <files>.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-02-PLAN.md</files>
  <action>Pause before running any staged guard command. Ask the human to approve that 15-02 is local_proof_only, non-mutating, and limited to Graphify staged guard plus protected paths staged guard. State explicitly that approval permits dirty `graphify-out/*` in the working tree as known issue `accepted_with_policy`, but does not permit `graphify-out/*` staged or committed. State explicitly that approval does not authorize safety automation implementation, scripts, package scripts, hooks/CI, compile/smoke/typecheck/tests/build, docs/help fixes, curation, artifact refresh, `DEFAULT_PATHS` changes, Graphify cleanup/revert/regeneration/staging/commit, protected path mutation, `STATE.md` updates, `ROADMAP.md` updates, or git commits. Continue only if the human responds with explicit approval for local proof execution.</action>
  <acceptance_criteria>
    - Source assertion: Human approval text explicitly permits only local_proof_only staged guard execution for 15-02.
    - Source assertion: Approval preserves SAFE-D-22 through SAFE-D-27.
    - Command assertion: No shell command has been run before approval except reading plan/context files.
    - No-mutation assertion: No protected paths, graphify-out files, source files, data files, docs/help files, hooks, CI files, package scripts, git state, STATE.md or ROADMAP.md were modified, cleaned, reverted, regenerated, staged, unstaged or committed.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — checkpoint approval is manual; before approval run no automated validation command for this task.</automated>
    <human-check>Type explicit approval for local_proof_only execution, or describe requested changes. Without approval, stop.</human-check>
  </verify>
  <done>Execution may proceed only after explicit approval; otherwise the plan remains non-executable.</done>
</task>

<task type="auto">
  <name>Task 2: Run Graphify staged guard without requiring working-tree cleanliness</name>
  <read_first>
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-CONTEXT.md SAFE-D-22 through SAFE-D-27.
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-01-SUMMARY.md Graphify dirty output and accepted no-remediation result.
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-VALIDATION.md Expected Failure Text for non-empty staged Graphify output.
  </read_first>
  <files>.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-02-PLAN.md</files>
  <action>After explicit approval only, run exactly this Graphify staged guard command: git diff --cached --name-only -- graphify-out. Do not run `git status --short -- graphify-out` as a cleanliness requirement for this task. Empty output passes. Any output path under `graphify-out/*` fails as blocking commit contamination. If output is non-empty, record the exact staged path list and stop without committing, cleaning, reverting, regenerating, staging, unstaging, editing hooks, editing `.gitignore`, or otherwise remediating Graphify. Record exact command, exit code, stdout, stderr, timestamp, interpretation, and no-mutation assertions. Also record that preexisting dirty `graphify-out/*` in the working tree is accepted_with_policy and is not a failure condition for this guard.</action>
  <acceptance_criteria>
    - Source assertion: Command is exactly git diff --cached --name-only -- graphify-out.
    - Expected output behavior: empty stdout means Graphify staged guard passes.
    - Failure behavior: any stdout path under `graphify-out/*` means blocking commit contamination and must stop/report without remediation.
    - Policy assertion: Dirty `graphify-out/*` in the working tree remains accepted_with_policy and is not treated as taxonomy evidence.
    - No-mutation assertion: The guard does not clean, revert, regenerate, stage, unstage, commit, edit hooks, edit `.gitignore`, or modify `graphify-out/*`.
  </acceptance_criteria>
  <verify>
    <automated>git diff --cached --name-only -- graphify-out</automated>
  </verify>
  <done>Graphify staged proof is recorded with exact command, exit code, stdout, stderr, timestamp, interpretation, accepted-with-policy working-tree note, and no-mutation assertion.</done>
</task>

<task type="auto">
  <name>Task 3: Run protected paths staged guard over the full protected boundary</name>
  <read_first>
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-CONTEXT.md SAFE-D-07 through SAFE-D-11 and SAFE-D-25 through SAFE-D-27.
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-PREFLIGHT.md Protected Boundaries.
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-VALIDATION.md row 15-02-02 and Expected Failure Text for staged protected-path output.
  </read_first>
  <files>.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-02-PLAN.md</files>
  <action>After Task 2 and explicit approval, run exactly this protected paths staged guard command: git diff --cached --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts. Empty output passes. Any output path under the protected boundary fails as unexpected staged protected-path commit contamination. If output is non-empty, record the exact staged path list and stop without committing, cleaning, reverting, regenerating, staging, unstaging, editing protected files, or otherwise remediating. Record exact command, exit code, stdout, stderr, timestamp, interpretation, and no-mutation assertions.</action>
  <acceptance_criteria>
    - Source assertion: Command is exactly git diff --cached --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts.
    - Source assertion: The command covers data/taxonomy, data/inference, data/compiled/v1, data/compiled/v2 and src/cli/parse_args.ts.
    - Expected output behavior: empty stdout means protected paths staged guard passes.
    - Failure behavior: any stdout path under the protected boundary means blocking commit contamination and must stop/report without remediation.
    - No-mutation assertion: The guard does not clean, revert, regenerate, stage, unstage, commit or modify protected paths.
  </acceptance_criteria>
  <verify>
    <automated>git diff --cached --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts</automated>
  </verify>
  <done>Protected paths staged proof is recorded with exact command, exit code, stdout, stderr, timestamp, interpretation and no-mutation assertion.</done>
</task>

<task type="auto">
  <name>Task 4: Record exclusions, deferred fronts and final no-mutation assertions</name>
  <read_first>
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-CONTEXT.md Hard Boundaries and Deferred Safety Fronts.
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-PREFLIGHT.md Do not execute yet.
    - .planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-VALIDATION.md Required Evidence Fields.
  </read_first>
  <files>.planning/phases/15-post-triage-safety-guards-current-state-docs-cleanup/15-02-PLAN.md</files>
  <action>Record final local proof interpretation and no-mutation assertions in the executor response or summary only if the execution workflow requires a summary. Confirm SAFETY-02 tmp-only compile guard remains deferred; SAFETY-03 defaults/fallback assertions remain deferred except staged protection for `src/cli/parse_args.ts`; DOCS-01 docs/help cleanup remains deferred. Confirm no safety automation implementation, scripts, package scripts, hooks/CI, compile/smoke/typecheck/tests/build, docs/help fixes, curation, artifact refresh, `DEFAULT_PATHS` changes, Graphify cleanup/revert/regeneration/staging/commit, protected path mutation, `STATE.md` updates, `ROADMAP.md` updates or git commits occurred.</action>
  <acceptance_criteria>
    - Deferred mapping: SAFETY-02 tmp-only compile guard remains deferred; no compile/smoke/typecheck/tests/build are run.
    - Deferred mapping: SAFETY-03 defaults/fallback assertions remain deferred; `src/cli/parse_args.ts` is covered only as a protected staged path.
    - Deferred mapping: DOCS-01 docs/help cleanup remains deferred; no README, CLI help, docs or historical audit docs are edited.
    - No-mutation assertion: No protected paths, source files, data files, compiled artifacts, graphify-out files, docs/help files, hooks, CI files, package scripts, git state, STATE.md or ROADMAP.md were modified, cleaned, reverted, regenerated, staged, unstaged, committed or refreshed by 15-02.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — final record is a local proof/source assertion; do not add extra validation commands beyond Tasks 2 and 3.</automated>
  </verify>
  <done>15-02 remains local_proof_only and non-mutating; staged commit guard results are recorded, and Graphify working-tree dirtiness remains accepted_with_policy.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | 15-02 Disposition |
|--------|------|-------------------|
| CONTEXT | SAFE-D-22 through SAFE-D-27 | Covered by Graphify staged guard and accepted-with-policy working-tree policy |
| PREFLIGHT | Next suggested plan: staged commit safety guard | Covered by objective and Tasks 2-3 |
| DISCUSSION | Blocking staged Graphify, not dirty worktree Graphify | Covered by Task 2 |
| 15-01-SUMMARY | Graphify dirty report_and_fail and no-remediation result | Consumed as known issue baseline |
| VALIDATION | 15-02 rows and expected failure texts | Covered by Tasks 2-4 |
| RESEARCH / PATTERNS | Non-mutating local proof, Graphify hygiene not taxonomy evidence, protected boundary policy | Preserved; no scripts/tests/hooks/CI introduced |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human approval -> local proof execution | The plan must not run staged guard commands until explicit approval is given. |
| Git index -> commit contamination interpretation | Staged path output is interpreted as pass/fail without remediation. |
| Graphify working tree -> accepted policy | Dirty worktree Graphify is known issue accepted_with_policy, while staged Graphify remains blocking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T15-04 | Tampering / Repudiation | `graphify-out/*` generated artifacts | mitigate | Staged guard: `git diff --cached --name-only -- graphify-out`. Empty output passes; any staged Graphify path blocks commit and reports without remediation. |
| T15-05 | Tampering | data/taxonomy, data/inference, data/compiled/v1, data/compiled/v2, src/cli/parse_args.ts | mitigate | Protected staged guard: `git diff --cached --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts`. Empty output passes; any staged protected path blocks commit and reports without remediation. |
| T15-06 | Elevation of Privilege / Tampering | Plan scope and downstream execution | mitigate | Manual approval gate plus explicit exclusions for implementation, scripts, hooks/CI, package scripts, compile/smoke/typecheck/tests/build, docs/help fixes, curation, artifact refresh, DEFAULT_PATHS changes, Graphify remediation, STATE/ROADMAP updates and commits. |
</threat_model>

<verification>
Validation mode: local_proof_only.

Overall checks after explicit approval:
- Task 1 approval exists before any staged guard command runs.
- Task 2 records `git diff --cached --name-only -- graphify-out` with exit code, stdout, stderr, timestamp and interpretation.
- Task 3 records `git diff --cached --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts` with exit code, stdout, stderr, timestamp and interpretation.
- Dirty `graphify-out/*` in the working tree is allowed as accepted_with_policy and is not a failure condition.
- Any staged `graphify-out/*` path is blocking.
- Any unexpected staged protected path is blocking.
- No compile/smoke/typecheck/tests/build/npm scripts are run.
- No safety automation implementation, docs/help fix, curation, protected path edit, graphify-out mutation, artifact refresh, DEFAULT_PATHS change, generated artifact cleanup/revert/regeneration/staging/commit, hook/CI/package script change, STATE.md update, ROADMAP.md update or git commit occurs.
</verification>

<success_criteria>
- Graphify staged guard uses exactly `git diff --cached --name-only -- graphify-out`.
- Graphify staged guard passes only with empty output and fails on any staged `graphify-out/*` path.
- Protected paths staged guard uses exactly `git diff --cached --name-only -- data/taxonomy data/inference data/compiled/v1 data/compiled/v2 src/cli/parse_args.ts`.
- Protected paths staged guard passes only with empty output and fails on any staged protected path.
- Dirty `graphify-out/*` in the working tree remains accepted_with_policy and is not treated as taxonomy evidence.
- SAFETY-02, SAFETY-03 defaults/fallback assertions and DOCS-01 remain deferred/out-of-scope, with only staged protection for `src/cli/parse_args.ts` in scope for SAFETY-03.
- The plan remains non-mutating and executable only after explicit approval.
</success_criteria>

<output>
Do not create a proof artifact. Do not create scripts, tests, hooks, CI config, package scripts, source, data, docs/help, graphify-out or compiled artifact changes. Do not modify `STATE.md` or `ROADMAP.md`. If the execution workflow requires a summary, keep it limited to reporting local proof outcomes and no-mutation assertions.
</output>
