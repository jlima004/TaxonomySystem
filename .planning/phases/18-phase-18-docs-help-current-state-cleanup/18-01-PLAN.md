---
phase: 18-phase-18-docs-help-current-state-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOCS18-01, DOCS18-02, DOCS18-03]
user_setup: []

must_haves:
  truths:
    - "O README.md principal documenta a v2 como versão default operacional e a v1.0.0 como baseline histórico arquivado."
    - "O README.md principal inclui instruções claras para execução de scripts/check-safety-guards.sh e de npm run safety:guard a partir do diretório src/."
    - "Nenhum arquivo de código, dados, CI ou git hooks protegidos foi modificado nesta fase."
    - "Os testes do builder (vitest) e typecheck passam sem erros."
  artifacts:
    - path: "README.md"
      provides: "Documentação atualizada do estado do projeto, cobrindo a v2 default, v1 baseline, e os safety guards."
  key_links:
    - from: "README.md"
      to: "scripts/check-safety-guards.sh"
      via: "Seção de Safety Guards detalhando o script local"
      pattern: "scripts/check-safety-guards.sh"
    - from: "README.md"
      to: "src/package.json"
      via: "Instruções do wrapper npm run safety:guard"
      pattern: "npm run safety:guard"
---

<objective>
Revisar e atualizar o README.md na raiz do projeto para documentar o estado atual: a promoção da v2 para padrão ativo, o arquivamento seguro da v1 como baseline histórico, e a introdução dos mecanismos de segurança do Taxonomy System (o script local check-safety-guards.sh e o wrapper npm run safety:guard). Assegurar de forma absoluta que nenhum código ou dado protegido seja mutado durante o processo.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/18-phase-18-docs-help-current-state-cleanup/18-CONTEXT.md
@.planning/phases/18-phase-18-docs-help-current-state-cleanup/18-RESEARCH.md
@.planning/phases/18-phase-18-docs-help-current-state-cleanup/18-PATTERNS.md
@.planning/phases/18-phase-18-docs-help-current-state-cleanup/18-VALIDATION.md

@scripts/check-safety-guards.sh
@src/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar o README.md com o estado v2 default, v1 baseline, e os Safety Guards</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/phases/18-phase-18-docs-help-current-state-cleanup/18-CONTEXT.md
    - .planning/phases/18-phase-18-docs-help-current-state-cleanup/18-RESEARCH.md
    - scripts/check-safety-guards.sh
    - src/package.json
  </read_first>
  <action>
    Editar o README.md principal na raiz do projeto. 
    1. Confirmar ou ajustar trechos da introdução e da seção "Current Taxonomy Status" para deixar inequívoco que a v2 é a versão padrão ativa e que a v1 está arquivada e preservada como baseline intocado sob data/compiled/v1.
    2. Adicionar uma nova seção denominada "## 🔒 Safety Guards (Mecanismos de Segurança)" antes de "## 📈 Status" no README.md.
    3. Documentar nessa nova seção o script de segurança não-mutante "scripts/check-safety-guards.sh". Explicar que ele bloqueia staged graphify-out/* e alterações staged ou dirty em caminhos protegidos (data/taxonomy, data/inference, data/compiled/v1, data/compiled/v2, src/cli/parse_args.ts) antes de um commit, impedindo mutações acidentais, enquanto permite graphify-out/* sujo na working tree. Explicar como executá-lo:
       - ./scripts/check-safety-guards.sh
       - bash scripts/check-safety-guards.sh
    4. Documentar também o script wrapper definido no package.json em src/, rodado via:
       - npm run safety:guard (a partir do diretório src/)
    5. Certificar-se de não alterar nenhum outro arquivo.
  </action>
  <acceptance_criteria>
    - README.md contém explicações claras sobre o status padrão v2 e baseline v1.
    - README.md contém a seção "## 🔒 Safety Guards (Mecanismos de Segurança)" ou equivalente.
    - README.md detalha com precisão o caminho do script "scripts/check-safety-guards.sh", seus checks e comportamento não-mutante.
    - README.md documenta o script npm "npm run safety:guard" com a instrução correta de que deve ser rodado a partir de "src/".
    - A formatação Markdown do README.md é válida e sem links quebrados.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "grep -q 'check-safety-guards.sh' README.md && grep -q 'safety:guard' README.md && grep -q 'v2' README.md"</automated>
  </verify>
  <done>README.md atualizado com o status do v2 default, baseline v1, e instruções de uso detalhadas do safety-guards.sh e wrapper npm.</done>
</task>

<task type="auto">
  <name>Task 2: Validar integridade e não-mutação pós-atualização</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/18-phase-18-docs-help-current-state-cleanup/18-VALIDATION.md
    - scripts/check-safety-guards.sh
    - src/package.json
  </read_first>
  <action>
    Rodar os testes e ferramentas do projeto para verificar a integridade da workspace pós-modificação:
    1. Executar o safety guard de forma preventiva via wrapper npm a partir de src/ para atestar que o wrapper funciona e que passa com sucesso:
       - Ir para o diretório src/ e executar: npm run safety:guard
    2. Executar o script bash diretamente para atestar que funciona:
       - A partir da raiz: bash scripts/check-safety-guards.sh
    3. Executar typecheck e testes do builder em src/:
       - Ir para src/ e rodar: npm run typecheck && npm run test
    4. Validar via git status que apenas o arquivo README.md (e os arquivos de planejamento da phase 18) foram modificados:
       - Executar git diff --name-only e garantir que nenhum arquivo protegido está listado.
  </action>
  <acceptance_criteria>
    - O wrapper "npm run safety:guard" executado em src/ reporta PASS com exit code 0.
    - O script "bash scripts/check-safety-guards.sh" na raiz reporta PASS com exit code 0.
    - "npm run typecheck && npm run test" em src/ passam com sucesso e sem falhas.
    - git status e git diff confirmam que nenhum arquivo de dados ou código protegido sob data/ ou src/ foi mutado.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "set -e; bash scripts/check-safety-guards.sh; cd src; npm run typecheck; npm run test; git status --short | grep -v -E 'README.md|.planning/' | wc -l | grep -q '^0$'"</automated>
  </verify>
  <done>Validação de testes, compilação, execução do safety guard e não-mutabilidade de caminhos protegidos realizada com sucesso.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → git repo | Modificações documentais não devem alterar arquivos executáveis ou fontes do projeto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | README.md changes | mitigate | Garantir via git diff que apenas arquivos Markdown de documentação foram modificados e nada mais. |
| T-18-02 | Denial of Service | Project tests/compile | mitigate | Rodar typecheck e testes completos em src/ para assegurar que nenhuma alteração quebrou a suíte de testes. |
</threat_model>

<verification>
- Execução bem-sucedida de `bash scripts/check-safety-guards.sh` resultando em PASS.
- Execução bem-sucedida de `npm run safety:guard` a partir de `src/` resultando em PASS.
- Execução de `npm run typecheck && npm run test` na pasta `src/` sem falhas.
- Checagem do `git diff` demonstrando apenas modificações puramente documentais.
</verification>

<success_criteria>
- README.md possui instruções corretas para v2 default, v1 baseline, scripts/check-safety-guards.sh e wrapper npm.
- Toda a suíte de testes e typecheck passa com sucesso.
- O script de safety guard roda com PASS na raiz e via wrapper npm.
- Zero mutações em arquivos protegidos do repositório.
</success_criteria>

<output>
Criar `.planning/phases/18-phase-18-docs-help-current-state-cleanup/18-01-SUMMARY.md` ao concluir a execução do plano.
</output>
