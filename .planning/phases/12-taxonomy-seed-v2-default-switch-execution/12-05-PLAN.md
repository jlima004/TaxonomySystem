---
phase: 12-taxonomy-seed-v2-default-switch-execution
plan: 05
type: execute
wave: 4
depends_on: [12-04]
files_modified:
  - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md
  - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-RELEASE-MIGRATION-NOTES.md
  - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-6-FINAL-CLOSURE.md
  - "README.md (conditional: only if it exists and mentions defaults/usage impacted by the v2 default switch)"
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-05-SUMMARY.md
autonomous: false
execution_readiness: not_ready_for_execution
blocked_until: gate-4-post-switch-validation-pass
requirements: [SWITCH-08, SWITCH-09, SWITCH-10, SWITCH-11]
user_setup: []
must_haves:
  truths:
    - "Rollback dry-run happens in temporary context before closure; implements SWITCH-D-38."
    - "Rollback is not git-only and restores v1 defaults while validating preserved v1; implements SWITCH-D-39 and SWITCH-D-40."
    - "Rollback does not delete `data/compiled/v2`; implements SWITCH-D-41."
    - "Rollback failure blocks closure and final report records `rollback_success: true`; implements SWITCH-D-42, SWITCH-D-43, SWITCH-D-55."
    - "Minimal docs/release notes state v2 default, official v2 artifacts, preserved v1 baseline, validated rollback and accepted soft findings without overclaiming; implements SWITCH-D-44, SWITCH-D-45, SWITCH-D-46, SWITCH-D-47, SWITCH-D-48, SWITCH-D-49, SWITCH-D-58."
    - "Final commit occurs only after post-switch validation and rollback success; implements SWITCH-D-59, SWITCH-D-62, SWITCH-D-63, SWITCH-D-64."
  artifacts:
    - path: ".planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md"
      provides: "Temporary rollback validation evidence with rollback_success true"
      contains: "rollback_success: true"
    - path: ".planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-RELEASE-MIGRATION-NOTES.md"
      provides: "Minimal release/migration notes for v2 default and rollback"
    - path: ".planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-6-FINAL-CLOSURE.md"
      provides: "Final closure evidence after rollback and docs checks"
  key_links:
    - from: "src/cli/parse_args.ts switched defaults"
      to: "rollback dry-run temporary patch/worktree"
      via: "restore five v1 DEFAULT_PATHS values in temporary context only"
      pattern: "version: '1.0.0'"
    - from: "12-GATE-5-ROLLBACK-DRY-RUN.md"
      to: "12-GATE-6-FINAL-CLOSURE.md"
      via: "rollback_success: true required before closure"
      pattern: "rollback_success: true"
---

<objective>
Validate rollback without undoing the main branch switch, then write minimal release/tracking documentation and final closure evidence that accurately states v2 is default, v1 is preserved, rollback is validated, and accepted soft findings are not claimed resolved.

Purpose: prove reversibility and communicate the migration without curation creep or misleading documentation.
Output: rollback dry-run evidence, release/migration notes, final closure report, and minimal impacted docs/tracking updates after validation.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-CONTEXT.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-PREFLIGHT.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-RESEARCH.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-PATTERNS.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-VALIDATION.md
@.planning/phases/11-taxonomy-seed-v2-promotion-readiness-default-migration/11-PATTERNS.md
@README.md
@src/cli/parse_args.ts
</context>

<preconditions>
- STOP unless Plan 12-04 completed and `12-GATE-4-POST-SWITCH-VALIDATION.md` records pass.
- STOP unless Gate 3/4 default switch and post-switch validation have been committed separately with commit message `feat(cli): switch taxonomy defaults to v2`, and the working tree is clean before rollback dry-run evidence starts.
- STOP unless official `data/compiled/v2` remains present and `src/cli/parse_args.ts` currently points to v2 defaults on the main working tree.
- STOP if protected v1/input paths have any diff before rollback dry-run.
- STOP if the rollback validation approach would permanently revert the main branch or delete `data/compiled/v2`.
- STOP before documentation if rollback dry-run does not record `rollback_success: true`.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Validate rollback in temporary context without deleting v2 artifacts</name>
  <read_first>
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-PREFLIGHT.md Future Rollback Dry-Run Policy and Gate 5
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-CONTEXT.md Rollback Execution section
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-VALIDATION.md row 66
    - .planning/phases/11-taxonomy-seed-v2-promotion-readiness-default-migration/11-PATTERNS.md rollback runbook pattern
    - src/cli/parse_args.ts switched defaults
  </read_first>
  <files>.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md</files>
  <action>Use a temporary patch/worktree/equivalent rollback context, starting only after the committed `feat(cli): switch taxonomy defaults to v2` baseline exists. In that temporary context only, restore `DEFAULT_PATHS.seedPath` to `data/taxonomy/taxonomy-seed.v1.json`, `relationsPath` to `data/inference/curated_relations.v1.json`, `accordsPath` to `data/inference/accord_map.v1.json`, `outputDir` to `data/compiled/v1`, and `version` to `1.0.0`. Validate typecheck/tests/build as appropriate for the rollback context, run explicit v1 compile to `/tmp/opencode/taxonomy-phase12-switch/rollback-v1-validation`, assert v1 inputs/artifacts are present, assert `data/compiled/v2/taxonomy.json`, `descriptor_aliases.json`, and `similarity_matrix.json` still exist, and record `rollback_success: true` only if all checks pass. Do not apply rollback permanently to the main working tree; after the dry-run, main `src/cli/parse_args.ts` must remain v2 default per successful switch. This implements SWITCH-D-38 through SWITCH-D-43.</action>
  <verify>
    <automated>git log --oneline --grep='feat(cli): switch taxonomy defaults to v2' -1 | grep -q 'feat(cli): switch taxonomy defaults to v2' && test -f data/compiled/v2/taxonomy.json && test -f data/compiled/v2/descriptor_aliases.json && test -f data/compiled/v2/similarity_matrix.json && test -f data/compiled/v1/taxonomy.json && test -f data/compiled/v1/descriptor_aliases.json && test -f data/compiled/v1/similarity_matrix.json && test -f /tmp/opencode/taxonomy-phase12-switch/rollback-v1-validation/taxonomy.json && test -f .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md && grep -q '^rollback_success: true' .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md && grep -q '^temporary_defaults_asserted: true' .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md && grep -q '^temporary_seedPath: data/taxonomy/taxonomy-seed.v1.json' .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md && grep -q '^temporary_relationsPath: data/inference/curated_relations.v1.json' .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md && grep -q '^temporary_accordsPath: data/inference/accord_map.v1.json' .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md && grep -q '^temporary_outputDir: data/compiled/v1' .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md && grep -q '^temporary_version: 1.0.0' .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md && grep -q '^temporary_v1_compile_exit: 0' .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md && grep -q '^main_defaults_after_dry_run: v2' .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md && node -e "const fs=require('fs'); const s=fs.readFileSync('src/cli/parse_args.ts','utf8'); const required=[\"seedPath: 'data/taxonomy/taxonomy-seed.v2.json'\",\"relationsPath: 'data/inference/curated_relations.v2.json'\",\"accordsPath: 'data/inference/accord_map.v2.json'\",\"outputDir: 'data/compiled/v2'\",\"version: '2.0.0'\"]; for (const x of required) { if (!s.includes(x)) { console.error(x); process.exit(1); } }"</automated>
  </verify>
  <acceptance_criteria>
    - Rollback assertion: automated verify checks `12-GATE-5-ROLLBACK-DRY-RUN.md` records all five temporary v1 default values and `temporary_defaults_asserted: true`.
    - Commit baseline assertion: default switch is already committed as `feat(cli): switch taxonomy defaults to v2` before rollback evidence begins.
    - Validation assertion: automated verify checks explicit v1 compile output exists at `/tmp/opencode/taxonomy-phase12-switch/rollback-v1-validation/taxonomy.json` and report records `temporary_v1_compile_exit: 0`.
    - Preservation assertion: official `data/compiled/v2` remains present after rollback dry-run and is not deleted.
    - Main-branch assertion: automated verify checks main `src/cli/parse_args.ts` still contains the five v2 default values and the report records `main_defaults_after_dry_run: v2`.
    - Report assertion: `12-GATE-5-ROLLBACK-DRY-RUN.md` includes commands, result, and `rollback_success: true`; failure blocks closure.
    - Decision trace: SWITCH-D-37, SWITCH-D-38, SWITCH-D-39, SWITCH-D-40, SWITCH-D-41, SWITCH-D-42, SWITCH-D-43, SWITCH-D-55, SWITCH-D-64.
  </acceptance_criteria>
  <done>Rollback dry-run proves v1 defaults can be restored and validated without deleting official v2 artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Update minimal release docs and final closure evidence accurately</name>
  <read_first>
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-4-POST-SWITCH-VALIDATION.md
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-5-ROLLBACK-DRY-RUN.md
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-PREFLIGHT.md Future Docs And Release Notes Policy and Gate 6
    - README.md Current Taxonomy Status
    - .planning/STATE.md Phase State
    - .planning/ROADMAP.md Phase 12 section
    - .planning/REQUIREMENTS.md Phase 12 requirements and traceability
  </read_first>
  <files>.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-RELEASE-MIGRATION-NOTES.md, .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-6-FINAL-CLOSURE.md, README.md conditional, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-05-SUMMARY.md</files>
  <action>After rollback_success is true, commit or verify committed rollback evidence separately as `test(12): record post-switch and rollback validation` before editing release/tracking docs. Then create/update minimal release and tracking docs only. `12-RELEASE-MIGRATION-NOTES.md` must declare v2 default, official `data/compiled/v2`, preserved `data/compiled/v1`, preserved v1 inputs, explicit v1 compile fallback, validated rollback, final approval path, and Phase 11 accepted soft findings/legacy alias exception. Update README only if `README.md` exists and currently mentions defaults or usage impacted by the v2 default switch; if README is absent or has no impacted defaults/usage statements, leave it unchanged and record that decision in the closure/summary docs. Update GSD tracking only as needed to record Phase 12 validation/closure. Do not claim v1 was removed, do not claim v2 physically replaced `data/compiled/v1`, do not hide rollback, do not claim accepted soft findings were resolved, and do not start new taxonomy curation per SWITCH-D-44 through SWITCH-D-49. Create `12-GATE-6-FINAL-CLOSURE.md` with Gate 0 through Gate 6 evidence links and final protected diff check. Do not alter seed/data input files, `data/compiled/v1`, or `data/compiled/v2` artifacts in this docs task.</action>
  <verify>
    <automated>git log --oneline --grep='test(12): record post-switch and rollback validation' -1 | grep -q 'test(12): record post-switch and rollback validation' && test -f .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-RELEASE-MIGRATION-NOTES.md && test -f .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-6-FINAL-CLOSURE.md && grep -q 'data/compiled/v2' .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-RELEASE-MIGRATION-NOTES.md && grep -q 'data/compiled/v1' .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-RELEASE-MIGRATION-NOTES.md && grep -q 'rollback_success: true' .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-6-FINAL-CLOSURE.md && ! grep -R -i 'v1 removed\|soft findings resolved' README.md .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-RELEASE-MIGRATION-NOTES.md .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-6-FINAL-CLOSURE.md && git diff --exit-code -- data/compiled/v1 data/compiled/v2 data/taxonomy/taxonomy-seed.v1.json data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/inference/curated_relations.v1.json data/inference/curated_relations.v2.json data/inference/accord_map.v1.json data/inference/accord_map.v2.json</automated>
  </verify>
  <acceptance_criteria>
    - Documentation assertion: release notes state v2 default, official v2 artifacts, preserved v1 baseline, rollback path, approval path, and accepted soft findings policy.
    - Commit baseline assertion: rollback validation evidence is already committed as `test(12): record post-switch and rollback validation` before release/tracking docs are edited.
    - Negative claim assertion: README/release/closure docs do not state v1 was removed, do not state `data/compiled/v1` was replaced, and do not state accepted soft findings were resolved.
    - Scope assertion: automated verify runs protected `git diff --exit-code` for taxonomy seed files, relation/accord inputs, alias seed, `data/compiled/v1`, and `data/compiled/v2` artifacts.
    - Closure assertion: `12-GATE-6-FINAL-CLOSURE.md` links Gate 0 through Gate 6 and states `rollback_success: true` before final commit.
    - Commit assertion: docs/release notes and tracking updates must be committed separately as `docs(12): document v2 default migration and rollback`, after rollback validation is committed.
    - Decision trace: SWITCH-D-44, SWITCH-D-45, SWITCH-D-46, SWITCH-D-47, SWITCH-D-48, SWITCH-D-49, SWITCH-D-50, SWITCH-D-51, SWITCH-D-52, SWITCH-D-55, SWITCH-D-58, SWITCH-D-59, SWITCH-D-62, SWITCH-D-63, SWITCH-D-64.
  </acceptance_criteria>
  <done>Release/tracking docs accurately communicate v2 default and rollback with no misleading claims or data curation changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| switched main defaults → rollback temp context | Temporary rollback proof must not permanently undo main switch. |
| validation evidence → release documentation | Docs must reflect evidence and not overstate status. |
| docs/tracking → user expectations | User-facing docs communicate defaults and rollback to downstream consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-rollback-false-positive | Repudiation | rollback dry-run | mitigate | Require temporary context, explicit v1 validation, `data/compiled/v2` preservation, and `rollback_success: true`. |
| T-12-05-v2-deletion | Tampering/Availability | `data/compiled/v2` | mitigate | Rollback task asserts all three v2 artifacts remain present. |
| T-12-05-doc-misrepresentation | Information Disclosure/Repudiation | README/release notes | mitigate | Required/forbidden messaging checks for v2 default, v1 preservation, rollback, and soft findings. |
| T-12-05-curation-creep | Tampering | seed/data input files | mitigate | Documentation task forbids taxonomy curation and runs protected diff checks. |
| T-12-05-SC | Tampering | npm/pip/cargo installs | accept | No package installation; use existing tools only. |
</threat_model>

<verification>
Overall Gate 5/6 verification: Plan 12-04 complete; rollback dry-run in temporary context restores v1 defaults and validates v1 without deleting v2; `rollback_success: true` is recorded; docs accurately describe v2 default, official v2, preserved v1, rollback, and accepted soft findings; final protected diffs are clean.
</verification>

<success_criteria>
- SWITCH-08, SWITCH-09, SWITCH-10 and SWITCH-11 are covered with rollback and release evidence.
- Phase 12 does not close unless rollback dry-run passes and final docs are accurate.
- No docs claim v1 removal or soft finding resolution unless separately true and approved; no new curation occurs.
</success_criteria>

<output>
Create `.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-05-SUMMARY.md` when done.
</output>
