---
phase: 12-taxonomy-seed-v2-default-switch-execution
plan: 04
type: execute
wave: 3
depends_on: [12-03]
files_modified:
  - src/cli/parse_args.ts
  - src/tests/cli/parse_args.test.ts
  - src/tests/curation/v1_v2_comparison.test.ts
  - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-3-DEFAULT-PATHS-SWITCH.md
  - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-4-POST-SWITCH-VALIDATION.md
  - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-04-SUMMARY.md
autonomous: false
execution_readiness: not_ready_for_execution
blocked_until: gate-2-official-v2-publication-pass
requirements: [SWITCH-03, SWITCH-04, SWITCH-07, SWITCH-08, SWITCH-11]
user_setup: []
must_haves:
  truths:
    - "DEFAULT_PATHS switch changes all five approved fields atomically; implements SWITCH-D-18."
    - "Switch occurs only after official v2 publication and validation; implements SWITCH-D-19, SWITCH-D-30, SWITCH-D-54."
    - "Partial v1/v2 combinations are prohibited; implements SWITCH-D-20."
    - "v1 paths/artifacts are preserved and explicit v1 compile still works in `/tmp`; implements SWITCH-D-21, SWITCH-D-23, SWITCH-D-31, SWITCH-D-32, SWITCH-D-33, SWITCH-D-35."
    - "Edit scope is limited to `src/cli/parse_args.ts` fields `seedPath`, `relationsPath`, `accordsPath`, `outputDir`, `version`; implements SWITCH-D-15 and SWITCH-D-22."
    - "Post-switch validation runs before rollback/docs closure; implements SWITCH-D-50, SWITCH-D-51, SWITCH-D-52, SWITCH-D-64."
  artifacts:
    - path: "src/cli/parse_args.ts"
      provides: "Atomic v2 DEFAULT_PATHS defaults"
      contains: "data/taxonomy/taxonomy-seed.v2.json"
    - path: ".planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-3-DEFAULT-PATHS-SWITCH.md"
      provides: "Source diff and atomic switch evidence"
    - path: ".planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-4-POST-SWITCH-VALIDATION.md"
      provides: "Post-switch default v2 and explicit v1 fallback evidence"
  key_links:
    - from: "data/compiled/v2"
      to: "src/cli/parse_args.ts DEFAULT_PATHS.outputDir"
      via: "Gate 2 pass before Gate 3"
      pattern: "outputDir: 'data/compiled/v2'"
    - from: "src/cli/parse_args.ts"
      to: "default compile"
      via: "parseCompileArgs([]) returns DEFAULT_PATHS"
      pattern: "parseCompileArgs\(\[\]\)"
---

<objective>
Atomically switch CLI defaults to v2 after official artifact publication, then prove default v2 behavior and explicit v1 fallback without mutating protected v1 paths or seed/input files.

Purpose: make v2 the default only after Gate 2, while preserving v1 rollback anchor and avoiding partial defaults.
Output: narrow `src/cli/parse_args.ts` default-field diff, test updates only if strictly necessary, Gate 3 and Gate 4 evidence docs.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-CONTEXT.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-PREFLIGHT.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-RESEARCH.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-PATTERNS.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-VALIDATION.md
@src/cli/parse_args.ts
@src/tests/cli/parse_args.test.ts
@src/tests/curation/v1_v2_comparison.test.ts
@src/package.json
</context>

<preconditions>
- STOP unless Plan 12-03 completed and `12-GATE-2-V2-PUBLICATION.md` records `gate_2_result: pass`.
- STOP unless Gate 2 official artifact publication has been committed separately with commit message `build(data): publish compiled v2 taxonomy artifacts`, and the working tree is clean before starting the default switch.
- STOP unless `data/compiled/v2/taxonomy.json`, `descriptor_aliases.json`, and `similarity_matrix.json` exist and validate.
- STOP if protected v1/input files have any diff before the switch.
- STOP if the intended diff includes any `src/cli/parse_args.ts` change outside `DEFAULT_PATHS.seedPath`, `relationsPath`, `accordsPath`, `outputDir`, and `version`.
- STOP if tests are modified for any reason other than reflecting approved v2 defaults and explicit v1 fallback preservation.
</preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply atomic DEFAULT_PATHS switch and narrow tests if necessary</name>
  <read_first>
    - src/cli/parse_args.ts current DEFAULT_PATHS block
    - src/tests/cli/parse_args.test.ts default parser test
    - src/tests/curation/v1_v2_comparison.test.ts current default guard and explicit v1/v2 compile tests
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-PATTERNS.md `src/cli/parse_args.ts` and test patterns
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-CONTEXT.md DEFAULT_PATHS Switch section
  </read_first>
  <files>src/cli/parse_args.ts, src/tests/cli/parse_args.test.ts, src/tests/curation/v1_v2_comparison.test.ts, .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-3-DEFAULT-PATHS-SWITCH.md</files>
  <behavior>
    - Test 1: `DEFAULT_PATHS.seedPath` equals `data/taxonomy/taxonomy-seed.v2.json` after the switch.
    - Test 2: `DEFAULT_PATHS.relationsPath` equals `data/inference/curated_relations.v2.json` and `DEFAULT_PATHS.accordsPath` equals `data/inference/accord_map.v2.json` after the switch.
    - Test 3: `DEFAULT_PATHS.outputDir` equals `data/compiled/v2` and `DEFAULT_PATHS.version` equals `2.0.0` after the switch.
    - Test 4: `aliasPath`, `corpusPath`, `noisePath`, parser behavior, `CliArgumentError`, flags, and `--generated-at` validation are unchanged.
    - Test 5: explicit v1 fallback remains covered via explicit CLI flags and temporary output path, not by defaults.
  </behavior>
  <action>Edit only `src/cli/parse_args.ts` `DEFAULT_PATHS` fields: `seedPath` to `data/taxonomy/taxonomy-seed.v2.json`, `relationsPath` to `data/inference/curated_relations.v2.json`, `accordsPath` to `data/inference/accord_map.v2.json`, `outputDir` to `data/compiled/v2`, and `version` to `2.0.0`. Preserve `aliasPath`, `corpusPath`, `noisePath`, `FLAG_TO_KEY`, generated-at validation, parsing behavior, and all exports. Update existing tests only if strictly necessary so they assert approved v2 defaults and retain explicit v1 compile/fallback coverage. Record before/after field table and diff scope in `12-GATE-3-DEFAULT-PATHS-SWITCH.md`. Do not edit seed/input files, `data/compiled/v1`, `data/compiled/v2` artifacts, docs, STATE, ROADMAP, or REQUIREMENTS in this task.</action>
  <verify>
    <automated>git log --oneline --grep='build(data): publish compiled v2 taxonomy artifacts' -1 | grep -q 'build(data): publish compiled v2 taxonomy artifacts' && cd src && npm test -- tests/cli/parse_args.test.ts tests/curation/v1_v2_comparison.test.ts && cd .. && node -e "const fs=require('fs'); const s=fs.readFileSync('src/cli/parse_args.ts','utf8'); const required=[\"seedPath: 'data/taxonomy/taxonomy-seed.v2.json'\",\"relationsPath: 'data/inference/curated_relations.v2.json'\",\"accordsPath: 'data/inference/accord_map.v2.json'\",\"outputDir: 'data/compiled/v2'\",\"version: '2.0.0'\"]; const preserved=[\"aliasPath: 'data/taxonomy/descriptor_aliases.seed.json'\",\"corpusPath: 'data/enriched_materials.json'\",\"noisePath: 'data/inference/semantic_noise.v1.json'\"]; for (const x of required.concat(preserved)) { if (!s.includes(x)) { console.error(x); process.exit(1); } }" && git diff --exit-code -- data/compiled/v1 data/compiled/v2 data/taxonomy/taxonomy-seed.v1.json data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/inference/curated_relations.v1.json data/inference/curated_relations.v2.json data/inference/accord_map.v1.json data/inference/accord_map.v2.json && node -e "const {execSync}=require('child_process'); const allowed=new Set(['src/cli/parse_args.ts','src/tests/cli/parse_args.test.ts','src/tests/curation/v1_v2_comparison.test.ts','.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-3-DEFAULT-PATHS-SWITCH.md']); const files=execSync('git diff --name-only',{encoding:'utf8'}).trim().split(/\\n/).filter(Boolean); const bad=files.filter((f)=>!allowed.has(f)); if (bad.length) { console.error(bad.join('\\n')); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: automated verify asserts exactly five approved `DEFAULT_PATHS` values are v2 and preserved defaults (`aliasPath`, `corpusPath`, `noisePath`) remain unchanged.
    - Commit baseline assertion: Gate 2 publication is already committed as `build(data): publish compiled v2 taxonomy artifacts` before the default switch begins.
    - Allowlist assertion: automated verify rejects any task diff outside `src/cli/parse_args.ts`, the two existing focused test files if needed, and `12-GATE-3-DEFAULT-PATHS-SWITCH.md`.
    - Negative source assertion: `aliasPath`, `corpusPath`, `noisePath`, parser flags, generated-at validation, and error handling are unchanged.
    - Test assertion: focused CLI/curation tests pass after any necessary test updates.
    - Protected assertion: no seed/input JSON, `data/compiled/v1/**`, or `data/compiled/v2/**` file is changed by this task.
    - Decision trace: SWITCH-D-15, SWITCH-D-18, SWITCH-D-19, SWITCH-D-20, SWITCH-D-21, SWITCH-D-22, SWITCH-D-23, SWITCH-D-30, SWITCH-D-54.
  </acceptance_criteria>
  <done>Defaults are atomically switched to v2 with no partial field state and no out-of-scope source/data diff.</done>
</task>

<task type="auto">
  <name>Task 2: Run post-switch validation and explicit v1 fallback compile</name>
  <read_first>
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-VALIDATION.md Gate 4 row
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-PREFLIGHT.md Future DEFAULT_PATHS Switch Policy and Gate 4
    - src/package.json scripts
    - src/cli/parse_args.ts switched defaults
  </read_first>
  <files>.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-4-POST-SWITCH-VALIDATION.md</files>
  <action>Run full validation after the switch: `cd src && npm run typecheck && npm test && npm run build`. Run default compile with current defaults and fixed `--generated-at 2026-01-01T00:00:00.000Z`, then confirm it writes v2 output/version to `data/compiled/v2` without dirtying official v2 artifacts. Run explicit v1 compile to `/tmp/opencode/taxonomy-phase12-switch/post-switch-v1-fallback` with v1 seed/relation/accord paths, version `1.0.0`, and fixed generated_at. Validate `data/compiled/v1` remains present/unchanged and official `data/compiled/v2` remains present and diff-clean. Record all commands, outputs, default v2 assertion, explicit v1 fallback assertion, and protected diff result in `12-GATE-4-POST-SWITCH-VALIDATION.md` per SWITCH-D-23, SWITCH-D-34, SWITCH-D-35 and SWITCH-D-64.</action>
  <verify>
    <automated>cd src && npm run typecheck && npm test && npm run build && node -e "import('./dist/cli/parse_args.js').then(({DEFAULT_PATHS})=>{const expected={seedPath:'data/taxonomy/taxonomy-seed.v2.json',relationsPath:'data/inference/curated_relations.v2.json',accordsPath:'data/inference/accord_map.v2.json',outputDir:'data/compiled/v2',version:'2.0.0'}; for (const [k,v] of Object.entries(expected)) { if (DEFAULT_PATHS[k] !== v) { console.error(k + '=' + DEFAULT_PATHS[k]); process.exit(1); } }})" && npm run compile -- --generated-at 2026-01-01T00:00:00.000Z && npm run compile -- --seed ../data/taxonomy/taxonomy-seed.v1.json --aliases ../data/taxonomy/descriptor_aliases.seed.json --relations ../data/inference/curated_relations.v1.json --accords ../data/inference/accord_map.v1.json --out /tmp/opencode/taxonomy-phase12-switch/post-switch-v1-fallback --version 1.0.0 --generated-at 2026-01-01T00:00:00.000Z && test -f ../data/compiled/v2/taxonomy.json && test -f /tmp/opencode/taxonomy-phase12-switch/post-switch-v1-fallback/taxonomy.json && cd .. && git diff --exit-code -- data/compiled/v2 data/compiled/v1 data/taxonomy/taxonomy-seed.v1.json data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/inference/curated_relations.v1.json data/inference/curated_relations.v2.json data/inference/accord_map.v1.json data/inference/accord_map.v2.json</automated>
  </verify>
  <acceptance_criteria>
    - Command assertion: typecheck, full tests, build, default compile, and explicit v1 fallback compile exit 0.
    - Default behavior assertion: automated verify imports built `DEFAULT_PATHS` and asserts v2 seed, relation, accord, outputDir and version values before default compile.
    - Non-dirty artifact assertion: automated verify runs default compile with fixed generated_at and asserts `git diff --exit-code -- data/compiled/v2` so the default-switch plan does not mix artifact rewrites into the switch diff.
    - Fallback assertion: explicit v1 compile writes to `/tmp/opencode/taxonomy-phase12-switch/post-switch-v1-fallback`, not `data/compiled/v1`.
    - Protected assertion: automated verify runs protected `git diff --exit-code` for `data/compiled/v1` and all protected taxonomy/inference inputs after validation.
    - Commit assertion: executor must commit Gate 3/4 source and post-switch validation evidence as `feat(cli): switch taxonomy defaults to v2` before Plan 12-05 starts.
    - Decision trace: SWITCH-D-21, SWITCH-D-23, SWITCH-D-31, SWITCH-D-32, SWITCH-D-33, SWITCH-D-34, SWITCH-D-35, SWITCH-D-36, SWITCH-D-50, SWITCH-D-51, SWITCH-D-52, SWITCH-D-57, SWITCH-D-59, SWITCH-D-60, SWITCH-D-62, SWITCH-D-64.
  </acceptance_criteria>
  <done>Post-switch validation passes, default compile uses official v2, explicit v1 fallback works in `/tmp`, and protected v1/input paths remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| official v2 artifacts → CLI defaults | Published data becomes default CLI target. |
| source defaults → generated artifacts | `parseCompileArgs([])` controls default write path and version. |
| rollback anchor → post-switch validation | v1 must remain physically preserved after default behavior changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-partial-default-switch | Tampering | `DEFAULT_PATHS` | mitigate | Change and assert all five approved fields together; reject partial combinations. |
| T-12-04-scope-creep | Tampering | `src/cli/parse_args.ts` | mitigate | Preserve parser behavior and non-approved defaults; tests cover parser behavior. |
| T-12-04-post-switch-regression | Denial of Service | CLI compile path | mitigate | Run full suite, default v2 compile, explicit v1 fallback compile, and protected diffs. |
| T-12-04-v1-baseline-drift | Tampering/Availability | v1 artifacts and inputs | mitigate | Explicit v1 compile uses `/tmp`; protected v1 paths remain diff-clean. |
| T-12-04-SC | Tampering | npm/pip/cargo installs | accept | No package installation; use existing npm scripts only. |
</threat_model>

<verification>
Overall Gate 3/4 verification: official v2 artifacts exist and validate; exactly five `DEFAULT_PATHS` fields switched to v2; focused tests pass; full suite/build pass; default compile writes v2; explicit v1 fallback compile works in `/tmp`; protected v1/input diffs are clean.
</verification>

<success_criteria>
- SWITCH-03, SWITCH-04, SWITCH-07, SWITCH-08 and SWITCH-11 are covered with source and post-switch command evidence.
- No partial default switch exists at any point after task completion.
- Plan 12-05 remains blocked until Gate 4 post-switch validation passes.
</success_criteria>

<output>
Create `.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-04-SUMMARY.md` when done.
</output>
