---
phase: 12-taxonomy-seed-v2-default-switch-execution
plan: 03
type: execute
wave: 2
depends_on: [12-02]
files_modified:
  - data/compiled/v2/taxonomy.json
  - data/compiled/v2/descriptor_aliases.json
  - data/compiled/v2/similarity_matrix.json
  - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-2-V2-PUBLICATION.md
  - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-03-SUMMARY.md
autonomous: false
execution_readiness: not_ready_for_execution
blocked_until: gate-1-pass-with-deterministic-temp-v2-output
requirements: [SWITCH-04, SWITCH-05, SWITCH-06, SWITCH-08, SWITCH-11]
user_setup: []
must_haves:
  truths:
    - "Official v2 artifacts are published only from validated current temp output; implements SWITCH-D-24 and SWITCH-D-25."
    - "Publication creates only three official files: taxonomy, descriptor aliases, similarity matrix; implements SWITCH-D-14 and SWITCH-D-26."
    - "`data/compiled/v1` remains preserved without overwrite and validates as rollback anchor; implements SWITCH-D-27, SWITCH-D-31, SWITCH-D-32, SWITCH-D-33, SWITCH-D-34, SWITCH-D-36."
    - "Official v2 artifacts pass schema/status/quality gates and generated_at policy; implements SWITCH-D-28 and SWITCH-D-29."
    - "DEFAULT_PATHS switch remains blocked until official v2 exists and validates; implements SWITCH-D-19, SWITCH-D-30, SWITCH-D-54."
    - "Artifact publication commit is separate from DEFAULT_PATHS switch; implements SWITCH-D-59, SWITCH-D-60, SWITCH-D-62, SWITCH-D-63."
  artifacts:
    - path: "data/compiled/v2/taxonomy.json"
      provides: "Official v2 taxonomy artifact copied from validated temp output"
    - path: "data/compiled/v2/descriptor_aliases.json"
      provides: "Official v2 descriptor aliases artifact copied from validated temp output"
    - path: "data/compiled/v2/similarity_matrix.json"
      provides: "Official v2 sparse graph/review queue artifact copied from validated temp output"
    - path: ".planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-2-V2-PUBLICATION.md"
      provides: "Publication evidence, equivalence checks and protected diff audit"
  key_links:
    - from: "/tmp/opencode/taxonomy-phase12-switch/v2-candidate"
      to: "data/compiled/v2"
      via: "copy after Gate 1 pass"
      pattern: "taxonomy.json|descriptor_aliases.json|similarity_matrix.json"
    - from: "data/compiled/v2"
      to: "12-04-PLAN.md"
      via: "Gate 2 validation required before DEFAULT_PATHS switch"
      pattern: "gate_2_result: pass"
---

<objective>
Publish official v2 artifacts after Gate 1 and validate them without touching defaults, seed/input files, or `data/compiled/v1`.

Purpose: create the side-by-side official v2 artifact set required before the atomic default switch.
Output: exactly three files under `data/compiled/v2/` plus `12-GATE-2-V2-PUBLICATION.md` evidence.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-CONTEXT.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-PREFLIGHT.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-RESEARCH.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-PATTERNS.md
@.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-VALIDATION.md
@src/cli/parse_args.ts
</context>

<preconditions>
- STOP unless Plan 12-02 completed and `12-GATE-1-PRE-SWITCH-REVALIDATION.md` records `gate_1_result: pass`.
- STOP unless Gate 0/1 approval and pre-switch validation evidence has been committed separately with commit message `docs(12): record final approval and pre-switch validation`, and the working tree is clean before starting publication.
- STOP unless `/tmp/opencode/taxonomy-phase12-switch/v2-candidate` and `/v2-candidate-repeat` exist and `cmp -s` passed for all three artifacts.
- STOP if `data/compiled/v2` already exists with unvalidated/stale files.
- STOP if protected v1/input/default paths have any diff before publication.
- STOP if the executor intends to edit `src/cli/parse_args.ts`; that is Plan 12-04 only.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Publish exactly three official v2 artifacts from validated temp output</name>
  <read_first>
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-1-PRE-SWITCH-REVALIDATION.md
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-CONTEXT.md Official V2 Artifacts and Expected Diffs sections
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-PREFLIGHT.md Future Official V2 Artifact Publication Policy
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-PATTERNS.md Official `data/compiled/v2` artifacts
  </read_first>
  <files>data/compiled/v2/taxonomy.json, data/compiled/v2/descriptor_aliases.json, data/compiled/v2/similarity_matrix.json, .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-2-V2-PUBLICATION.md</files>
  <action>Create `data/compiled/v2` only now, after Gate 1 pass and after the approval/prevalidation commit exists. Copy only `taxonomy.json`, `descriptor_aliases.json`, and `similarity_matrix.json` from `/tmp/opencode/taxonomy-phase12-switch/v2-candidate` into `data/compiled/v2`. Do not copy old Phase 10 temp outputs. Do not run a compile directly to official paths. Do not modify `data/compiled/v1`, any seed/input JSON, or `src/cli/parse_args.ts`. Record the source temp directory, generated_at policy, file names, sizes/checksums if available, prior commit `docs(12): record final approval and pre-switch validation`, and publication timestamp in `12-GATE-2-V2-PUBLICATION.md` per SWITCH-D-24 through SWITCH-D-30.</action>
  <verify>
    <automated>git log --oneline --grep='docs(12): record final approval and pre-switch validation' -1 | grep -q 'docs(12): record final approval and pre-switch validation' && test -f /tmp/opencode/taxonomy-phase12-switch/v2-candidate/taxonomy.json && test -f /tmp/opencode/taxonomy-phase12-switch/v2-candidate/descriptor_aliases.json && test -f /tmp/opencode/taxonomy-phase12-switch/v2-candidate/similarity_matrix.json && test -f data/compiled/v2/taxonomy.json && test -f data/compiled/v2/descriptor_aliases.json && test -f data/compiled/v2/similarity_matrix.json && test "$(find data/compiled/v2 -maxdepth 1 -type f | wc -l)" -eq 3</automated>
  </verify>
  <acceptance_criteria>
    - Artifact assertion: official `data/compiled/v2` contains exactly three files and no sidecars.
    - Commit baseline assertion: approval/prevalidation evidence is already committed as `docs(12): record final approval and pre-switch validation` before publication begins.
    - Source assertion: official files originate from `/tmp/opencode/taxonomy-phase12-switch/v2-candidate`, not Phase 10 output.
    - Scope assertion: `src/cli/parse_args.ts` is unchanged and `DEFAULT_PATHS` still point to v1 after publication.
    - Protected assertion: no seed/input file and no `data/compiled/v1/**` file is modified.
    - Decision trace: SWITCH-D-14, SWITCH-D-24, SWITCH-D-25, SWITCH-D-26, SWITCH-D-27, SWITCH-D-29, SWITCH-D-30, SWITCH-D-53, SWITCH-D-54.
  </acceptance_criteria>
  <done>Official v2 artifact set exists side-by-side with preserved v1 and contains only the approved three files.</done>
</task>

<task type="auto">
  <name>Task 2: Validate official artifacts and publication diff scope</name>
  <read_first>
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-VALIDATION.md Gate 2 row
    - .planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-RESEARCH.md Artifact Gates and Diff Gates
    - src/package.json scripts
    - src/cli/parse_args.ts read-only current defaults
  </read_first>
  <files>.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-2-V2-PUBLICATION.md</files>
  <action>Compare official `data/compiled/v2` files to `/tmp/opencode/taxonomy-phase12-switch/v2-candidate` using `cmp -s` for all three files. Run `cd src && npm run typecheck && npm test && npm run build` after publication. Run protected diff checks for v1 artifacts and all seed/input JSON files. Audit `git diff --name-only` and record that the publication stage includes only `data/compiled/v2/{taxonomy.json,descriptor_aliases.json,similarity_matrix.json}` and Gate 2 evidence docs. Mark `gate_2_result: pass` only if equivalence, validation commands, and protected diff checks pass per SWITCH-D-28, SWITCH-D-34, SWITCH-D-56 and SWITCH-D-57.</action>
  <verify>
    <automated>cmp -s /tmp/opencode/taxonomy-phase12-switch/v2-candidate/taxonomy.json data/compiled/v2/taxonomy.json && cmp -s /tmp/opencode/taxonomy-phase12-switch/v2-candidate/descriptor_aliases.json data/compiled/v2/descriptor_aliases.json && cmp -s /tmp/opencode/taxonomy-phase12-switch/v2-candidate/similarity_matrix.json data/compiled/v2/similarity_matrix.json && cd src && npm run typecheck && npm test && npm run build && node -e "import('./dist/cli/parse_args.js').then(({DEFAULT_PATHS})=>{const expected={seedPath:'data/taxonomy/taxonomy-seed.v1.json',relationsPath:'data/inference/curated_relations.v1.json',accordsPath:'data/inference/accord_map.v1.json',outputDir:'data/compiled/v1',version:'1.0.0'}; for (const [k,v] of Object.entries(expected)) { if (DEFAULT_PATHS[k] !== v) { console.error(k + '=' + DEFAULT_PATHS[k]); process.exit(1); } }})" && cd .. && git diff --exit-code -- data/compiled/v1 data/taxonomy/taxonomy-seed.v1.json data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/inference/curated_relations.v1.json data/inference/curated_relations.v2.json data/inference/accord_map.v1.json data/inference/accord_map.v2.json src/cli/parse_args.ts && node -e "const {execSync}=require('child_process'); const allowed=new Set(['data/compiled/v2/taxonomy.json','data/compiled/v2/descriptor_aliases.json','data/compiled/v2/similarity_matrix.json','.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-GATE-2-V2-PUBLICATION.md']); const files=execSync('git diff --name-only',{encoding:'utf8'}).trim().split(/\\n/).filter(Boolean); const bad=files.filter((f)=>!allowed.has(f)); if (bad.length) { console.error(bad.join('\\n')); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - Equivalence assertion: official v2 files are byte-identical to validated temp v2 files unless final approval recorded a generated_at comparison policy and the report applies it explicitly.
    - Validation assertion: typecheck, tests, and build pass after publication.
    - Diff assertion: automated verify runs protected `git diff --exit-code` for v1 artifacts, all protected inputs and `src/cli/parse_args.ts`.
    - Allowlist assertion: automated verify rejects any publication-stage `git diff --name-only` entry outside `data/compiled/v2/taxonomy.json`, `data/compiled/v2/descriptor_aliases.json`, `data/compiled/v2/similarity_matrix.json`, and `12-GATE-2-V2-PUBLICATION.md`.
    - Default assertion: automated verify asserts `DEFAULT_PATHS` still point to v1 after artifact publication.
    - Commit assertion: executor must commit only the Gate 2 artifact publication diff as `build(data): publish compiled v2 taxonomy artifacts` before Plan 12-04 starts.
    - Decision trace: SWITCH-D-17, SWITCH-D-27, SWITCH-D-28, SWITCH-D-29, SWITCH-D-30, SWITCH-D-34, SWITCH-D-36, SWITCH-D-56, SWITCH-D-57, SWITCH-D-59, SWITCH-D-60, SWITCH-D-62, SWITCH-D-63.
  </acceptance_criteria>
  <done>`12-GATE-2-V2-PUBLICATION.md` records validated official v2 publication and clean protected diff scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| temp v2 output → official artifact directory | Validated temporary output becomes committed official artifact. |
| artifact publication → default switch | Existence of official v2 is necessary but does not itself authorize default switch. |
| artifact diff → git commit | Publication diff must not include defaults, seed inputs, or v1 changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-artifact-mismatch | Tampering | `data/compiled/v2/*` | mitigate | Copy only from Gate 1 temp output and compare with `cmp -s`. |
| T-12-03-old-output-contamination | Tampering | Phase 10 temp outputs | mitigate | Precondition forbids old temp output; report records current `/tmp/opencode/taxonomy-phase12-switch/v2-candidate` source. |
| T-12-03-v1-overwrite | Tampering/Availability | `data/compiled/v1/**` | mitigate | Protected diff and existence checks before/after publication. |
| T-12-03-premature-switch | Elevation of Privilege | `src/cli/parse_args.ts` | mitigate | This plan forbids default edits; Plan 12-04 depends on Gate 2 pass. |
| T-12-03-SC | Tampering | npm/pip/cargo installs | accept | No package installation; use existing npm scripts only. |
</threat_model>

<verification>
Overall Gate 2 verification: Plan 12-02 complete; official `data/compiled/v2` has exactly three files; official files match validated temp output; typecheck/tests/build pass; protected v1/input/default paths are clean; `src/cli/parse_args.ts` remains v1.
</verification>

<success_criteria>
- SWITCH-04, SWITCH-05, SWITCH-06, SWITCH-08 and SWITCH-11 are covered by official publication evidence.
- Plan 12-04 remains blocked until `12-GATE-2-V2-PUBLICATION.md` records pass.
- Artifact publication and default switch are not mixed.
</success_criteria>

<output>
Create `.planning/phases/12-taxonomy-seed-v2-default-switch-execution/12-03-SUMMARY.md` when done.
</output>
