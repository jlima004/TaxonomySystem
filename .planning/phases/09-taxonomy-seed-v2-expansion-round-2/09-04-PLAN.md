---
phase: 09-taxonomy-seed-v2-expansion-round-2
plan: "04"
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - .planning/phases/09-taxonomy-seed-v2-expansion-round-2/curation/v1-v2-comparison.md
autonomous: false
requirements:
  - EXP2-06
  - EXP2-07
must_haves:
  truths:
    - "v1 baseline compiled output is compared side-by-side with v2 candidate compile."
    - "10 validation metrics are recorded: coverage counts, group coverage, generic pressure, review queue, graph coverage, vanilla gap status, alias quality, zero-frequency seeds, determinism/schema, and curation traceability."
    - "No modifications are made to data/compiled/v1/ or CLI defaults."
  artifacts:
    - path: ".planning/phases/09-taxonomy-seed-v2-expansion-round-2/curation/v1-v2-comparison.md"
      provides: "Round 2 comparative validation report"
---

<objective>
Generate the v1-vs-v2-expanded comparison validation report.
Compiles both v1 and the expanded v2 candidate, runs curation tests, extracts all 10 validation metrics, and documents them in the Phase 9 comparison report to verify promotion readiness without mutating baseline defaults.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-taxonomy-seed-v2-expansion-round-2/09-CONTEXT.md
@.planning/phases/09-taxonomy-seed-v2-expansion-round-2/09-RESEARCH.md
@.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
@data/taxonomy/taxonomy-seed.v2.json
@data/taxonomy/descriptor_aliases.seed.json
@data/inference/curated_relations.v2.json
@data/inference/accord_map.v2.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile baselines and candidates to temporary directories</name>
  <files></files>
  <read_first>data/taxonomy/taxonomy-seed.v2.json</read_first>
  <action>Run the CLI compiler to generate baseline v1 and candidate v2 compiled artifacts in temporary directories.
  - Compile v1 baseline:
    ```bash
    cd src && npm run compile -- \
      --seed ../data/taxonomy/taxonomy-seed.v1.json \
      --aliases ../data/taxonomy/descriptor_aliases.seed.json \
      --relations ../data/inference/curated_relations.v1.json \
      --accords ../data/inference/accord_map.v1.json \
      --out /tmp/opencode/taxonomy-phase9-comparison/v1-baseline \
      --version 1.0.0 \
      --generated-at 2026-01-01T00:00:00.000Z
    ```
  - Compile v2 candidate:
    ```bash
    cd src && npm run compile -- \
      --seed ../data/taxonomy/taxonomy-seed.v2.json \
      --aliases ../data/taxonomy/descriptor_aliases.seed.json \
      --relations ../data/inference/curated_relations.v2.json \
      --accords ../data/inference/accord_map.v2.json \
      --out /tmp/opencode/taxonomy-phase9-comparison/v2-candidate \
      --version 2.0.0 \
      --generated-at 2026-01-01T00:00:00.000Z
    ```
  Verify both compile commands execute successfully with exit code 0.</action>
  <verify><automated>test -d /tmp/opencode/taxonomy-phase9-comparison/v1-baseline && test -d /tmp/opencode/taxonomy-phase9-comparison/v2-candidate</automated></verify>
  <acceptance_criteria>
    - Behavior assertion: Both compile runs succeed without errors.
    - Behavior assertion: Files are generated under `/tmp/opencode/taxonomy-phase9-comparison/` and not in `data/compiled/v1/`.
  </acceptance_criteria>
  <done>Temporary compiled files generated for comparison.</done>
</task>

<task type="auto">
  <name>Task 2: Generate Phase 9 v1-vs-v2 comparison report</name>
  <files>.planning/phases/09-taxonomy-seed-v2-expansion-round-2/curation/v1-v2-comparison.md</files>
  <read_first>data/compiled/v1/taxonomy.json, /tmp/opencode/taxonomy-phase9-comparison/v2-candidate/taxonomy.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</read_first>
  <action>Create `.planning/phases/09-taxonomy-seed-v2-expansion-round-2/curation/v1-v2-comparison.md`. Populate it with:
  - Setup and compilation configurations.
  - Verification of 10 metrics: coverage counts, group coverage (green, fruity, spicy), generic pressure changes, review queue reduction, graph coverage (edges, density), vanilla gap resolution status, alias target analysis, zero-frequency seeds, determinism/schema check, and curation traceability.
  - Analysis of soft warnings (legacy ylang ylang alias target absent) and confirmation of zero hard failures.
  - Outline of the 10 future promotion readiness criteria for default switch without executing any default change.</action>
  <verify><automated>test -f .planning/phases/09-taxonomy-seed-v2-expansion-round-2/curation/v1-v2-comparison.md && grep -q 'Families' .planning/phases/09-taxonomy-seed-v2-expansion-round-2/curation/v1-v2-comparison.md && grep -q 'Subfamilies' .planning/phases/09-taxonomy-seed-v2-expansion-round-2/curation/v1-v2-comparison.md && grep -q 'Review queue' .planning/phases/09-taxonomy-seed-v2-expansion-round-2/curation/v1-v2-comparison.md</automated></verify>
  <acceptance_criteria>
    - Source assertion: v1-v2-comparison.md exists and registers all 10 required validation metrics.
    - Source assertion: The report records the 10 future promotion readiness criteria.
  </acceptance_criteria>
  <done>v1-vs-v2 comparison report generated successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Execute all curation validation tests</name>
  <files></files>
  <read_first>src/tests/curation/v1_v2_comparison.test.ts</read_first>
  <action>Run all curation tests to prove data integrity and regression containment:
  ```bash
  cd src && npm test -- tests/curation/
  ```
  Ensure all 5 test files in `src/tests/curation/` pass successfully.</action>
  <verify><automated>cd src && npm test -- tests/curation/</automated></verify>
  <acceptance_criteria>
    - Test-command assertion: `cd src && npm test -- tests/curation/` passes.
  </acceptance_criteria>
  <done>All curation tests executed and passed.</done>
</task>

<task type="auto">
  <name>Task 4: Verify protected files remain unchanged</name>
  <files></files>
  <read_first></read_first>
  <action>Verify that `data/compiled/v1/`, `data/taxonomy/taxonomy-seed.v1.json`, `data/inference/curated_relations.v1.json`, `data/inference/accord_map.v1.json`, and `src/cli/parse_args.ts` have a completely empty diff (i.e. no changes made to them).</action>
  <verify><automated>git diff --exit-code data/compiled/v1 data/taxonomy/taxonomy-seed.v1.json data/inference/curated_relations.v1.json data/inference/accord_map.v1.json src/cli/parse_args.ts</automated></verify>
  <acceptance_criteria>
    - Behavior assertion: git diff --exit-code returns 0 for all protected files, confirming they are unchanged.
  </acceptance_criteria>
  <done>Protected files verified as unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Artifact boundaries: verification step compares files compiled from candidate inputs in a sandbox directory (/tmp) to avoid default drift.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Repudiation | comparison report | mitigate | Document the 10 metrics side-by-side to prove that no unauthorized promotion or default change occurred. |
</threat_model>

<verification>
Verify by ensuring that `.planning/phases/09-taxonomy-seed-v2-expansion-round-2/curation/v1-v2-comparison.md` is populated and all curation tests pass.
</verification>

<success_criteria>
The validation report is created and the tests pass.
</success_criteria>

<output>
Create `.planning/phases/09-taxonomy-seed-v2-expansion-round-2/09-04-SUMMARY.md` when done.
</output>
