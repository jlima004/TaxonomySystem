---
phase: 09-taxonomy-seed-v2-expansion-round-2
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
autonomous: false
requirements:
  - EXP2-02
  - EXP2-03
must_haves:
  truths:
    - "Curation workbook uses existing candidate-review.md by appending a new Round 2 section."
    - "Every proposed seed change requires explicit manual_approval: approved, primary_disposition: promote_to_seed, and rationale/evidence before data updates."
  artifacts:
    - path: ".planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md"
      provides: "Updated Curation workbook with Round 2 section and approval ledger"
---

<objective>
Configure the curation workbook for Phase 9 Round 2.
Appends the "Phase 09 / Round 2 Curation" section to `candidate-review.md` containing candidate priorities and explicit manual approvals for priority descriptors in the green, fruity, and spicy groups.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-taxonomy-seed-v2-expansion-round-2/09-CONTEXT.md
@.planning/phases/09-taxonomy-seed-v2-expansion-round-2/09-RESEARCH.md
@.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Phase 09 Round 2 curation section to workbook</name>
  <files>.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</files>
  <read_first>.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, .planning/phases/09-taxonomy-seed-v2-expansion-round-2/09-CONTEXT.md</read_first>
  <action>Edit `.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md` to append the new `# Phase 09 / Round 2 Curation` section. Populate it with the priority groups `green` (subfamilies: `herbal_green`, `leafy_green`), `fruity` (subfamilies: `tropical_fruit`, `orchard_fruit`, `red_fruit`), and `spicy` (subfamilies: `warm_spice`, `fresh_spice`). Include candidate entries for:
  - `pineapple` -> tropical_fruit (pending)
  - `banana` -> tropical_fruit (pending)
  - `strawberry` -> red_fruit (pending)
  - `blackberry` -> red_fruit (pending)
  - `cinnamon` -> warm_spice (pending)
  - `clove` -> warm_spice (pending)
  - `allspice` -> warm_spice (pending)
  - `basil` -> herbal_green (pending)
  - `tomato_leaf` -> leafy_green (pending)
  - `melon` -> orchard_fruit (pending)
  Ensure each candidate entry has `approval_id: r2-approval-NNN`, `round: phase_09_round_2`, `manual_approval: pending`, `primary_disposition: promote_to_seed`, `promotion_effect: none`, individual rationale, and evidence. Include deferred entries `warm`, `green`, `fruity`, `spicy`, `minty` with `primary_disposition: defer` and `manual_approval: pending`. Ensure legacy ylang ylang target is registered as a deferred cleanup item `r2-alias-cleanup-01`.
  Also include candidate proposals for cross-family relations and accords with `approval_id` starting with `r2-relation-NNN` and `r2-accord-NNN`, `round: phase_09_round_2`, `manual_approval: pending`, and `primary_disposition: approve_relation_accord`.</action>
  <verify><automated>grep -q '# Phase 09 / Round 2 Curation' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md && grep -q 'r2-approval-' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md && grep -q 'manual_approval: pending' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</automated></verify>
  <acceptance_criteria>
    - Source assertion: candidate-review.md contains `# Phase 09 / Round 2 Curation` and does not overwrite or remove the Phase 8 approvals.
    - Source assertion: Curation ledger records candidates with prefix `r2-approval-*` and disposition `promote_to_seed` with `promotion_effect: none` and pending status.
  </acceptance_criteria>
  <done>Workbook is updated with Round 2 section and proposed candidate ledger.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm Round 2 workbook ledger approvals</name>
  <files>.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</files>
  <read_first>.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</read_first>
  <action>Pause for human confirmation of the Round 2 curation workbook proposals. The curator must manually edit candidate-review.md and change `manual_approval: pending` to `manual_approval: approved` (and optionally remove `promotion_effect: none`) for each approved descriptor in the `phase_09_round_2` round, adding custom rationale and evidence persisted in the ledger.</action>
  <what-built>Round 2 curation workbook section and candidate proposals were appended to the curation workbook.</what-built>
  <how-to-verify>Verify `candidate-review.md` has the new Phase 09 section. In the `r2-approval-*` blocks specifically, verify that `round: phase_09_round_2`, `manual_approval: approved`, and `primary_disposition: promote_to_seed` are present.</how-to-verify>
  <verify><automated>awk '/r2-approval-/{f=1; buf=""} f{buf=buf $0 ORS} /primary_disposition:/{if(f && buf ~ /round: phase_09_round_2/ && buf ~ /manual_approval: approved/ && buf ~ /primary_disposition: promote_to_seed/){found=1; exit}} END{exit !found}' .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</automated></verify>
  <acceptance_criteria>
    - Human assertion: Curator verifies and manually edits the Round 2 ledger to mark approvals before proceeding to data modifications.
  </acceptance_criteria>
  <done>Human verification of Round 2 ledger is signed-off.</done>
  <resume-signal>type "approved" to proceed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Curation workbook input: manually curated entries mapping to specific round IDs and rationales prevent automated candidate insertion.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | candidate-review.md | mitigate | Require explicit r2-approval-NNN prefix and manual approval fields to validate curation origins. |
</threat_model>

<verification>
Verify by inspecting `candidate-review.md` and ensuring the new section and approved ledger are written correctly.
</verification>

<success_criteria>
Workbook has the Round 2 section and approvals correctly written.
</success_criteria>

<output>
Create `.planning/phases/09-taxonomy-seed-v2-expansion-round-2/09-01-SUMMARY.md` when done.
</output>
