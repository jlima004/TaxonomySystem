---
phase: 09-taxonomy-seed-v2-expansion-round-2
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - data/taxonomy/taxonomy-seed.v2.json
  - data/taxonomy/descriptor_aliases.seed.json
  - src/tests/curation/taxonomy_seed_v2.test.ts
  - src/tests/curation/alias_seed_v2.test.ts
autonomous: false
requirements:
  - EXP2-01
  - EXP2-04
must_haves:
  truths:
    - "taxonomy-seed.v1.json is preserved unchanged; defaults remain v1."
    - "taxonomy-seed.v2.json is updated to add the 3 new families (green, fruity, spicy), up to 7 subfamilies (conditional on approvals), and approved seed descriptors."
    - "Approved aliases are mapped to canonical descriptors in descriptor_aliases.seed.json; legacy ylang ylang alias target check is deferred."
    - "Test files are updated if Phase 8 had fixed assertions that block dynamic additions."
  artifacts:
    - path: "data/taxonomy/taxonomy-seed.v2.json"
      provides: "Updated v2 candidate seed with Round 2 expansion"
    - path: "data/taxonomy/descriptor_aliases.seed.json"
      provides: "Updated seed aliases map with approved Round 2 aliases"
---

<objective>
Implement the approved Round 2 taxonomy seed and alias expansions.
Modifies `taxonomy-seed.v2.json` to include the families `green`, `fruity`, `spicy` along with their respective subfamilies and approved descriptors, and updates `descriptor_aliases.seed.json` with manual aliases corresponding to these descriptors as approved in the workbook.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-taxonomy-seed-v2-expansion-round-2/09-CONTEXT.md
@.planning/phases/09-taxonomy-seed-v2-expansion-round-2/09-RESEARCH.md
@.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
@data/taxonomy/taxonomy-seed.v2.json
@data/taxonomy/descriptor_aliases.seed.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Round 2 families and descriptors to v2 seed</name>
  <files>data/taxonomy/taxonomy-seed.v2.json</files>
  <read_first>data/taxonomy/taxonomy-seed.v2.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</read_first>
  <action>Read `candidate-review.md` and only edit `data/taxonomy/taxonomy-seed.v2.json` if there is a concrete approval block containing: `approval_id: r2-approval-*`, `round: phase_09_round_2`, `manual_approval: approved`, `primary_disposition: promote_to_seed`, family_id/subfamily_id/descriptor_id, rationale, and evidence. For each candidate being applied, require complete approval. Candidates that are pending or incomplete must be ignored and left pending. Stop only if the plan attempts to apply a candidate without complete approval.
  Add the new families (`green`, `fruity`, `spicy`) and subfamilies only based on these approved entries.
  Do not write `fresh_spice` empty unless there is an explicit approval of scaffold/gap with rationale. Remove `fresh_spice` from the write operation if no descriptor is approved for it.
  Ensure formatting matches the existing JSON structure exactly. Increment version metadata if necessary or keep it as `2.0.0` but update author/created_at to reflect Round 2.</action>
  <verify><automated>cd src && npm test -- tests/curation/taxonomy_seed_v2.test.ts</automated></verify>
  <acceptance_criteria>
    - Source assertion: taxonomy-seed.v2.json includes green, fruity, and spicy families (if approved).
    - Source assertion: All approved subfamilies and descriptors are written under their correct parent hierarchies. `fresh_spice` is strictly conditional on having an approved descriptor or explicit scaffold gap.
    - Behavior assertion: `data/taxonomy/taxonomy-seed.v1.json` is completely unchanged.
  </acceptance_criteria>
  <done>taxonomy-seed.v2.json updated with Round 2 additions.</done>
</task>

<task type="auto">
  <name>Task 2: Add approved Round 2 aliases</name>
  <files>data/taxonomy/descriptor_aliases.seed.json</files>
  <read_first>data/taxonomy/descriptor_aliases.seed.json, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</read_first>
  <action>Read `candidate-review.md` and only edit `data/taxonomy/descriptor_aliases.seed.json` if there is a concrete approval block containing: `manual_approval: approved`, `primary_disposition: add_alias`, `alias_key`, canonical target existing in seed v2, rationale, and evidence.
  If no add_alias is approved, preserve `descriptor_aliases.seed.json` unchanged.</action>
  <verify><automated>cd src && npm test -- tests/curation/alias_seed_v2.test.ts</automated></verify>
  <acceptance_criteria>
    - Test-command assertion: `cd src && npm test -- tests/curation/alias_seed_v2.test.ts` passes.
    - Behavior assertion: No legacy alias is modified or removed.
  </acceptance_criteria>
  <done>descriptor_aliases.seed.json updated with approved aliases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Curation inputs: validation suite verifies that aliases map to descriptors present in the seed, preventing orphan alias tags.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Tampering | seed and aliases JSON | mitigate | Use `alias_seed_v2.test.ts` to assert that all aliases have valid, existing targets in v2 seed. |
</threat_model>

<verification>
Run `cd src && npm test -- tests/curation/taxonomy_seed_v2.test.ts tests/curation/alias_seed_v2.test.ts` to verify seed schema and alias target integrity. Update the tests if they contain hardcoded Phase 8 assertions that block Phase 9 valid additions.
</verification>

<success_criteria>
Seed and alias updates are written, and both tests pass successfully.
</success_criteria>

<output>
Create `.planning/phases/09-taxonomy-seed-v2-expansion-round-2/09-02-SUMMARY.md` when done.
</output>
