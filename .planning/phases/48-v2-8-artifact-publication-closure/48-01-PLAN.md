---
phase: 48-v2-8-artifact-publication-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/compiled/v2/taxonomy.json
  - data/compiled/v2/descriptor_aliases.json
  - data/compiled/v2/similarity_matrix.json
  - .planning/releases/v2.8.0-CLOSURE.md
  - .planning/phases/48-v2-8-artifact-publication-closure/48-VERIFICATION.md
  - .planning/phases/48-v2-8-artifact-publication-closure/48-01-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [PUB-01, PUB-02, PUB-03]
requirements_addressed: [PUB-01, PUB-02, PUB-03]
user_setup: []
must_haves:
  truths:
    - "PUB-01: Curator can validate v2.8 compilation in /tmp before official publication, using --out and --version 2.8.0 per D-48-F01/D-48-SB01."
    - "PUB-02: Curator can publish exactly three aligned v2.8.0 JSON artifacts in data/compiled/v2 with validation_status=ok and quality_gate_status=PASS per D-48-V03/D-48-PUB01."
    - "PUB-03: Curator can read v2.8 closure metrics measured from published data/compiled/v2/*.json, not /tmp or stdout, per D-48-CR03."
    - "Protected boundaries remain byte-identical except authorized compiled v2 artifacts and bookkeeping, including DEFAULT_PATHS.version staying 2.1.0 per D-48-V02/D-48-PB03."
    - "WR-01 stability gate confirms src/tests/fixtures/curation/46-DECISION-MATRIX.md exists and full Vitest passes before publication per D-48-WR01/D-48-P02."
  artifacts:
    - path: "data/compiled/v2/taxonomy.json"
      provides: "Published v2.8.0 taxonomy artifact"
      contains: "\"version\": \"2.8.0\""
    - path: "data/compiled/v2/descriptor_aliases.json"
      provides: "Published v2.8.0 descriptor alias artifact"
      contains: "\"version\": \"2.8.0\""
    - path: "data/compiled/v2/similarity_matrix.json"
      provides: "Published v2.8.0 similarity/review queue artifact"
      contains: "\"version\": \"2.8.0\""
    - path: ".planning/releases/v2.8.0-CLOSURE.md"
      provides: "Release closure report with published-JSON metrics, v2.7→v2.8 deltas, protected-boundary proof, and 12 promoted seed paths"
    - path: ".planning/phases/48-v2-8-artifact-publication-closure/48-VERIFICATION.md"
      provides: "Exact command output, sandbox metrics, baseline metrics, published metrics, hashes, diffs, and test evidence"
    - path: ".planning/phases/48-v2-8-artifact-publication-closure/48-01-SUMMARY.md"
      provides: "Plan execution summary and milestone closure handoff"
  key_links:
    - from: "src/cli/parse_args.ts"
      to: "sandbox compile command"
      via: "supported --out flag and DEFAULT_PATHS.version guard"
      pattern: "--out"
    - from: "src/cli/compile.ts"
      to: "data/compiled/v2/*.json"
      via: "npm run compile -- --version 2.8.0 default output resolution from src"
      pattern: "resolveOutputDir"
    - from: "data/compiled/v2/*.json"
      to: ".planning/releases/v2.8.0-CLOSURE.md"
      via: "post-publication JSON re-parse, not /tmp artifacts"
      pattern: "data/compiled/v2"
    - from: "scripts/check-safety-guards.sh"
      to: "git diff/hash publication boundary"
      via: "staged Graphify/protected-path guard plus explicit allow-list for authorized compiled artifact diffs"
      pattern: "PROTECTED_DIFF"
---

<objective>
Publish v2.8.0 compiled artifacts and close the v2.8 milestone with auditable metrics measured from the published JSON outputs.

Purpose: Turn the Phase 47 approved 12 seed additions into official `data/compiled/v2/` artifacts while preserving defaults and protected boundaries.
Output: `data/compiled/v2/{taxonomy.json,descriptor_aliases.json,similarity_matrix.json}`, `.planning/releases/v2.8.0-CLOSURE.md`, `48-VERIFICATION.md`, `48-01-SUMMARY.md`, and completion-only bookkeeping in `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-v2-8-artifact-publication-closure/48-CONTEXT.md
@.planning/phases/48-v2-8-artifact-publication-closure/48-RESEARCH.md
@.planning/phases/48-v2-8-artifact-publication-closure/48-VALIDATION.md
@.planning/phases/48-v2-8-artifact-publication-closure/48-PATTERNS.md
@.planning/phases/47-controlled-curation-mutation/47-VERIFICATION.md
@.planning/phases/47-controlled-curation-mutation/47-01-SUMMARY.md
@.planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md
@src/cli/parse_args.ts
@src/cli/compile.ts
@src/compiler/write_outputs.ts
@src/compiler/validate_output.ts
@src/tests/curation/taxonomy_seed_v2.test.ts
@src/tests/fixtures/curation/46-DECISION-MATRIX.md
@src/package.json
@scripts/check-safety-guards.sh
@data/taxonomy/taxonomy-seed.v2.json
@data/compiled/v2/taxonomy.json
@data/compiled/v2/descriptor_aliases.json
@data/compiled/v2/similarity_matrix.json
</context>

<source_coverage_audit>
GOAL covered: validate, publish, version-align, and close v2.8 using metrics from published JSON outputs.
REQ covered: PUB-01 in Tasks 1-2; PUB-02 in Tasks 3-5; PUB-03 in Tasks 5-7.
RESEARCH covered: `--out` flag, no package installs, existing CLI invocation, JSON re-parse metrics, hash/diff proofs, safety guard handling, full Vitest.
CONTEXT covered: D-48-F01/F02/F03, D-48-V01/V02/V03, D-48-CR01 through D-48-CR08, D-48-P01 through D-48-P05, D-48-WR01 through D-48-WR03, D-48-SB01 through D-48-SB03, D-48-PUB01 through D-48-PUB04, D-48-PB01 through D-48-PB04, D-48-CA01 through D-48-CA03, and D-48-HC01/D-48-HC02.
Deferred/out-of-scope honored: no schema push, no UI, no Graphify mutation, no source/package changes, no alias/relation/accord/seed mutation, no v2.8 milestone archival, no conflict reopening, no 28 non-executable row reconsideration.
</source_coverage_audit>

<scope_exception>
This plan intentionally remains one plan with exactly seven tasks despite normal scope-sanity split thresholds. D-48-P01 locks Phase 48 to a single `48-01-PLAN.md`, D-48-P02 locks the user's mandatory seven-step publication gate list, and D-48-P03 requires each of those seven steps to be a separate task with a pass/fail signal. Do not split this plan and do not merge, remove, or add tasks; the exception is controlled by strict sequential execution and halt/checkpoint rules.
</scope_exception>

<execution_controls>
- Execute Tasks 1 through 7 strictly in order; no task may begin until the prior task's automated verify and acceptance criteria pass.
- Treat the plan as interactive/non-autonomous because Tasks 5 and 7 may require a blocking user checkpoint if `scripts/check-safety-guards.sh` cannot exit 0 after the official publication diff exists.
- Any non-zero safety-guard result is a halt. Continuing is allowed only after the executor proves the diff is exactly the allowed Phase 48 compiled v2 artifact set plus permitted closure/bookkeeping files, proves every protected-boundary hash is unchanged, documents the exact guard output, and obtains explicit user approval. There is no automatic reconciliation or automatic continuation.
- Any failure outside the authorized compiled v2 publication diff remains a hard halt with no approval bypass: Graphify staging/commit drift, source/package/default-version drift, seed/alias/inference/v1 drift, missing baseline, missing `/tmp` sandbox evidence, failed Vitest, or closure metrics that do not match a fresh parse of published JSON.
</execution_controls>

<tasks>

<task type="auto">
  <name>Task 1: Pre-publication stability gate (WR-01)</name>
  <read_first>
    @.planning/phases/48-v2-8-artifact-publication-closure/48-CONTEXT.md
    @.planning/phases/48-v2-8-artifact-publication-closure/48-VALIDATION.md
    @src/tests/fixtures/curation/46-DECISION-MATRIX.md
    @src/tests/curation/taxonomy_seed_v2.test.ts
    @src/package.json
  </read_first>
  <files>src/tests/fixtures/curation/46-DECISION-MATRIX.md, src/tests/curation/taxonomy_seed_v2.test.ts, src/package.json</files>
  <action>Implement D-48-P02 step 1 and D-48-WR01/D-48-WR02 as a halt-on-failure confirmation only: confirm `src/tests/fixtures/curation/46-DECISION-MATRIX.md` exists, then run the full suite with `cd src && npm run test`. Do not create the milestone archive fallback, do not modify tests or fixtures, and do not continue if the fixture is missing or Vitest exits non-zero. Record fixture existence and full test output for later inclusion in `48-VERIFICATION.md`.</action>
  <verify>
    <automated>test -f src/tests/fixtures/curation/46-DECISION-MATRIX.md && cd src && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Command `test -f src/tests/fixtures/curation/46-DECISION-MATRIX.md` exits 0.
    - Command `cd src && npm run test` exits 0 and invokes `vitest run` through `src/package.json`.
    - No files are modified by this task.
    - Failure of either assertion halts Phase 48 publication; executor must not proceed to sandbox compile.
  </acceptance_criteria>
  <done>WR-01 fixture exists, full Vitest passes before publication, and no repo files are modified.</done>
</task>

<task type="auto">
  <name>Task 2: Sandbox compile in /tmp</name>
  <read_first>
    @.planning/phases/48-v2-8-artifact-publication-closure/48-CONTEXT.md
    @.planning/phases/48-v2-8-artifact-publication-closure/48-RESEARCH.md
    @src/cli/parse_args.ts
    @src/cli/compile.ts
    @src/package.json
    @data/taxonomy/taxonomy-seed.v2.json
  </read_first>
  <files>/tmp/compile-2.8-validate/taxonomy.json, /tmp/compile-2.8-validate/descriptor_aliases.json, /tmp/compile-2.8-validate/similarity_matrix.json</files>
  <action>Implement PUB-01, D-48-F01/D-48-F02/D-48-F03, D-48-SB01/D-48-SB02/D-48-SB03, and D-48-P02 step 2. Remove any stale `/tmp/compile-2.8-validate/`, run `cd src && npm run precompile && npm run compile -- --version 2.8.0 --out /tmp/compile-2.8-validate --generated-at 2026-06-04T00:00:00.000Z --quality-report`, and capture stdout/stderr plus the three produced JSON files. Use `--out` exactly; never use `--output`. Parse `/tmp/compile-2.8-validate/*.json` only for sandbox evidence and assert `validation_status=ok`, `quality_gate_status=PASS`, all artifact `version` fields equal `2.8.0`, and zero hard validation errors. Do not copy `/tmp` artifacts into the repo and do not cite `/tmp` metrics as closure source of truth.</action>
  <verify>
    <automated>rm -rf /tmp/compile-2.8-validate && cd src && npm run precompile && npm run compile -- --version 2.8.0 --out /tmp/compile-2.8-validate --generated-at 2026-06-04T00:00:00.000Z --quality-report && test -f /tmp/compile-2.8-validate/taxonomy.json && test -f /tmp/compile-2.8-validate/descriptor_aliases.json && test -f /tmp/compile-2.8-validate/similarity_matrix.json</automated>
  </verify>
  <acceptance_criteria>
    - Sandbox command contains `--out /tmp/compile-2.8-validate` and does not contain `--output`.
    - `/tmp/compile-2.8-validate/taxonomy.json`, `/tmp/compile-2.8-validate/descriptor_aliases.json`, and `/tmp/compile-2.8-validate/similarity_matrix.json` exist.
    - Captured compile output includes `validation_status=ok` and `quality_gate_status=PASS`.
    - Parsed sandbox JSON versions are all `2.8.0` and no hard failures are present.
    - Git working tree has no repo file changes from the sandbox task.
    - Any command, JSON, validation, or status failure halts Phase 48 publication.
  </acceptance_criteria>
  <done>Sandbox `/tmp/compile-2.8-validate` contains three v2.8.0 JSON artifacts with validation_status=ok, quality_gate_status=PASS, zero hard failures, and no repo artifact publication.</done>
</task>

<task type="auto">
  <name>Task 3: Baseline and official compile preparation</name>
  <read_first>
    @.planning/phases/48-v2-8-artifact-publication-closure/48-CONTEXT.md
    @.planning/phases/48-v2-8-artifact-publication-closure/48-RESEARCH.md
    @data/compiled/v2/taxonomy.json
    @data/compiled/v2/descriptor_aliases.json
    @data/compiled/v2/similarity_matrix.json
    @scripts/check-safety-guards.sh
    @src/cli/parse_args.ts
    @src/package.json
  </read_first>
  <files>data/compiled/v2/taxonomy.json, data/compiled/v2/descriptor_aliases.json, data/compiled/v2/similarity_matrix.json, data/taxonomy/taxonomy-seed.v1.json, data/taxonomy/descriptor_aliases.seed.json, data/inference/*.json, data/compiled/v1/*.json, src/cli/parse_args.ts, src/package.json, src/package-lock.json</files>
  <action>Implement PUB-02 preparation, D-48-P02 step 3, D-48-CR05, D-48-PB02, and D-48-PB03 before any overwrite. Capture the current v2.7.0 baseline metrics directly from `data/compiled/v2/taxonomy.json`, `data/compiled/v2/descriptor_aliases.json`, and `data/compiled/v2/similarity_matrix.json` into execution notes for `48-VERIFICATION.md`: versions, family_count, subfamily_count, seed_descriptor_count, compiled_descriptor_count, alias_count, graph_edge_count, review_queue_total, review_queue_by_type, review_queue_by_severity, validation_status, quality_gate_status, and generated_at. Capture a pre-publication SHA-256 manifest for every protected path in D-48-CR06: `data/taxonomy/taxonomy-seed.v1.json`, `data/taxonomy/descriptor_aliases.seed.json`, `data/inference/*.json`, `data/compiled/v1/*.json`, `src/cli/parse_args.ts`, `src/package.json`, and `src/package-lock.json`. Run `bash scripts/check-safety-guards.sh` before official publication and require exit 0. Confirm `src/cli/parse_args.ts` contains `version: '2.1.0'` in `DEFAULT_PATHS` before publication.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const tax=JSON.parse(fs.readFileSync('data/compiled/v2/taxonomy.json','utf8')); const al=JSON.parse(fs.readFileSync('data/compiled/v2/descriptor_aliases.json','utf8')); const sim=JSON.parse(fs.readFileSync('data/compiled/v2/similarity_matrix.json','utf8')); if (![tax.version,al.version,sim.version].every(v=>v==='2.7.0')) throw new Error('baseline is not v2.7.0'); console.log(JSON.stringify({versions:[tax.version,al.version,sim.version], family_count:tax.stats.family_count, subfamily_count:tax.stats.subfamily_count, seed_descriptor_count:tax.stats.seed_descriptor_count, compiled_descriptor_count:tax.stats.descriptor_count, alias_count:Object.keys(al.aliases).length, graph_edge_count:sim.stats.edge_count, review_queue_total:sim.review_queue.length}, null, 2));" && sha256sum data/taxonomy/taxonomy-seed.v1.json data/taxonomy/descriptor_aliases.seed.json data/inference/*.json data/compiled/v1/*.json src/cli/parse_args.ts src/package.json src/package-lock.json && bash scripts/check-safety-guards.sh && grep -q "version: '2.1.0'" src/cli/parse_args.ts</automated>
  </verify>
  <acceptance_criteria>
    - Baseline metrics are captured before the official compile overwrites `data/compiled/v2/*.json`.
    - Baseline versions from all three current published artifacts equal `2.7.0`.
    - Pre-publication hash manifest includes every D-48-CR06 protected path group.
    - `bash scripts/check-safety-guards.sh` exits 0 before official publication.
    - `src/cli/parse_args.ts` contains the exact `DEFAULT_PATHS` version value `version: '2.1.0'`.
    - Any baseline, hash, guard, or DEFAULT_PATHS failure halts Phase 48 publication.
  </acceptance_criteria>
  <done>v2.7.0 baseline metrics and protected-boundary hash manifest are captured before overwrite, safety guard passes, and DEFAULT_PATHS.version is confirmed as 2.1.0.</done>
</task>

<task type="auto">
  <name>Task 4: Official compile to data/compiled/v2</name>
  <read_first>
    @.planning/phases/48-v2-8-artifact-publication-closure/48-CONTEXT.md
    @src/cli/parse_args.ts
    @src/cli/compile.ts
    @src/compiler/write_outputs.ts
    @src/package.json
    @data/compiled/v2/taxonomy.json
    @data/compiled/v2/descriptor_aliases.json
    @data/compiled/v2/similarity_matrix.json
  </read_first>
  <files>data/compiled/v2/taxonomy.json, data/compiled/v2/descriptor_aliases.json, data/compiled/v2/similarity_matrix.json</files>
  <action>Implement PUB-02, D-48-F01/D-48-F02, D-48-V01/D-48-V03, D-48-PUB01/D-48-PUB02/D-48-PUB03/D-48-PUB04, and D-48-P02 step 4. From the `src` package, run the official compile once with default output resolution: `cd src && npm run compile -- --version 2.8.0`. Do not pass `--out` for the official compile; let `resolveOutputDir` write to `../data/compiled/v2`. Capture stdout. If the compile exits non-zero, `validation_status` is not `ok`, `quality_gate_status` is not `PASS`, or any hard failure appears, immediately restore the v2.7 artifacts with `git checkout -- data/compiled/v2/taxonomy.json data/compiled/v2/descriptor_aliases.json data/compiled/v2/similarity_matrix.json`, mark Phase 48 failed in notes, and halt without writing the closure report or committing.</action>
  <verify>
    <automated>cd src && npm run compile -- --version 2.8.0 && cd .. && node -e "const fs=require('fs'); for (const p of ['data/compiled/v2/taxonomy.json','data/compiled/v2/descriptor_aliases.json','data/compiled/v2/similarity_matrix.json']) { const j=JSON.parse(fs.readFileSync(p,'utf8')); if (j.version !== '2.8.0') throw new Error(p + ' version mismatch: ' + j.version); }"</automated>
  </verify>
  <acceptance_criteria>
    - Official compile command is exactly `cd src && npm run compile -- --version 2.8.0` or semantically identical with no `--out` and no `--output`.
    - Official compile stdout contains `validation_status=ok` and `quality_gate_status=PASS`.
    - `data/compiled/v2/taxonomy.json`, `data/compiled/v2/descriptor_aliases.json`, and `data/compiled/v2/similarity_matrix.json` are overwritten as one artifact set and each parses as JSON with `version` equal to `2.8.0`.
    - If official compile fails or emits a hard failure, the executor restores all three `data/compiled/v2` files from git and halts without closure report or commit.
  </acceptance_criteria>
  <done>Official compile writes the three canonical `data/compiled/v2` artifacts with version 2.8.0 and PASS status, or restores v2.7 artifacts and halts on failure.</done>
</task>

<task type="auto">
  <name>Task 5: Published-artifact verification and protected-boundary assertions</name>
  <read_first>
    @.planning/phases/48-v2-8-artifact-publication-closure/48-CONTEXT.md
    @.planning/phases/48-v2-8-artifact-publication-closure/48-RESEARCH.md
    @src/tests/curation/taxonomy_seed_v2.test.ts
    @data/compiled/v2/taxonomy.json
    @data/compiled/v2/descriptor_aliases.json
    @data/compiled/v2/similarity_matrix.json
    @scripts/check-safety-guards.sh
    @src/cli/parse_args.ts
    @src/package.json
    @src/package-lock.json
  </read_first>
  <files>data/compiled/v2/taxonomy.json, data/compiled/v2/descriptor_aliases.json, data/compiled/v2/similarity_matrix.json</files>
  <action>Implement PUB-02/PUB-03 verification, D-48-CR03/D-48-CR04/D-48-CR05/D-48-CR07, D-48-PB01/D-48-PB02/D-48-PB03/D-48-PB04, D-48-P02 step 5, and the resolved safety-guard handling from `48-RESEARCH.md`. Re-parse the published `data/compiled/v2/*.json` after the official compile and compute the mandatory metric set: versions, family_count, subfamily_count, seed_descriptor_count, compiled_descriptor_count, alias_count, graph_edge_count, review_queue_total, review_queue_by_type, review_queue_by_severity, validation_status, quality_gate_status, and generated_at. Compute deltas against the v2.7 baseline captured in Task 3. Assert all three versions equal `2.8.0`, `validation_status=ok`, and `quality_gate_status=PASS`. Assert the 12 promoted seed paths are present in published `taxonomy.json`: `spicy/warm_spice/carrot_seed`, `floral/floral_white/freesia`, `spicy/warm_spice/cardamom`, `citrus/citrus_fresh/tangerine`, `spicy/warm_spice/saffron`, `floral/floral_white/osmanthus`, `spicy/warm_spice/cubeb`, `floral/floral_white/elderflower`, `spicy/warm_spice/mace`, `floral/floral_white/linden_flower`, `woody/woody_dry/agarwood`, `amber_resinous/balsamic_resin/tolu`. Recompute SHA-256 for every protected path from Task 3 and compare to the pre-publication manifest. Run `git diff --name-only` and allow only `data/compiled/v2/taxonomy.json`, `data/compiled/v2/descriptor_aliases.json`, and `data/compiled/v2/similarity_matrix.json` at this point, plus the already-created plan file. Run `bash scripts/check-safety-guards.sh`; any non-zero result is an immediate halt/checkpoint. Continuing after a non-zero guard result is allowed only after explicit user approval and only after the executor provides exact allowed-diff proof that the diff is limited to the authorized compiled v2 publication artifacts plus permitted plan file, exact protected-boundary hash proof that all D-48-CR06 paths are byte-identical to the Task 3 manifest, and the exact guard output. Do not automatically reconcile, bypass, or continue after a non-zero guard result. Halt with no approval bypass for Graphify, source, package, seed, inference, v1 compiled, DEFAULT_PATHS, or any unlisted path drift.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const tax=JSON.parse(fs.readFileSync('data/compiled/v2/taxonomy.json','utf8')); const al=JSON.parse(fs.readFileSync('data/compiled/v2/descriptor_aliases.json','utf8')); const sim=JSON.parse(fs.readFileSync('data/compiled/v2/similarity_matrix.json','utf8')); const versions=[tax.version,al.version,sim.version]; if (!versions.every(v=>v==='2.8.0')) throw new Error('version mismatch '+versions.join(',')); const paths=['spicy/warm_spice/carrot_seed','floral/floral_white/freesia','spicy/warm_spice/cardamom','citrus/citrus_fresh/tangerine','spicy/warm_spice/saffron','floral/floral_white/osmanthus','spicy/warm_spice/cubeb','floral/floral_white/elderflower','spicy/warm_spice/mace','floral/floral_white/linden_flower','woody/woody_dry/agarwood','amber_resinous/balsamic_resin/tolu']; const families=tax.families||[]; const seen=new Set(); for (const f of families) for (const sf of (f.subfamilies||[])) for (const d of (sf.descriptors||[])) seen.add(`${f.id}/${sf.id}/${typeof d==='string'?d:d.id}`); const missing=paths.filter(p=>!seen.has(p)); if (missing.length) throw new Error('missing promoted paths '+missing.join(',')); console.log(JSON.stringify({versions, family_count:tax.stats.family_count, subfamily_count:tax.stats.subfamily_count, seed_descriptor_count:tax.stats.seed_descriptor_count, compiled_descriptor_count:tax.stats.descriptor_count, alias_count:Object.keys(al.aliases).length, graph_edge_count:sim.stats.edge_count, review_queue_total:sim.review_queue.length}, null, 2));" && sha256sum data/taxonomy/taxonomy-seed.v1.json data/taxonomy/descriptor_aliases.seed.json data/inference/*.json data/compiled/v1/*.json src/cli/parse_args.ts src/package.json src/package-lock.json && git diff --name-only && bash scripts/check-safety-guards.sh</automated>
  </verify>
  <acceptance_criteria>
    - Published metrics are computed from `data/compiled/v2/*.json`, not `/tmp` and not stdout.
    - All three published artifact versions equal `2.8.0`.
    - Published artifact metrics include every D-48-CR04 metric and a v2.7.0→v2.8.0 delta for each metric.
    - All 12 promoted seed paths are present in published `taxonomy.json` at the approved family/subfamily/descriptor path.
    - Protected-boundary post-publication hashes match the Task 3 pre-publication manifest for every D-48-CR06 path.
    - `git diff --name-only` contains no unlisted path; at this task stage the only data diffs allowed are the three `data/compiled/v2/*.json` files and the plan file.
    - `scripts/check-safety-guards.sh` exits 0. If it exits non-zero, the executor halts/checkpoints; continuation requires explicit user approval after exact allowed-diff proof, exact protected-hash proof, and exact guard-output documentation. There is no automatic continuation after a non-zero guard result.
  </acceptance_criteria>
  <done>Published JSON metrics, 12 promoted seed paths, allowed diff set, protected hashes, and safety guard exit 0 pass against `data/compiled/v2` outputs, or execution is halted at a user checkpoint with exact allowed-diff and protected-hash proof for any non-zero guard result.</done>
</task>

<task type="auto">
  <name>Task 6: Full test suite and default-version guard</name>
  <read_first>
    @.planning/phases/48-v2-8-artifact-publication-closure/48-CONTEXT.md
    @src/package.json
    @src/cli/parse_args.ts
    @src/tests/curation/taxonomy_seed_v2.test.ts
    @data/compiled/v2/taxonomy.json
    @data/compiled/v2/descriptor_aliases.json
    @data/compiled/v2/similarity_matrix.json
  </read_first>
  <files>src/cli/parse_args.ts, src/package.json, data/compiled/v2/taxonomy.json, data/compiled/v2/descriptor_aliases.json, data/compiled/v2/similarity_matrix.json</files>
  <action>Implement PUB-03 gate, D-48-V02, D-48-CR08, and D-48-P02 step 6. Run the full test suite from the `src` package with `cd src && npm run test`. Assert `src/cli/parse_args.ts` still contains the exact `DEFAULT_PATHS` value `version: '2.1.0'` and does not contain a changed default version such as `version: '2.8.0'`. Do not edit source or package files. Capture full suite output and DEFAULT_PATHS assertion output for `48-VERIFICATION.md`.</action>
  <verify>
    <automated>cd src && npm run test && cd .. && grep -q "version: '2.1.0'" src/cli/parse_args.ts && ! grep -q "version: '2.8.0'" src/cli/parse_args.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src && npm run test` exits 0 after official publication.
    - `src/cli/parse_args.ts` contains exact default `version: '2.1.0'`.
    - `src/cli/parse_args.ts` does not contain `version: '2.8.0'` as a DEFAULT_PATHS value.
    - No `src/**`, `src/package.json`, or `src/package-lock.json` modifications are introduced.
    - Any test or DEFAULT_PATHS assertion failure halts Phase 48 closure and commit.
  </acceptance_criteria>
  <done>Post-publication full Vitest passes and `src/cli/parse_args.ts` still preserves DEFAULT_PATHS.version as 2.1.0.</done>
</task>

<task type="auto">
  <name>Task 7: Closure report and bookkeeping</name>
  <read_first>
    @.planning/phases/48-v2-8-artifact-publication-closure/48-CONTEXT.md
    @.planning/phases/48-v2-8-artifact-publication-closure/48-RESEARCH.md
    @.planning/phases/48-v2-8-artifact-publication-closure/48-PATTERNS.md
    @.planning/releases/v2.5.0-CLOSURE.md
    @.planning/ROADMAP.md
    @.planning/STATE.md
    @.planning/REQUIREMENTS.md
    @data/compiled/v2/taxonomy.json
    @data/compiled/v2/descriptor_aliases.json
    @data/compiled/v2/similarity_matrix.json
    @scripts/check-safety-guards.sh
  </read_first>
  <files>.planning/releases/v2.8.0-CLOSURE.md, .planning/phases/48-v2-8-artifact-publication-closure/48-VERIFICATION.md, .planning/phases/48-v2-8-artifact-publication-closure/48-01-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md, data/compiled/v2/taxonomy.json, data/compiled/v2/descriptor_aliases.json, data/compiled/v2/similarity_matrix.json</files>
  <action>Implement PUB-03, D-48-CR01 through D-48-CR08, D-48-CA01 through D-48-CA03, D-48-HC01/D-48-HC02, D-48-P04/D-48-P05, D-48-P02 step 7, and the resolved safety-guard handling from `48-RESEARCH.md`. Write `.planning/releases/v2.8.0-CLOSURE.md` as the release-style closure report and make it the source of truth for milestone closure metrics. The closure report must state version `2.8.0`, include a metrics table measured by re-parsing `data/compiled/v2/taxonomy.json`, `data/compiled/v2/descriptor_aliases.json`, and `data/compiled/v2/similarity_matrix.json`, include a v2.7.0→v2.8.0 delta table for every mandatory metric, include review_queue breakdown by `type` and by `severity`, include `generated_at`, include `validation_status=ok` and `quality_gate_status=PASS`, list all 12 promoted seed descriptors with their approved paths, and include `Protected Boundaries Unchanged` with hash/diff/safety-guard evidence. Write `48-VERIFICATION.md` with exact commands, stdout/stderr summaries, sandbox metrics, the Task 3 v2.7 baseline metrics, v2.8 published metrics, hash manifests, `git diff --name-only`, `scripts/check-safety-guards.sh` output, full Vitest output, and DEFAULT_PATHS assertion. Write `48-01-SUMMARY.md` with seven task pass/fail statuses, final metrics, and a pointer that `/gsd-complete-milestone` is the next separate workflow. Update `.planning/ROADMAP.md`, `.planning/STATE.md`, and `.planning/REQUIREMENTS.md` only for Phase 48 completion/bookkeeping: mark Phase 48 complete, set Plans to 1 plan with `48-01-PLAN.md`, mark PUB-01/PUB-02/PUB-03 complete, and update active state to milestone closure handoff. Before commit, re-run the published JSON parser, full diff allow-list, protected hashes, `bash scripts/check-safety-guards.sh`, and `cd src && npm run test`; commit only after every gate passes. If final `bash scripts/check-safety-guards.sh` exits non-zero, halt/checkpoint; continuing is allowed only after explicit user approval and only after exact allowed-diff proof, exact protected-hash proof, and exact guard-output documentation. Do not automatically reconcile, bypass, stage, commit, or continue after a non-zero guard result. Use one or two commits per D-48-P04, but keep the three compiled JSON artifacts consistent in the same artifact commit. Do not stage or commit `graphify-out/*`.</action>
  <verify>
    <automated>test -f .planning/releases/v2.8.0-CLOSURE.md && test -f .planning/phases/48-v2-8-artifact-publication-closure/48-VERIFICATION.md && test -f .planning/phases/48-v2-8-artifact-publication-closure/48-01-SUMMARY.md && node -e "const fs=require('fs'); const report=fs.readFileSync('.planning/releases/v2.8.0-CLOSURE.md','utf8'); const ver=fs.readFileSync('.planning/phases/48-v2-8-artifact-publication-closure/48-VERIFICATION.md','utf8'); const tax=JSON.parse(fs.readFileSync('data/compiled/v2/taxonomy.json','utf8')); const al=JSON.parse(fs.readFileSync('data/compiled/v2/descriptor_aliases.json','utf8')); const sim=JSON.parse(fs.readFileSync('data/compiled/v2/similarity_matrix.json','utf8')); const countBy=(items,key)=>items.reduce((m,x)=>{const k=String(x[key]??'missing'); m[k]=(m[k]||0)+1; return m;},{}); const published={version:['2.8.0','2.8.0','2.8.0'], family_count:tax.stats.family_count, subfamily_count:tax.stats.subfamily_count, seed_descriptor_count:tax.stats.seed_descriptor_count, compiled_descriptor_count:tax.stats.descriptor_count, alias_count:Object.keys(al.aliases||{}).length, graph_edge_count:sim.stats.edge_count, review_queue_total:(sim.review_queue||[]).length, review_queue_by_type:countBy(sim.review_queue||[],'type'), review_queue_by_severity:countBy(sim.review_queue||[],'severity'), validation_status:tax.validation_status, quality_gate_status:tax.quality_gate_status, generated_at:tax.generated_at}; if (![tax.version,al.version,sim.version].every(v=>v==='2.8.0')) throw new Error('published versions not aligned'); if (published.validation_status!=='ok') throw new Error('validation_status mismatch '+published.validation_status); if (published.quality_gate_status!=='PASS') throw new Error('quality_gate_status mismatch '+published.quality_gate_status); const required=['family_count','subfamily_count','seed_descriptor_count','compiled_descriptor_count','alias_count','graph_edge_count','review_queue_total','validation_status','quality_gate_status','generated_at']; for (const k of required) { const v=String(published[k]); if (!report.includes(v)) throw new Error('closure report missing fresh published '+k+' value '+v); } for (const [k,n] of Object.entries(published.review_queue_by_type)) if (!report.includes(k)||!report.includes(String(n))) throw new Error('closure report missing review_queue_by_type '+k+'='+n); for (const [k,n] of Object.entries(published.review_queue_by_severity)) if (!report.includes(k)||!report.includes(String(n))) throw new Error('closure report missing review_queue_by_severity '+k+'='+n); const baselineKeys=['family_count','subfamily_count','seed_descriptor_count','compiled_descriptor_count','alias_count','graph_edge_count','review_queue_total']; for (const k of baselineKeys) { const re=new RegExp(k+'[^\\n\\r]*(?:v2\\.7|2\\.7|baseline)[^\\n\\r]*([0-9]+)|(?:v2\\.7|2\\.7|baseline)[^\\n\\r]*'+k+'[^\\n\\r]*([0-9]+)','i'); const m=ver.match(re); if (!m) throw new Error('48-VERIFICATION.md missing Task 3 baseline '+k); const base=Number(m[1]||m[2]); const delta=published[k]-base; if (!report.includes(String(base))) throw new Error('closure report missing v2.7 baseline '+k+' value '+base); if (!report.includes(String(delta)) && !report.includes((delta>=0?'+':'')+String(delta))) throw new Error('closure report missing delta for '+k+' value '+delta); } for (const token of ['Protected Boundaries Unchanged','carrot_seed','freesia','cardamom','tangerine','saffron','osmanthus','cubeb','elderflower','mace','linden_flower','agarwood','tolu']) if (!report.includes(token)) throw new Error('closure missing mandatory token '+token);" && grep -q "\[x\].*PUB-01" .planning/REQUIREMENTS.md && grep -q "\[x\].*PUB-02" .planning/REQUIREMENTS.md && grep -q "\[x\].*PUB-03" .planning/REQUIREMENTS.md && cd src && npm run test && cd .. && bash scripts/check-safety-guards.sh</automated>
  </verify>
  <acceptance_criteria>
    - D-48-CR02 is satisfied: `.planning/releases/v2.8.0-CLOSURE.md` is the source of truth for milestone closure metrics, and `48-VERIFICATION.md` plus `48-01-SUMMARY.md` are audit-trail artifacts only.
    - `.planning/releases/v2.8.0-CLOSURE.md` exists and its metrics match a fresh parse of published `data/compiled/v2/*.json`, not `/tmp`.
    - Closure report includes v2.7.0 baseline, v2.8.0 published values, and deltas for every D-48-CR04 mandatory metric.
    - Closure report includes `Protected Boundaries Unchanged` with hash/diff assertions and Graphify staging/commit assertion.
    - Closure report lists all 12 promoted seed paths and confirms their presence in published `taxonomy.json`.
    - D-48-CA02 is satisfied: `48-VERIFICATION.md` includes exact commands/output evidence for all seven tasks, including sandbox metrics, official compile output, hash assertions, safety-guard output, Vitest output, and DEFAULT_PATHS assertion.
    - `48-01-SUMMARY.md` includes final published metrics and milestone closure handoff.
    - `.planning/ROADMAP.md`, `.planning/STATE.md`, and `.planning/REQUIREMENTS.md` contain only Phase 48 completion/bookkeeping changes; no historical scope rewrite.
    - `git diff --name-only` before commit contains only allowed paths from D-48-PB01 plus this plan file and Phase 48 closure/bookkeeping artifacts.
    - `graphify-out/*` is not staged or committed.
    - Final `cd src && npm run test` passes, final DEFAULT_PATHS guard still proves `version: '2.1.0'`, and final protected hash/diff assertions pass.
    - Final `scripts/check-safety-guards.sh` exits 0; if non-zero, execution halts/checkpoints and may continue only after explicit user approval following exact allowed-diff and protected-hash proof.
    - Commit occurs only after every Phase 48 gate passes; no closure report or bookkeeping commit is allowed after a failed gate.
  </acceptance_criteria>
  <done>Closure report, verification file, summary, completion bookkeeping, final gates, and permitted commit are complete with metrics matching published JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed/aliases/inference inputs → compiler CLI | Existing curated data enters compile pipeline; Phase 48 invokes but must not mutate inputs. |
| compiler CLI → `data/compiled/v2/*.json` | Official publication overwrites previously published v2.7 artifacts with v2.8 artifacts. |
| published JSON → closure report | Metrics must be re-read from official artifacts, not copied from `/tmp` or stdout. |
| working tree → commit | Only authorized compiled artifacts and closure/bookkeeping may enter the commit; Graphify and protected paths are guarded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | Protected paths: `data/taxonomy/*`, `data/inference/*`, `data/compiled/v1/*`, `src/cli/parse_args.ts`, package files | mitigate | Tasks 3, 5, and 7 capture and compare SHA-256 manifests, run `git diff --name-only` allow-list checks, and halt on any unlisted path or hash mismatch per D-48-PB03/D-48-PB04. |
| T-48-02 | Tampering / Integrity | `data/compiled/v2/{taxonomy,descriptor_aliases,similarity_matrix}.json` | mitigate | Tasks 2, 4, and 5 require sandbox PASS, official compile PASS, all three versions equal `2.8.0`, and atomic artifact-set treatment in the same commit per D-48-F02/D-48-P04. |
| T-48-03 | Repudiation / Integrity | `.planning/releases/v2.8.0-CLOSURE.md` metrics | mitigate | Tasks 5 and 7 re-parse published `data/compiled/v2/*.json` after official compile and forbid `/tmp` or stdout as closure metric source per D-48-CR03. |
| T-48-04 | Tampering | `src/cli/parse_args.ts` `DEFAULT_PATHS.version` and source/package files | mitigate | Tasks 3, 6, and 7 assert exact `version: '2.1.0'`, forbid source/package diffs, and include these files in protected hash manifests per D-48-V02. |
| T-48-05 | Tampering | `graphify-out/*` staging or commit | mitigate | Tasks 5 and 7 run `scripts/check-safety-guards.sh`, require `git diff --name-only` allow-list checks, and explicitly forbid staging/committing Graphify outputs per D-48-CR06/D-48-PB04. |
</threat_model>

<verification>
Overall phase checks:
- Exactly one Phase 48 plan file exists: `.planning/phases/48-v2-8-artifact-publication-closure/48-01-PLAN.md`.
- The plan executes exactly seven tasks in the locked D-48-P02/D-48-P03 order.
- `--out` is used for sandbox compile; `--output` is never used.
- v2.7 baseline metrics and pre-publication protected hashes are captured before overwriting `data/compiled/v2/*.json`.
- Sandbox compile in `/tmp` passes with `--version 2.8.0`, `validation_status=ok`, `quality_gate_status=PASS`, and zero hard failures.
- Official compile runs from `src` with `npm run compile -- --version 2.8.0` and publishes to default-resolved `data/compiled/v2/`.
- Published JSON verification proves all three artifact versions equal `2.8.0`, all mandatory metrics are computed from `data/compiled/v2/*.json`, and all 12 promoted seed paths are present.
- Protected-boundary hash/diff assertions pass; any authorized compiled artifact diff is explicit and no unauthorized source/package/seed/inference/v1/Graphify diff is accepted.
- Full `cd src && npm run test` passes after publication.
- `src/cli/parse_args.ts` keeps `DEFAULT_PATHS.version` as `version: '2.1.0'`.
- `.planning/releases/v2.8.0-CLOSURE.md`, `48-VERIFICATION.md`, and `48-01-SUMMARY.md` are written only after all gates pass.
- `.planning/ROADMAP.md`, `.planning/STATE.md`, and `.planning/REQUIREMENTS.md` are updated only for Phase 48 completion/bookkeeping.
</verification>

<success_criteria>
- PUB-01 complete: sandbox v2.8 compile in `/tmp` passes before official publication.
- PUB-02 complete: official v2.8 artifacts are published as a consistent three-file `data/compiled/v2` set with aligned `2.8.0` versions.
- PUB-03 complete: v2.8 closure report metrics are measured from published JSON outputs and include v2.7→v2.8 deltas, protected-boundary assertions, full test evidence, and promoted seed path proof.
- No schema push, UI work, source/package mutation, Graphify mutation, alias/relation/accord/seed mutation, conflict reopening, or milestone archival is performed in Phase 48.
</success_criteria>

<output>
Create `.planning/phases/48-v2-8-artifact-publication-closure/48-01-SUMMARY.md` when done.
</output>
