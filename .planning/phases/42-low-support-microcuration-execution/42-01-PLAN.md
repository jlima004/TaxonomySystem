---
phase: 42-low-support-microcuration-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/taxonomy/taxonomy-seed.v2.json
  - src/tests/curation/taxonomy_seed_v2.test.ts
autonomous: true
requirements:
  - CUR-02
must_haves:
  truths:
    - "CUR-02, D-01, and D-02: taxonomy-seed.v2.json contains exactly the six approved Phase 41 mutation_allowed=true promote_to_seed descriptors at the locked target paths."
    - "D-03, D-04, and D-05: no alias, reject, defer, needs_external_reference, rationale, evidence, expected_effect, or notes field produces any mutation."
    - "D-06 and D-07: every mutation uses an already-existing family/subfamily; no family, subfamily, or structural node is created."
    - "D-08 and D-10: the six descriptors are globally unique, lower snake_case ASCII, approval-traceable, and covered by curation seed invariants."
    - "D-09: official data/compiled/v2 artifacts are not modified by this plan."
  artifacts:
    - path: data/taxonomy/taxonomy-seed.v2.json
      provides: "Authorized data-only seed mutation target for Phase 42 CUR-02"
      contains: "peppermint, rosemary, cumin, spearmint, caraway, opoponax"
    - path: src/tests/curation/taxonomy_seed_v2.test.ts
      provides: "Approval traceability and invariant coverage for Phase 42 additions"
      contains: "phase42"
  key_links:
    - from: .planning/phases/41-low-support-batch-decision-matrix/41-DECISION-MATRIX.md
      to: data/taxonomy/taxonomy-seed.v2.json
      via: "rows 06, 07, 10, 13, 14, 15 where mutation_allowed=true"
      pattern: "peppermint|rosemary|cumin|spearmint|caraway|opoponax"
    - from: src/tests/curation/taxonomy_seed_v2.test.ts
      to: data/taxonomy/taxonomy-seed.v2.json
      via: "descriptorKeys and assertApprovedExpansionTraceability"
      pattern: "assertApprovedExpansionTraceability"
---

<objective>
Execute the narrow Phase 42 CUR-02 seed mutation by adding only the six approved Phase 41 low_support descriptors to existing taxonomy seed targets, with approval traceability and invariant validation.

Purpose: Safely apply approved matrix decisions without reinterpreting Phase 41 rationale or creating unapproved taxonomy, alias, relation, accord, compiled artifact, or sidecar changes.
Output: Updated `data/taxonomy/taxonomy-seed.v2.json` and focused traceability coverage in `src/tests/curation/taxonomy_seed_v2.test.ts`.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-low-support-microcuration-execution/42-CONTEXT.md
@.planning/phases/42-low-support-microcuration-execution/42-RESEARCH.md
@.planning/phases/42-low-support-microcuration-execution/42-VALIDATION.md
@.planning/phases/42-low-support-microcuration-execution/42-PATTERNS.md
@.planning/phases/41-low-support-batch-decision-matrix/41-DECISION-MATRIX.md
@.planning/phases/41-low-support-batch-decision-matrix/41-CONTEXT.md
@data/taxonomy/taxonomy-seed.v2.json
@data/taxonomy/descriptor_aliases.seed.json
@src/tests/curation/taxonomy_seed_v2.test.ts
@src/tests/curation/review_dispositions.test.ts
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Safely apply approved matrix decisions to the taxonomy | Covered by Tasks 1-3 |
| REQ | CUR-02: Apply only decisions explicitly approved in the decision matrix; no mutation without prior recorded disposition | Covered by Tasks 1-3 and frontmatter requirements |
| RESEARCH | Exactly six rows are mutation_allowed=true; no package installs; no official compiled artifact publication; preserve alias file | Covered by Tasks 1-3 and threat model |
| CONTEXT | D-01 through D-11 locked decisions | Covered by Task 1 preflight, Task 2 mutation, Task 3 validation; D-11 confirms no extra decision checkpoint needed |
| DEFERRED | Other 24 rows and new structural nodes | Excluded from mutation per Tasks 1-3 |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 42 traceability requirements before seed mutation</name>
  <files>src/tests/curation/taxonomy_seed_v2.test.ts</files>
  <read_first>
    @src/tests/curation/taxonomy_seed_v2.test.ts
    @src/tests/curation/review_dispositions.test.ts
    @.planning/phases/41-low-support-batch-decision-matrix/41-DECISION-MATRIX.md
    @.planning/phases/41-low-support-batch-decision-matrix/41-CONTEXT.md
    @.planning/phases/42-low-support-microcuration-execution/42-CONTEXT.md
    @.planning/phases/42-low-support-microcuration-execution/42-PATTERNS.md
  </read_first>
  <action>Update `src/tests/curation/taxonomy_seed_v2.test.ts` so Phase 42 additions are explicit approval-traceable seed additions. Add Phase 42 approval entries or a Phase 42-specific approved path constant for exactly `fresh_spice/fresh_spice/peppermint`, `green/herbal_green/rosemary`, `spicy/warm_spice/cumin`, `fresh_spice/fresh_spice/spearmint`, `spicy/warm_spice/caraway`, and `amber_resinous/balsamic_resin/opoponax` per D-01 and D-02. Require those six paths to be present after mutation and require all non-approved Phase 41 candidates to be absent: `nutty`, `coffee`, `hay`, `orri`, `eucalyptus`, `hazelnut`, `fir_needle`, `maple`, `orchid`, `sulfurous`, `roasted`, `buttery`, `mentholic`, `savory`, `bready`, `marine`, `alcoholic`, `meaty`, `garlic`, `alliaceous`, `fishy`, `potato`, `cabbage`, `radish` per D-03, D-04, and D-05. Preserve existing helper style: ESM imports, no semicolons, lower snake_case descriptor IDs, and existing `descriptorKeys` / `assertApprovedExpansionTraceability` patterns.</action>
  <acceptance_criteria>
    - Source assertion: `src/tests/curation/taxonomy_seed_v2.test.ts` contains all six approved Phase 42 paths and no approved path outside the D-02 list.
    - Source assertion: the test file contains absence assertions or equivalent guard coverage for all 24 non-approved Phase 41 candidates listed in this task.
    - Behavior assertion: the updated test still uses existing seed invariant helpers (`descriptorKeys`, `assertNoGlobalDescriptorDuplicates`, `assertApprovedExpansionTraceability`) rather than a separate unvalidated curation path.
    - Diff assertion: no production source file is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run test -- tests/curation/review_dispositions.test.ts</automated>
    <automated>grep -v '^#' src/tests/curation/taxonomy_seed_v2.test.ts | grep -E 'peppermint|rosemary|cumin|spearmint|caraway|opoponax'</automated>
  </verify>
  <done>The test contract explicitly authorizes only the six D-02 Phase 42 seed paths and blocks non-approved Phase 41 rows from appearing as seed descriptors.</done>
</task>

<task type="auto">
  <name>Task 2: Append exactly the six approved descriptors to existing seed targets</name>
  <files>data/taxonomy/taxonomy-seed.v2.json</files>
  <read_first>
    @data/taxonomy/taxonomy-seed.v2.json
    @data/taxonomy/descriptor_aliases.seed.json
    @.planning/phases/41-low-support-batch-decision-matrix/41-DECISION-MATRIX.md
    @.planning/phases/42-low-support-microcuration-execution/42-CONTEXT.md
    @.planning/phases/42-low-support-microcuration-execution/42-RESEARCH.md
    @.planning/phases/42-low-support-microcuration-execution/42-PATTERNS.md
  </read_first>
  <action>Before editing, audit `data/taxonomy/taxonomy-seed.v2.json` and confirm these existing family/subfamily targets: `fresh_spice/fresh_spice`, `green/herbal_green`, `spicy/warm_spice`, and `amber_resinous/balsamic_resin` per D-06 and D-07. Confirm the global descriptor set does not already contain `peppermint`, `rosemary`, `cumin`, `spearmint`, `caraway`, or `opoponax` per D-08. Then edit only `data/taxonomy/taxonomy-seed.v2.json` by appending `peppermint` and `spearmint` under `fresh_spice/fresh_spice`, `rosemary` under `green/herbal_green`, `cumin` and `caraway` under `spicy/warm_spice`, and `opoponax` under `amber_resinous/balsamic_resin` per D-01 and D-02. Do not create or rename families/subfamilies. Do not edit `data/taxonomy/descriptor_aliases.seed.json`, relation inputs, accord inputs, `data/compiled/v2`, `src/cli/parse_args.ts`, or `graphify-out/*` per D-03, D-04, D-09, and D-10.</action>
  <acceptance_criteria>
    - Source assertion: `fresh_spice/fresh_spice` descriptor array contains `anise`, `peppermint`, and `spearmint` after the edit.
    - Source assertion: `green/herbal_green` descriptor array contains `basil` and `rosemary` after the edit.
    - Source assertion: `spicy/warm_spice` descriptor array contains `cinnamon`, `clove`, `allspice`, `cumin`, and `caraway` after the edit.
    - Source assertion: `amber_resinous/balsamic_resin` descriptor array contains `labdanum`, `benzoin`, and `opoponax` after the edit.
    - Diff assertion: `git diff -- data/taxonomy/taxonomy-seed.v2.json` shows descriptor-array additions only, with no family/subfamily creation, deletion, rename, or metadata/version change.
    - Diff assertion: `git diff --quiet -- data/taxonomy/descriptor_aliases.seed.json data/compiled/v2` exits 0, proving protected alias and official compiled artifacts are unchanged.
    - Diff assertion: the Phase 42 allowlist check exits 0 and fails if `git diff --name-only` contains any path outside `data/taxonomy/taxonomy-seed.v2.json`, `src/tests/curation/taxonomy_seed_v2.test.ts`, `.planning/phases/42-low-support-microcuration-execution/42-SUMMARY.md`, or `.planning/phases/42-low-support-microcuration-execution/42-VERIFICATION.md`.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run test -- tests/curation/taxonomy_seed_v2.test.ts tests/curation/review_dispositions.test.ts</automated>
    <automated>git diff --quiet -- data/taxonomy/descriptor_aliases.seed.json data/compiled/v2</automated>
    <automated>python3 -c "import subprocess, sys; allowed={'data/taxonomy/taxonomy-seed.v2.json','src/tests/curation/taxonomy_seed_v2.test.ts','.planning/phases/42-low-support-microcuration-execution/42-SUMMARY.md','.planning/phases/42-low-support-microcuration-execution/42-VERIFICATION.md'}; changed=set(subprocess.check_output(['git','diff','--name-only'], text=True).splitlines()); extra=changed-allowed; print('\n'.join(sorted(extra))); sys.exit(1 if extra else 0)"</automated>
  </verify>
  <done>Only `data/taxonomy/taxonomy-seed.v2.json` contains the six authorized descriptor additions, and protected alias/official compiled artifacts remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Prove mutation scope and safety boundaries</name>
  <files>data/taxonomy/taxonomy-seed.v2.json, src/tests/curation/taxonomy_seed_v2.test.ts</files>
  <read_first>
    @data/taxonomy/taxonomy-seed.v2.json
    @src/tests/curation/taxonomy_seed_v2.test.ts
    @src/tests/curation/review_dispositions.test.ts
    @data/taxonomy/descriptor_aliases.seed.json
    @.planning/phases/42-low-support-microcuration-execution/42-VALIDATION.md
  </read_first>
  <action>Run the focused curation tests and safety guard exactly from the repository paths described in `42-VALIDATION.md`. Inspect the final diff and ensure the only production data mutation is the six-descriptor addition in `data/taxonomy/taxonomy-seed.v2.json`; the test file may change only to encode Phase 42 traceability/absence checks. If any official `data/compiled/v2` change appears, remove it from the worktree because Phase 43 owns official publication per D-09. If compile validation is desired, run it only with output outside the repository, such as under `/tmp`, and do not stage that output.</action>
  <acceptance_criteria>
    - Test-command assertion: `cd src && npm run test -- tests/curation/taxonomy_seed_v2.test.ts tests/curation/review_dispositions.test.ts` exits 0.
    - CLI assertion: `cd src && npm run safety:guard` exits 0.
    - Diff assertion: `git diff --quiet -- data/taxonomy/descriptor_aliases.seed.json data/compiled/v2` exits 0, proving `data/taxonomy/descriptor_aliases.seed.json` and official `data/compiled/v2` artifacts are unchanged.
    - Diff assertion: the Phase 42 allowlist check exits 0 and fails if `git diff --name-only` contains any path outside `data/taxonomy/taxonomy-seed.v2.json`, `src/tests/curation/taxonomy_seed_v2.test.ts`, `.planning/phases/42-low-support-microcuration-execution/42-SUMMARY.md`, or `.planning/phases/42-low-support-microcuration-execution/42-VERIFICATION.md`.
    - Source assertion: no descriptor outside `peppermint`, `rosemary`, `cumin`, `spearmint`, `caraway`, and `opoponax` is added to `taxonomy-seed.v2.json` by this phase.
  </acceptance_criteria>
  <verify>
    <automated>cd src && npm run test -- tests/curation/taxonomy_seed_v2.test.ts tests/curation/review_dispositions.test.ts</automated>
    <automated>cd src && npm run safety:guard</automated>
    <automated>git diff --quiet -- data/taxonomy/descriptor_aliases.seed.json data/compiled/v2</automated>
    <automated>python3 -c "import subprocess, sys; allowed={'data/taxonomy/taxonomy-seed.v2.json','src/tests/curation/taxonomy_seed_v2.test.ts','.planning/phases/42-low-support-microcuration-execution/42-SUMMARY.md','.planning/phases/42-low-support-microcuration-execution/42-VERIFICATION.md'}; changed=set(subprocess.check_output(['git','diff','--name-only'], text=True).splitlines()); extra=changed-allowed; print('\n'.join(sorted(extra))); sys.exit(1 if extra else 0)"</automated>
  </verify>
  <done>Focused tests pass, safety guard passes, and diff inspection proves there are no alias, relation, accord, official compiled artifact, structural taxonomy, or sidecar mutations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 41 matrix → seed JSON | Markdown decision rows authorize data mutation only when normalized fields satisfy D-01/D-02 and CUR-02. |
| Seed JSON → compiler/tests | Curated seed data is consumed by validation and later Phase 43 artifact publication. |
| Repository worktree → official artifacts | Unintended diffs can publish alias or compiled artifact changes outside Phase 42 authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | `41-DECISION-MATRIX.md` interpretation | mitigate | Task 1 and Task 2 gate mutation on rows 06, 07, 10, 13, 14, 15 where `mutation_allowed=true`, ignoring rationale/evidence/expected_effect/notes per D-04/D-05. |
| T-42-02 | Tampering | `taxonomy-seed.v2.json` descriptor arrays | mitigate | Task 2 pre-audits existing targets and global absence; Task 3 runs curation tests including duplicate and snake_case checks. |
| T-42-03 | Repudiation | Approval traceability | mitigate | Task 1 adds explicit Phase 42 traceability expectations for all six D-02 paths; Task 3 records command outputs for Plan 02 closeout. |
| T-42-04 | Tampering | `descriptor_aliases.seed.json` | mitigate | Task 2 prohibits alias edits; Task 2 and Task 3 verify `git diff --quiet -- data/taxonomy/descriptor_aliases.seed.json data/compiled/v2` exits 0. |
| T-42-05 | Tampering / Repudiation | `data/compiled/v2` official artifacts | mitigate | Task 2 and Task 3 prohibit official compiled output changes; any compile validation must use `/tmp` output only. |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No package installs are in scope; RESEARCH.md Package Legitimacy Audit states no external packages are required. |
</threat_model>

<verification>
Run after all tasks in this plan:
- `cd src && npm run test -- tests/curation/taxonomy_seed_v2.test.ts tests/curation/review_dispositions.test.ts`
- `cd src && npm run safety:guard`
- `git diff --quiet -- data/taxonomy/descriptor_aliases.seed.json data/compiled/v2`
- `python3 -c "import subprocess, sys; allowed={'data/taxonomy/taxonomy-seed.v2.json','src/tests/curation/taxonomy_seed_v2.test.ts','.planning/phases/42-low-support-microcuration-execution/42-SUMMARY.md','.planning/phases/42-low-support-microcuration-execution/42-VERIFICATION.md'}; changed=set(subprocess.check_output(['git','diff','--name-only'], text=True).splitlines()); extra=changed-allowed; print('\n'.join(sorted(extra))); sys.exit(1 if extra else 0)"`
- `git diff -- data/taxonomy/taxonomy-seed.v2.json src/tests/curation/taxonomy_seed_v2.test.ts`
</verification>

<success_criteria>
- CUR-02 is satisfied for seed mutation: only the six explicitly approved Phase 41 `mutation_allowed=true` rows are applied.
- No unapproved Phase 41 row produces taxonomy, alias, relation, accord, compiled artifact, or sidecar mutation.
- Existing target families/subfamilies are reused; no structural taxonomy nodes are created.
- Focused curation tests and safety guard pass.
</success_criteria>

<output>
Create `.planning/phases/42-low-support-microcuration-execution/42-01-SUMMARY.md` when done.
</output>
