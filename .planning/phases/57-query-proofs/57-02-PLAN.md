---
phase: 57-query-proofs
plan: 02
type: execute
wave: 2
depends_on:
  - 57-01
files_modified:
  - src/graph_read_model/query_graph.ts
  - src/tests/graph_read_model/query_graph.test.ts
  - src/tests/graph_read_model/query_live_baseline.test.ts
  - src/tests/graph_read_model/live_artifact_baseline.test.ts
autonomous: true
requirements:
  - GQRY-04
  - GQRY-05
must_haves:
  truths:
    - "Phase 57 remains in-memory query proofs only with no infrastructure scope per D-01"
    - "Three similarity functions complete GQRY-04 mapping per D-14 with 1-hop bidirectional neighborhoods per D-16, hub by max degree per D-17, cross-family bridges per D-18, and score-sorted neighborhoods per D-19"
    - "Live aggregate regression proves baseline-scale consumability for future agent/RAG per D-15 and D-25"
    - "Live regression uses structural/count assertions not full-catalog snapshots per D-26"
    - "Hybrid test layout mirrors Phase 56 with query_live_baseline.test.ts per D-27"
    - "Build+validate+query twice yields deep equal similarity proofs per D-28"
    - "Production query_graph.ts stays fs-free; readFile remains test-layer only per D-29"
  artifacts:
    - path: src/graph_read_model/query_graph.ts
      provides: Three similarity query functions
      exports:
        - getSimilarityNeighborhood
        - getCrossFamilyBridges
        - getSimilarityHub
    - path: src/tests/graph_read_model/query_live_baseline.test.ts
      provides: Aggregate catalog regression over data/compiled/v2/*
      min_lines: 60
    - path: src/tests/graph_read_model/live_artifact_baseline.test.ts
      provides: Extended fs-free guard including query_graph.ts
      contains: queryGraph
  key_links:
    - from: getSimilarityNeighborhood
      to: similar_to edges
      via: bidirectional 1-hop match on source or target
      pattern: similar_to
    - from: query_live_baseline.test.ts
      to: data/compiled/v2/taxonomy.json
      via: read-only test-layer readJson
      pattern: compiledPaths
    - from: getCrossFamilyBridges
      to: subfamily family_id properties
      via: different family_id comparison per D-18
      pattern: family_id
---

<objective>
Implement similarity query proofs and prove baseline-scale consumability via live aggregate regression and production fs-free guard.

Purpose: Complete GQRY-04 similarity proofs and GQRY-05 agent/RAG-ready static evidence at full v2 catalog scale.
Output: Three similarity functions in query_graph.ts, extended inline tests, new query_live_baseline.test.ts, extended live_artifact_baseline fs guard.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.cursor/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-query-proofs/57-CONTEXT.md
@.planning/phases/57-query-proofs/57-RESEARCH.md
@.planning/phases/57-query-proofs/57-PATTERNS.md
@.planning/phases/56-pure-builder-structural-validation/56-02-SUMMARY.md

# Prior plan output
@.planning/phases/57-query-proofs/57-01-SUMMARY.md

<interfaces>
From plan 01 types.ts (must exist before this plan):
```typescript
export type SimilarityNeighborhoodProof = GraphQueryProof<'similarity_neighborhood', { readonly subfamily_id: string }, { readonly neighbors: readonly SimilarityNeighborhoodEntry[] }>
export type CrossFamilyBridgesProof = GraphQueryProof<'cross_family_bridges', Record<string, never>, { readonly bridges: readonly CrossFamilyBridgeItem[] }>
export type SimilarityHubProof = GraphQueryProof<'similarity_hub', Record<string, never>, { readonly hub: SimilarityHubResult }>
```

Baseline counts from RESEARCH (live regression targets):
- 10 families, 18 aliases, 13 similar_to edges, 5 cross-family bridges
- Hub: floral_rose degree 3
- Cross-family bridges include floral_rose→woody_dry, floral_rose→woody_mossy
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement similarity query functions</name>
  <files>src/graph_read_model/query_graph.ts</files>
  <read_first>
    src/graph_read_model/query_graph.ts
    src/graph_read_model/types.ts
    src/graph_read_model/build_graph.ts (similarity edge properties lines 150-163)
    .planning/phases/57-query-proofs/57-CONTEXT.md (D-14, D-16, D-17, D-18, D-19)
    .planning/phases/57-query-proofs/57-RESEARCH.md (Pitfall 1 directed vs undirected)
  </read_first>
  <acceptance_criteria>
    - getSimilarityNeighborhood(graph, subfamilyId) returns query_kind similarity_neighborhood with 1-hop bidirectional neighbors only per D-16
    - Neighborhood entries include neighbor subfamily_id, score, dimensions, evidence, final_score when present; sorted by (final_score ?? score) descending then neighbor_id lexicographic per D-19
    - getCrossFamilyBridges(graph) returns query_kind cross_family_bridges listing edges where endpoint subfamilies have different properties.family_id per D-18; bridges sorted deterministically by source subfamily id then target subfamily id
    - getSimilarityHub(graph) returns query_kind similarity_hub with hub subfamily_id, degree (in+out similar_to count), and family_id; tie-break by subfamily id lexicographic per D-17
    - path is undefined for all three similarity functions per A4
    - No score recomputation; read edge properties only per D-19; no fs imports per D-29
  </acceptance_criteria>
  <action>
    Append to existing query_graph.ts per D-03. getSimilarityNeighborhood: collect similar_to edges where subfamilyNodeId(subfamilyId) matches edge.source OR edge.target; normalize neighbor to the other endpoint; project SimilarityNeighborhoodEntry from edge properties. getCrossFamilyBridges: scan all similar_to edges, resolve both endpoint subfamily nodes, compare properties.family_id, include bridge items with source/target subfamily ids and similarity fields. getSimilarityHub: count in+out similar_to degree per subfamily node, select max degree with id localeCompare tie-break. Return typed proofs using types from plan 01. Missing subfamily returns empty neighbors array, not throw.
  </action>
  <verify>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <done>Three similarity functions compile and return typed proofs with correct query_kind literals</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend inline tests for GQRY-04 and full determinism</name>
  <files>src/tests/graph_read_model/query_graph.test.ts</files>
  <read_first>
    src/tests/graph_read_model/query_graph.test.ts
    src/graph_read_model/query_graph.ts
    .planning/phases/57-query-proofs/57-CONTEXT.md (D-24, D-28)
    .planning/phases/57-query-proofs/57-RESEARCH.md (baseline bridge and hub IDs)
  </read_first>
  <behavior>
    - Inline fixture includes cross-family similar_to edge involving floral_rose and woody_dry or woody_mossy subfamilies
    - getSimilarityNeighborhood returns bidirectional neighbors including inbound edges
    - getCrossFamilyBridges returns at least one bridge in minimal fixture with different family_id endpoints
    - getSimilarityHub selects correct hub by degree with tie-break
    - All eight query functions including similarity three pass build-twice deep equality per D-28
  </behavior>
  <acceptance_criteria>
    - query_graph.test.ts extended with similarity describe blocks using inline fixture only (no readFile)
    - Exact toEqual snapshots for similarity_neighborhood, cross_family_bridges, similarity_hub exemplars
    - Determinism it block covers all eight public query functions with toEqual and JSON.stringify
    - npm --prefix src test -- tests/graph_read_model/query_graph.test.ts exits 0
  </acceptance_criteria>
  <action>
    Extend inline fixture from plan 01 to include cross-family similar_to edge (e.g., floral_rose↔woody_dry) with score/final_score/dimensions/evidence properties matching build_graph output shape. Add snapshot tests for three similarity functions. Add combined determinism test iterating all eight exported query functions. Keep file fs-free.
  </action>
  <verify>
    <automated>npm --prefix src test -- tests/graph_read_model/query_graph.test.ts</automated>
  </verify>
  <done>All eight query functions have passing inline snapshots and determinism proofs</done>
</task>

<task type="auto">
  <name>Task 3: Add live baseline regression and fs-free guard</name>
  <files>src/tests/graph_read_model/query_live_baseline.test.ts, src/tests/graph_read_model/live_artifact_baseline.test.ts</files>
  <read_first>
    src/tests/graph_read_model/live_artifact_baseline.test.ts
    src/graph_read_model/contract.ts (GRAPH_EXPECTED_BASELINE_STATS, GRAPH_ALLOWED_PRODUCTION_INPUTS)
    .planning/phases/57-query-proofs/57-CONTEXT.md (D-25, D-26, D-29)
  </read_first>
  <acceptance_criteria>
    - query_live_baseline.test.ts loads data/compiled/v2/taxonomy.json, descriptor_aliases.json, similarity_matrix.json read-only per D-25
    - Validates graph before queries; loops all 10 families for getDescriptorsByFamily with non-empty descriptor lists sorted by id
    - Loops all 18 aliases for resolveAliasPath with populated paths
    - Runs getSimilarityNeighborhood for every subfamily with at least one similar_to edge
    - Asserts getCrossFamilyBridges result length 5 and getSimilarityHub hub subfamily_id floral_rose with degree 3
    - Selective content check: cedar alias resolves to cedarwood descriptor in proof result
    - Uses structural/count assertions not full-catalog JSON snapshot per D-26
    - live_artifact_baseline.test.ts adds queryGraph to productionModulePaths and includes query_graph.ts in fs-free loop per D-29
    - npm --prefix src test -- tests/graph_read_model/query_live_baseline.test.ts tests/graph_read_model/live_artifact_baseline.test.ts exits 0
  </acceptance_criteria>
  <action>
    Create query_live_baseline.test.ts copying readJson/compiledPaths pattern from live_artifact_baseline.test.ts. Import all eight query functions from query_graph.js. After build+validate, run aggregate loops per D-25. Assert descriptor list sort order via localeCompare check. Assert bridge count 5 and hub floral_rose degree 3 from RESEARCH verified baseline. Extend live_artifact_baseline.test.ts: add queryGraph path to productionModulePaths, read query_graph.ts source in fs-free test alongside build and validate modules.
  </action>
  <verify>
    <automated>npm --prefix src test -- tests/graph_read_model/query_live_baseline.test.ts tests/graph_read_model/live_artifact_baseline.test.ts</automated>
  </verify>
  <done>Live aggregate regression passes at baseline scale; query_graph.ts included in production fs-free guard</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→data/compiled/v2/* | Read-only sanctioned inputs per GRAPH_ALLOWED_PRODUCTION_INPUTS |
| query module→graph | Read-only traversal; no mutation of nodes/edges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Information Disclosure | query_graph.ts | mitigate | Extend live_artifact_baseline fs-free guard; assert no readFile in production source |
| T-57-02 | Tampering | OlfactoryGraph during query | mitigate | Pure functions; readonly types; no writes to graph arrays |
| T-57-03 | Denial of Service | live regression loops | accept | Bounded baseline scale (341 descriptors, 13 edges); projected proof fields only |
| T-57-04 | Spoofing | live artifact paths | mitigate | Assert compiledPaths match GRAPH_ALLOWED_PRODUCTION_INPUTS before read |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase |
</threat_model>

<verification>
npm --prefix src run typecheck && npm --prefix src test
</verification>

<success_criteria>
- GQRY-04 satisfied via three similarity functions with inline and live proof coverage
- GQRY-05 satisfied via typed proof contract plus live aggregate regression at full baseline catalog
- Full test suite green including extended fs-free guard
</success_criteria>

<output>
Create `.planning/phases/57-query-proofs/57-02-SUMMARY.md` when done
</output>
