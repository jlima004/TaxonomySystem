---
phase: 10-taxonomy-seed-v2-expansion-round-3
plan: "04"
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - .planning/phases/10-taxonomy-seed-v2-expansion-round-3/curation/v1-v2-comparison.md
autonomous: true
requirements:
  - EXP3-08
must_haves:
  truths:
    - "v1 baseline and v2 candidate are compared using temporary compile outputs only."
    - "No official `data/compiled/v2` artifact is created and `data/compiled/v1` is not overwritten."
    - "Hard gates fail on schema invalidity, nondeterminism, excludes, alias contamination, auto-promotion, default drift, protected mutation, or untraceable curation."
    - "Soft findings and promotion readiness criteria are documented only; v2 remains candidate-only."
  artifacts:
    - path: ".planning/phases/10-taxonomy-seed-v2-expansion-round-3/curation/v1-v2-comparison.md"
      provides: "Round 3 v1-v2 comparison and validation report"
  key_links:
    - from: "/tmp/opencode/taxonomy-phase10-comparison/v2-candidate"
      to: ".planning/phases/10-taxonomy-seed-v2-expansion-round-3/curation/v1-v2-comparison.md"
      via: "metrics extraction and hard/soft gate report"
      pattern: "coverage counts|amber/resinous|animalic|fresh spice"
---

<objective>
Create the Round 3 validation and comparison report without promoting v2.

Purpose: prove candidate-only changes are schema-valid, deterministic, traceable, and safe against v1/default drift while documenting soft findings and future promotion criteria.
Output: `.planning/phases/10-taxonomy-seed-v2-expansion-round-3/curation/v1-v2-comparison.md`.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-CONTEXT.md
@.planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-PATTERNS.md
@.planning/phases/09-taxonomy-seed-v2-expansion-round-2/curation/v1-v2-comparison.md
@.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
@data/taxonomy/taxonomy-seed.v2.json
@data/inference/curated_relations.v2.json
@data/inference/accord_map.v2.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile v1 and v2 candidate to temporary comparison paths</name>
  <files>/tmp/opencode/taxonomy-phase10-comparison/v1-baseline, /tmp/opencode/taxonomy-phase10-comparison/v2-candidate</files>
  <read_first>.planning/phases/09-taxonomy-seed-v2-expansion-round-2/09-04-PLAN.md, .planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-RESEARCH.md, data/taxonomy/taxonomy-seed.v2.json</read_first>
  <action>During future execution, compile v1 baseline only to `/tmp/opencode/taxonomy-phase10-comparison/v1-baseline` and v2 candidate only to `/tmp/opencode/taxonomy-phase10-comparison/v2-candidate`. Use these explicit-path commands, not the CLI defaults:
  `cd src && npm run compile -- --seed ../data/taxonomy/taxonomy-seed.v1.json --aliases ../data/taxonomy/descriptor_aliases.seed.json --relations ../data/inference/curated_relations.v1.json --accords ../data/inference/accord_map.v1.json --out /tmp/opencode/taxonomy-phase10-comparison/v1-baseline --version 1.0.0 --generated-at 2026-01-01T00:00:00.000Z`
  `cd src && npm run compile -- --seed ../data/taxonomy/taxonomy-seed.v2.json --aliases ../data/taxonomy/descriptor_aliases.seed.json --relations ../data/inference/curated_relations.v2.json --accords ../data/inference/accord_map.v2.json --out /tmp/opencode/taxonomy-phase10-comparison/v2-candidate --version 2.0.0-round-3-candidate --generated-at 2026-01-01T00:00:00.000Z`
  Never run the default compile command that writes to `data/compiled/v1`; never write `data/compiled/v2`; never edit `src/cli/parse_args.ts`. Use v1 inputs for the baseline and v2 inputs for the candidate exactly as specified in 10-RESEARCH. These commands are future execution commands and must not be run during planning.</action>
  <verify><automated>test -d /tmp/opencode/taxonomy-phase10-comparison/v1-baseline && test -d /tmp/opencode/taxonomy-phase10-comparison/v2-candidate && test ! -d data/compiled/v2</automated></verify>
  <acceptance_criteria>
    - Behavior assertion: both explicit-path compile outputs exist under `/tmp/opencode/taxonomy-phase10-comparison/`.
    - Boundary assertion: no official `data/compiled/v2` exists and `data/compiled/v1` is not written by this task.
  </acceptance_criteria>
  <done>Temporary v1/v2 comparison outputs are available outside official compiled artifact paths.</done>
</task>

<task type="auto">
  <name>Task 2: Write Round 3 validation comparison report</name>
  <files>.planning/phases/10-taxonomy-seed-v2-expansion-round-3/curation/v1-v2-comparison.md</files>
  <read_first>.planning/phases/09-taxonomy-seed-v2-expansion-round-2/curation/v1-v2-comparison.md, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, data/taxonomy/taxonomy-seed.v2.json, data/inference/curated_relations.v2.json, data/inference/accord_map.v2.json</read_first>
  <action>Create `.planning/phases/10-taxonomy-seed-v2-expansion-round-3/curation/v1-v2-comparison.md`. Record compilation setup using `/tmp/opencode/taxonomy-phase10-comparison/v1-baseline` and `/tmp/opencode/taxonomy-phase10-comparison/v2-candidate`. Validate hard gates: schema invalid, nondeterminism, hard/pattern-excludes, alias contamination, auto-promotion, default drift, protected artifact mutation, and curated change without workbook traceability. Validate soft gates: zero-frequency seeds, sparse graph/density drop, high review_queue, deferred gaps, ambiguous candidates, and legacy alias target absent. Record metrics: coverage counts, amber/resinous coverage, animalic coverage, fresh spice status, approved-or-gap traceability, graph coverage, generic pressure, review_queue quality, alias targeted cleanup quality, zero-frequency seeds, determinism/schema/defaults, and curation traceability. State expected outcome: v2-round-3 remains candidate and not default; hard failures are none if implementation is correct; soft findings are documented; promotion readiness criteria are documentation only.</action>
  <verify><automated>test -f .planning/phases/10-taxonomy-seed-v2-expansion-round-3/curation/v1-v2-comparison.md && grep -q 'amber/resinous coverage' .planning/phases/10-taxonomy-seed-v2-expansion-round-3/curation/v1-v2-comparison.md && grep -q 'animalic coverage' .planning/phases/10-taxonomy-seed-v2-expansion-round-3/curation/v1-v2-comparison.md && grep -q 'fresh spice status' .planning/phases/10-taxonomy-seed-v2-expansion-round-3/curation/v1-v2-comparison.md && grep -q 'v2 remains candidate' .planning/phases/10-taxonomy-seed-v2-expansion-round-3/curation/v1-v2-comparison.md</automated></verify>
  <acceptance_criteria>
    - Source assertion: report contains all hard gates, soft gates, and mandatory Round 3 metrics.
    - Source assertion: report explicitly states no v2 default promotion and no official v2 compiled artifact.
  </acceptance_criteria>
  <done>Round 3 comparison report is written with gates, metrics, soft findings, and future-only readiness criteria.</done>
</task>

<task type="auto">
  <name>Task 3: Run curation validation and protected-file diff checks</name>
  <files>src/tests/curation/, data/compiled/v1, data/taxonomy/taxonomy-seed.v1.json, data/inference/curated_relations.v1.json, data/inference/accord_map.v1.json, src/cli/parse_args.ts</files>
  <read_first>src/tests/curation/v1_v2_comparison.test.ts, src/cli/parse_args.ts</read_first>
  <action>During future execution, run `cd src && npm test -- tests/curation/`. Then verify protected files have a clean diff: `data/compiled/v1`, `data/taxonomy/taxonomy-seed.v1.json`, `data/inference/curated_relations.v1.json`, `data/inference/accord_map.v1.json`, and `src/cli/parse_args.ts`. Confirm `DEFAULT_PATHS` still points to v1, no protected v1 input was altered, no default compile command wrote to `data/compiled/v1`, and no official `data/compiled/v2` artifact was created.</action>
  <verify><automated>cd src && npm test -- tests/curation/ && git diff --exit-code data/compiled/v1 data/taxonomy/taxonomy-seed.v1.json data/inference/curated_relations.v1.json data/inference/accord_map.v1.json src/cli/parse_args.ts && test ! -d data/compiled/v2</automated></verify>
  <acceptance_criteria>
    - Test-command assertion: future curation suite passes.
    - Diff assertion: protected files are unchanged.
    - Boundary assertion: `DEFAULT_PATHS` and v1/default artifacts remain v1.
  </acceptance_criteria>
  <done>Curation tests pass and protected file diff is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate inputs -> temp compiled outputs | Candidate data is materialized only under `/tmp/opencode/...`. |
| temp outputs -> report | Validation metrics become a planning/curation report, not official artifact publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | compiled artifacts/defaults | mitigate | Compile only to `/tmp/opencode/...` and verify protected v1/default diff is clean. |
| T-10-09 | Repudiation | comparison report | mitigate | Record hard gates, soft gates, metrics, and workbook traceability in the report. |
| T-10-10 | Information Disclosure | report metrics | accept | Report contains project curation metrics only and is stored in planning artifacts. |
| T-10-SC | Tampering | package installs | accept | No package-manager install tasks in this plan. |
</threat_model>

<verification>
Future execution runs `cd src && npm test -- tests/curation/` and protected diff checks. Planning does not run tests, builds, compiles, or validation commands.
</verification>

<success_criteria>
The Round 3 report exists, comparison outputs are temporary only, no hard gate fails, soft findings are documented, and v2 remains candidate-only.
</success_criteria>

<output>
Create `.planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-04-SUMMARY.md` when done.
</output>
