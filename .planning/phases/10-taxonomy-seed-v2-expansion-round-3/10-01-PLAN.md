---
phase: 10-taxonomy-seed-v2-expansion-round-3
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
autonomous: false
requirements:
  - EXP3-01
  - EXP3-02
  - EXP3-03
  - EXP3-04
  - EXP3-05
  - EXP3-06
  - EXP3-07
must_haves:
  truths:
    - "Workbook contains Phase 10 / Round 3 proposals for amber_resinous, animalic, and conditional fresh_spice."
    - "Every Round 3 proposal starts as manual_approval: pending with promotion_effect: none."
    - "No JSON data, compiled artifact, DEFAULT_PATHS, protected v1 input, or official v2 compiled artifact is changed."
    - "Execution pauses until at least one complete persisted r3-approval-* promote_to_seed block exists in the workbook."
  artifacts:
    - path: ".planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md"
      provides: "Round 3 proposal and approval ledger"
  key_links:
    - from: ".planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md"
      to: "10-02 seed/alias curation"
      via: "r3-approval-* and r3-alias-cleanup-* persisted blocks"
      pattern: "r3-approval-.*phase_10_round_3"
---

<objective>
Append `# Phase 10 / Round 3 Curation` to the existing curation workbook and create the persisted approval surface for Round 3.

Purpose: preserve the workbook-first approval boundary from R3-D-01 through R3-D-52 before any v2 candidate JSON mutation.
Output: pending Round 3 proposal blocks plus a blocking human checkpoint requiring persisted workbook approval, not chat approval.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-CONTEXT.md
@.planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-RESEARCH.md
@.planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-PATTERNS.md
@.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Round 3 workbook proposals</name>
  <files>.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</files>
  <read_first>.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, .planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-CONTEXT.md, .planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-PATTERNS.md</read_first>
  <action>Append `# Phase 10 / Round 3 Curation` to the workbook without editing prior Phase 8/9 sections. Add guardrails stating `round: phase_10_round_3`, chat approval is insufficient, corpus/review_queue/frequency/generic pressure are support only, v2 remains candidate-only, and protected v1/default files are not touched. Create pending seed proposal blocks with IDs `r3-approval-*` for `amber_resinous/amber/amber`, `amber_resinous/balsamic_resin/resinous`, `amber_resinous/balsamic_resin/labdanum`, `amber_resinous/balsamic_resin/benzoin`, `amber_resinous/balsamic_resin/balsamic`, `animalic/musky/musk`, `animalic/musky/musky`, `animalic/musky/ambrette`, `animalic/leathery/leathery`, and conditional `fresh_spice/fresh_spice/anise`, per R3-D-01 through R3-D-27. Every proposal starts with `manual_approval: pending`, `primary_disposition: promote_to_seed`, `promotion_effect: none`, rationale, evidence, and validation expectation. Add explicit defer/support blocks using `r3-defer-*` for extra gourmand review, `animal`, `civet` unless explicitly approved later, `anisic`, `minty`, `wintergreen`, unresolved generic `resinous`/`balsamic` caution, absent `fresh_spice` if `anise` is not approved, and ylang cleanup deferral per R3-D-04, R3-D-05, R3-D-10, R3-D-17, R3-D-18, R3-D-24 through R3-D-26, and R3-D-30. Add targeted alias cleanup proposal blocks using `r3-alias-cleanup-*` for `musk`/`musky`, `leather`/`leathery`, `ambery` if evidence exists, direct amber/resinous variants if evidence is clear, and a defer block for `ylang ylang -> ylang_ylang`; all start pending and `promotion_effect: none` unless later manually approved as `add_alias`. Add relation and accord proposal blocks using `r3-relation-*` and `r3-accord-*` for amber/balsamic/vanilla/woody/warm, musky/leathery/floral_rose/vanilla, and fresh_spice/warm_spice/citrus_fresh candidate pairs; all start pending with proposed manual scores in [0,1], rationale/evidence, and endpoint-exists warning. Do not alter JSON data, compiled artifacts, `DEFAULT_PATHS`, protected v1 inputs, or create `data/compiled/v2`.</action>
  <verify><automated>node - <<'NODE'
const fs = require('node:fs')
const workbook = '.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md'
const text = fs.readFileSync(workbook, 'utf8')
const start = text.indexOf('# Phase 10 / Round 3 Curation')
if (start < 0) process.exit(1)
const section = text.slice(start)
const blocks = section.split(/\n###\s+/).slice(1).filter((block) => /^r3-(approval|relation|accord|alias-cleanup|defer)-/.test(block))
const requiredPrefixes = ['r3-approval-', 'r3-relation-', 'r3-accord-', 'r3-alias-cleanup-', 'r3-defer-']
const hasEveryPrefix = requiredPrefixes.every((prefix) => blocks.some((block) => block.startsWith(prefix)))
const hasPending = (block) => /`?manual_approval`?:\s*pending/.test(block)
const hasNoEffect = (block) => /`?promotion_effect`?:\s*none/.test(block)
const hasApproved = (block) => /`?manual_approval`?:\s*approved/.test(block)
const hasRound = (block) => /`?round`?:\s*phase_10_round_3/.test(block)
const hasLabel = (block, label) => new RegExp('`?' + label + '`?:').test(block)
const hasValue = (block, value) => block.includes(value)
const everyBlockPending = blocks.length > 0 && blocks.every((block) => hasRound(block) && hasPending(block) && hasNoEffect(block) && !hasApproved(block) && hasLabel(block, 'primary_disposition') && hasLabel(block, 'rationale') && hasLabel(block, 'evidence'))
const approvals = [
  ['amber_resinous', 'amber', 'amber'],
  ['amber_resinous', 'balsamic_resin', 'resinous'],
  ['amber_resinous', 'balsamic_resin', 'labdanum'],
  ['amber_resinous', 'balsamic_resin', 'benzoin'],
  ['amber_resinous', 'balsamic_resin', 'balsamic'],
  ['animalic', 'musky', 'musk'],
  ['animalic', 'musky', 'musky'],
  ['animalic', 'musky', 'ambrette'],
  ['animalic', 'leathery', 'leathery'],
  ['fresh_spice', 'fresh_spice', 'anise'],
]
const relationAccordPairs = [
  ['amber', 'balsamic_resin'],
  ['balsamic_resin', 'vanilla'],
  ['amber', 'vanilla'],
  ['balsamic_resin', 'woody_dry'],
  ['amber', 'warm_spice'],
  ['musky', 'leathery'],
  ['musky', 'floral_rose'],
  ['musky', 'vanilla'],
  ['leathery', 'woody_dry'],
  ['leathery', 'balsamic_resin'],
  ['fresh_spice', 'warm_spice'],
  ['fresh_spice', 'citrus_fresh'],
]
const approvalBlocks = blocks.filter((block) => block.startsWith('r3-approval-'))
const relationBlocks = blocks.filter((block) => block.startsWith('r3-relation-'))
const accordBlocks = blocks.filter((block) => block.startsWith('r3-accord-'))
const aliasBlocks = blocks.filter((block) => block.startsWith('r3-alias-cleanup-'))
const deferBlocks = blocks.filter((block) => block.startsWith('r3-defer-'))
const hasApprovalPath = ([family, subfamily, descriptor]) => approvalBlocks.some((block) => hasValue(block, family) && hasValue(block, subfamily) && hasValue(block, descriptor) && /`?primary_disposition`?:\s*promote_to_seed/.test(block))
const hasPair = (candidateBlocks, [source, target]) => candidateBlocks.some((block) => hasValue(block, source) && hasValue(block, target) && /`?primary_disposition`?:\s*approve_relation_accord/.test(block) && (/`?proposed_score`?:/.test(block) || /`?score`?:/.test(block)))
const hasAliasCoverage = aliasBlocks.some((block) => hasValue(block, 'musk') && hasValue(block, 'musky')) && aliasBlocks.some((block) => hasValue(block, 'leather') && hasValue(block, 'leathery')) && aliasBlocks.some((block) => hasValue(block, 'ylang ylang') && hasValue(block, 'ylang_ylang'))
const hasDefers = ['gourmand', 'animal', 'civet', 'anisic', 'minty', 'wintergreen', 'fresh_spice'].every((term) => deferBlocks.some((block) => hasValue(block, term)))
const ok = hasEveryPrefix && everyBlockPending && approvals.every(hasApprovalPath) && relationAccordPairs.every((pair) => hasPair(relationBlocks, pair)) && relationAccordPairs.every((pair) => hasPair(accordBlocks, pair)) && hasAliasCoverage && hasDefers
process.exit(ok ? 0 : 1)
NODE</automated></verify>
  <acceptance_criteria>
    - Workbook contains a Phase 10 Round 3 section and keeps prior Phase 8/9 sections intact.
    - Every new Round 3 proposal block starts with `manual_approval: pending` and `promotion_effect: none`.
    - Proposal IDs use only `r3-approval-*`, `r3-relation-*`, `r3-accord-*`, `r3-alias-cleanup-*`, and `r3-defer-*`.
  </acceptance_criteria>
  <done>Round 3 proposal ledger is appended and no data artifacts are modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Require persisted Round 3 workbook approval</name>
  <files>.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</files>
  <read_first>.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</read_first>
  <action>Pause until the human curator edits the workbook itself. Approval in chat is not acceptable. Before 10-02 can run, the workbook must contain at least one complete Round 3 approved promotion block with `approval_id: r3-approval-*`, `round: phase_10_round_3`, `manual_approval: approved`, `primary_disposition: promote_to_seed`, `family_id`, `subfamily_id`, `descriptor_id`, `rationale`, and `evidence`. This gate must validate `r3-approval-*` blocks specifically and must not false-positive on Phase 8 `approval-*` or Phase 9 `r2-approval-*` approvals.</action>
  <what-built>Round 3 workbook proposals are present and pending.</what-built>
  <how-to-verify>Open `.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md`, edit one or more `r3-approval-*` blocks to approved with rationale and evidence, save the file, then resume. Do not rely on chat approval.</how-to-verify>
  <verify><automated>node - <<'NODE'
const fs = require('node:fs')
const workbook = '.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md'
const text = fs.readFileSync(workbook, 'utf8')
const start = text.indexOf('# Phase 10 / Round 3 Curation')
if (start < 0) process.exit(1)
const section = text.slice(start)
const blocks = section.split(/\n###\s+/).slice(1).filter((block) => /^r3-approval-/.test(block))
const has = (block, pattern) => pattern.test(block)
const ok = blocks.some((block) =>
  has(block, /`?approval_id`?:\s*r3-approval-/) &&
  has(block, /`?round`?:\s*phase_10_round_3/) &&
  has(block, /`?manual_approval`?:\s*approved/) &&
  has(block, /`?primary_disposition`?:\s*promote_to_seed/) &&
  has(block, /`?family_id`?:/) &&
  has(block, /`?subfamily_id`?:/) &&
  has(block, /`?descriptor_id`?:/) &&
  has(block, /`?rationale`?:/) &&
  has(block, /`?evidence`?:/)
)
process.exit(ok ? 0 : 1)
NODE</automated></verify>
  <acceptance_criteria>
    - Human assertion: persisted workbook approval exists for at least one complete `r3-approval-*` promotion block.
    - Automated assertion: the gate only scans `r3-approval-*` blocks and ignores `approval-*` / `r2-approval-*` blocks.
  </acceptance_criteria>
  <done>Round 3 persisted workbook approval is present; 10-02 may proceed.</done>
  <resume-signal>Type "approved" only after the workbook file is saved with complete r3 approval fields.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human curator -> workbook | Persisted manual approval enters the curation system. |
| workbook -> future data edits | Future plans may mutate candidate v2 inputs only from `r3-*` blocks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | candidate-review.md approvals | mitigate | Require `r3-*` IDs, `round: phase_10_round_3`, persisted rationale/evidence, and human checkpoint before data edits. |
| T-10-02 | Repudiation | curation decisions | mitigate | Store every seed/alias/relation/accord decision in the workbook with rationale and evidence. |
| T-10-SC | Tampering | package installs | accept | No package-manager install tasks in this plan. |
</threat_model>

<verification>
Future execution verifies the workbook section and the blocking persisted-approval gate. No tests, builds, compiles, or validation commands are run during planning.
</verification>

<success_criteria>
`candidate-review.md` contains Round 3 pending proposals, and execution is blocked until at least one complete `r3-approval-*` block is approved in the workbook.
</success_criteria>

<output>
Create `.planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-01-SUMMARY.md` when done.
</output>
