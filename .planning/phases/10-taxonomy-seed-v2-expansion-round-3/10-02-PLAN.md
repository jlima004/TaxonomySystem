---
phase: 10-taxonomy-seed-v2-expansion-round-3
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - data/taxonomy/taxonomy-seed.v2.json
  - data/taxonomy/descriptor_aliases.seed.json
  - src/tests/curation/taxonomy_seed_v2.test.ts
  - src/tests/curation/alias_seed_v2.test.ts
autonomous: true
requirements:
  - EXP3-02
  - EXP3-03
  - EXP3-04
  - EXP3-05
  - EXP3-06
must_haves:
  truths:
    - "Only complete approved Round 3 workbook entries mutate taxonomy-seed.v2.json."
    - "Pending or incomplete Round 3 entries are ignored and remain pending."
    - "No empty subfamily is created; fresh_spice exists only with an approved concrete descriptor such as anise."
    - "Aliases change only with approved add_alias blocks and existing canonical v2 targets."
  artifacts:
    - path: "data/taxonomy/taxonomy-seed.v2.json"
      provides: "Round 3 candidate-only seed additions"
    - path: "data/taxonomy/descriptor_aliases.seed.json"
      provides: "Approved targeted alias additions, if any"
  key_links:
    - from: ".planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md"
      to: "data/taxonomy/taxonomy-seed.v2.json"
      via: "complete approved r3-approval-* blocks"
      pattern: "r3-approval-.*manual_approval.*approved"
    - from: ".planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md"
      to: "data/taxonomy/descriptor_aliases.seed.json"
      via: "complete approved r3-alias-cleanup-* blocks with primary_disposition: add_alias"
      pattern: "r3-alias-cleanup-.*primary_disposition.*add_alias"
    - from: "data/taxonomy/descriptor_aliases.seed.json"
      to: "data/taxonomy/taxonomy-seed.v2.json"
      via: "canonical target existence check before writing alias"
      pattern: "alias_key -> existing descriptor_id"
---

<objective>
Apply approved Round 3 seed and targeted alias curation to candidate v2 inputs only.

Purpose: turn persisted workbook approvals into candidate v2 taxonomy/alias changes while preserving v1/default boundaries and ignoring pending/incomplete items.
Output: approved `taxonomy-seed.v2.json` changes and optional approved alias seed updates.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-CONTEXT.md
@.planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-PATTERNS.md
@.planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md
@data/taxonomy/taxonomy-seed.v2.json
@data/taxonomy/descriptor_aliases.seed.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply approved Round 3 seed entries only</name>
  <files>data/taxonomy/taxonomy-seed.v2.json, src/tests/curation/taxonomy_seed_v2.test.ts</files>
  <read_first>data/taxonomy/taxonomy-seed.v2.json, src/tests/curation/taxonomy_seed_v2.test.ts, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md, .planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-CONTEXT.md</read_first>
  <behavior>
    - Approved `r3-approval-*` entries with required fields are present in v2 seed under the exact approved family/subfamily/descriptor path.
    - Pending or incomplete `r3-approval-*` entries do not appear in v2 seed.
    - No subfamily is empty; `fresh_spice` is absent unless `fresh_spice/fresh_spice/anise` or another concrete descriptor is approved.
    - `animal` is not auto-added; `civet` is not added without explicit approval; `musk` and `musky` do not both appear as duplicate descriptors unless the workbook contains explicit rationale.
  </behavior>
  <action>Update the seed test first if its fixed parser only recognizes Phase 8/9 approvals; keep the parser round-specific so Phase 10 checks require `approval_id: r3-approval-*` and `round: phase_10_round_3` rather than broad historical approvals. Then edit only `data/taxonomy/taxonomy-seed.v2.json`. Apply only complete approved workbook entries for the approved scopes: `amber_resinous / amber / amber`, `amber_resinous / balsamic_resin / resinous`, `amber_resinous / balsamic_resin / labdanum`, `amber_resinous / balsamic_resin / benzoin`, `amber_resinous / balsamic_resin / balsamic`, `animalic / musky / musk`, `animalic / musky / musky`, `animalic / musky / ambrette`, `animalic / leathery / leathery`, and `fresh_spice / fresh_spice / anise`. Require `approval_id: r3-approval-*`, `round: phase_10_round_3`, `manual_approval: approved`, `primary_disposition: promote_to_seed`, `family_id`, `subfamily_id`, `descriptor_id`, `rationale`, and `evidence`. Ignore pending/incomplete blocks and leave them pending. Stop only if attempting to apply a candidate without approval. Do not create an empty subfamily, do not create `fresh_spice` unless `anise` or another concrete descriptor is approved, do not automatically add `animal`, do not add `civet` without explicit approval, and do not leave `musk` and `musky` as duplicate descriptors without explicit workbook rationale. Do not edit `taxonomy-seed.v1.json`, compiled artifacts, `DEFAULT_PATHS`, or create `data/compiled/v2`.</action>
  <verify><automated>cd src && npm test -- tests/curation/taxonomy_seed_v2.test.ts</automated></verify>
  <acceptance_criteria>
    - Test-command assertion: the future command passes.
    - Source assertion: every new Round 3 seed descriptor has a complete approved `r3-approval-*` trace.
    - Boundary assertion: v1 seed and default paths remain untouched.
  </acceptance_criteria>
  <done>Candidate v2 seed contains only approved Round 3 additions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply approved targeted alias cleanup only</name>
  <files>data/taxonomy/descriptor_aliases.seed.json, src/tests/curation/alias_seed_v2.test.ts</files>
  <read_first>data/taxonomy/descriptor_aliases.seed.json, data/taxonomy/taxonomy-seed.v2.json, src/tests/curation/alias_seed_v2.test.ts, .planning/phases/08-taxonomy-seed-expansion-curation/curation/candidate-review.md</read_first>
  <behavior>
    - Alias seed changes only when a complete approved `r3-alias-cleanup-*` block has `primary_disposition: add_alias`, `alias_key`, existing canonical target, rationale, and evidence.
    - If no approved `add_alias` exists, `descriptor_aliases.seed.json` remains byte-for-byte semantically unchanged.
    - `ylang ylang -> ylang_ylang` remains deferred unless explicit Round 3 approval opens it.
  </behavior>
  <action>Update the alias test only if a fixed assertion blocks valid approved Round 3 aliases; keep the test requiring Round 3 `add_alias` approval and existing canonical v2 target. Edit `data/taxonomy/descriptor_aliases.seed.json` only for approved `add_alias` blocks with `approval_id` or block ID `r3-alias-cleanup-*`, `round: phase_10_round_3`, `manual_approval: approved`, `primary_disposition: add_alias`, `alias_key`, canonical target present in `taxonomy-seed.v2.json`, rationale, and evidence. Otherwise preserve alias seed unchanged. Do not remove/remap legacy aliases, do not use corpus/string similarity alone as approval, and keep `ylang ylang -> ylang_ylang` deferred unless an explicit Round 3 approval block exists.</action>
  <verify><automated>cd src && npm test -- tests/curation/alias_seed_v2.test.ts</automated></verify>
  <acceptance_criteria>
    - Test-command assertion: the future command passes.
    - Source assertion: all new aliases have approved `add_alias` trace and existing v2 canonical targets.
    - Boundary assertion: no legacy alias is removed or remapped automatically.
  </acceptance_criteria>
  <done>Alias seed is preserved or updated only from approved targeted Round 3 alias blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workbook -> v2 seed | Manual persisted approvals authorize candidate seed mutations. |
| workbook -> alias seed | Manual persisted add_alias approvals authorize targeted alias mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | taxonomy-seed.v2.json | mitigate | Require complete `r3-approval-*` blocks and tests that reject unapproved Round 3 additions. |
| T-10-04 | Tampering | descriptor_aliases.seed.json | mitigate | Require approved `add_alias` block and existing canonical v2 target before alias changes. |
| T-10-SC | Tampering | package installs | accept | No package-manager install tasks in this plan. |
</threat_model>

<verification>
Future execution runs `cd src && npm test -- tests/curation/taxonomy_seed_v2.test.ts tests/curation/alias_seed_v2.test.ts`. Do not run tests during planning.
</verification>

<success_criteria>
Round 3 seed/alias changes are candidate-only, approved-only, traceable to `r3-*` workbook blocks, and protected v1/default files remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-taxonomy-seed-v2-expansion-round-3/10-02-SUMMARY.md` when done.
</output>
