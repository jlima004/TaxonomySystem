---
phase: 46-batch-2-decision-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md
autonomous: true
requirements:
  - DEC-01
  - DEC-02
  - DEC-03
requirements_addressed:
  - DEC-01
  - DEC-02
  - DEC-03
user_setup: []
must_haves:
  truths:
    - "DEC-01: Curator can review 46-DECISION-MATRIX.md before any mutation, with exactly 40 rows mapped 1:1 to Phase 45 selected candidates."
    - "DEC-02: Every row has exactly one locked disposition enum: promote_to_seed, add_alias, reject, defer_manual_review, defer_future_batch, or needs_external_reference."
    - "DEC-03: Every row has documented rationale and evidence, and every mutation_allowed=false row has phase47_instruction=none."
    - "D-46-01/D-46-42: Phase 46 changes no taxonomy, alias, compiled artifact, source, Graphify, scoring, MVP/SaaS, Knowledge Engine, UI, or schema-relevant file."
  artifacts:
    - path: .planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md
      provides: "Parseable Batch 2 decision matrix with execution summary and 40 decision rows"
      contains: "## Decision Matrix"
  key_links:
    - from: .planning/phases/45-batch-2-candidate-selection/45-BATCH2-SELECTION.md
      to: .planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md
      via: "ids 01-40, candidate names, source_phase45_rank, phase45_inferred_subfamily"
      pattern: "\\| 01 \\| `hay` \\| 1 \\| `amber`"
    - from: .planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md
      to: .planning/phases/47-controlled-curation-mutation
      via: "mutation_allowed=true rows only, with mechanical phase47_instruction values"
      pattern: "mutation_allowed"
---

<objective>
Create the Phase 46 Batch 2 decision matrix before any curation mutation. This plan implements DEC-01, DEC-02, DEC-03 and locked context decisions D-46-01, D-46-02, D-46-03, D-46-04, D-46-05, D-46-06, D-46-07, D-46-08, D-46-09, D-46-10, D-46-11, D-46-12, D-46-13, D-46-14, D-46-15, D-46-16, D-46-17, D-46-18, D-46-19, D-46-20, D-46-21, D-46-22, D-46-23, D-46-24, D-46-25, D-46-26, D-46-27, D-46-28, D-46-29, D-46-30, D-46-31, D-46-32, D-46-33, D-46-34, D-46-35, D-46-36, D-46-37, D-46-38, D-46-39, D-46-40, D-46-41, D-46-42, and D-46-43.

Purpose: Make Phase 47 executable only from explicit, evidence-backed, parser-checkable authorization rows, while preserving Phase 46 as decide-only work.

Output: `.planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md` containing an execution summary and exactly 40 row-level dispositions.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-batch-2-decision-matrix/46-CONTEXT.md
@.planning/phases/46-batch-2-decision-matrix/46-RESEARCH.md
@.planning/phases/46-batch-2-decision-matrix/46-VALIDATION.md
@.planning/phases/46-batch-2-decision-matrix/46-PATTERNS.md
@.planning/phases/45-batch-2-candidate-selection/45-BATCH2-SELECTION.md
@.planning/phases/45-batch-2-candidate-selection/45-CONTEXT.md
@.planning/phases/45-batch-2-candidate-selection/45-01-SUMMARY.md
@.planning/phases/45-batch-2-candidate-selection/45-VERIFICATION.md
@.planning/milestones/v2.6-phases/38-group-b-conflict-microcuration/38-DECISION-MATRIX.md
@src/tests/fixtures/curation/41-DECISION-MATRIX.md
@data/compiled/v2/taxonomy.json
@data/compiled/v2/descriptor_aliases.json
@data/compiled/v2/similarity_matrix.json
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Every selected Batch 2 candidate has an explicit evidence-backed disposition before any curation mutation occurs | Covered by Task 1 matrix creation and Task 2 parser/protected-boundary verification |
| REQ | DEC-01 decision matrix for every selected candidate before mutation | Covered by Task 1 and row-count/mapping parser in Task 2 |
| REQ | DEC-02 explicit disposition per candidate | Covered by Task 1 locked enum and Task 2 enum parser |
| REQ | DEC-03 rationale/evidence traceability | Covered by Task 1 required fields and Task 2 non-empty field parser |
| RESEARCH | Exact Phase 45 candidate set ids 01-40 and evidence snapshot | Covered by Task 1 read-first sources and Task 2 candidate sequence assertion |
| RESEARCH | Existing taxonomy/alias state is read-only target proof | Covered by Task 1 target validation and Task 2 allowed existing family/subfamily assertions |
| RESEARCH | No package installs, no source-code changes, no new structure | Covered by Task 2 protected-boundary diff assertion |
| CONTEXT | D-46-01 through D-46-43 locked decisions | Covered by Task 1 schema/disposition/gate instructions and Task 2 parser invariants |

No source items are missing. Deferred ideas: none.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Author the locked Batch 2 decision matrix</name>
  <files>.planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md</files>
  <read_first>
    .planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md if it exists;
    .planning/phases/46-batch-2-decision-matrix/46-CONTEXT.md;
    .planning/phases/46-batch-2-decision-matrix/46-RESEARCH.md;
    .planning/phases/46-batch-2-decision-matrix/46-PATTERNS.md;
    .planning/phases/45-batch-2-candidate-selection/45-BATCH2-SELECTION.md;
    src/tests/fixtures/curation/41-DECISION-MATRIX.md;
    data/compiled/v2/taxonomy.json;
    data/compiled/v2/descriptor_aliases.json;
    data/compiled/v2/similarity_matrix.json
  </read_first>
  <action>Create `.planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md` per D-46-11 through D-46-16. Use this exact table header: `id`, `candidate`, `source_phase45_rank`, `phase45_inferred_subfamily`, `disposition`, `mutation_allowed`, `target_family`, `target_subfamily`, `target_descriptor`, `alias_target`, `confidence`, `investigation_depth`, `rationale`, `evidence`, `phase47_instruction`, `expected_effect`, `notes`. Add an execution summary for Phase 47 with counts for `promote_to_seed`, `add_alias`, `reject`, `defer_manual_review`, `defer_future_batch`, `needs_external_reference`, `mutation_allowed=true`, and `mutation_allowed=false`. Populate exactly 40 rows, ids `01` through `40`, preserving Phase 45 order and copying `candidate`, `source_phase45_rank`, and `phase45_inferred_subfamily` exactly from `45-BATCH2-SELECTION.md` per D-46-12 and D-46-38. Assign one disposition from the locked enum per D-46-03. Use only `low`, `medium_high`, or `high` confidence per D-46-29 and D-46-39. Use `baseline_check`, `targeted_check`, or `deep_check`; every row must be at least `baseline_check`, executable rows must be at least `targeted_check`, and ambiguous/food-linked/off-note/stretch-risk rows use `deep_check` when required by D-46-23 through D-46-28. Set `mutation_allowed=true` only for `promote_to_seed` or `add_alias` rows that satisfy every D-46-05/D-46-17/D-46-18/D-46-24/D-46-33/D-46-34/D-46-36 condition: existing family/subfamily, exact descriptor or alias target, no stretch placement, confidence `medium_high` or `high`, complete target fields, documented rationale/evidence, and mechanical `phase47_instruction`. For `reject`, `defer_manual_review`, `defer_future_batch`, and `needs_external_reference`, force `mutation_allowed=false`, empty executable target fields, and `phase47_instruction` exactly `none` per D-46-04, D-46-06, D-46-07, D-46-35, and D-46-41. Do not treat frequency, weighted score, or `phase45_inferred_subfamily` as approval evidence per D-46-02 and D-46-15. Do not create or imply new families/subfamilies, weak placements, or stretch promotions per D-46-08 through D-46-10. Use notes to record mismatches or D-36-style rationale per D-46-43. Keep Phase 46 decide-only: do not modify taxonomy, alias, compiled artifact, source, Graphify, scoring, MVP/SaaS, Knowledge Engine, UI, or schema-relevant files per D-46-01 and D-46-42.</action>
  <action>Follow the Phase 41 execution-summary and matrix-before-mutation analog explicitly: copy the `## Execution Summary for Phase 42` count-block shape from `src/tests/fixtures/curation/41-DECISION-MATRIX.md` as adapted in `46-PATTERNS.md`, rename it to `## Execution Summary for Phase 47`, then place `## Decision Matrix` after the summary. Keep the Phase 41 pattern of making the matrix the sole downstream mutation authorization layer, but use the Phase 46 richer locked schema and locked enum instead of Phase 41's older columns.</action>
  <action>For executable rows, make the safety contract parser-provable in source text: `add_alias` rows must populate `alias_target` with an existing seed descriptor from compiled taxonomy/seed inventory and use `phase47_instruction` exactly matching `add_alias alias=&lt;candidate&gt; target=&lt;existing_seed&gt;`; `promote_to_seed` rows must use `phase47_instruction` exactly matching `add_seed target_family=&lt;family&gt; target_subfamily=&lt;subfamily&gt; descriptor=&lt;descriptor&gt;`. Every executable row must include evidence/rationale wording that explicitly proves the target family/subfamily already exists, states the placement is not a stretch/weak fit, and states authorization is not based only on frequency, weighted score, or Phase 45 inferred placement. No executable row may name a new family/subfamily target.</action>
  <acceptance_criteria>
    - `46-DECISION-MATRIX.md` exists and contains `## Execution Summary for Phase 47` plus `## Decision Matrix`.
    - The decision table has exactly 40 data rows, ids `01`-`40`, and candidate order exactly `hay`, `coffee`, `orri`, `cananga`, `carrot_seed`, `orchid`, `cornmint`, `pennyroyal`, `asparagus`, `freesia`, `cardamom`, `malty`, `butterscotch`, `tangerine`, `saffron`, `hazelnut`, `forest`, `cascarilla`, `sesame`, `osmanthus`, `cubeb`, `elderflower`, `macadamia`, `marzipan`, `quince`, `curry`, `humus`, `davana`, `mace`, `linden_flower`, `pomegranate`, `agarwood`, `hibiscus`, `orange_rind`, `balsam`, `beeswax`, `tolu`, `acrylate`, `tea_green_tea`, `kumquat`.
    - Every row has one disposition from the six locked values and one confidence from `low`, `medium_high`, `high`; the literal confidence value `medium` is absent.
    - `mutation_allowed=true` appears only with `promote_to_seed` or `add_alias`; all other dispositions have `mutation_allowed=false` and `phase47_instruction` exactly `none`.
    - No new family or subfamily is named as an executable target; any executable target family/subfamily belongs to the existing v2.7 set in `46-RESEARCH.md`.
    - Every executable row has a mechanical `phase47_instruction`; `add_alias` rows reference an existing seed descriptor in `alias_target`/instruction target; rationale/evidence proves existing target, non-stretch placement, and non-frequency-only approval.
  </acceptance_criteria>
  <verify>
    <automated>python3 - <<'PY'
import json
import re
from pathlib import Path

matrix_path = Path('.planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md')
selection_path = Path('.planning/phases/45-batch-2-candidate-selection/45-BATCH2-SELECTION.md')
taxonomy_path = Path('data/compiled/v2/taxonomy.json')
matrix = matrix_path.read_text()
selection = selection_path.read_text()
taxonomy = json.loads(taxonomy_path.read_text())

def norm(value):
    return str(value).strip('` ').lower()

def collect_seed_descriptor_ids(taxonomy):
    seeds = set()
    for family in taxonomy.get('families', []):
        for subfamily in family.get('subfamilies', []):
            for descriptor in subfamily.get('descriptors', []):
                if descriptor.get('source') == 'seed' and isinstance(descriptor.get('id'), str):
                    seeds.add(norm(descriptor['id']))
    return seeds

seed_ids = collect_seed_descriptor_ids(taxonomy)
assert seed_ids, 'seed descriptor inventory empty'

selected = []
for line in selection.splitlines():
    m = re.match(r'\|\s*(\d+)\s*\|\s*`([^`]+)`\s*\|\s*`?([^`|]+)`?\s*\|', line)
    if m:
        selected.append((f'{int(m.group(1)):02d}', m.group(2), str(int(m.group(1))), m.group(3).strip()))
selected = selected[:40]
assert len(selected) == 40, len(selected)

rows = []
for line in matrix.splitlines():
    if re.match(r'\|\s*\d{2}\s*\|', line):
        rows.append([c.strip() for c in line.strip().strip('|').split('|')])

expected_header = ['id','candidate','source_phase45_rank','phase45_inferred_subfamily','disposition','mutation_allowed','target_family','target_subfamily','target_descriptor','alias_target','confidence','investigation_depth','rationale','evidence','phase47_instruction','expected_effect','notes']
header_line = next(line for line in matrix.splitlines() if line.strip().startswith('| id | candidate |'))
header = [c.strip() for c in header_line.strip().strip('|').split('|')]
assert header == expected_header, header
assert len(rows) == 40, len(rows)
assert [(r[0], r[1].strip('`'), r[2], r[3].strip('`')) for r in rows] == selected

allowed_disp = {'promote_to_seed','add_alias','reject','defer_manual_review','defer_future_batch','needs_external_reference'}
allowed_conf = {'low','medium_high','high'}
allowed_depth = {'baseline_check','targeted_check','deep_check'}
existing = {
  'amber_resinous': {'amber','balsamic_resin'},
  'animalic': {'leathery','musky'},
  'citrus': {'citrus_bitter','citrus_fresh'},
  'floral': {'floral_rose','floral_white'},
  'fresh_spice': {'fresh_spice'},
  'fruity': {'orchard_fruit','red_fruit','tropical_fruit'},
  'gourmand': {'vanilla'},
  'green': {'herbal_green','leafy_green'},
  'spicy': {'warm_spice'},
  'woody': {'woody_dry','woody_mossy'},
}
counts = {k: 0 for k in allowed_disp}
true_count = false_count = 0
for r in rows:
    assert len(r) == 17, (r[0], len(r))
    disp, mut, fam, sub, desc, alias, conf, depth = r[4], r[5], r[6], r[7], r[8], r[9], r[10], r[11]
    rationale, evidence, instruction = r[12], r[13], r[14]
    assert disp in allowed_disp, (r[0], disp)
    assert conf in allowed_conf, (r[0], conf)
    assert depth in allowed_depth, (r[0], depth)
    assert rationale and evidence, r[0]
    counts[disp] += 1
    assert mut in {'true','false'}, (r[0], mut)
    if mut == 'true':
        true_count += 1
        assert disp in {'promote_to_seed','add_alias'}, (r[0], disp)
        assert conf in {'medium_high','high'}, (r[0], conf)
        assert depth in {'targeted_check','deep_check'}, (r[0], depth)
        assert fam in existing and sub in existing[fam], (r[0], fam, sub)
        assert desc, r[0]
        if disp == 'add_alias':
            assert alias, r[0]
            assert norm(alias) in seed_ids, (r[0], alias)
            m = re.fullmatch(r'add_alias alias=([a-z0-9_]+) target=([a-z0-9_]+)', instruction)
            assert m, (r[0], instruction)
            assert m.group(1) == norm(r[1].strip('`')), (r[0], instruction)
            assert m.group(2) == norm(alias) and m.group(2) in seed_ids, (r[0], instruction)
        else:
            assert not alias, r[0]
            m = re.fullmatch(r'add_seed target_family=([a-z0-9_]+) target_subfamily=([a-z0-9_]+) descriptor=([a-z0-9_]+)', instruction)
            assert m, (r[0], instruction)
            assert (m.group(1), m.group(2), m.group(3)) == (norm(fam), norm(sub), norm(desc)), (r[0], instruction)
        proof_text = f'{rationale} {evidence}'.lower()
        assert re.search(r'existing (family|subfamily|target|seed|taxonomy)', proof_text), (r[0], proof_text)
        assert re.search(r'not (a )?(stretch|weak|forced)|no (stretch|weak|forced)', proof_text), (r[0], proof_text)
        assert re.search(r'not (frequency|weighted score|phase 45|inferred)|not based (only|solely) on|not .*approval evidence', proof_text), (r[0], proof_text)
        assert instruction != 'none' and instruction, r[0]
    else:
        false_count += 1
        assert instruction == 'none', (r[0], instruction)
        if disp in {'reject','defer_manual_review','defer_future_batch','needs_external_reference'}:
            assert not fam and not sub and not desc and not alias, r[0]
assert 'medium' not in {r[10] for r in rows}
for label, count in counts.items():
    assert re.search(rf'- {label}: {count}\b', matrix), (label, count)
assert re.search(rf'- \*\*mutation_allowed=true\*\*: {true_count}\b', matrix), true_count
assert re.search(rf'- \*\*mutation_allowed=false\*\*: {false_count}\b', matrix), false_count
PY</automated>
  </verify>
  <done>`46-DECISION-MATRIX.md` is complete, parseable, evidence-backed, and mutation-gated for all 40 selected Phase 45 candidates.</done>
</task>

<task type="auto">
  <name>Task 2: Prove parser invariants and protected-boundary compliance</name>
  <files>.planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md</files>
  <read_first>
    .planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md;
    .planning/phases/46-batch-2-decision-matrix/46-VALIDATION.md;
    .planning/phases/46-batch-2-decision-matrix/46-CONTEXT.md;
    .planning/phases/45-batch-2-candidate-selection/45-VERIFICATION.md;
    .planning/REQUIREMENTS.md;
    .planning/ROADMAP.md
  </read_first>
  <action>Run the parser from Task 1 and fix only `46-DECISION-MATRIX.md` until all assertions pass. Then run a protected-boundary diff check that fails if any changed path is outside `.planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md`, `.planning/phases/46-batch-2-decision-matrix/46-01-SUMMARY.md`, or `.planning/phases/46-batch-2-decision-matrix/46-VERIFICATION.md`. This verifies DEC-01 through DEC-03 plus D-46-01, D-46-30, D-46-31, D-46-32, and D-46-42: malformed row authorization is blocked, weak evidence cannot authorize mutation, and protected boundaries remain untouched. Do not create committed helper scripts; validation remains an inline parser/CLI assertion.</action>
  <acceptance_criteria>
    - Parser exits 0 and proves row count, Phase 45 mapping, locked disposition enum, confidence enum, investigation depth enum, target completeness, mutation gate, and execution-summary count reconciliation.
    - `git diff --name-only` contains no taxonomy seed, alias seed, compiled artifact, source, Graphify, scoring, MVP/SaaS, Knowledge Engine, UI, database/ORM schema, package, lockfile, or test fixture mutation.
    - `reject`, `defer_manual_review`, `defer_future_batch`, and `needs_external_reference` rows all have `mutation_allowed=false` and `phase47_instruction=none`.
    - `mutation_allowed=true` rows, if any, are only `promote_to_seed` or `add_alias` and contain mechanical Phase 47 instructions that do not require inference.
  </acceptance_criteria>
  <verify>
    <automated>python3 - <<'PY'
import json
import re
from pathlib import Path

matrix = Path('.planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md').read_text()
selection = Path('.planning/phases/45-batch-2-candidate-selection/45-BATCH2-SELECTION.md').read_text()
taxonomy = json.loads(Path('data/compiled/v2/taxonomy.json').read_text())

def norm(value): return str(value).strip('` ').lower()
def collect_seed_descriptor_ids(taxonomy):
    seeds = set()
    for family in taxonomy.get('families', []):
        for subfamily in family.get('subfamilies', []):
            for descriptor in subfamily.get('descriptors', []):
                if descriptor.get('source') == 'seed' and isinstance(descriptor.get('id'), str):
                    seeds.add(norm(descriptor['id']))
    return seeds
seed_ids = collect_seed_descriptor_ids(taxonomy)
assert seed_ids, 'seed descriptor inventory empty'

selected = []
for line in selection.splitlines():
    m = re.match(r'\|\s*(\d+)\s*\|\s*`([^`]+)`\s*\|\s*`?([^`|]+)`?\s*\|', line)
    if m: selected.append((f'{int(m.group(1)):02d}', m.group(2), str(int(m.group(1))), m.group(3).strip()))
selected = selected[:40]
rows = [[c.strip() for c in line.strip().strip('|').split('|')] for line in matrix.splitlines() if re.match(r'\|\s*\d{2}\s*\|', line)]
header = [c.strip() for c in next(line for line in matrix.splitlines() if line.strip().startswith('| id | candidate |')).strip().strip('|').split('|')]
assert header == ['id','candidate','source_phase45_rank','phase45_inferred_subfamily','disposition','mutation_allowed','target_family','target_subfamily','target_descriptor','alias_target','confidence','investigation_depth','rationale','evidence','phase47_instruction','expected_effect','notes']
assert len(selected) == 40 and len(rows) == 40
assert [(r[0], r[1].strip('`'), r[2], r[3].strip('`')) for r in rows] == selected
allowed_disp = {'promote_to_seed','add_alias','reject','defer_manual_review','defer_future_batch','needs_external_reference'}
allowed_conf = {'low','medium_high','high'}
allowed_depth = {'baseline_check','targeted_check','deep_check'}
existing = {'amber_resinous': {'amber','balsamic_resin'}, 'animalic': {'leathery','musky'}, 'citrus': {'citrus_bitter','citrus_fresh'}, 'floral': {'floral_rose','floral_white'}, 'fresh_spice': {'fresh_spice'}, 'fruity': {'orchard_fruit','red_fruit','tropical_fruit'}, 'gourmand': {'vanilla'}, 'green': {'herbal_green','leafy_green'}, 'spicy': {'warm_spice'}, 'woody': {'woody_dry','woody_mossy'}}
counts = {k: 0 for k in allowed_disp}; true_count = false_count = 0
for r in rows:
    assert len(r) == 17, (r[0], len(r))
    disp, mut, fam, sub, desc, alias, conf, depth = r[4], r[5], r[6], r[7], r[8], r[9], r[10], r[11]
    rationale, evidence, instruction = r[12], r[13], r[14]
    assert disp in allowed_disp and conf in allowed_conf and depth in allowed_depth and rationale and evidence
    counts[disp] += 1
    assert mut in {'true','false'}
    if mut == 'true':
        true_count += 1
        assert disp in {'promote_to_seed','add_alias'} and conf in {'medium_high','high'} and depth in {'targeted_check','deep_check'}
        assert fam in existing and sub in existing[fam] and desc
        proof_text = f'{rationale} {evidence}'.lower()
        assert re.search(r'existing (family|subfamily|target|seed|taxonomy)', proof_text), (r[0], proof_text)
        assert re.search(r'not (a )?(stretch|weak|forced)|no (stretch|weak|forced)', proof_text), (r[0], proof_text)
        assert re.search(r'not (frequency|weighted score|phase 45|inferred)|not based (only|solely) on|not .*approval evidence', proof_text), (r[0], proof_text)
        if disp == 'add_alias':
            assert alias and norm(alias) in seed_ids
            m = re.fullmatch(r'add_alias alias=([a-z0-9_]+) target=([a-z0-9_]+)', instruction)
            assert m and m.group(1) == norm(r[1].strip('`')) and m.group(2) == norm(alias) and m.group(2) in seed_ids
        else:
            assert not alias
            m = re.fullmatch(r'add_seed target_family=([a-z0-9_]+) target_subfamily=([a-z0-9_]+) descriptor=([a-z0-9_]+)', instruction)
            assert m and (m.group(1), m.group(2), m.group(3)) == (norm(fam), norm(sub), norm(desc))
    else:
        false_count += 1
        assert instruction == 'none'
        if disp in {'reject','defer_manual_review','defer_future_batch','needs_external_reference'}: assert not fam and not sub and not desc and not alias
assert 'medium' not in {r[10] for r in rows}
for label, count in counts.items(): assert re.search(rf'- {label}: {count}\b', matrix), (label, count)
assert re.search(rf'- \*\*mutation_allowed=true\*\*: {true_count}\b', matrix)
assert re.search(rf'- \*\*mutation_allowed=false\*\*: {false_count}\b', matrix)
PY</automated>
    <automated>python3 - <<'PY'
import subprocess
changed = [p.strip() for p in subprocess.check_output(['git','diff','--name-only'], text=True).splitlines() if p.strip()]
allowed = {
  '.planning/phases/46-batch-2-decision-matrix/46-DECISION-MATRIX.md',
  '.planning/phases/46-batch-2-decision-matrix/46-01-SUMMARY.md',
  '.planning/phases/46-batch-2-decision-matrix/46-VERIFICATION.md',
}
for path in changed:
    assert path in allowed, f'protected or out-of-scope path changed: {path}'
PY</automated>
  </verify>
  <done>Automated parser and diff checks prove Phase 46 remains a documentation-only decision gate with no protected-boundary or schema-relevant mutations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 45 selection artifact → Phase 46 matrix | Selected candidate rows become disposition rows; malformed mapping could authorize the wrong candidate. |
| Phase 46 matrix → Phase 47 executor | `mutation_allowed` and `phase47_instruction` become downstream mutation authorization. |
| Phase 46 executor → repository working tree | Documentation-only work could accidentally mutate protected taxonomy/source/artifact paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | `46-DECISION-MATRIX.md` row parser | mitigate | Parser asserts exactly 40 rows, ids/candidates equal Phase 45 selection, locked enum values, and valid mutation/disposition pairings. |
| T-46-02 | Tampering | `mutation_allowed` gate | mitigate | Parser asserts `mutation_allowed=true` only for `promote_to_seed`/`add_alias`; non-executable dispositions always have `false` and `phase47_instruction=none`. |
| T-46-03 | Repudiation | row rationale/evidence | mitigate | Parser asserts non-empty rationale and evidence for every row so Phase 47 authorization remains traceable. |
| T-46-04 | Elevation of Privilege | weak evidence to mutation authorization | mitigate | Plan requires frequency, weighted score, and inferred subfamily to remain non-approval evidence; executable rows require existing target proof, target completeness, confidence gate, and investigation depth gate. |
| T-46-05 | Tampering | protected repository paths | mitigate | `git diff --name-only` check fails any taxonomy, alias, compiled artifact, source, Graphify, scoring, MVP/SaaS, Knowledge Engine, UI, schema, package, lockfile, or test fixture mutation. |
| T-46-SC | Tampering | package installs | accept | No npm/pip/cargo install tasks exist; package legitimacy gate is not applicable. |
</threat_model>

<verification>
Overall phase checks:
- Parser proves `46-DECISION-MATRIX.md` exists, has exactly 40 rows, and maps 1:1 to Phase 45 selected candidates.
- Parser proves all row-level disposition, confidence, investigation-depth, `mutation_allowed`, target-completeness, evidence/rationale, and `phase47_instruction` invariants.
- Protected-boundary diff check proves no taxonomy, alias, compiled artifact, source, Graphify, scoring, MVP/SaaS, Knowledge Engine, UI, schema-relevant, package, lockfile, or fixture mutation occurred.
</verification>

<success_criteria>
- `46-DECISION-MATRIX.md` exists.
- Matrix has exactly 40 rows.
- Every row maps to one selected Phase 45 candidate.
- Every row has one locked disposition enum value.
- `mutation_allowed=true` appears only for `promote_to_seed`/`add_alias`.
- `reject`/`defer_manual_review`/`defer_future_batch`/`needs_external_reference` always have `mutation_allowed=false`.
- `phase47_instruction=none` whenever `mutation_allowed=false`.
- No stretch placements, no new family/subfamily targets, and no taxonomy/alias/compiled/source/Graphify/scoring/MVP/SaaS/Knowledge Engine/UI/schema mutation.
</success_criteria>

<output>
Create `.planning/phases/46-batch-2-decision-matrix/46-01-SUMMARY.md` when done.
</output>
