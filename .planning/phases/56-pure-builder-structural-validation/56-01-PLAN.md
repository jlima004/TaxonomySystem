---
phase: 56-pure-builder-structural-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph_read_model/types.ts
  - src/graph_read_model/build_graph.ts
  - src/tests/graph_read_model/build_graph.test.ts
autonomous: true
requirements:
  - GBLD-01
  - GBLD-02
  - GBLD-03
  - GBLD-04
  - GBLD-05
must_haves:
  truths:
    - Maintainer can build a deterministic in-memory olfactory graph from allowed compiled-artifact objects only per D-01, D-03, and D-24.
    - Maintainer can inspect family, subfamily, descriptor, alias, hierarchy, alias-resolution, and subfamily-similarity graph members with contract IDs and stable ordering per D-02 and D-14 through D-17.
    - Maintainer can prove repeated in-memory builds serialize identically without wall-clock metadata per D-18 and D-19.
  artifacts:
    - path: src/graph_read_model/types.ts
      provides: Readonly graph node, edge, stats, input, and validation-result types aligned to contract and compiler-validation patterns per D-05 and D-07 through D-10, with `stats` keys fixed to `families`, `subfamilies`, `descriptors`, `aliases`, and `subfamily_similarity_edges`.
    - path: src/graph_read_model/build_graph.ts
      provides: Pure buildOlfactoryGraph(input) implementation with deterministic ordering, contract IDs, and derived stats per D-04 through D-06 and D-14 through D-19.
    - path: src/tests/graph_read_model/build_graph.test.ts
      provides: Inline fixture coverage for node/edge construction, similarity property preservation, and determinism per D-22.
  key_links:
    - from: src/graph_read_model/build_graph.ts
      to: src/graph_read_model/contract.ts
      via: Imported schema constants, endpoint kinds, baseline stats, and ID rules
      pattern: GRAPH_(SCHEMA_VERSION|NODE_KINDS|EDGE_KINDS|EDGE_ENDPOINT_KINDS|EXPECTED_BASELINE_STATS)
    - from: src/graph_read_model/build_graph.ts
      to: src/graph_read_model/types.ts
      via: buildOlfactoryGraph input/output typing
      pattern: buildOlfactoryGraph
    - from: src/tests/graph_read_model/build_graph.test.ts
      to: src/graph_read_model/build_graph.ts
      via: Deterministic inline-fixture assertions
      pattern: buildOlfactoryGraph
---

<objective>
Implement the pure graph model and builder for Phase 56 so maintainers can construct the complete in-memory read model before any writer, CLI, or runtime boundary exists.

Purpose: Deliver GBLD-01 through GBLD-05 exactly as locked in D-01 through D-06.
Output: `src/graph_read_model/types.ts`, `src/graph_read_model/build_graph.ts`, and `src/tests/graph_read_model/build_graph.test.ts`.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-pure-builder-structural-validation/56-CONTEXT.md
@.planning/phases/56-pure-builder-structural-validation/56-RESEARCH.md
@.planning/phases/56-pure-builder-structural-validation/56-PATTERNS.md
@.planning/phases/56-pure-builder-structural-validation/56-VALIDATION.md
@.planning/phases/55-graph-contract-boundary-decisions/55-01-SUMMARY.md
@src/graph_read_model/contract.ts
@docs/olfactory_graph_contract.md
@src/compiler/types.ts
@src/types/taxonomy.ts
@src/types/similarity.ts
@src/inference/build_similarity_graph.ts
@src/tests/inference/build_similarity_graph.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define graph model and builder contracts</name>
  <read_first>
    - src/graph_read_model/contract.ts
    - src/compiler/types.ts
    - src/types/taxonomy.ts
    - src/types/similarity.ts
    - .planning/phases/56-pure-builder-structural-validation/56-CONTEXT.md
  </read_first>
  <files>src/graph_read_model/types.ts, src/graph_read_model/build_graph.ts</files>
  <action>Create readonly graph types and `buildOlfactoryGraph(input): OlfactoryGraph` per D-04 and D-05. Accept only already-loaded compiled artifact objects per D-03, import contract constants instead of duplicating literals per D-08 and D-24, emit only `family`, `subfamily`, `descriptor`, and `alias` nodes plus `contains_subfamily`, `contains_descriptor`, `resolves_to`, and `similar_to` edges per D-12, format node and edge IDs exactly per D-15 and D-16, preserve similarity `score`, `dimensions`, `evidence`, and optional `final_score` without reinterpretation per D-17, derive `stats` from emitted arrays per D-13 using the exact keys `families`, `subfamilies`, `descriptors`, `aliases`, and `subfamily_similarity_edges`, and omit any file I/O, CLI coupling, output-path logic, timestamps, or Graphify/runtime behavior per D-01, D-06, D-18, and user scope fences.</action>
  <verify>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>`src/graph_read_model/types.ts` and `build_graph.ts` compile under strict TypeScript, expose the locked public API, keep the final graph object serializable arrays with no persisted indexes or production fs/path usage, and define `graph.stats` with the exact keys `families`, `subfamilies`, `descriptors`, `aliases`, and `subfamily_similarity_edges`.</acceptance_criteria>
  <done>Builder types and implementation exist, compile, and encode the Phase 55 contract without scope leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove builder determinism and graph construction with inline fixtures</name>
  <read_first>
    - src/tests/inference/build_similarity_graph.test.ts
    - src/inference/build_similarity_graph.ts
    - .planning/phases/56-pure-builder-structural-validation/56-PATTERNS.md
    - .planning/phases/56-pure-builder-structural-validation/56-VALIDATION.md
  </read_first>
  <files>src/tests/graph_read_model/build_graph.test.ts</files>
  <behavior>
    - Test 1: Minimal compiled taxonomy + aliases + similarity fixture builds all four node kinds and all four edge kinds per D-02 and D-22.
    - Test 2: Rebuilding the same input twice returns deep-equal `nodes`, `edges`, `stats`, and `JSON.stringify(graph)` per D-14, D-18, and D-19.
    - Test 3: Node ordering is `kind` then `id`, edge ordering is `kind` then `source` then `target` then `id`, and similarity edge properties are preserved unchanged per D-14 through D-17.
  </behavior>
  <action>Add focused Vitest coverage mirroring repo patterns from `build_similarity_graph.test.ts`: use inline minimal fixtures only, assert hierarchy edges, alias-resolution edges, subfamily-similarity edges, derived stats, and deterministic ordering per D-22, and keep production modules fs-free by placing all fixture setup in the test file.</action>
  <verify>
    <automated>npm --prefix src test -- tests/graph_read_model/build_graph.test.ts</automated>
  </verify>
  <acceptance_criteria>Targeted tests fail without the builder, pass with the builder, and explicitly lock deterministic ordering, stable IDs, preserved similarity properties, and no wall-clock dependence.</acceptance_criteria>
  <done>Executable builder tests cover GBLD-01 through GBLD-05 with inline fixtures only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled artifact objects → builder | Untrusted in-memory input crosses into graph construction and ID generation. |
| contract constants → builder output | Contract drift or literal duplication could corrupt kinds, IDs, and output ordering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Spoofing | `src/graph_read_model/build_graph.ts` | mitigate | Use Phase 55 contract prefixes and endpoint kinds only; never pass raw IDs through as graph-global IDs; assert type-prefixed IDs in builder tests per D-15 and D-16. |
| T-56-02 | Tampering | `src/graph_read_model/build_graph.ts` | mitigate | Derive all nodes, edges, and stats from the three allowed in-memory inputs only, preserve similarity payloads exactly, and prohibit file/path/CLI behavior per D-03, D-06, and D-17. |
| T-56-03 | Repudiation | `src/tests/graph_read_model/build_graph.test.ts` | mitigate | Lock deterministic sort order and build-twice deep equality so regressions leave reproducible failing evidence per D-14 and D-19. |
| T-56-SC | Tampering | npm/pip/cargo installs | mitigate | No package installation is allowed in this plan; keep existing toolchain only and reject any install attempt as out of scope for Phase 56. |
</threat_model>

<verification>
- `npm --prefix src run typecheck`
- `npm --prefix src test -- tests/graph_read_model/build_graph.test.ts`
</verification>

<success_criteria>
- `buildOlfactoryGraph(input)` exists and consumes only in-memory taxonomy, alias, and similarity objects per D-03.
- Built graphs contain only contract node/edge kinds and exact ID formats per D-12, D-15, and D-16.
- Inline tests prove deterministic ordering, stable serialization, and preserved similarity semantics per D-14 through D-19.
</success_criteria>

<output>
Create `.planning/phases/56-pure-builder-structural-validation/56-01-SUMMARY.md` when done
</output>
