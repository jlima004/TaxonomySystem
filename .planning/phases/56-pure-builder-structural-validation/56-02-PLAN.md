---
phase: 56-pure-builder-structural-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 56-01
files_modified:
  - src/graph_read_model/validate_graph.ts
  - src/tests/graph_read_model/validate_graph.test.ts
  - src/tests/graph_read_model/live_artifact_baseline.test.ts
autonomous: true
requirements:
  - GVAL-01
  - GVAL-02
must_haves:
  truths:
    - Maintainer can run structured in-memory graph validation that reports expected structural failures without throwing for normal graph errors per D-07, D-20, and D-21, while keeping `warnings` present but empty in Phase 56 unless a later phase adds warning-only invariants.
    - Maintainer can verify alias targets, similarity endpoints, and baseline stats against the protected v2 counts `10/18/341/18/13` per D-02, D-13, and D-23.
    - Maintainer can prove live-artifact regression stays read-only and Phase 56 production modules do not take on CLI, writer, path-based, or Graphify scope per D-01, D-03, and D-06.
  artifacts:
    - path: src/graph_read_model/validate_graph.ts
      provides: Pure validateOlfactoryGraph(graph) with structured invariant-coded errors and stats reconciliation per D-05, D-07, and D-20.
    - path: src/tests/graph_read_model/validate_graph.test.ts
      provides: Inline invariant tests for duplicate IDs, missing endpoints, wrong endpoint kinds, invalid alias targets, invalid similarity endpoints, and stats mismatches per D-22.
    - path: src/tests/graph_read_model/live_artifact_baseline.test.ts
      provides: Read-only regression against `data/compiled/v2/*.json` proving `10/18/341/18/13`, alias resolution integrity, and valid subfamily similarity endpoints per D-23.
  key_links:
    - from: src/graph_read_model/validate_graph.ts
      to: src/compiler/types.ts
      via: Compiler-style result aggregation and error-path shaping
      pattern: makeCompilerError|combineResults|appendJsonPathKey
    - from: src/graph_read_model/validate_graph.ts
      to: src/graph_read_model/contract.ts
      via: Imported invariant names, endpoint-kind contract, and baseline stats
      pattern: GRAPH_(PHASE_56_INVARIANTS|EDGE_ENDPOINT_KINDS|EXPECTED_BASELINE_STATS)
    - from: src/tests/graph_read_model/live_artifact_baseline.test.ts
      to: src/graph_read_model/build_graph.ts
      via: Build live graph from compiled artifacts before validation
      pattern: buildOlfactoryGraph
---

<objective>
Implement Phase 56 structural validation and read-only live baseline regression so maintainers can trust the graph shape before any query-proof or writer phase begins.

Purpose: Deliver GVAL-01 and GVAL-02 exactly as locked in D-02, D-07, D-20, D-21, and D-23.
Output: `src/graph_read_model/validate_graph.ts`, `src/tests/graph_read_model/validate_graph.test.ts`, and `src/tests/graph_read_model/live_artifact_baseline.test.ts`.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-pure-builder-structural-validation/56-CONTEXT.md
@.planning/phases/56-pure-builder-structural-validation/56-RESEARCH.md
@.planning/phases/56-pure-builder-structural-validation/56-PATTERNS.md
@.planning/phases/56-pure-builder-structural-validation/56-VALIDATION.md
@.planning/phases/55-graph-contract-boundary-decisions/55-01-SUMMARY.md
@src/graph_read_model/contract.ts
@src/compiler/validate_output.ts
@src/compiler/types.ts
@src/tests/compiler/validate_output.test.ts
@src/tests/inventory/alias_target_inventory.test.ts
@src/tests/compiler/alias_target_integrity.test.ts
@.planning/phases/56-pure-builder-structural-validation/56-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement structured graph invariant validation</name>
  <read_first>
    - src/compiler/validate_output.ts
    - src/compiler/types.ts
    - src/graph_read_model/contract.ts
    - .planning/phases/56-pure-builder-structural-validation/56-CONTEXT.md
  </read_first>
  <files>src/graph_read_model/validate_graph.ts</files>
  <action>Create `validateOlfactoryGraph(graph): GraphValidationResult` per D-04, D-05, and D-07 using compiler-style `{ ok, errors, warnings }` aggregation per D-08. Keep `warnings` available in the result shape but return an empty warnings array in Phase 56 unless a concrete warning-only invariant is required. Implement separate scans for `duplicate_node_id_detection`, `duplicate_edge_id_detection`, `missing_edge_endpoints`, `wrong_endpoint_kinds`, `invalid_alias_targets`, and `invalid_subfamily_similarity_endpoints` per D-20 and D-21; validate duplicates before any Map/Set overwrite; reconcile `graph.stats` against actual arrays and `GRAPH_EXPECTED_BASELINE_STATS` per D-13 using the exact stats keys `families`, `subfamilies`, `descriptors`, `aliases`, and `subfamily_similarity_edges`; and return stable `code`, `path`, `message`, plus `node_id` or `edge_id` where relevant without introducing filesystem, CLI, writer, or Graphify behavior per D-01, D-03, and D-06.</action>
  <verify>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>The validator compiles, returns structured results instead of expected-error throws, covers every Phase 56 invariant family, keeps `warnings` present but empty unless explicitly needed, and derives baseline/stat validation from the graph object rather than copied metadata using the exact contract-aligned stats keys.</acceptance_criteria>
  <done>Validator module exists with invariant-specific passes and compiler-style result semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add invariant-focused validator tests with inline graph fixtures</name>
  <read_first>
    - src/tests/compiler/validate_output.test.ts
    - .planning/phases/56-pure-builder-structural-validation/56-PATTERNS.md
    - src/graph_read_model/contract.ts
  </read_first>
  <files>src/tests/graph_read_model/validate_graph.test.ts</files>
  <behavior>
    - Test 1: A valid minimal graph returns `{ ok: true, errors: [], warnings: [] }`.
    - Test 2: Targeted mutations produce stable coded/path-based failures for duplicate node IDs, duplicate edge IDs, missing endpoints, wrong endpoint kinds, invalid alias targets, and invalid subfamily similarity endpoints per D-20 and D-21.
    - Test 3: Stats mismatch between arrays and `GRAPH_EXPECTED_BASELINE_STATS` returns structured failures instead of silent acceptance.
  </behavior>
  <action>Create a minimal valid graph fixture, then mutate it one invariant at a time following the repo pattern from `validate_output.test.ts`. Assert exact `code` and `path` values, preserve traceability to the invariant families per D-21, and keep the file fully in-memory with no production file I/O.</action>
  <verify>
    <automated>npm --prefix src test -- tests/graph_read_model/validate_graph.test.ts</automated>
  </verify>
  <acceptance_criteria>Tests lock the validator contract for every required structural failure path and fail if any invariant stops producing stable structured errors.</acceptance_criteria>
  <done>Invariant coverage exists for all required Phase 56 validation failures.</done>
</task>

<task type="auto">
  <name>Task 3: Add live compiled-artifact baseline regression</name>
  <read_first>
    - src/tests/inventory/alias_target_inventory.test.ts
    - src/graph_read_model/contract.ts
    - docs/olfactory_graph_contract.md
    - .planning/phases/56-pure-builder-structural-validation/56-VALIDATION.md
  </read_first>
  <files>src/tests/graph_read_model/live_artifact_baseline.test.ts</files>
  <action>Add one read-only Vitest regression that loads only `data/compiled/v2/taxonomy.json`, `data/compiled/v2/descriptor_aliases.json`, and `data/compiled/v2/similarity_matrix.json` in the test layer per D-23, builds the graph with `buildOlfactoryGraph`, validates it with `validateOlfactoryGraph`, asserts baseline stats `10/18/341/18/13`, asserts all 18 aliases resolve to existing descriptor nodes, asserts all 13 similarity edges connect existing subfamily nodes, and optionally guards that `src/graph_read_model/build_graph.ts` and `validate_graph.ts` do not import `node:fs` or take path-based APIs per D-01, D-03, and D-06. Do not read `graphify-out/**`, `data/enriched_materials.json`, or any writer/output paths.</action>
  <verify>
    <automated>npm --prefix src test -- tests/graph_read_model/validate_graph.test.ts tests/graph_read_model/live_artifact_baseline.test.ts</automated>
  </verify>
  <acceptance_criteria>The live regression passes only when current compiled v2 artifacts still produce a valid graph with exact protected baseline counts and boundary-safe read-only behavior.</acceptance_criteria>
  <done>Live baseline regression proves GVAL-02 against the protected repository artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph arrays → validator | Potentially malformed node/edge arrays cross into invariant enforcement. |
| live compiled artifacts → test-only regression | Repository baseline inputs are trusted for provenance but still need structural proof and scope-bound loading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-04 | Tampering | `src/graph_read_model/validate_graph.ts` | mitigate | Scan original arrays before indexing so duplicate IDs cannot be overwritten silently, and compare endpoints/kinds against contract constants per D-20 and D-21. |
| T-56-05 | Spoofing | `src/graph_read_model/validate_graph.ts` | mitigate | Reject alias and similarity edges whose endpoints do not resolve to `descriptor:*` and `subfamily:*` nodes respectively, with stable coded errors and paths. |
| T-56-06 | Information Disclosure | `src/tests/graph_read_model/live_artifact_baseline.test.ts` | accept | Test reads only the three sanctioned compiled artifacts already committed in-repo; no secrets or external systems are involved. |
| T-56-07 | Tampering | `src/tests/graph_read_model/live_artifact_baseline.test.ts` | mitigate | Keep all filesystem access in the test file, restrict reads to sanctioned compiled artifacts, and assert production graph modules stay fs-free and path-free per D-03 and D-06. |
| T-56-SC | Tampering | npm/pip/cargo installs | mitigate | No package installation is allowed in this plan; keep the validator and regression on existing TypeScript/Vitest tooling only. |
</threat_model>

<verification>
- `npm --prefix src run typecheck`
- `npm --prefix src test -- tests/graph_read_model/validate_graph.test.ts tests/graph_read_model/live_artifact_baseline.test.ts`
- `npm --prefix src test`
</verification>

<success_criteria>
- `validateOlfactoryGraph(graph)` returns structured invariant failures with stable codes and JSON-style paths instead of expected-error throws.
- Inline validator tests cover duplicate IDs, missing endpoints, wrong endpoint kinds, invalid alias targets, invalid similarity endpoints, and stats reconciliation.
- Live compiled-artifact regression proves the exact protected baseline `10/18/341/18/13` and read-only phase boundaries.
</success_criteria>

<output>
Create `.planning/phases/56-pure-builder-structural-validation/56-02-SUMMARY.md` when done
</output>
