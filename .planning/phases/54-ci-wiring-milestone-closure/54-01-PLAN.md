---
phase: 54-ci-wiring-milestone-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/analysis/stress.test.ts
autonomous: true
requirements:
  - CI-03
user_setup: []
must_haves:
  truths:
    - "D-03: `src/tests/analysis/stress.test.ts` is stabilized before CI wiring depends on `npm --prefix src test`."
    - "D-04/D-05/D-06: The stress benchmark remains active, auditable, and CI-safe without deleting or skipping the performance-regression guard."
    - "D-07: The known flaky ceiling `CI_SAFE_ANALYSIS_5K_CEILING_MS = 1500` is replaced with explicit local/CI ceiling behavior."
    - "CI-03: `npm --prefix src test` can be trusted as a GitHub Actions step after the focused stress test passes."
  artifacts:
    - path: "src/tests/analysis/stress.test.ts"
      provides: "CI-safe analysis stress benchmark"
      contains: "process.env['CI']"
  key_links:
    - from: "src/tests/analysis/stress.test.ts"
      to: "npm --prefix src test"
      via: "Vitest discovery in `src/vitest.config.ts`"
      pattern: "tests/**/*.test.ts"
---

<objective>
Stabilize the analysis stress benchmark so the full test suite can serve as CI-03 evidence on cold GitHub Actions runners.

Purpose: Remove the known Phase 54 CI flake source while preserving the performance-regression intent of the existing 5k synthetic-material benchmark per D-03 through D-07.
Output: `src/tests/analysis/stress.test.ts` with explicit local and CI ceiling constants, active positive assertions, and auditable log output.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v2.10-MILESTONE-AUDIT.md
@.planning/phases/54-ci-wiring-milestone-closure/54-CONTEXT.md
@.planning/phases/54-ci-wiring-milestone-closure/54-RESEARCH.md
@.planning/phases/54-ci-wiring-milestone-closure/54-VALIDATION.md
@.planning/phases/54-ci-wiring-milestone-closure/54-PATTERNS.md
@src/tests/analysis/stress.test.ts
@src/package.json
@src/vitest.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 54-01-01: Make the 5k analysis stress benchmark CI-safe without skipping it</name>
  <files>src/tests/analysis/stress.test.ts</files>
  <read_first>
    - `src/tests/analysis/stress.test.ts` current benchmark, imports, assertions, and `CI_SAFE_ANALYSIS_5K_CEILING_MS = 1500` constant.
    - `.planning/phases/54-ci-wiring-milestone-closure/54-CONTEXT.md` decisions D-03 through D-07.
    - `.planning/phases/54-ci-wiring-milestone-closure/54-PATTERNS.md` stress-test pattern and auditable log pattern.
    - `.planning/v2.10-MILESTONE-AUDIT.md` lines describing the 389/390 flake evidence.
  </read_first>
  <behavior>
    - Test expectation: with no `CI=true`, the benchmark still generates 5000 synthetic materials with seed `42`, calls `analyzeCorpus`, asserts non-empty `frequency` and `cooccurrence`, and enforces a local ceiling of `1500` milliseconds.
    - Test expectation: with `CI=true`, the benchmark still generates 5000 synthetic materials with seed `42`, calls `analyzeCorpus`, asserts non-empty `frequency` and `cooccurrence`, and enforces a CI ceiling of `3000` milliseconds.
    - Test expectation: console output includes the active mode string (`local` or `ci`), `analysis(5k)`, elapsed milliseconds, and the active ceiling value.
  </behavior>
  <action>Update `src/tests/analysis/stress.test.ts` per D-03 through D-07. Replace the single `CI_SAFE_ANALYSIS_5K_CEILING_MS = 1500` constant with concrete constants named `ANALYSIS_5K_MATERIALS = 5000`, `LOCAL_ANALYSIS_5K_CEILING_MS = 1500`, `CI_ANALYSIS_5K_CEILING_MS = 3000`, `ANALYSIS_STRESS_MODE = process.env['CI'] === 'true' ? 'ci' : 'local'`, and `ANALYSIS_5K_CEILING_MS` selected from those mode-specific ceilings. Keep the test active; do not add `.skip`, `.todo`, conditional early return, or a reduced material count. Keep `generateSyntheticCorpus({ materials: 5000, seed: 42 })` behavior via the new constant, keep both positive assertions on `analysis.frequency.size` and `analysis.cooccurrence.size`, and assert `elapsed` against `ANALYSIS_5K_CEILING_MS`. Update the log string so it reports `analysis(5k)`, the mode, elapsed time, and the active ceiling.</action>
  <acceptance_criteria>
    - Source assertion: `src/tests/analysis/stress.test.ts` contains `LOCAL_ANALYSIS_5K_CEILING_MS = 1500` and `CI_ANALYSIS_5K_CEILING_MS = 3000`.
    - Source assertion: `src/tests/analysis/stress.test.ts` contains `process.env['CI'] === 'true'`.
    - Source assertion: `src/tests/analysis/stress.test.ts` does not contain `.skip`, `.todo`, or `return` before `analyzeCorpus(corpus)`.
    - Behavior assertion: the benchmark still calls `generateSyntheticCorpus` with `materials: ANALYSIS_5K_MATERIALS` and `seed: 42`.
    - Behavior assertion: the benchmark still asserts `analysis.frequency.size` and `analysis.cooccurrence.size` are greater than `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix src test -- tests/analysis/stress.test.ts && CI=true npm --prefix src test -- tests/analysis/stress.test.ts && npm --prefix src run typecheck</automated>
  </verify>
  <done>`src/tests/analysis/stress.test.ts` preserves the 5k benchmark and positive analysis assertions while using explicit local/CI ceilings and passing both local-mode and CI-mode focused Vitest runs.</done>
</task>

<task type="auto">
  <name>Task 54-01-02: Prove the stabilized benchmark does not break the full test gate</name>
  <files>src/tests/analysis/stress.test.ts</files>
  <read_first>
    - `src/tests/analysis/stress.test.ts` final content from Task 54-01-01.
    - `src/package.json` scripts `test` and `typecheck`.
    - `.planning/phases/54-ci-wiring-milestone-closure/54-VALIDATION.md` rows 54-01-01 and 54-01-02.
  </read_first>
  <action>Run the post-change local proof for CI-03. Execute `npm --prefix src run typecheck` and `npm --prefix src test` from the repository root. If failures occur, fix only `src/tests/analysis/stress.test.ts` and preserve D-04 through D-06: no test deletion, no skip, no reduction below 5000 materials, and no production-code changes. Do not modify `src/package.json` to hide the stress benchmark from `npm --prefix src test`.</action>
  <acceptance_criteria>
    - Command assertion: `npm --prefix src run typecheck` exits `0`.
    - Command assertion: `npm --prefix src test` exits `0` with `tests/analysis/stress.test.ts` included in the run.
    - Source assertion: `src/package.json` still has `scripts.test` equal to `vitest run`.
    - Scope assertion: only `src/tests/analysis/stress.test.ts` is modified by this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix src run typecheck && npm --prefix src test</automated>
  </verify>
  <done>Typecheck and the full test suite pass after the benchmark stabilization, making CI-03 ready for workflow wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner timing → test verdict | Cold runner performance variance crosses into Vitest pass/fail behavior. |
| Test stabilization → regression coverage | A timing fix could accidentally weaken or bypass the benchmark. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Denial of Service | `src/tests/analysis/stress.test.ts` | mitigate | Use an explicit `CI=true` timing ceiling of `3000`ms while keeping the 5000-material workload, positive assertions, and local `1500`ms ceiling. |
| T-54-02 | Tampering | Vitest coverage for analysis stress | mitigate | Forbid `.skip`, `.todo`, early return, reduced material count, and production-code changes; verify focused local and `CI=true` runs. |
| T-54-SC | Tampering | npm installs | accept | No package installs or package additions are planned in this plan. |
</threat_model>

<verification>
- `npm --prefix src test -- tests/analysis/stress.test.ts`
- `CI=true npm --prefix src test -- tests/analysis/stress.test.ts`
- `npm --prefix src run typecheck`
- `npm --prefix src test`
</verification>

<success_criteria>
- CI-03 flake source is addressed without deleting, skipping, or reducing the benchmark below 5000 materials.
- Stress-test output is auditable by mode and active ceiling.
- Full suite and typecheck are green before CI workflow wiring begins.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
------ | -- | ------------------- | ---- | ------ | -----
GOAL | — | CI must run tests reliably | 01 | COVERED | Stress flake closed before workflow wiring.
REQ | CI-03 | CI verifies `npm --prefix src test` | 01,02 | COVERED | Plan 01 stabilizes test; Plan 02 consumes it in workflow.
RESEARCH | — | Current `CI_SAFE_ANALYSIS_5K_CEILING_MS = 1500` is flake source | 01 | COVERED | Explicit CI ceiling replaces single ceiling.
CONTEXT | D-03 | Stabilize stress test first | 01 | COVERED | Wave 1 plan.
CONTEXT | D-04 | Preserve benchmark intent | 01 | COVERED | No skip/delete/reduced workload.
CONTEXT | D-05 | Use explicit auditable CI-safe mechanism | 01 | COVERED | Local/CI constants and log.
CONTEXT | D-06 | Do not change core purpose | 01 | COVERED | Same 5k analyzeCorpus path.
CONTEXT | D-07 | Known ceiling source | 01 | COVERED | Replaced with mode-specific constants.
</source_audit>

<output>
Create `.planning/phases/54-ci-wiring-milestone-closure/54-01-SUMMARY.md` when done.
</output>
