---
phase: 54-ci-wiring-milestone-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 54-01
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
  - CI-04
user_setup: []
must_haves:
  truths:
    - "D-08/D-12: A single simple GitHub Actions workflow exists under `.github/workflows/` with no matrix, cache, deployment, publication, or notification work."
    - "D-09: CI installs dependencies reproducibly with `npm ci --prefix src` against `src/package-lock.json`."
    - "D-10/D-11: CI runs `typecheck`, `test`, `alias:integrity -- --json`, and `verify:integrity -- --json` in that order after install."
    - "D-13/D-14/D-15: CI triggers on push and pull_request targeting `master` and uses Node.js 24 only."
    - "D-16/D-17/D-18: Workflow consumes existing scripts while preserving `scripts.compile` exactly as `node dist/cli/compile.js`."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Phase 54 CI consumer for install, typecheck, tests, alias proof, and verify proof"
      contains: "npm ci --prefix src"
    - path: "src/package.json"
      provides: "Existing script contracts consumed by CI"
      contains: "\"compile\": \"node dist/cli/compile.js\""
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "src/package-lock.json"
      via: "npm ci --prefix src"
      pattern: "npm ci --prefix src"
    - from: ".github/workflows/ci.yml"
      to: "src/package.json scripts"
      via: "npm --prefix src run <script>"
      pattern: "typecheck.*test.*alias:integrity.*verify:integrity"
---

<objective>
Add the minimal GitHub Actions workflow that covers CI-01 through CI-04 using the existing `src` npm scripts.

Purpose: Create an automated CI consumer for reproducible install, typecheck, full tests, alias JSON proof, and Phase 53 local guardrail proof while preserving compile isolation and milestone scope.
Output: `.github/workflows/ci.yml` with one Node 24 job triggered on `push` and `pull_request` to `master`.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-ci-wiring-milestone-closure/54-CONTEXT.md
@.planning/phases/54-ci-wiring-milestone-closure/54-RESEARCH.md
@.planning/phases/54-ci-wiring-milestone-closure/54-PATTERNS.md
@.planning/phases/54-ci-wiring-milestone-closure/54-01-SUMMARY.md
@src/package.json
@src/package-lock.json
</context>

<tasks>

<task type="auto">
  <name>Task 54-02-01: Create the single-job Node 24 CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.planning/phases/54-ci-wiring-milestone-closure/54-CONTEXT.md` decisions D-08 through D-15.
    - `.planning/phases/54-ci-wiring-milestone-closure/54-RESEARCH.md` Pattern 1 minimal CI workflow consumer.
    - `.planning/phases/54-ci-wiring-milestone-closure/54-PATTERNS.md` `.github/workflows/ci.yml` pattern assignment.
    - `src/package.json` scripts for `typecheck`, `test`, `alias:integrity`, `verify:integrity`, and `compile`.
    - `src/package-lock.json` existence as the lockfile consumed by `npm ci --prefix src`.
  </read_first>
  <action>Create `.github/workflows/ci.yml` per D-08. Use workflow name `CI`. Configure triggers exactly for `push` branches `[master]` and `pull_request` branches `[master]` per D-13. Add one job named `src` on `ubuntu-latest` per D-12. Add `permissions: contents: read` at workflow or job level to limit token scope. Steps must be, in order: checkout with `actions/checkout@v4`; setup Node with `actions/setup-node@v4` and `node-version: '24'` per D-14; run `npm ci --prefix src` per D-09/CI-01; run `npm --prefix src run typecheck` per CI-02; run `npm --prefix src test` per CI-03; run `npm --prefix src run alias:integrity -- --json` per CI-04; run `npm --prefix src run verify:integrity -- --json` per D-11. Do not add a matrix, caching, deploy, artifact upload, package publish, path filters, notifications, or extra Node versions per D-12/D-15 and deferred ideas.</action>
  <acceptance_criteria>
    - Source assertion: `.github/workflows/ci.yml` exists and contains `name: CI`.
    - Source assertion: workflow trigger branches are exactly `master` for both `push` and `pull_request`.
    - Source assertion: workflow contains one job key `src` and `runs-on: ubuntu-latest`.
    - Source assertion: workflow contains `node-version: '24'` or `node-version: "24"` and does not contain Node versions `20`, `22`, or a matrix.
    - Source assertion: workflow command order is `npm ci --prefix src`, `npm --prefix src run typecheck`, `npm --prefix src test`, `npm --prefix src run alias:integrity -- --json`, then `npm --prefix src run verify:integrity -- --json`.
    - Security assertion: workflow contains `permissions` with `contents: read` and contains no `deploy`, `publish`, `upload-artifact`, `release`, or `npm publish` step.
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/workflows/ci.yml && node -e "const fs=require('fs'); const y=fs.readFileSync('.github/workflows/ci.yml','utf8'); for (const s of ['name: CI','push:','pull_request:','master','runs-on: ubuntu-latest','npm ci --prefix src','npm --prefix src run typecheck','npm --prefix src test','npm --prefix src run alias:integrity -- --json','npm --prefix src run verify:integrity -- --json','contents: read']) if (!y.includes(s)) throw new Error('missing '+s); if (/matrix:|node-version:.*(20|22)|npm publish|upload-artifact|release|deploy/i.test(y)) throw new Error('forbidden CI expansion token found'); const order=['npm ci --prefix src','npm --prefix src run typecheck','npm --prefix src test','npm --prefix src run alias:integrity -- --json','npm --prefix src run verify:integrity -- --json'].map(s=>y.indexOf(s)); if (order.some(i=>i<0) || order.some((v,i,a)=>i&&v<a[i-1])) throw new Error('CI command order invalid');"</automated>
  </verify>
  <done>`.github/workflows/ci.yml` exists with the locked single-job Node 24 workflow and exact CI command sequence.</done>
</task>

<task type="auto">
  <name>Task 54-02-02: Prove CI consumes existing scripts and preserves compile isolation</name>
  <files>.github/workflows/ci.yml, src/package.json</files>
  <read_first>
    - `.github/workflows/ci.yml` created in Task 54-02-01.
    - `src/package.json` final script contracts.
    - `.planning/phases/53-alias-integrity-gate-hardening/53-03-SUMMARY.md` Phase 53 proof outputs.
    - `.planning/phases/54-ci-wiring-milestone-closure/54-CONTEXT.md` decisions D-10, D-11, D-16, D-17, and D-18.
  </read_first>
  <action>Run the local equivalent of the workflow command sequence from the repo root to prove the workflow consumes existing scripts. Mandatory proof starts with `npm ci --prefix src` per D-09/CI-01, then `npm --prefix src run typecheck`, `npm --prefix src test`, `npm --prefix src run alias:integrity -- --json`, and `npm --prefix src run verify:integrity -- --json`. If local dependency state was dirty before `npm ci --prefix src`, record that it was intentionally replaced by the lockfile install and restore only executor-local, untracked dependency-state side effects if needed; do not mark CI-01 complete unless this local `npm ci --prefix src` proof passes or a later Task 54-03 final proof passes it. Parse `src/package.json` with Node and fail unless `scripts.compile` is exactly `node dist/cli/compile.js` and contains none of `alias:integrity`, `verify:integrity`, or `compile:quality` per D-16/D-17. Do not edit `src/package.json` for Phase 54 unless restoring an accidental local change; CI must use existing scripts per D-18.</action>
  <acceptance_criteria>
    - Command assertion: `npm ci --prefix src` exits `0`; CI-01 cannot be marked complete without this passing local proof or the final Task 54-03 proof.
    - Command assertion: `npm --prefix src run typecheck` exits `0`.
    - Command assertion: `npm --prefix src test` exits `0`.
    - CLI assertion: `npm --prefix src run alias:integrity -- --json` exits `0` and output includes `PASS`, `341`, `18`, `0`, and empty `unresolved`.
    - CLI assertion: `npm --prefix src run verify:integrity -- --json` exits `0` and output includes `PASS`, `341`, `18`, `0`, and empty `unresolved`.
    - Static assertion: parsed `src/package.json` has `scripts.compile === 'node dist/cli/compile.js'` and `scripts.compile` contains none of `alias:integrity`, `verify:integrity`, or `compile:quality`.
    - Scope assertion: `src/package.json` is not modified by this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm ci --prefix src && npm --prefix src run typecheck && npm --prefix src test && npm --prefix src run alias:integrity -- --json && npm --prefix src run verify:integrity -- --json && node -e "const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('src/package.json','utf8')); const compile=pkg.scripts.compile; if (compile !== 'node dist/cli/compile.js') throw new Error('compile script changed: '+compile); if (/alias:integrity|verify:integrity|compile:quality/.test(compile)) throw new Error('compile script contains forbidden integrity/quality token')"</automated>
  </verify>
  <done>Workflow source and local command proof, including passing `npm ci --prefix src`, show CI-01 through CI-04 are wired to existing scripts, and normal compile isolation remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub event → CI runner | Untrusted repository events trigger automated commands. |
| CI runner → npm dependency graph | CI installs dependencies from `src/package-lock.json`. |
| CI workflow → repository scripts | Workflow invokes local scripts that must preserve compile isolation and guardrail behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-03 | Tampering | Dependency install | mitigate | Use `npm ci --prefix src`, not `npm install`, and do not modify `src/package.json` or `src/package-lock.json`. |
| T-54-04 | Repudiation | Alias integrity CI proof | mitigate | Run both `alias:integrity -- --json` and `verify:integrity -- --json`; acceptance requires PASS `341/18/0` JSON evidence. |
| T-54-05 | Elevation of Privilege | GitHub token permissions | mitigate | Set `permissions: contents: read`; do not add publish/deploy/release/upload steps. |
| T-54-06 | Tampering | Normal compile path | mitigate | Parse `src/package.json` and fail unless `scripts.compile` is exactly `node dist/cli/compile.js` and alias-gate-free. |
| T-54-SC | Tampering | npm installs | mitigate | No new packages are added; CI install uses existing lockfile via `npm ci --prefix src`; no package-manager add/install task modifies dependencies. |
</threat_model>

<verification>
- Static workflow source assertion for triggers, Node 24, permissions, command order, and no matrix/deploy/publish expansion.
- `npm ci --prefix src`
- `npm --prefix src run typecheck`
- `npm --prefix src test`
- `npm --prefix src run alias:integrity -- --json`
- `npm --prefix src run verify:integrity -- --json`
- Node static compile-isolation proof against `src/package.json`.
</verification>

<success_criteria>
- CI-01 through CI-04 are represented in `.github/workflows/ci.yml` and backed by local command proof including passing `npm ci --prefix src`.
- Workflow remains one simple Node 24 job with no advanced CI features or scope creep.
- `scripts.compile` remains exactly `node dist/cli/compile.js` and contains no integrity/quality tokens.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
------ | -- | ------------------- | ---- | ------ | -----
GOAL | — | Add GitHub Actions or equivalent CI for install/typecheck/test/alias integrity | 02 | COVERED | `.github/workflows/ci.yml`.
REQ | CI-01 | Reproducible dependency install | 02 | COVERED | `npm ci --prefix src`.
REQ | CI-02 | CI verifies typecheck | 02 | COVERED | `npm --prefix src run typecheck`.
REQ | CI-03 | CI verifies full tests | 01,02 | COVERED | Stress stabilized then workflow runs `npm --prefix src test`.
REQ | CI-04 | CI verifies alias JSON proof | 02 | COVERED | `alias:integrity -- --json`.
RESEARCH | — | Include `verify:integrity` as official Phase 53 guardrail | 02 | COVERED | Workflow runs `verify:integrity -- --json`.
CONTEXT | D-08 | Create workflow under `.github/workflows/` | 02 | COVERED | `.github/workflows/ci.yml`.
CONTEXT | D-09 | Use `npm ci --prefix src` | 02 | COVERED | Install step.
CONTEXT | D-10 | Run locked command sequence | 02 | COVERED | Ordered workflow steps.
CONTEXT | D-11 | Include `alias:integrity` and `verify:integrity` | 02 | COVERED | Both JSON steps.
CONTEXT | D-12 | Simple one-job workflow | 02 | COVERED | No matrix/deploy/cache.
CONTEXT | D-13 | Trigger on push/PR to master | 02 | COVERED | Workflow triggers.
CONTEXT | D-14 | Use Node 24 | 02 | COVERED | setup-node version.
CONTEXT | D-15 | No multiple Node versions | 02 | COVERED | Static assertion forbids matrix/20/22.
CONTEXT | D-16 | Preserve compile script exactly | 02,03 | COVERED | Static proof.
CONTEXT | D-17 | Do not add integrity to compile path | 02,03 | COVERED | Static proof.
CONTEXT | D-18 | Preserve Phase 53 guardrail behavior | 02 | COVERED | Existing scripts consumed.
</source_audit>

<output>
Create `.planning/phases/54-ci-wiring-milestone-closure/54-02-SUMMARY.md` when done.
</output>
