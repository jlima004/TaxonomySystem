---
phase: 54-ci-wiring-milestone-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 54-01
  - 54-02
files_modified:
  - .planning/phases/54-ci-wiring-milestone-closure/54-03-SUMMARY.md
  - .planning/phases/54-ci-wiring-milestone-closure/54-SUMMARY.md
  - .planning/phases/54-ci-wiring-milestone-closure/54-VERIFICATION.md
  - .planning/phases/54-ci-wiring-milestone-closure/54-VALIDATION.md
  - .planning/phases/54-ci-wiring-milestone-closure/54-UAT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
  - CI-04
  - BOUND-01
  - BOUND-02
  - BOUND-03
user_setup: []
must_haves:
  truths:
    - "D-01: Phase 54 delivers only CI Wiring & Milestone Closure for v2.10."
    - "D-02: CI-01, CI-02, CI-03, CI-04, BOUND-01, BOUND-02, and BOUND-03 are all satisfied before marking the milestone ready for completion."
    - "D-19/D-20/D-21/D-22/D-23: A Phase 54 boundary audit proves protected taxonomy, alias policy, compiled v2, Graphify, scoring, UI, and Knowledge Engine scopes are not mutated or claimed, while `.github/workflows/**` is allowed only as intentional CI wiring."
    - "D-24: Phase 54 produces SUMMARY, VERIFICATION, VALIDATION, and UAT artifacts with command and boundary evidence."
    - "D-25: ROADMAP, STATE, and REQUIREMENTS are updated only after CI-01 through CI-04 and BOUND-01 through BOUND-03 are proven."
    - "D-26/D-27: Closure artifacts route to `/gsd-audit-milestone v2.10` and explicitly do not run `/gsd-complete-milestone v2.10` until that audit is clean."
    - "D-28/D-29: Optional traceability is included only as a light cross-reference if it does not delay CI and boundary proof."
  artifacts:
    - path: ".planning/phases/54-ci-wiring-milestone-closure/54-VERIFICATION.md"
      provides: "Formal CI and boundary proof for Phase 54"
      contains: "CI-01"
    - path: ".planning/phases/54-ci-wiring-milestone-closure/54-SUMMARY.md"
      provides: "Phase 54 execution summary and boundary notes"
      contains: "341/18/0"
    - path: ".planning/phases/54-ci-wiring-milestone-closure/54-UAT.md"
      provides: "Human-facing acceptance checks for CI and closure routing"
      contains: "/gsd-audit-milestone v2.10"
  key_links:
    - from: "git diff --name-only"
      to: "BOUND-01/BOUND-02/BOUND-03"
      via: "adapted Phase 53 boundary proof"
      pattern: "no protected paths changed"
    - from: "54-VERIFICATION.md"
      to: "REQUIREMENTS.md"
      via: "requirements_verified list and evidence table"
      pattern: "CI-01.*CI-02.*CI-03.*CI-04.*BOUND-01.*BOUND-02.*BOUND-03"
---

<objective>
Run the final Phase 54 proof package, record CI and boundary evidence, and close v2.10 only up to the clean milestone-audit handoff.

Purpose: Prove CI-01 through CI-04 and BOUND-01 through BOUND-03 after implementation, protect preexisting Graphify dirty state from being staged or claimed, and update milestone state only after proof is collected.
Output: Phase 54 SUMMARY, VERIFICATION, VALIDATION, and UAT artifacts plus post-proof updates to ROADMAP, STATE, and REQUIREMENTS.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v2.10-MILESTONE-AUDIT.md
@.planning/phases/54-ci-wiring-milestone-closure/54-CONTEXT.md
@.planning/phases/54-ci-wiring-milestone-closure/54-VALIDATION.md
@.planning/phases/54-ci-wiring-milestone-closure/54-PATTERNS.md
@.planning/phases/53-alias-integrity-gate-hardening/53-03-PLAN.md
@.planning/phases/53-alias-integrity-gate-hardening/53-03-SUMMARY.md
@.planning/phases/53-alias-integrity-gate-hardening/53-VERIFICATION.md
@.github/workflows/ci.yml
@src/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 54-03-01: Run final CI-equivalent proof and protected boundary audit</name>
  <files>.planning/phases/54-ci-wiring-milestone-closure/54-VERIFICATION.md, .planning/phases/54-ci-wiring-milestone-closure/54-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/54-ci-wiring-milestone-closure/54-CONTEXT.md` decisions D-19 through D-23.
    - `.planning/phases/54-ci-wiring-milestone-closure/54-PATTERNS.md` Phase 54 boundary proof adaptation.
    - `.planning/phases/53-alias-integrity-gate-hardening/53-03-PLAN.md` boundary proof pattern.
    - `.planning/phases/53-alias-integrity-gate-hardening/53-03-SUMMARY.md` Phase 53 proof outputs and Graphify dirty-state wording.
    - `.github/workflows/ci.yml` final workflow.
    - `src/package.json` final scripts.
  </read_first>
  <action>Run the final local CI-equivalent proof commands in the locked workflow order: `npm ci --prefix src`, `npm --prefix src run typecheck`, `npm --prefix src test`, `npm --prefix src run alias:integrity -- --json`, and `npm --prefix src run verify:integrity -- --json`. CI-01 and milestone state cannot be marked complete unless this final `npm ci --prefix src` proof passes; if the install replaces a dirty local dependency tree, record that the lockfile install was intentional and restore only executor-local, untracked dependency-state side effects if needed. Inspect both JSON outputs for `status: PASS`, `compiled_descriptor_count: 341`, `valid_target_count: 18`, `unresolved_target_count: 0`, and `unresolved: []`. Parse `src/package.json` to prove `scripts.compile` remains exactly `node dist/cli/compile.js` and alias-gate-free per D-16/D-17. Run the adapted Phase 54 boundary audit per D-19 through D-22: staged `graphify-out/**` is forbidden; preexisting unstaged `graphify-out/**` may remain but must not be staged or claimed; staged and unstaged changes are forbidden for `data/taxonomy/taxonomy-seed.v2.json`, `data/taxonomy/descriptor_aliases.seed.json`, `data/taxonomy/alias_target_exceptions.v1.json`, `data/compiled/v2`, `src/scoring`, `src/ui`, and `src/knowledge-engine`; `.github/workflows/ci.yml` is allowed and any other `.github/**` path must be reviewed as out of scope. Stop if forbidden paths are modified; revert only unintended forbidden changes and do not stage `graphify-out/**`.</action>
  <acceptance_criteria>
    - Command assertion: `npm ci --prefix src`, typecheck, full suite, alias JSON proof, and verify JSON proof all exit `0` in the locked workflow order.
    - JSON assertion: both integrity proof outputs include `PASS`, `compiled_descriptor_count: 341`, `valid_target_count: 18`, `unresolved_target_count: 0`, and `unresolved: []`.
    - Static assertion: `src/package.json` has `scripts.compile === 'node dist/cli/compile.js'` and no `alias:integrity`, `verify:integrity`, or `compile:quality` token inside `scripts.compile`.
    - Boundary assertion: staged diffs contain no `graphify-out/**` paths; unstaged `graphify-out/**` may remain but is recorded as preexisting and unclaimed.
    - Boundary assertion: staged and unstaged diffs contain no protected paths under `data/taxonomy/taxonomy-seed.v2.json`, `data/taxonomy/descriptor_aliases.seed.json`, `data/taxonomy/alias_target_exceptions.v1.json`, `data/compiled/v2`, `src/scoring`, `src/ui`, or `src/knowledge-engine`.
    - Scope assertion: `.github/**` changes are limited to `.github/workflows/ci.yml`.
  </acceptance_criteria>
  <verify>
    <automated>npm ci --prefix src && npm --prefix src run typecheck && npm --prefix src test && npm --prefix src run alias:integrity -- --json && npm --prefix src run verify:integrity -- --json && node -e "const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('src/package.json','utf8')); const compile=pkg.scripts.compile; if (compile !== 'node dist/cli/compile.js') throw new Error('compile script changed: '+compile); if (/alias:integrity|verify:integrity|compile:quality/.test(compile)) throw new Error('compile script contains forbidden integrity/quality token')" && bash -lc 'set -e; graphify_staged=$(git diff --cached --name-only -- graphify-out 2>/dev/null); if [ -n "$graphify_staged" ]; then printf "staged graphify-out forbidden changes:\n%s\n" "$graphify_staged"; exit 1; fi; for mode in unstaged staged; do if [ "$mode" = staged ]; then files=$(git diff --cached --name-only -- data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/taxonomy/alias_target_exceptions.v1.json data/compiled/v2 src/scoring src/ui src/knowledge-engine 2>/dev/null); else files=$(git diff --name-only -- data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json data/taxonomy/alias_target_exceptions.v1.json data/compiled/v2 src/scoring src/ui src/knowledge-engine 2>/dev/null); fi; if [ -n "$files" ]; then printf "%s forbidden changes:\n%s\n" "$mode" "$files"; exit 1; fi; done; github_changed=$(git diff --name-only -- .github 2>/dev/null; git diff --cached --name-only -- .github 2>/dev/null); bad=$(printf "%s\n" "$github_changed" | grep -v "^$" | grep -v "^\.github/workflows/ci\.yml$" || true); if [ -n "$bad" ]; then printf "unexpected .github changes:\n%s\n" "$bad"; exit 1; fi'</automated>
  </verify>
  <done>Final local CI-equivalent proof, including passing `npm ci --prefix src`, and adapted Phase 54 boundary audit pass, with `.github/workflows/ci.yml` as the only allowed `.github/**` change and Graphify dirty state unstaged/unclaimed.</done>
</task>

<task type="auto">
  <name>Task 54-03-02: Write Phase 54 closure artifacts and post-proof milestone state updates</name>
  <files>.planning/phases/54-ci-wiring-milestone-closure/54-SUMMARY.md, .planning/phases/54-ci-wiring-milestone-closure/54-VERIFICATION.md, .planning/phases/54-ci-wiring-milestone-closure/54-VALIDATION.md, .planning/phases/54-ci-wiring-milestone-closure/54-UAT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/54-ci-wiring-milestone-closure/54-CONTEXT.md` decisions D-23 through D-29.
    - `.planning/phases/54-ci-wiring-milestone-closure/54-VALIDATION.md` draft validation map.
    - `.planning/phases/54-ci-wiring-milestone-closure/54-PATTERNS.md` summary, verification, validation, UAT, roadmap, state, and requirements patterns.
    - `.planning/phases/53-alias-integrity-gate-hardening/53-03-SUMMARY.md` boundary notes pattern.
    - `.planning/phases/53-alias-integrity-gate-hardening/53-VERIFICATION.md` verification table pattern.
    - `.planning/ROADMAP.md`, `.planning/STATE.md`, and `.planning/REQUIREMENTS.md` current Phase 54 pending status before any updates.
  </read_first>
  <action>After Task 54-03-01 passes, create or update Phase 54 GSD artifacts per D-24 and D-25. Write `54-VERIFICATION.md` with frontmatter `phase: 54`, `status: passed`, `requirements_verified` listing `CI-01`, `CI-02`, `CI-03`, `CI-04`, `BOUND-01`, `BOUND-02`, and `BOUND-03`, plus evidence rows for the passing local `npm ci --prefix src` proof, `typecheck`, `test`, `alias:integrity -- --json`, `verify:integrity -- --json`, static compile isolation, `.github/workflows/ci.yml`, and boundary audit. Write `54-SUMMARY.md` with requirements-completed `[CI-01, CI-02, CI-03, CI-04, BOUND-01, BOUND-02, BOUND-03]`, changed files, verification results including passing `npm ci --prefix src`, boundary notes that `.github/workflows/ci.yml` is intentional Phase 54 scope, and a statement that preexisting unstaged `graphify-out/**` remains unclaimed if present. Update `54-VALIDATION.md` from draft to `status: passed`, `nyquist_compliant: true`, and mark rows green with the exact commands run, including `npm ci --prefix src` for CI-01. Write `54-UAT.md` with manual checks for remote GitHub Actions run visibility, CI command step presence, boundary proof review, and the required next command `/gsd-audit-milestone v2.10`; explicitly state not to run `/gsd-complete-milestone v2.10` until the audit is clean per D-26/D-27. Then update `.planning/REQUIREMENTS.md` to check `[x]` for CI-01 through CI-04 and BOUND-01 through BOUND-03, update `.planning/ROADMAP.md` Phase 54 plan count/list/status and progress only after evidence is recorded, and update `.planning/STATE.md` to reflect Phase 54 verified/ready for milestone audit. If adding optional D-28 traceability, keep it to one short cross-reference sentence and do not create `52-VERIFICATION.md` or edit Phase 50/52/53 artifacts.</action>
  <acceptance_criteria>
    - Artifact assertion: `54-VERIFICATION.md`, `54-SUMMARY.md`, `54-VALIDATION.md`, and `54-UAT.md` exist under `.planning/phases/54-ci-wiring-milestone-closure/`.
    - Verification assertion: `54-VERIFICATION.md` lists all seven requirements `CI-01`, `CI-02`, `CI-03`, `CI-04`, `BOUND-01`, `BOUND-02`, and `BOUND-03` and cites command/boundary evidence for each, including passing local `npm ci --prefix src` evidence for CI-01.
    - Validation assertion: `54-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `status: passed` after evidence is collected.
    - UAT assertion: `54-UAT.md` contains `/gsd-audit-milestone v2.10` and contains a prohibition on `/gsd-complete-milestone v2.10` until the audit is clean.
    - Requirements assertion: `.planning/REQUIREMENTS.md` has `[x]` for CI-01, CI-02, CI-03, CI-04, BOUND-01, BOUND-02, and BOUND-03.
    - Scope assertion: no Phase 50, 52, or 53 artifacts are modified for optional traceability.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/54-ci-wiring-milestone-closure/54-VERIFICATION.md && test -f .planning/phases/54-ci-wiring-milestone-closure/54-SUMMARY.md && test -f .planning/phases/54-ci-wiring-milestone-closure/54-VALIDATION.md && test -f .planning/phases/54-ci-wiring-milestone-closure/54-UAT.md && node -e "const fs=require('fs'); const files=['.planning/phases/54-ci-wiring-milestone-closure/54-VERIFICATION.md','.planning/phases/54-ci-wiring-milestone-closure/54-SUMMARY.md','.planning/phases/54-ci-wiring-milestone-closure/54-VALIDATION.md','.planning/phases/54-ci-wiring-milestone-closure/54-UAT.md','.planning/REQUIREMENTS.md']; const text=Object.fromEntries(files.map(f=>[f,fs.readFileSync(f,'utf8')])); for (const id of ['CI-01','CI-02','CI-03','CI-04','BOUND-01','BOUND-02','BOUND-03']) { if (!text[files[0]].includes(id)) throw new Error('verification missing '+id); if (!text['.planning/REQUIREMENTS.md'].match(new RegExp('- \\[x\\] \\*\\*'+id+'\\*\\*'))) throw new Error('requirement not checked '+id); } if (!/nyquist_compliant:\s*true/.test(text['.planning/phases/54-ci-wiring-milestone-closure/54-VALIDATION.md'])) throw new Error('validation not nyquist true'); if (!text['.planning/phases/54-ci-wiring-milestone-closure/54-UAT.md'].includes('/gsd-audit-milestone v2.10')) throw new Error('UAT missing audit command'); if (!/do not run|não rode|not run/i.test(text['.planning/phases/54-ci-wiring-milestone-closure/54-UAT.md']) || !text['.planning/phases/54-ci-wiring-milestone-closure/54-UAT.md'].includes('/gsd-complete-milestone v2.10')) throw new Error('UAT missing complete-milestone prohibition');"</automated>
  </verify>
  <done>Phase 54 artifacts record all CI and boundary proof, planning state files are updated after proof only, and the next required action is a clean `/gsd-audit-milestone v2.10` before milestone completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Proof commands → closure artifacts | Command output and repository state become the audit record for Phase 54. |
| Git working tree → protected boundary verdict | Dirty/staged state determines whether BOUND-01 through BOUND-03 are satisfied. |
| Phase verification → milestone closure | Phase completion may update roadmap/state/requirements only after proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-07 | Repudiation | Phase 54 verification artifacts | mitigate | Record exact command proofs, JSON `341/18/0`, workflow path, static compile proof, and boundary audit outcomes in `54-VERIFICATION.md` and `54-SUMMARY.md`. |
| T-54-08 | Tampering | Protected taxonomy and compiled paths | mitigate | Run staged and unstaged `git diff --name-only` checks for taxonomy seed, alias policy files, and `data/compiled/v2`; block closure if any path appears. |
| T-54-09 | Tampering | Preexisting `graphify-out/**` state | mitigate | Check staged Graphify paths only as blocking, preserve unstaged dirty state as unclaimed, and document it without staging. |
| T-54-10 | Tampering | Deferred scopes | mitigate | Include `src/scoring`, `src/ui`, and `src/knowledge-engine` in boundary checks; record FUT-01/FUT-02/Graphify/scoring/UI/MVP/Knowledge Engine as not opened. |
| T-54-11 | Repudiation | Milestone closure routing | mitigate | `54-UAT.md` and `54-SUMMARY.md` require `/gsd-audit-milestone v2.10` and prohibit `/gsd-complete-milestone v2.10` until audit is clean. |
| T-54-SC | Tampering | npm installs | mitigate | Closure proof consumes existing lockfile/scripts only; no new package additions. |
</threat_model>

<verification>
- `npm ci --prefix src`
- `npm --prefix src run typecheck`
- `npm --prefix src test`
- `npm --prefix src run alias:integrity -- --json`
- `npm --prefix src run verify:integrity -- --json`
- Node static compile-isolation proof for `src/package.json`
- Adapted Phase 54 boundary proof for Graphify, taxonomy seed, alias policy files, compiled v2 artifacts, scoring, UI, Knowledge Engine, and limited `.github/workflows/ci.yml` scope.
- Source assertions for Phase 54 artifacts and post-proof status updates.
</verification>

<success_criteria>
- CI-01 through CI-04 and BOUND-01 through BOUND-03 are proven and documented.
- No protected taxonomy truth, alias policy, compiled v2, Graphify, scoring, UI, MVP, or Knowledge Engine work is claimed or staged as Phase 54 work.
- `.github/workflows/ci.yml` is the only intended `.github/**` change.
- v2.10 is routed to milestone audit, not prematurely completed.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
------ | -- | ------------------- | ---- | ------ | -----
GOAL | — | Add/verify CI and close v2.10 | 01,02,03 | COVERED | Stress stabilization, workflow, proof, closure artifacts.
REQ | CI-01 | Reproducible CI install | 02,03 | COVERED | Workflow install step and verification evidence.
REQ | CI-02 | CI typecheck | 02,03 | COVERED | Workflow and command proof.
REQ | CI-03 | CI tests | 01,02,03 | COVERED | Stress stabilization, workflow, full suite proof.
REQ | CI-04 | CI alias integrity JSON | 02,03 | COVERED | Workflow and PASS 341/18/0 proof.
REQ | BOUND-01 | No taxonomy seed v2 mutation | 03 | COVERED | Boundary audit.
REQ | BOUND-02 | No compiled v2 publication/mutation | 03 | COVERED | Boundary audit.
REQ | BOUND-03 | No FUT/Graphify/scoring/UI/MVP/Knowledge Engine work | 03 | COVERED | Boundary audit and closure docs.
RESEARCH | — | Stress flake before CI | 01 | COVERED | First wave.
RESEARCH | — | One simple Node 24 workflow | 02 | COVERED | No matrix/cache/deploy.
RESEARCH | — | Boundary proof adapted from Phase 53 allowing `.github/workflows/**` | 03 | COVERED | Updated forbidden list.
RESEARCH | — | Package Legitimacy Audit: no new packages | 01,02,03 | COVERED | No package additions.
CONTEXT | D-01 | Phase 54 only CI Wiring & Milestone Closure | 01,02,03 | COVERED | Plans limited to stress, workflow, closure.
CONTEXT | D-02 | Satisfy all Phase 54 reqs before milestone ready | 03 | COVERED | Post-proof state updates.
CONTEXT | D-03 | Stabilize stress first | 01 | COVERED | Wave 1.
CONTEXT | D-04 | Preserve stress intent | 01 | COVERED | No skip/delete.
CONTEXT | D-05 | Explicit auditable CI-safe mechanism | 01 | COVERED | Mode constants/log.
CONTEXT | D-06 | Do not change core purpose | 01 | COVERED | Same 5k benchmark.
CONTEXT | D-07 | Known 1500ms flake source | 01 | COVERED | Mode-specific ceiling.
CONTEXT | D-08 | Create `.github/workflows/` workflow | 02 | COVERED | `.github/workflows/ci.yml`.
CONTEXT | D-09 | Use `npm ci --prefix src` | 02 | COVERED | Workflow install.
CONTEXT | D-10 | CI command sequence | 02 | COVERED | Ordered steps.
CONTEXT | D-11 | Include alias and verify integrity | 02 | COVERED | Both commands.
CONTEXT | D-12 | Simple one job | 02 | COVERED | No matrix.
CONTEXT | D-13 | Push/PR master triggers | 02 | COVERED | Workflow triggers.
CONTEXT | D-14 | Node 24 | 02 | COVERED | setup-node.
CONTEXT | D-15 | No multiple Node versions | 02 | COVERED | Static assertion.
CONTEXT | D-16 | Preserve compile exactly | 02,03 | COVERED | Static proof.
CONTEXT | D-17 | Do not add integrity to compile | 02,03 | COVERED | Static proof.
CONTEXT | D-18 | Preserve Phase 53 verify/compile:quality behavior | 02,03 | COVERED | Existing scripts consumed; no package change.
CONTEXT | D-19 | Final boundary audit protected paths | 03 | COVERED | Boundary proof.
CONTEXT | D-20 | Reuse Phase 53 pattern | 03 | COVERED | Adapted diff gate.
CONTEXT | D-21 | `.github/**` allowed only for CI | 02,03 | COVERED | Allow `.github/workflows/ci.yml` only.
CONTEXT | D-22 | Protect dirty graphify-out state | 03 | COVERED | staged-only block and unclaimed note.
CONTEXT | D-23 | Record audit results | 03 | COVERED | SUMMARY/VERIFICATION.
CONTEXT | D-24 | Produce GSD artifacts | 03 | COVERED | SUMMARY/VERIFICATION/VALIDATION/UAT.
CONTEXT | D-25 | Update roadmap/state/requirements after proof only | 03 | COVERED | Task order.
CONTEXT | D-26 | Re-run milestone audit after verification | 03 | COVERED | UAT/summary route.
CONTEXT | D-27 | Do not complete milestone before clean audit | 03 | COVERED | UAT prohibition.
CONTEXT | D-28 | Optional light traceability | 03 | COVERED | Allowed as one sentence only.
CONTEXT | D-29 | Optional traceability must not delay CI | 03 | COVERED | No separate traceability task.
</source_audit>

<output>
Create `.planning/phases/54-ci-wiring-milestone-closure/54-03-SUMMARY.md` when done.
</output>
