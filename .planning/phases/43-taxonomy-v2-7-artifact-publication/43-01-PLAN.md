---
phase: 43-taxonomy-v2-7-artifact-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/compiled/v2/taxonomy.json
  - data/compiled/v2/descriptor_aliases.json
  - data/compiled/v2/similarity_matrix.json
  - .planning/phases/43-taxonomy-v2-7-artifact-publication/v2.7-closure-report.md
autonomous: true
requirements:
  - ART-01
  - ART-02
  - ART-03
must_haves:
  truths:
    - "ART-01: Taxonomy invariants pass validation — `compileAll()` returns `result.ok === true` and `quality_gate_status === PASS` with zero validation errors."
    - "ART-02: Official v2.7 compiled artifacts are published to `data/compiled/v2/` with version 2.7.0 — `taxonomy.json`, `descriptor_aliases.json`, and `similarity_matrix.json` are overwritten in place."
    - "ART-03: A `v2.7-closure-report.md` is produced with Starting State (v2.6: 43 curated, 275 low_support, 8 conflict), Triage Batch Details (30 selected), Decision Matrix Summary (6 promote, 4 reject, 19 defer, 1 needs_external_reference), and Final Metrics dynamically measured from compiler output."
    - "Version 2.7.0 is passed via --version flag to the CLI; DEFAULT_PATHS.version in parse_args.ts is NOT modified."
    - "No alias, relation, accord, seed, noise, stopwords, or parse_args.ts input files are modified by this plan."
    - "Compilation is run twice: first to /tmp for validation evidence capture, then to official data/compiled/v2/ only after /tmp validation PASS."
  artifacts:
    - path: data/compiled/v2/taxonomy.json
      provides: "Official v2.7 compiled taxonomy with 324 descriptors"
      contains: "version 2.7.0"
    - path: data/compiled/v2/descriptor_aliases.json
      provides: "Official v2.7 compiled alias mappings"
      contains: "18 alias mappings"
    - path: data/compiled/v2/similarity_matrix.json
      provides: "Official v2.7 similarity matrix with updated review_queue"
      contains: "review_queue with ~269 items"
    - path: .planning/phases/43-taxonomy-v2-7-artifact-publication/v2.7-closure-report.md
      provides: "Formal v2.7 rebaseline/closure report"
      contains: "Starting State, Triage Batch Details, Decision Matrix Summary, Final Metrics"
  key_links:
    - from: .planning/phases/42-low-support-microcuration-execution/42-SUMMARY.md
      to: data/compiled/v2/taxonomy.json
      via: "Phase 42 added 6 seed descriptors; Phase 43 compiles them into official artifacts"
      pattern: "peppermint|rosemary|cumin|spearmint|caraway|opoponax"
    - from: .planning/phases/41-low-support-batch-decision-matrix/41-DECISION-MATRIX.md
      to: .planning/phases/43-taxonomy-v2-7-artifact-publication/v2.7-closure-report.md
      via: "Decision matrix disposition summary feeds closure report"
      pattern: "promote_to_seed|reject|defer"
    - from: .planning/phases/40-low-support-curation-planning/40-BATCH-SELECTION.md
      to: .planning/phases/43-taxonomy-v2-7-artifact-publication/v2.7-closure-report.md
      via: "Batch selection details feed closure report"
      pattern: "30 selected"
---

<objective>
Compile, validate, and publish the official v2.7 taxonomy artifacts, then produce a formal closure report documenting the entire v2.7 low-support triage lifecycle.

Purpose: Phase 42 added 6 approved seed descriptors but deferred artifact publication to Phase 43. This plan compiles the updated seed into official `data/compiled/v2/` artifacts, validates all taxonomy invariants, and creates a closure report with dynamically measured metrics covering the full v2.7 milestone (Phases 40–43).
Output: Updated `data/compiled/v2/taxonomy.json`, `data/compiled/v2/descriptor_aliases.json`, `data/compiled/v2/similarity_matrix.json`, and `v2.7-closure-report.md`.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.agents/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-taxonomy-v2-7-artifact-publication/43-CONTEXT.md
@.planning/phases/43-taxonomy-v2-7-artifact-publication/43-RESEARCH.md
@.planning/phases/42-low-support-microcuration-execution/42-SUMMARY.md
@.planning/phases/42-low-support-microcuration-execution/42-VERIFICATION.md
@.planning/phases/41-low-support-batch-decision-matrix/41-DECISION-MATRIX.md
@.planning/phases/40-low-support-curation-planning/40-BATCH-SELECTION.md
@data/compiled/v2/taxonomy.json
@data/compiled/v2/similarity_matrix.json
@src/cli/compile.ts
@src/cli/parse_args.ts
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Validate artifacts, update metrics, and produce the closure report | Covered by Tasks 1-4 |
| REQ | ART-01: Validate taxonomy invariants against modified curation inputs | Covered by Task 1 (compile to /tmp with validation) and Task 2 (invariant verification) |
| REQ | ART-02: Publish v2.7 compiled artifacts and updated review_queue metrics | Covered by Task 3 (compile to official path) |
| REQ | ART-03: Produce v2.7 rebaseline/closure report | Covered by Task 4 (closure report generation) |
| CONTEXT | Publish directly to data/compiled/v2/ | Covered by Task 3 |
| CONTEXT | Closure report as v2.7-closure-report.md with Starting State, Triage Batch, Decision Matrix, Final Metrics | Covered by Task 4 |
| CONTEXT | Do not hardcode metrics — measure dynamically from compiler output | Covered by Task 1 (capture stdout) and Task 4 (parse captured metrics) |
| CONTEXT | Fail strictly on invariant failures | Covered by Task 1 and Task 2 (exit 1 on any error) |
| RESEARCH | DEFAULT_PATHS.version must not change | Covered by all tasks — use --version flag only |
| DEFERRED | Tracking time spent per curation decision | Not in scope |
| DEFERRED | Automating future low-support triage | Not in scope |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Validate compilation in /tmp sandbox</name>
  <files>None (output to /tmp only)</files>
  <read_first>
    @src/cli/compile.ts
    @src/cli/parse_args.ts
    @data/taxonomy/taxonomy-seed.v2.json
    @data/compiled/v2/taxonomy.json
    @.planning/phases/43-taxonomy-v2-7-artifact-publication/43-CONTEXT.md
    @.planning/phases/43-taxonomy-v2-7-artifact-publication/43-RESEARCH.md
  </read_first>
  <action>Build TypeScript with `cd src && npm run build`. Then compile to /tmp sandbox with `cd src && node dist/cli/compile.js --out /tmp/taxonomy-v27-validation --version 2.7.0 --quality-report`. Capture the full stdout output for metric extraction in Task 4. Verify: (a) exit code is 0, (b) `validation_status=ok` appears in stdout, (c) `quality_gate_status=PASS` appears in stdout, (d) output files exist at `/tmp/taxonomy-v27-validation/taxonomy.json`, `/tmp/taxonomy-v27-validation/descriptor_aliases.json`, `/tmp/taxonomy-v27-validation/similarity_matrix.json`. Parse the taxonomy.json to confirm version is `2.7.0` and descriptor count is >= 320. Parse similarity_matrix.json to extract review_queue length and type breakdown. Record all extracted metrics in a temporary file at `/tmp/taxonomy-v27-metrics.txt` for Task 4.</action>
  <acceptance_criteria>
    - CLI assertion: `cd src && node dist/cli/compile.js --out /tmp/taxonomy-v27-validation --version 2.7.0 --quality-report` exits 0.
    - CLI assertion: stdout contains `validation_status=ok`.
    - CLI assertion: stdout contains `quality_gate_status=PASS`.
    - Source assertion: `/tmp/taxonomy-v27-validation/taxonomy.json` contains `"version": "2.7.0"`.
    - Source assertion: `/tmp/taxonomy-v27-validation/taxonomy.json` descriptor_count >= 320.
    - Source assertion: `/tmp/taxonomy-v27-validation/similarity_matrix.json` has a `review_queue` array.
    - Diff assertion: `git diff --quiet -- src/cli/parse_args.ts` exits 0 (DEFAULT_PATHS unchanged).
    - Diff assertion: `git diff --quiet -- data/compiled/v2` exits 0 (official artifacts NOT yet published).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jlima/Projetos/TaxonomySystem/src && node dist/cli/compile.js --out /tmp/taxonomy-v27-validation --version 2.7.0 --quality-report 2>&1 | grep -E "validation_status|quality_gate_status"</automated>
    <automated>python3 -c "import json; d=json.load(open('/tmp/taxonomy-v27-validation/taxonomy.json')); assert d['version']=='2.7.0', f'version={d[\"version\"]}'; assert d['stats']['descriptor_count']>=320, f'descriptors={d[\"stats\"][\"descriptor_count\"]}'; print('PASS:', d['version'], d['stats']['descriptor_count'], 'descriptors')"</automated>
    <automated>git -C /home/jlima/Projetos/TaxonomySystem diff --quiet -- src/cli/parse_args.ts data/compiled/v2</automated>
  </verify>
  <done>Compilation to /tmp validates with PASS, all invariants hold, and official artifacts remain untouched until Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Run existing test suite and safety guard</name>
  <files>None (read-only verification)</files>
  <read_first>
    @src/tests/curation/taxonomy_seed_v2.test.ts
    @src/tests/curation/review_dispositions.test.ts
    @scripts/check-safety-guards.sh
  </read_first>
  <action>Run the existing curation test suite with `cd src && npm run test -- tests/curation/taxonomy_seed_v2.test.ts tests/curation/review_dispositions.test.ts tests/curation/alias_seed_v2.test.ts tests/curation/relation_accord_v2.test.ts`. Then run the safety guard with `cd src && npm run safety:guard`. Confirm all tests pass and the safety guard exits 0. This validates that the seed changes from Phase 42 are consistent and no protected paths have been tampered with before publishing official artifacts.</action>
  <acceptance_criteria>
    - Test-command assertion: `cd src && npm run test -- tests/curation/taxonomy_seed_v2.test.ts tests/curation/review_dispositions.test.ts tests/curation/alias_seed_v2.test.ts tests/curation/relation_accord_v2.test.ts` exits 0.
    - CLI assertion: `cd src && npm run safety:guard` exits 0.
    - Diff assertion: no production source files are modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jlima/Projetos/TaxonomySystem/src && npm run test -- tests/curation/taxonomy_seed_v2.test.ts tests/curation/review_dispositions.test.ts tests/curation/alias_seed_v2.test.ts tests/curation/relation_accord_v2.test.ts</automated>
    <automated>cd /home/jlima/Projetos/TaxonomySystem/src && npm run safety:guard</automated>
  </verify>
  <done>All curation tests pass and safety guard is clean, confirming Phase 42 seed state is ready for official publication.</done>
</task>

<task type="auto">
  <name>Task 3: Publish official v2.7 artifacts to data/compiled/v2/</name>
  <files>data/compiled/v2/taxonomy.json, data/compiled/v2/descriptor_aliases.json, data/compiled/v2/similarity_matrix.json</files>
  <read_first>
    @src/cli/compile.ts
    @src/cli/parse_args.ts
    @src/compiler/write_outputs.ts
    @data/compiled/v2/taxonomy.json
    @.planning/phases/43-taxonomy-v2-7-artifact-publication/43-CONTEXT.md
  </read_first>
  <action>After Task 1 PASS and Task 2 PASS, compile to the official output directory with `cd src && node dist/cli/compile.js --version 2.7.0 --quality-report`. This uses the default `--out data/compiled/v2` path. Verify the compilation exits 0 and the three official artifacts are updated. Confirm: (a) `data/compiled/v2/taxonomy.json` version is `2.7.0`, (b) `data/compiled/v2/descriptor_aliases.json` exists, (c) `data/compiled/v2/similarity_matrix.json` exists. Do NOT modify `src/cli/parse_args.ts` or any input files (seed, aliases, relations, accords, noise, stopwords). Do NOT modify `graphify-out/*`.</action>
  <acceptance_criteria>
    - CLI assertion: `cd src && node dist/cli/compile.js --version 2.7.0 --quality-report` exits 0.
    - Source assertion: `data/compiled/v2/taxonomy.json` contains `"version": "2.7.0"`.
    - Source assertion: `data/compiled/v2/taxonomy.json` `descriptor_count` matches the /tmp validation result from Task 1.
    - Source assertion: `data/compiled/v2/similarity_matrix.json` `review_queue` length matches the /tmp validation result from Task 1.
    - Diff assertion: `git diff --quiet -- src/cli/parse_args.ts` exits 0 (DEFAULT_PATHS unchanged).
    - Diff assertion: `git diff --quiet -- data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json` exits 0 (inputs unchanged by this task).
    - Diff assertion: `git diff --name-only -- data/compiled/v2/` shows exactly `taxonomy.json`, `descriptor_aliases.json`, and `similarity_matrix.json`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('/home/jlima/Projetos/TaxonomySystem/data/compiled/v2/taxonomy.json')); assert d['version']=='2.7.0', f'version={d[\"version\"]}'; print('PASS: v2.7.0,', d['stats']['descriptor_count'], 'descriptors')"</automated>
    <automated>python3 -c "import json; d=json.load(open('/home/jlima/Projetos/TaxonomySystem/data/compiled/v2/similarity_matrix.json')); print('PASS: review_queue', len(d['review_queue']), 'items,', d['stats']['edge_count'], 'edges')"</automated>
    <automated>git -C /home/jlima/Projetos/TaxonomySystem diff --quiet -- src/cli/parse_args.ts data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json</automated>
  </verify>
  <done>Official v2.7 artifacts published to data/compiled/v2/ with version 2.7.0, updated descriptor count, and updated review_queue metrics.</done>
</task>

<task type="auto">
  <name>Task 4: Generate v2.7 closure report with dynamically measured metrics</name>
  <files>.planning/phases/43-taxonomy-v2-7-artifact-publication/v2.7-closure-report.md</files>
  <read_first>
    @data/compiled/v2/taxonomy.json
    @data/compiled/v2/similarity_matrix.json
    @.planning/phases/40-low-support-curation-planning/40-BATCH-SELECTION.md
    @.planning/phases/41-low-support-batch-decision-matrix/41-DECISION-MATRIX.md
    @.planning/phases/42-low-support-microcuration-execution/42-SUMMARY.md
    @.planning/phases/43-taxonomy-v2-7-artifact-publication/43-CONTEXT.md
    @.planning/phases/43-taxonomy-v2-7-artifact-publication/43-RESEARCH.md
  </read_first>
  <action>Read the freshly published `data/compiled/v2/taxonomy.json` to extract: version, family_count, subfamily_count, descriptor_count, generated_at. Read `data/compiled/v2/similarity_matrix.json` to extract: review_queue length, review_queue breakdown by type (low_support vs conflict), edge_count. Read `40-BATCH-SELECTION.md` for batch selection details (30 candidates selected). Read `41-DECISION-MATRIX.md` for disposition summary: count rows by disposition type (promote_to_seed, reject, defer_manual_review, defer_future_batch, needs_external_reference). Read `42-SUMMARY.md` for the 6 applied mutations. Create `.planning/phases/43-taxonomy-v2-7-artifact-publication/v2.7-closure-report.md` with the following sections:

## v2.7 Closure Report structure:
1. **Header** with generated date and milestone reference
2. **Starting State (v2.6 Baseline)**: 10 families, 18 subfamilies, 43 curated seed descriptors, 308 compiled descriptors, review_queue 283 (275 low_support + 8 conflict), 13 graph edges
3. **Triage Batch Details**: 30 candidates selected from 275 low_support in Phase 40, with selection criteria summary
4. **Decision Matrix Summary**: From Phase 41 — count by disposition (promote_to_seed, reject, defer_manual_review, defer_future_batch, needs_external_reference) with the 6 promoted descriptor names and target paths
5. **Applied Mutations (Phase 42)**: Table of 6 seed additions with target family/subfamily paths
6. **Final Metrics (v2.7)**: Dynamically measured from the just-published artifacts — version, families, subfamilies, seed descriptors, compiled descriptors, review_queue total, review_queue by type, graph edges
7. **Delta Summary**: Before/after comparison table
8. **Invariant Validation**: PASS/FAIL status from compilation

All metrics in sections 6 and 7 MUST be read dynamically from the published artifact JSON files — do not hardcode values from the research document.</action>
  <acceptance_criteria>
    - Source assertion: `v2.7-closure-report.md` exists in the phase directory.
    - Source assertion: the report contains a "Starting State" section with v2.6 baseline numbers.
    - Source assertion: the report contains a "Final Metrics" section with values that match `data/compiled/v2/taxonomy.json` and `data/compiled/v2/similarity_matrix.json`.
    - Source assertion: the report contains the 6 promoted descriptors: peppermint, rosemary, cumin, spearmint, caraway, opoponax.
    - Source assertion: the report contains disposition counts from Phase 41.
    - Diff assertion: no data files, source files, or compiled artifacts are modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>test -f /home/jlima/Projetos/TaxonomySystem/.planning/phases/43-taxonomy-v2-7-artifact-publication/v2.7-closure-report.md && echo "PASS: closure report exists"</automated>
    <automated>grep -c "peppermint\|rosemary\|cumin\|spearmint\|caraway\|opoponax" /home/jlima/Projetos/TaxonomySystem/.planning/phases/43-taxonomy-v2-7-artifact-publication/v2.7-closure-report.md</automated>
    <automated>grep "Starting State\|Final Metrics\|Decision Matrix\|Triage Batch\|Delta Summary" /home/jlima/Projetos/TaxonomySystem/.planning/phases/43-taxonomy-v2-7-artifact-publication/v2.7-closure-report.md</automated>
  </verify>
  <done>Formal v2.7 closure report produced with dynamically measured metrics covering the complete v2.7 lifecycle from Phase 40 triage through Phase 43 publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed JSON → compiler | Curated seed data (modified in Phase 42) feeds the compiler pipeline; any seed corruption would propagate to official artifacts. |
| Compiler output → official artifacts | The atomic write in `writeCompileResults()` prevents partial artifact updates; validation gate ensures only valid compilations are published. |
| /tmp validation → official publication | Two-step compilation (validate in /tmp, then publish officially) prevents publishing artifacts that fail invariants. |
| Phase artifact docs → closure report | The closure report reconstructs the v2.7 lifecycle from Phase 40-42 artifacts; incorrect parsing could misrepresent the triage history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | `data/compiled/v2/*` artifacts | mitigate | Tasks 1-3: two-step compilation with /tmp validation before official publication; compiler's built-in schema validation prevents invalid artifact writes. |
| T-43-02 | Tampering | `src/cli/parse_args.ts` DEFAULT_PATHS | mitigate | All tasks: use `--version 2.7.0` flag only; `git diff --quiet -- src/cli/parse_args.ts` verified in Tasks 1 and 3. |
| T-43-03 | Information Disclosure | Closure report metrics | mitigate | Task 4: all metrics read dynamically from published JSON files, not hardcoded; prevents stale/incorrect numbers. |
| T-43-04 | Tampering | Seed/input files | mitigate | Tasks 1 and 3: `git diff --quiet` on seed, alias, relation, accord inputs proves no input file modification occurred. |
| T-43-05 | Tampering | npm/pip/cargo installs | accept | No package installs are in scope; RESEARCH.md Package Legitimacy Audit confirms no external packages are required. |
</threat_model>

<verification>
Run after all tasks in this plan:
- `cd /home/jlima/Projetos/TaxonomySystem/src && npm run test -- tests/curation/taxonomy_seed_v2.test.ts tests/curation/review_dispositions.test.ts tests/curation/alias_seed_v2.test.ts tests/curation/relation_accord_v2.test.ts`
- `cd /home/jlima/Projetos/TaxonomySystem/src && npm run safety:guard`
- `python3 -c "import json; d=json.load(open('/home/jlima/Projetos/TaxonomySystem/data/compiled/v2/taxonomy.json')); assert d['version']=='2.7.0'; assert d['stats']['descriptor_count']>=320; print('PASS:', d['version'], d['stats']['descriptor_count'],'descriptors')"`
- `python3 -c "import json; d=json.load(open('/home/jlima/Projetos/TaxonomySystem/data/compiled/v2/similarity_matrix.json')); print('PASS:', len(d['review_queue']), 'review items,', d['stats']['edge_count'], 'edges')"`
- `git -C /home/jlima/Projetos/TaxonomySystem diff --quiet -- src/cli/parse_args.ts data/taxonomy/taxonomy-seed.v2.json data/taxonomy/descriptor_aliases.seed.json`
- `test -f /home/jlima/Projetos/TaxonomySystem/.planning/phases/43-taxonomy-v2-7-artifact-publication/v2.7-closure-report.md && echo "PASS: closure report exists"`
</verification>

<success_criteria>
- ART-01 is satisfied: taxonomy invariants pass validation with `validation_status=ok` and `quality_gate_status=PASS`.
- ART-02 is satisfied: official v2.7 artifacts are published to `data/compiled/v2/` with version 2.7.0 and updated metrics.
- ART-03 is satisfied: formal closure report covers Starting State, Triage Batch, Decision Matrix Summary, and dynamically measured Final Metrics.
- No input files (seed, aliases, relations, accords, noise, stopwords) are modified.
- `src/cli/parse_args.ts` DEFAULT_PATHS remain unchanged.
- Existing test suite passes.
- Safety guard passes.
</success_criteria>

<output>
Create `.planning/phases/43-taxonomy-v2-7-artifact-publication/43-01-SUMMARY.md` when done.
</output>
