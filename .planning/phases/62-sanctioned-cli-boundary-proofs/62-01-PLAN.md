---
phase: 62-sanctioned-cli-boundary-proofs
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/cli/sanctioned_graph_workflow.ts
  - src/cli/graph_read_model.ts
  - src/tests/cli/sanctioned_graph_workflow.test.ts
autonomous: true
requirements: [GVAL-08, GVAL-10]
must_haves:
  truths:
    - "GVAL-08: the sanctioned non-dry-run workflow can be executed through an internal seam with injectable guardrail execution, without turning the public CLI into a broader API."
    - "GVAL-10: forbidden-path misuse is represented as deterministic boundary behavior before write/guardrails, not only as an uncaught writer exception."
    - "D-01, D-05, D-06, D-07, D-16, D-18, D-19 and D-20 are honored by extracting an internal orchestrator while keeping `graph:build` as a thin public adapter."
  artifacts:
    - "A new internal workflow module under `src/cli/` exports `runSanctionedGraphWorkflow(...)` and supporting types for guardrail execution/results."
    - "`src/tests/cli/sanctioned_graph_workflow.test.ts` proves success ordering, guardrail injection, and forbidden-path rejection boundaries."
    - "`src/cli/graph_read_model.ts` delegates to the internal workflow and preserves current public flags/JSON shape."
  key_links:
    - "sanctioned_graph_workflow.ts -> write_graph.ts via `validateOutputPath` and/or `writeGraphOutput`."
    - "sanctioned_graph_workflow.ts -> boundary_audit.ts via `discoverProtectedFiles`, `capturePreDigests`, and `runBoundaryAudit`."
    - "graph_read_model.ts -> sanctioned_graph_workflow.ts as thin CLI composition, not a second orchestration implementation."
---

<objective>
Extract the sanctioned graph build workflow into an internal, testable boundary without broadening the public CLI contract.

Purpose: create the seam Phase 62 needs for sandboxed non-dry-run proofing, deterministic forbidden-path handling, and injectable guardrail execution.
Output: new internal workflow module, updated CLI wiring, and Wave 0 tests that prove orchestration order and typed failure boundaries.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-sanctioned-cli-boundary-proofs/62-CONTEXT.md
@.planning/phases/62-sanctioned-cli-boundary-proofs/62-RESEARCH.md
@.planning/phases/62-sanctioned-cli-boundary-proofs/62-VALIDATION.md
@.planning/phases/61-fail-closed-query-consumption/61-CONTEXT.md
@src/cli/graph_read_model.ts
@src/graph_read_model/write_graph.ts
@src/graph_read_model/boundary_audit.ts
@src/graph_read_model/contract.ts
@src/tests/cli/graph_read_model.test.ts
@src/tests/graph_read_model/write_graph.test.ts
@src/tests/graph_read_model/boundary_audit.test.ts

<interfaces>
Existing source contracts the executor must respect:
- Public CLI flags remain `--json`, `--help`, `--dry-run`, `--skip-guardrails`.
- `writeGraphOutput(graph, outputDir)` and `validateOutputPath(outputDir)` remain the output policy authority.
- `runBoundaryAudit(outputWritten, baseDir, preDigests)` remains the production proof of protected-file integrity.
- `runGuardrails` currently encodes the sanctioned order `typecheck -> test -> alias:integrity -> verify:integrity`.

New Phase 62 contracts to create:
- `runSanctionedGraphWorkflow({ outputDir, dryRun, skipGuardrails, guardrailExecutor, baseDir? })`.
- Functional `GuardrailExecutor` dependency and typed guardrail definitions/results.
- Internal result union that distinguishes sanctioned success, forbidden-path rejection, guardrail failure, and other deterministic failure modes.
- Internal-only CLI composition options for `runGraphBuildCli(args, { workflowRunner, sanctionedOutputDir, stdout, stderr })` so tests can bind a sandbox destination and stream sinks without adding public parser flags.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed internal workflow boundary tests before extraction</name>
  <files>src/tests/cli/sanctioned_graph_workflow.test.ts</files>
  <read_first>
    - .planning/phases/62-sanctioned-cli-boundary-proofs/62-CONTEXT.md
    - .planning/phases/62-sanctioned-cli-boundary-proofs/62-VALIDATION.md
    - src/cli/graph_read_model.ts
    - src/graph_read_model/write_graph.ts
    - src/graph_read_model/boundary_audit.ts
    - src/tests/cli/graph_read_model.test.ts
    - src/tests/graph_read_model/write_graph.test.ts
  </read_first>
  <behavior>
    - The internal workflow exposes a deterministic success/failure result boundary suitable for direct tests.
    - Forbidden-path inputs fail before guardrails and before any write.
    - Guardrail execution order is observable and remains `typecheck`, `test`, `alias:integrity`, `verify:integrity`.
  </behavior>
  <action>Create `src/tests/cli/sanctioned_graph_workflow.test.ts` before implementing the extracted workflow. Add tests that import a not-yet-created `runSanctionedGraphWorkflow` and cover: `returns forbidden_path before invoking guardrails for a forbidden output dir`, `requests guardrails in sanctioned order`, and `stops after the first failing guardrail`. Use spies or injected stub executors instead of shelling out directly. The `forbidden_path` and `guardrail_failed` tests must explicitly assert `expect(writeGraphOutput).not.toHaveBeenCalled()` through an appropriate module spy or writer dependency so fail-closed ordering is proven, not inferred from artifact absence. Keep this file free of `graphify-out/**` snapshots for Wave 0; that measured harness belongs to Wave 1. Do not add a public `--out` flag to satisfy these tests.</action>
  <verify>
    <automated>test -f src/tests/cli/sanctioned_graph_workflow.test.ts</automated>
    <automated>rg -n "forbidden_path|sanctioned order|failing guardrail" src/tests/cli/sanctioned_graph_workflow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `sanctioned_graph_workflow.test.ts` imports `runSanctionedGraphWorkflow` from a new `src/cli/` internal module, not from the public CLI file.
    - Behavior assertion: a forbidden path such as `graphify-out/export` produces a result branch or reason named `forbidden_path`.
    - Behavior assertion: `forbidden_path` explicitly proves `writeGraphOutput` was not called.
    - Behavior assertion: the injected executor observes exactly four requested guardrail names in the sanctioned order on success.
    - Behavior assertion: a first failing guardrail prevents downstream guardrails from running.
    - Behavior assertion: `guardrail_failed` explicitly proves `writeGraphOutput` was not called.
    - Scope assertion: no test in this task introduces `--out` parsing or query-consumer shortcuts through the CLI.
  </acceptance_criteria>
  <done>Wave 0 has a failing contract for the internal workflow boundary before production extraction starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract `runSanctionedGraphWorkflow(...)` into `src/cli/`</name>
  <files>src/cli/sanctioned_graph_workflow.ts, src/cli/graph_read_model.ts, src/tests/cli/sanctioned_graph_workflow.test.ts</files>
  <read_first>
    - .planning/phases/62-sanctioned-cli-boundary-proofs/62-CONTEXT.md
    - .planning/phases/62-sanctioned-cli-boundary-proofs/62-RESEARCH.md
    - src/cli/graph_read_model.ts
    - src/graph_read_model/write_graph.ts
    - src/graph_read_model/boundary_audit.ts
    - src/graph_read_model/contract.ts
    - src/tests/cli/sanctioned_graph_workflow.test.ts
  </read_first>
  <behavior>
    - The new internal workflow validates the output path before loading inputs, guardrails, and any write.
    - Guardrails run after graph validation and before `writeGraphOutput`, so a failing guardrail cannot leave a partial sandbox `graph.json`.
    - The public CLI becomes a thin adapter that resolves sanctioned paths, selects JSON/non-JSON rendering, and delegates orchestration.
    - The workflow result surface is typed and deterministic for both success and forbidden-path failure.
  </behavior>
  <action>Create `src/cli/sanctioned_graph_workflow.ts` and move the main orchestration there. Implement the sanctioned non-dry-run order as: `validateOutputPath` -> `capturePreDigests` -> load inputs -> build -> validate graph -> guardrails -> `writeGraphOutput` -> boundary audit -> success. In `dry-run`, stop after graph validation and guardrails and never call the writer. Introduce a functional `GuardrailExecutor` plus typed guardrail definitions/results so tests can inject behavior without monkeypatching `execFileSync`. Convert forbidden-path rejection into a typed internal result branch at the workflow boundary, while keeping `validateOutputPath` / `writeGraphOutput` as the single output-policy authority. Update `src/cli/graph_read_model.ts` to call the new workflow and preserve all current public flags, stdout markers, and JSON top-level shape on success. Add an internal-only composition seam such as `runGraphBuildCli(args, { workflowRunner, sanctionedOutputDir, stdout, stderr })`; production composition must always inject the canonical sanctioned destination, while tests may inject the sandbox destination through this dependency and never through parser support. Do not add `--out`, do not expose query commands, and do not add new public JSON fields for Graphify measurement in this task.</action>
  <verify>
    <automated>npm --prefix src run typecheck</automated>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/cli/sanctioned_graph_workflow.test.ts tests/cli/graph_read_model.test.ts tests/graph_read_model/write_graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `runGraphBuildCli` delegates to `runSanctionedGraphWorkflow(...)` instead of reimplementing the workflow inline.
    - Source assertion: `runGraphBuildCli` accepts internal dependency injection for workflow runner, sanctioned destination, and stream sinks without exposing those knobs as public CLI flags.
    - Source assertion: the internal workflow module exports a `GuardrailExecutor`-style dependency and typed guardrail/result contracts.
    - Behavior assertion: forbidden-path rejection happens before guardrails and before `graph.json` creation.
    - Behavior assertion: guardrail failure happens before `writeGraphOutput`, and tests assert the writer was not called.
    - Behavior assertion: CLI `--json` success output still serializes `ok`, `graph_output`, `boundary_audit`, and `guardrails` without new Phase 62-specific top-level fields.
    - Scope assertion: no new public flag named `--out` exists, and `graph:build` still does not expose query functions.
    - Command assertion: `npm --prefix src run typecheck` passes.
    - Command assertion: `env TMPDIR=/tmp npm --prefix src test -- tests/cli/sanctioned_graph_workflow.test.ts tests/cli/graph_read_model.test.ts tests/graph_read_model/write_graph.test.ts` passes.
  </acceptance_criteria>
  <done>The CLI has a testable internal workflow seam that preserves public behavior and deterministic forbidden-path handling.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Prove sanctioned non-dry-run write path in safe sandbox | 62-01, 62-02 | COVERED | Wave 0 creates the internal seam; Wave 1 proves it in sandbox.
REQ | GVAL-08 | Automated sandboxed proof of sanctioned non-dry-run workflow | 62-01, 62-02 | COVERED | Internal workflow + hybrid executor unlock safe sandbox execution.
REQ | GVAL-10 | Deterministic proof outputs including forbidden-path rejection | 62-01, 62-02 | COVERED | Typed boundary result plus CLI translation preserve deterministic behavior.
RESEARCH | Internal orchestrator + thin CLI | 62-01 | COVERED | New `src/cli/` workflow seam.
RESEARCH | Guardrail injection without recursion | 62-01, 62-02 | COVERED | Typed executor added in W0, hybrid evidence used in W1.
CONTEXT | D-01 D-05 | `runSanctionedGraphWorkflow({ outputDir, dryRun, guardrailExecutor })` | 62-01 | COVERED | Internal workflow extraction centers on this seam.
CONTEXT | D-07 | Preserve the sanctioned four-guardrail order | 62-01 | COVERED | Tests assert order and stop-on-failure behavior.
CONTEXT | D-16 D-18 D-19 D-20 | No public `--out`; pre-write forbidden-path handling | 62-01 | COVERED | Public CLI stays thin while tests hit forbidden paths through the internal workflow.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public CLI -> internal workflow | The CLI must not broaden its contract while delegating orchestration. |
| workflow -> output path policy | Forbidden destinations must fail before side effects occur. |
| workflow -> guardrail executor | Tests need a safe injection seam without altering production behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Elevation of Privilege | public CLI output selection | mitigate | Keep `--out` absent; sanctioned path is still composed only by the public CLI. |
| T-62-02 | Tampering | forbidden-path writes | mitigate | Validate output destination before guardrails and before any write, then return a typed `forbidden_path` branch. |
| T-62-03 | Repudiation | guardrail execution order | mitigate | Make requested guardrails explicit data and assert exact ordering/stoppage in tests. |
| T-62-SC | Tampering | package/runtime scope creep | mitigate | No new dependencies; keep work inside existing TypeScript/Vitest stack. |
</threat_model>

<verification>
Plan-level checks:
- `npm --prefix src run typecheck`
- `env TMPDIR=/tmp npm --prefix src test -- tests/cli/sanctioned_graph_workflow.test.ts tests/cli/graph_read_model.test.ts tests/graph_read_model/write_graph.test.ts`
</verification>

<success_criteria>
- There is a single internal workflow seam for sanctioned build/validate/write/audit/guardrail orchestration.
- Forbidden-path misuse fails deterministically before guardrails and before any artifact write.
- The public CLI remains thin and preserves its current success JSON/flag surface.
</success_criteria>

<output>
Create `.planning/phases/62-sanctioned-cli-boundary-proofs/62-01-SUMMARY.md` when done.
</output>
