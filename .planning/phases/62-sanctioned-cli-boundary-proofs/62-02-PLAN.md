---
phase: 62-sanctioned-cli-boundary-proofs
plan: 02
type: execute
wave: 1
depends_on: [62-01]
files_modified:
  - src/tests/cli/sanctioned_graph_workflow.test.ts
  - src/tests/cli/graph_read_model.test.ts
  - src/tests/helpers/directory_snapshot.ts
autonomous: true
requirements: [GVAL-08, GVAL-09, GVAL-10]
must_haves:
  truths:
    - "GVAL-08: the sanctioned non-dry-run graph workflow is proven in a sandbox with a real artifact write, protected-file audit evidence, and pre-write guardrails requested in the sanctioned order."
    - "GVAL-09: `graphify-out/**` isolation is backed by measured pre/post directory snapshots rather than only the declarative `graphify_out_accesses: 0` field."
    - "GVAL-10: deterministic evidence covers sanctioned output destination, protected-file integrity, and forbidden-path rejection behavior without inventing a new public CLI contract."
  artifacts:
    - "`src/tests/helpers/directory_snapshot.ts` (or equivalent test helper) captures deterministic directory inventories and aggregate SHA-256 snapshots."
    - "`src/tests/cli/sanctioned_graph_workflow.test.ts` contains sandboxed non-dry-run success, graphify isolation, forbidden-path, and guardrail-failure proofs."
    - "`src/tests/cli/graph_read_model.test.ts` preserves the public CLI contract: no `--out`, stable `--json`, and sanctioned output messaging."
  key_links:
    - "sandbox workflow test -> query_consumer.ts via `asValidatedGraph -> createValidatedQueryConsumer` reentry on sandbox `graph.json`."
    - "sandbox workflow test -> directory_snapshot helper to compare `graphify-out/**` before/after."
    - "public CLI tests -> internal `runGraphBuildCli(args, { workflowRunner, sanctionedOutputDir, stdout, stderr })` seam to bind the same sandboxed run to process output, file output, and measured snapshots without broadening flags."
---

<objective>
Prove the sanctioned CLI boundary end-to-end in a safe sandbox, with measured Graphify isolation and deterministic evidence.

Purpose: complete the proof layer around the internal workflow extracted in Wave 0 without broadening the public CLI contract.
Output: sandbox harness tests, deterministic directory snapshots, public CLI regression checks, and reentry proof through the validated query consumer.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-sanctioned-cli-boundary-proofs/62-CONTEXT.md
@.planning/phases/62-sanctioned-cli-boundary-proofs/62-RESEARCH.md
@.planning/phases/62-sanctioned-cli-boundary-proofs/62-VALIDATION.md
@.planning/phases/62-sanctioned-cli-boundary-proofs/62-01-SUMMARY.md
@.planning/phases/61-fail-closed-query-consumption/61-CONTEXT.md
@src/cli/sanctioned_graph_workflow.ts
@src/cli/graph_read_model.ts
@src/graph_read_model/boundary_audit.ts
@src/graph_read_model/query_consumer.ts
@src/tests/cli/sanctioned_graph_workflow.test.ts
@src/tests/cli/graph_read_model.test.ts
@src/tests/graph_read_model/live_artifact_baseline.test.ts
@src/tests/graph_read_model/query_live_baseline.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic `graphify-out/**` snapshot helper and sandbox success proof</name>
  <files>src/tests/helpers/directory_snapshot.ts, src/tests/cli/sanctioned_graph_workflow.test.ts</files>
  <read_first>
    - .planning/phases/62-sanctioned-cli-boundary-proofs/62-CONTEXT.md
    - .planning/phases/62-sanctioned-cli-boundary-proofs/62-RESEARCH.md
    - src/cli/sanctioned_graph_workflow.ts
    - src/graph_read_model/boundary_audit.ts
    - src/tests/graph_read_model/boundary_audit.test.ts
    - src/tests/graph_read_model/query_live_baseline.test.ts
  </read_first>
  <behavior>
    - Missing `graphify-out/**` is treated as a deterministic empty snapshot.
    - Any file creation/removal/rename/content change under `graphify-out/**` would fail the sandbox proof.
    - Successful sandbox run writes only to the allowed sandbox output path and leaves `graphify-out/**` unchanged.
    - The same non-dry-run invocation supplies the artifact path, protected-file audit evidence, guardrail evidence, stdout/stderr, exit code, and `graphify-out/**` measurements.
  </behavior>
  <action>Create a test-only helper such as `src/tests/helpers/directory_snapshot.ts` that walks a directory deterministically, normalizes relative paths to forward slashes, captures `sha256`, `size_bytes`, and an `aggregate_sha256`, and treats absent directories as an empty valid state. Expand `sanctioned_graph_workflow.test.ts` with a non-dry-run sandbox proof that snapshots `graphify-out/**` before and after one invocation of `runGraphBuildCli(args, { workflowRunner, sanctionedOutputDir, stdout, stderr })` using the internal composition seam from Wave 0. The test must inject the sandbox destination through `sanctionedOutputDir`, not by parsing `--out`, and must assert in the same run: `exitCode === 0`, empty `stderr`, current stdout/JSON markers, `graph.json` exists only in the allowed sandbox location, pre/post `graphify-out/**` snapshots are identical, and the sandbox artifact can be parsed and reentered through `asValidatedGraph -> createValidatedQueryConsumer`. The test must not mutate production `data/read-models/olfactory-graph/v2.11/` or touch protected taxonomy/compiled inputs.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/cli/sanctioned_graph_workflow.test.ts tests/graph_read_model/query_live_baseline.test.ts tests/graph_read_model/boundary_audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `graphify-out/**` pre/post snapshots are exactly equal for a successful sandbox run.
    - Behavior assertion: `exitCode === 0`, empty `stderr`, stdout/JSON assertions, sandbox `graph.json`, boundary-audit evidence, guardrail evidence, and measured snapshots all come from the same non-dry-run sandbox invocation.
    - Behavior assertion: successful sandbox output path ends with `graph.json`, exists only under the allowed sandbox destination, and is readable JSON.
    - Behavior assertion: sandbox `graph.json` validates via `asValidatedGraph` and supports `createValidatedQueryConsumer` without touching public CLI query scope.
    - Scope assertion: the helper lives in test scope only and introduces no new public CLI JSON fields.
  </acceptance_criteria>
  <done>Measured Graphify isolation and sandbox artifact reentry are proven with deterministic test-only instrumentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove hybrid guardrail evidence and deterministic failure branches</name>
  <files>src/tests/cli/sanctioned_graph_workflow.test.ts</files>
  <read_first>
    - .planning/phases/62-sanctioned-cli-boundary-proofs/62-CONTEXT.md
    - src/cli/sanctioned_graph_workflow.ts
    - src/tests/cli/sanctioned_graph_workflow.test.ts
  </read_first>
  <behavior>
    - Hybrid executor runs three guardrails for real and intercepts only `test` with explicit evidence.
    - Guardrail failure stops the workflow before `writeGraphOutput`, before boundary audit success, and before any success proof is emitted.
    - Forbidden-path misuse produces deterministic no-artifact behavior.
  </behavior>
  <action>In `sanctioned_graph_workflow.test.ts`, implement a hybrid executor used only by sandbox tests: real execution for `typecheck`, `alias:integrity`, and `verify:integrity`; explicit injected evidence for `test` (for example `execution_mode: 'injected_test_evidence'`, `reason: 'recursive test invocation prevented'`). Add assertions that the workflow requested all four guardrails in order before writing, that only `test` was intercepted, and that the result surfaces enough deterministic evidence to distinguish sanctioned success, guardrail failure, and forbidden-path rejection. For failure cases, assert `graph.json` is absent, stdout does not claim success, `writeGraphOutput` was not called for `guardrail_failed` and `forbidden_path`, and no Graphify/protected-file mutation occurred.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/cli/sanctioned_graph_workflow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: hybrid executor records `typecheck`, `test`, `alias:integrity`, `verify:integrity` in exact order.
    - Behavior assertion: `test` interception is explicit and does not masquerade as a real recursive suite run.
    - Behavior assertion: guardrail failure path exits before `writeGraphOutput`, without success markers and without a persisted `graph.json`.
    - Behavior assertion: forbidden-path failure also exits before `writeGraphOutput`, without artifact creation or Graphify mutation.
  </acceptance_criteria>
  <done>Sandbox proof covers both the safe success path and deterministic failure branches with explicit guardrail evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock public CLI regression markers while preserving thin-surface behavior</name>
  <files>src/tests/cli/graph_read_model.test.ts, src/tests/cli/sanctioned_graph_workflow.test.ts</files>
  <read_first>
    - .planning/phases/62-sanctioned-cli-boundary-proofs/62-CONTEXT.md
    - src/cli/graph_read_model.ts
    - src/tests/cli/graph_read_model.test.ts
    - docs/olfactory_graph_read_model.md
  </read_first>
  <behavior>
    - Public CLI still exposes no `--out`.
    - Public `--json` output remains parseable and does not gain invented Phase 62 top-level fields.
    - Human-readable stdout/stderr checks use stable markers rather than brittle full-text snapshots.
    - Internal CLI composition can be tested with sandbox injection while production public composition still always uses the canonical sanctioned output directory.
  </behavior>
  <action>Expand `src/tests/cli/graph_read_model.test.ts` only where needed to lock the thin public boundary: keep asserting the absence of `--out`, keep `--json` parseable with the same top-level shape, and add stable-marker assertions for success/failure paths if the extracted CLI adapter changed rendering. Add a composition test that invokes `runGraphBuildCli(args, { workflowRunner, sanctionedOutputDir, stdout, stderr })` and proves the parser did not accept an output flag while the internal dependency injection still routes the same sandbox run to the requested sandbox destination. Do not snapshot entire human-readable output; assert targeted substrings such as sanctioned output path presence, `graph:build complete`, deterministic error prefixes, and the absence of false success markers on failure.</action>
  <verify>
    <automated>npm --prefix src run typecheck</automated>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/cli/graph_read_model.test.ts tests/cli/sanctioned_graph_workflow.test.ts tests/graph_read_model/live_artifact_baseline.test.ts tests/graph_read_model/query_live_baseline.test.ts tests/graph_read_model/write_graph.test.ts tests/graph_read_model/boundary_audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: no new `--out` public flag exists.
    - Behavior assertion: CLI success JSON remains parseable with top-level `ok`, `graph_output`, `boundary_audit`, and `guardrails`.
    - Behavior assertion: the sanctioned sandbox CLI proof binds `exitCode`, `stdout`, `stderr`, sandbox file output, boundary-audit result, guardrail evidence, and `graphify-out/**` measurements to one execution.
    - Behavior assertion: CLI success/failure checks rely on stable markers, not complete text snapshots.
    - Scope assertion: no query commands, runtime services, DB/Neo4J integrations, or Graphify production coupling are introduced.
    - Command assertion: the full phase validation command passes.
  </acceptance_criteria>
  <done>The public CLI surface stays intentionally narrow while the new proof harness closes the Phase 62 evidence gap.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Prove sanctioned write path, boundary audit, and Graphify isolation in sandbox | 62-02 | COVERED | All three concerns are tied together in the sandbox harness.
REQ | GVAL-08 | Automated sandboxed proof of sanctioned non-dry-run workflow | 62-02 | COVERED | Sandbox success path runs guardrails before write, writes artifact only after guardrails pass, audits protected files, and ties process output to the same execution.
REQ | GVAL-09 | Measured Graphify isolation evidence | 62-02 | COVERED | Deterministic directory snapshot helper and equality assertions.
REQ | GVAL-10 | Deterministic boundary-audit proof outputs incl. forbidden path | 62-02 | COVERED | Success/failure evidence checks include output path, protected-file integrity, and forbidden-path behavior.
RESEARCH | Test-only measured snapshot helper | 62-02 | COVERED | Keeps production JSON stable while satisfying measured evidence.
RESEARCH | Hybrid guardrail executor with explicit intercepted `test` proof | 62-02 | COVERED | Three real runs + one explicit interception.
CONTEXT | D-08 D-09 D-24 | Explicit recursive-test interception proof | 62-02 | COVERED | Harness records interception rather than pretending to run `npm test`.
CONTEXT | D-11 D-12 D-13 D-14 D-15 | Deterministic Graphify measurement | 62-02 | COVERED | Snapshot helper treats absent directories as valid empty state and fails on any mutation.
CONTEXT | D-21 D-22 D-23 D-25 D-26 | Stable success/failure evidence without new public contract | 62-02 | COVERED | Tests bind exit code, stderr/stdout markers, sandbox artifact checks, guardrail evidence, boundary audit, and measured snapshots to one run.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandbox workflow -> filesystem | The proof must write only to the allowed sandbox destination and leave protected/forbidden paths untouched. |
| workflow -> guardrail evidence | The harness must prove requested guardrails truthfully, especially the intercepted `test` step. |
| sandbox artifact -> validated query consumer | Generated `graph.json` remains raw data and must reenter the sanctioned query boundary explicitly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-04 | Tampering | `graphify-out/**` | mitigate | Compare deterministic pre/post directory snapshots and fail on any delta. |
| T-62-05 | Repudiation | hybrid guardrail evidence | mitigate | Record exact guardrail names, order, exit codes, and explicit injected-test metadata. |
| T-62-06 | Tampering | sandbox artifact reentry | mitigate | Validate generated `graph.json` and route it through `asValidatedGraph -> createValidatedQueryConsumer`. |
| T-62-07 | Information Disclosure / Elevation of Privilege | public CLI surface creep | mitigate | Keep no `--out`, no query commands, and no new public JSON contract fields beyond existing shape. |
</threat_model>

<verification>
Overall phase checks:
- `npm --prefix src run typecheck`
- `env TMPDIR=/tmp npm --prefix src test -- tests/cli/graph_read_model.test.ts tests/cli/sanctioned_graph_workflow.test.ts tests/graph_read_model/live_artifact_baseline.test.ts tests/graph_read_model/query_live_baseline.test.ts tests/graph_read_model/write_graph.test.ts tests/graph_read_model/boundary_audit.test.ts`
</verification>

<success_criteria>
- Automated tests execute the sanctioned non-dry-run graph workflow in a sandbox without mutating protected taxonomy or compiled inputs.
- Boundary-audit outputs and failure branches provide deterministic evidence for sanctioned destination, protected-file integrity, and forbidden-path rejection.
- The sanctioned sandbox CLI proof ties process behavior, stdout/stderr, file output, guardrail evidence, boundary audit, and measurements to the same execution.
- `graphify-out/**` isolation is proven by measured snapshots, not only by declarative zero-access reporting.
</success_criteria>

<output>
Create `.planning/phases/62-sanctioned-cli-boundary-proofs/62-02-SUMMARY.md` when done.
</output>
