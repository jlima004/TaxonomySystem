---
phase: 60-contract-constants-validation-hardening
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - src/graph_read_model/graph_id.ts
  - src/tests/graph_read_model/graph_id.test.ts
autonomous: true
requirements: [GCON-05, GCON-06, GVAL-06]
must_haves:
  truths:
    - "GCON-05/GCON-06: graph ID construction, guards, stripping and parsing have one typed boundary backed by contract constants."
    - "D-01 D-02 D-03 D-04 are covered by the new graph_id.ts API and deterministic tests."
  artifacts:
    - "src/graph_read_model/graph_id.ts exports makeFamilyGraphId, makeSubfamilyGraphId, makeDescriptorGraphId, makeAliasGraphId, stripGraphIdPrefix, isFamilyGraphId, isSubfamilyGraphId, isDescriptorGraphId, isAliasGraphId, and parseGraphId."
    - "src/tests/graph_read_model/graph_id.test.ts proves success and typed failure behavior."
  key_links:
    - "graph_id.ts imports GRAPH_ID_PREFIXES/contract-derived prefix types from contract.ts and no caller-local prefix literals."
---

<objective>
Criar o boundary central de `graph_id` exigido pela Phase 60.

Purpose: impedir montagem/parsing divergente de IDs antes da migracao de builder, validator e query.
Output: novo modulo `graph_id.ts` com API oficial de IDs e teste unitario dedicado.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
@.planning/phases/60-contract-constants-validation-hardening/60-RESEARCH.md
@.planning/phases/60-contract-constants-validation-hardening/60-VALIDATION.md
@.planning/phases/60-contract-constants-validation-hardening/60-PATTERNS.md
@.planning/phases/60-contract-constants-validation-hardening/60-01-SUMMARY.md
@src/graph_read_model/contract.ts
@src/graph_read_model/types.ts
@src/graph_read_model/build_graph.ts
@src/graph_read_model/query_graph.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add graph_id makers, guards and strip helper</name>
  <files>src/graph_read_model/graph_id.ts, src/tests/graph_read_model/graph_id.test.ts</files>
  <read_first>
    - .planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
    - .planning/phases/60-contract-constants-validation-hardening/60-PATTERNS.md
    - src/graph_read_model/contract.ts
    - src/graph_read_model/build_graph.ts
    - src/graph_read_model/query_graph.ts
  </read_first>
  <behavior>
    - Makers return exact existing ID strings: `family:*`, `subfamily:*`, `descriptor:*`, and `alias:*`.
    - Type guards accept only IDs with the matching authoritative prefix and a non-empty raw ID.
    - `stripGraphIdPrefix(graphId)` reuses central parsing and returns the raw ID for valid graph IDs or `null` for invalid graph IDs.
  </behavior>
  <action>Create `src/graph_read_model/graph_id.ts`. Implement and export `makeFamilyGraphId`, `makeSubfamilyGraphId`, `makeDescriptorGraphId`, `makeAliasGraphId`, `stripGraphIdPrefix`, `isFamilyGraphId`, `isSubfamilyGraphId`, `isDescriptorGraphId`, and `isAliasGraphId`. Use only `GRAPH_ID_PREFIXES` and contract-derived types from `contract.ts`; do not duplicate prefix arrays locally. Create `src/tests/graph_read_model/graph_id.test.ts` with exact maker/guard/strip assertions for family, subfamily, descriptor and alias IDs.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/graph_id.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `graph_id.ts` imports graph ID prefix data from `./contract.js`.
    - Behavior assertion: `makeSubfamilyGraphId('floral_rose')` returns `subfamily:floral_rose`.
    - Behavior assertion: each `is*GraphId` rejects empty raw IDs such as `family:`.
    - Traceability assertion: D-01 and D-02 are named in test descriptions or comments.
  </acceptance_criteria>
  <done>The graph ID maker/guard/strip surface exists and is backed by contract prefixes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic parseGraphId union and JSON-safe failures</name>
  <files>src/graph_read_model/graph_id.ts, src/tests/graph_read_model/graph_id.test.ts</files>
  <read_first>
    - .planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
    - src/graph_read_model/contract.ts
    - src/graph_read_model/types.ts
    - src/graph_read_model/graph_id.ts
    - src/tests/graph_read_model/graph_id.test.ts
  </read_first>
  <behavior>
    - `parseGraphId` returns a discriminated union and never throws generic errors for malformed input.
    - Unknown prefix, empty raw ID and ambiguous format produce deterministic JSON-safe typed error payloads.
    - Success results include the resolved kind, prefix, raw ID and original graph ID.
  </behavior>
  <action>In `graph_id.ts`, add `GraphIdKind`, `ParsedGraphId`, `GraphIdParseError`, `GraphIdParseResult`, and `parseGraphId(graphId: string)`. Failure codes must come from the contract parse vocabulary and include `empty_graph_id`, `unknown_graph_id_prefix`, and `ambiguous_graph_id_format`. Failure payloads must contain stable `code`, `message`, `expected`, and `actual` fields typed as JSON-safe values. Add tests for valid IDs, `''`, `'family:'`, `'material:rose'`, and ambiguous values containing more than one known graph prefix token.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/graph_id.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `parseGraphId('descriptor:cedarwood')` returns `{ ok: true, kind: 'descriptor', prefix: 'descriptor:', raw_id: 'cedarwood', graph_id: 'descriptor:cedarwood' }` or the same fields in an equivalent readonly object.
    - Behavior assertion: `parseGraphId('')`, `parseGraphId('family:')`, and `parseGraphId('material:cedar')` return `{ ok: false, error: ... }`, not throws.
    - Payload assertion: parse failures do not include `undefined`, functions, stack traces, `Date`, `Map`, `Set`, `BigInt`, or classes.
    - Traceability assertion: D-03 and D-04 are cited in test names or acceptance comments.
  </acceptance_criteria>
  <done>`parseGraphId` is a deterministic typed boundary ready for builder, validator and query migration.</done>
</task>

</tasks>

<must_haves>
- GOAL: IDs do grafo passam a ter um ponto unico de construcao, parsing e remocao de prefixo.
- REQ GCON-05: prefixes continuam normativos em `contract.ts`.
- REQ GCON-06: o novo helper cria a superficie que remove templates/regex locais em planos posteriores.
- REQ GVAL-06: IDs invalidos podem ser reportados com erros deterministicos JSON-safe.
- CONTEXT: D-01, D-02, D-03 e D-04 estao cobertos nesta camada.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external/query parameter -> graph ID helper | Future consumers may pass malformed graph IDs into parser/guards. |
| graph ID helper -> builder/query/validator | Downstream modules will trust maker/parse semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03 | Tampering | `parseGraphId` | mitigate | Return typed failure objects for unknown prefix, empty raw ID and ambiguous format; no generic throws. |
| T-60-04 | Spoofing | graph ID type guards | mitigate | Reject empty raw IDs and mismatched prefixes through shared parser semantics. |
| T-60-SC | Tampering | npm installs | mitigate | No package install tasks; continue zero-dependency TypeScript. |
</threat_model>

<verification>
Plan-level checks:
- `env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/graph_id.test.ts`
- `npm --prefix src run typecheck`
</verification>

<success_criteria>
- The required `graph_id.ts` API exists exactly by name.
- Tests prove maker, guard, strip and parse behavior including malformed inputs.
- No builder/query/validator migration is performed in this plan; that remains layer 5.
</success_criteria>

<output>
Create `.planning/phases/60-contract-constants-validation-hardening/60-02-SUMMARY.md` when done.
</output>
