---
phase: 60-contract-constants-validation-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph_read_model/contract.ts
  - src/graph_read_model/types.ts
  - src/tests/graph_read_model/contract.test.ts
autonomous: true
requirements: [GCON-05, GVAL-06]
must_haves:
  truths:
    - "GCON-05: contract.ts is the authoritative source for validation error codes, invariant IDs, graph ID parse vocabulary, and the sanctioned v2.11 validation profile."
    - "GVAL-06: validation payload types support deterministic JSON-safe expected/actual fields without changing the base error shape."
    - "D-05 D-06 D-07 D-08 D-12 D-14 D-15 are covered by contract/type exports and tests."
  artifacts:
    - "src/graph_read_model/contract.ts exports GRAPH_VALIDATION_ERROR_CODES, GRAPH_INVARIANT_IDS, GRAPH_VALIDATION_CODE_TO_INVARIANT_ID, GRAPH_ID_PARSE_ERROR_CODES, SANCTIONED_V2_11_GRAPH_VALIDATION_PROFILE, and derived union types."
    - "src/graph_read_model/types.ts exports JsonValue, typed GraphValidationError fields, and a typed GraphValidationProfile surface."
    - "src/tests/graph_read_model/contract.test.ts locks exact vocabulary/profile values."
  key_links:
    - "contract.ts -> types.ts via GraphValidationErrorCode, GraphInvariantId, GraphValidationProfileId, and JsonValue-compatible validation payloads."
---

<objective>
Centralizar contrato, tipos e vocabularios autoritativos para a Phase 60 antes de qualquer migracao de codigo consumidor.

Purpose: reduzir drift de constantes e preparar as superficies estaveis que builder, validator, query e CLI vao consumir nas ondas seguintes.
Output: `contract.ts`, `types.ts` e `contract.test.ts` com vocabulario de validacao, invariantes, profile sancionado v2.11 e payloads JSON-safe.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
@.planning/phases/60-contract-constants-validation-hardening/60-RESEARCH.md
@.planning/phases/60-contract-constants-validation-hardening/60-VALIDATION.md
@.planning/phases/60-contract-constants-validation-hardening/60-PATTERNS.md
@src/graph_read_model/contract.ts
@src/graph_read_model/types.ts
@src/tests/graph_read_model/contract.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock authoritative validation vocabularies and sanctioned profile</name>
  <files>src/graph_read_model/contract.ts, src/tests/graph_read_model/contract.test.ts</files>
  <read_first>
    - .planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
    - .planning/phases/60-contract-constants-validation-hardening/60-PATTERNS.md
    - src/graph_read_model/contract.ts
    - src/tests/graph_read_model/contract.test.ts
  </read_first>
  <behavior>
    - Contract exports the complete observable validation error vocabulary required by D-05 while preserving existing codes per D-07.
    - Contract exports invariant IDs and a stable optional code-to-invariant mapping per D-06 and D-08.
    - Contract exports `SANCTIONED_V2_11_GRAPH_VALIDATION_PROFILE` with a stable `profile_id`, `schema_version`, and baseline `10/18/341/18/13` per D-12.
  </behavior>
  <action>Add `GRAPH_VALIDATION_ERROR_CODES` with at least `invalid_schema_version`, `duplicate_node_id_detection`, `duplicate_edge_id_detection`, `missing_edge_endpoints`, `wrong_endpoint_kinds`, `invalid_alias_targets`, `invalid_subfamily_similarity_endpoints`, `inconsistent_stats`, `invalid_graph_id`, `profile_baseline_mismatch`, and `graph_not_validated`. Add `GRAPH_INVARIANT_IDS`, `GRAPH_VALIDATION_CODE_TO_INVARIANT_ID`, `GRAPH_ID_PARSE_ERROR_CODES`, `GRAPH_VALIDATION_PROFILE_IDS`, `SANCTIONED_V2_11_GRAPH_VALIDATION_PROFILE`, derived union types, and include the new constants in `OLFACTORY_GRAPH_CONTRACT`. Keep `contract.ts` import-free and zero-I/O. Expand `contract.test.ts` with exact equality assertions for the new arrays, mapping, and sanctioned profile.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/contract.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/graph_read_model/contract.ts` contains `GRAPH_VALIDATION_ERROR_CODES`, `GRAPH_INVARIANT_IDS`, `GRAPH_VALIDATION_CODE_TO_INVARIANT_ID`, `GRAPH_ID_PARSE_ERROR_CODES`, and `SANCTIONED_V2_11_GRAPH_VALIDATION_PROFILE`.
    - Behavior assertion: `SANCTIONED_V2_11_GRAPH_VALIDATION_PROFILE.expected_stats` equals `{ families: 10, subfamilies: 18, descriptors: 341, aliases: 18, subfamily_similarity_edges: 13 }`.
    - Traceability assertion: D-05, D-06, D-07, D-08, and D-12 are named in the relevant test descriptions or assertions.
    - Command assertion: `env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/contract.test.ts` passes.
  </acceptance_criteria>
  <done>Contract constants and tests make the validation vocabulary, invariant mapping, ID parse vocabulary and sanctioned v2.11 profile inspectable from one authoritative module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend validation types for JSON-safe deterministic payloads</name>
  <files>src/graph_read_model/types.ts, src/tests/graph_read_model/contract.test.ts</files>
  <read_first>
    - .planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
    - src/graph_read_model/contract.ts
    - src/graph_read_model/types.ts
    - src/tests/graph_read_model/contract.test.ts
  </read_first>
  <behavior>
    - `GraphValidationError` preserves `{ code, path, message, node_id?, edge_id? }` and adds `invariant_id?`, `expected?`, and `actual?` per D-14.
    - `expected` and `actual` use explicit recursive `JsonValue`, excluding `unknown`, `Date`, `Map`, `Set`, `BigInt`, functions, classes, `undefined`, stack traces and variable metadata per D-15.
    - Profile objects are typed and readonly so later validation surfaces can accept immutable profiles per D-11 and D-12.
  </behavior>
  <action>In `types.ts`, import the new contract-derived types with `import type`, add recursive `JsonValue`, update `GraphValidationError.code` to `GraphValidationErrorCode`, add optional `invariant_id`, `expected`, and `actual` fields, and add `GraphValidationProfile` with readonly `profile_id`, `schema_version`, and `expected_stats`. Keep `makeGraphError` available for compatibility but make its signature typed and payload-aware. Add type-level or runtime contract tests that confirm `OLFACTORY_GRAPH_CONTRACT.sanctioned_v2_11_validation_profile` is available and that `GraphValidationError` remains structurally compatible with existing code.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/contract.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/graph_read_model/types.ts` exports `JsonValue` and `GraphValidationProfile`.
    - Source assertion: `GraphValidationError` includes `readonly invariant_id?`, `readonly expected?`, and `readonly actual?`.
    - Behavior assertion: existing callers of `makeGraphError(code, path, message, options)` still typecheck without changing call sites in this plan.
    - Traceability assertion: D-14 and D-15 are named in tests or adjacent assertions, and no `expected`/`actual` type is `unknown`.
  </acceptance_criteria>
  <done>Shared validation types can represent deterministic contract-bound errors without breaking the existing base error surface.</done>
</task>

</tasks>

<must_haves>
- GOAL: centralizar constantes definidas por contrato e tornar expectativas de validacao deterministicas com `contract.ts` e `types.ts` como fonte autoritativa inicial.
- REQ GCON-05: mantenedor consegue inspecionar prefixes, codigos, invariantes e profile sancionado em `contract.ts`.
- REQ GVAL-06: erros de validacao agora possuem campos tipados para associacao normativa e payloads JSON-safe.
- CONTEXT: D-05, D-06, D-07, D-08, D-12, D-14 e D-15 estao cobertos nesta camada.
- RESEARCH: zero dependencies, `as const` literal contracts, TypeScript strict e compatibilidade de codigos existentes preservados.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract constants -> validators | Validator behavior will trust exported vocabularies and profiles. |
| validation error payload -> CLI/future consumers | Error payloads may be serialized or inspected outside the validator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | `contract.ts` validation vocabularies | mitigate | Lock exact arrays/mapping/profile in `contract.test.ts`; keep contract import-free and static. |
| T-60-02 | Information Disclosure | `GraphValidationError.expected/actual` | mitigate | Type as `JsonValue`; prohibit runtime objects, stack traces and variable metadata by construction and factory usage in later plans. |
| T-60-SC | Tampering | npm installs | mitigate | No package install tasks in this plan set; preserve zero-dependency scope. |
</threat_model>

<verification>
Plan-level checks:
- `env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/contract.test.ts`
- `npm --prefix src run typecheck`
</verification>

<success_criteria>
- `contract.ts` exports authoritative validation vocabularies and the sanctioned v2.11 profile.
- `types.ts` supports JSON-safe structured validation errors while preserving current observable fields.
- No runtime dependency, filesystem import, Graphify access, query proof envelope change or Phase 61 fail-closed query-consumption block is introduced.
</success_criteria>

<output>
Create `.planning/phases/60-contract-constants-validation-hardening/60-01-SUMMARY.md` when done.
</output>
