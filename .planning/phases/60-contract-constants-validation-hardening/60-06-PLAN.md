---
phase: 60-contract-constants-validation-hardening
plan: 06
type: execute
wave: 6
depends_on: [60-05]
files_modified:
  - src/tests/graph_read_model/contract.test.ts
  - src/tests/graph_read_model/live_artifact_baseline.test.ts
  - src/tests/graph_read_model/query_live_baseline.test.ts
  - src/tests/graph_read_model/build_graph.test.ts
  - src/tests/graph_read_model/query_graph.test.ts
  - src/tests/cli/graph_read_model.test.ts
autonomous: true
requirements: [GCON-05, GCON-06, GVAL-06]
must_haves:
  truths:
    - "Final phase proof: drift checks, sanctioned baseline 10/18/341/18/13, CLI compatibility and query proof compatibility are all automated."
    - "D-01 through D-17 are traceable across tests, must-haves and acceptance criteria."
  artifacts:
    - "Static tests detect local graph ID templates, local validation code literals where contract constants exist, and regex prefix stripping regressions."
    - "Live artifact tests call validateSanctionedV211Graph and protect the 10/18/341/18/13 baseline."
    - "CLI/query compatibility tests prove Phase 60 did not change proof envelope shape or sanctioned dry-run behavior."
  key_links:
    - "Final wave command runs contract, graph_id, build, validate, query, live baseline and CLI tests with TMPDIR=/tmp."
---

<objective>
Adicionar provas finais de drift, baseline e compatibilidade para encerrar a Phase 60.

Purpose: garantir que as novas superficies nao apenas existam, mas tambem impeçam regressao para strings/prefixes locais e preservem compatibilidade operacional.
Output: testes estaticos/live/CLI cobrindo drift de contrato, baseline `10/18/341/18/13`, proof envelope e caminho sancionado.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
@.planning/phases/60-contract-constants-validation-hardening/60-RESEARCH.md
@.planning/phases/60-contract-constants-validation-hardening/60-VALIDATION.md
@.planning/phases/60-contract-constants-validation-hardening/60-PATTERNS.md
@.planning/phases/60-contract-constants-validation-hardening/60-05-SUMMARY.md
@src/graph_read_model/contract.ts
@src/graph_read_model/graph_id.ts
@src/graph_read_model/build_graph.ts
@src/graph_read_model/validate_graph.ts
@src/graph_read_model/query_graph.ts
@src/cli/graph_read_model.ts
@src/tests/graph_read_model/live_artifact_baseline.test.ts
@src/tests/graph_read_model/query_live_baseline.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add static drift checks for constants, IDs and validation codes</name>
  <files>src/tests/graph_read_model/contract.test.ts, src/tests/graph_read_model/build_graph.test.ts, src/tests/graph_read_model/query_graph.test.ts</files>
  <read_first>
    - .planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
    - src/graph_read_model/contract.ts
    - src/graph_read_model/graph_id.ts
    - src/graph_read_model/build_graph.ts
    - src/graph_read_model/query_graph.ts
    - src/graph_read_model/validate_graph.ts
    - src/tests/graph_read_model/contract.test.ts
  </read_first>
  <behavior>
    - Tests fail if builder/query reintroduce local ID maker templates or regex prefix stripping.
    - Tests fail if validator reintroduces local observable validation code strings that bypass contract/factories.
    - Tests confirm the authoritative contract still contains every required D-05/D-08 vocabulary item.
  </behavior>
  <action>Add source-level tests using `readFile` in existing graph read-model test style. In `contract.test.ts`, assert `GRAPH_VALIDATION_ERROR_CODES`, `GRAPH_INVARIANT_IDS`, `GRAPH_VALIDATION_CODE_TO_INVARIANT_ID`, `GRAPH_ID_PARSE_ERROR_CODES`, and `SANCTIONED_V2_11_GRAPH_VALIDATION_PROFILE` remain exported and included in `OLFACTORY_GRAPH_CONTRACT`. In builder/query tests or contract drift tests, assert `build_graph.ts` and `query_graph.ts` import `./graph_id.js`, do not declare local `familyNodeId`/`subfamilyNodeId`/`descriptorNodeId`/`aliasNodeId`, and do not contain `replace(/^subfamily:/`. Add a validator drift assertion that required observable codes appear in `contract.ts` or `validation_errors.ts`, not as new local ad-hoc code vocabularies in `validate_graph.ts`.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/contract.test.ts tests/graph_read_model/build_graph.test.ts tests/graph_read_model/query_graph.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Static assertion: tests fail on local graph ID maker declarations in `build_graph.ts` or `query_graph.ts`.
    - Static assertion: tests fail on `replace(/^subfamily:/` in `query_graph.ts`.
    - Static assertion: contract drift tests cover D-01 through D-08 and D-14 through D-17 vocabulary/factory expectations.
    - Grep hygiene assertion: any grep-like static check filters comments before asserting absence of banned implementation tokens.
  </acceptance_criteria>
  <done>Static tests guard against future drift back to duplicated prefixes, regex stripping and ad-hoc validation codes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protect sanctioned live baseline and query compatibility</name>
  <files>src/tests/graph_read_model/live_artifact_baseline.test.ts, src/tests/graph_read_model/query_live_baseline.test.ts, src/tests/graph_read_model/query_graph.test.ts</files>
  <read_first>
    - .planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
    - src/graph_read_model/contract.ts
    - src/graph_read_model/validate_graph.ts
    - src/graph_read_model/query_graph.ts
    - src/tests/graph_read_model/live_artifact_baseline.test.ts
    - src/tests/graph_read_model/query_live_baseline.test.ts
    - src/tests/graph_read_model/query_graph.test.ts
  </read_first>
  <behavior>
    - Live artifact regression uses `validateSanctionedV211Graph(graph)`.
    - Protected live baseline remains exactly `10/18/341/18/13`.
    - Query proof compatibility keeps the success envelope shape `{ query_kind, params, result, path }` and current missing-target semantics.
  </behavior>
  <action>Update live graph regression tests to call `validateSanctionedV211Graph` for the protected v2.11 artifact. Keep explicit assertions that `graph.stats` equals the sanctioned baseline. Add compatibility assertions in query tests that proof objects keep only the established success fields and that no Phase 61 invalid/unvalidated graph guard behavior has been added. Keep any current structural validation calls as `validateOlfactoryGraphStructure` or compatibility alias only where the test is intentionally structural.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/live_artifact_baseline.test.ts tests/graph_read_model/query_live_baseline.test.ts tests/graph_read_model/query_graph.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: live artifact validation result equals `{ ok: true, errors: [], warnings: [] }` through `validateSanctionedV211Graph`.
    - Baseline assertion: live graph stats equal `{ families: 10, subfamilies: 18, descriptors: 341, aliases: 18, subfamily_similarity_edges: 13 }`.
    - Compatibility assertion: query proof tests still expect `{ query_kind, params, result, path }` for success outputs and do not expect new fail-closed errors.
    - Traceability assertion: D-09 through D-13 and the Phase 61 scope fence are covered.
  </acceptance_criteria>
  <done>Live baseline and query compatibility tests prove the sanctioned profile without expanding into Phase 61.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove CLI compatibility and run full phase gate</name>
  <files>src/tests/cli/graph_read_model.test.ts</files>
  <read_first>
    - .planning/phases/60-contract-constants-validation-hardening/60-VALIDATION.md
    - src/cli/graph_read_model.ts
    - src/graph_read_model/validate_graph.ts
    - src/tests/cli/graph_read_model.test.ts
  </read_first>
  <behavior>
    - CLI dry-run compatibility remains green after sanctioned validation wrapper migration.
    - Targeted wave command from `60-VALIDATION.md` passes with `env TMPDIR=/tmp`.
    - Full typecheck and full Vitest suite pass before phase handoff.
  </behavior>
  <action>Extend `src/tests/cli/graph_read_model.test.ts` only if needed to assert the dry-run sanctioned path still succeeds after the wrapper migration. Then run the phase-level targeted command from `60-VALIDATION.md`, `npm --prefix src run typecheck`, and the full `env TMPDIR=/tmp npm --prefix src test`. Do not add external installs, runtime/API/database work, non-dry-run publication, or Graphify access.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/contract.test.ts tests/graph_read_model/graph_id.test.ts tests/graph_read_model/build_graph.test.ts tests/graph_read_model/validate_graph.test.ts tests/graph_read_model/query_graph.test.ts tests/graph_read_model/live_artifact_baseline.test.ts tests/cli/graph_read_model.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
    <automated>env TMPDIR=/tmp npm --prefix src test</automated>
  </verify>
  <acceptance_criteria>
    - Command assertion: targeted wave command passes with `env TMPDIR=/tmp`.
    - Command assertion: `npm --prefix src run typecheck` passes.
    - Command assertion: `env TMPDIR=/tmp npm --prefix src test` passes.
    - Scope assertion: no test or source change reads from or writes to `graphify-out/**`, mutates `data/taxonomy/**`, mutates `data/compiled/v2/**`, changes proof envelope shape, or implements Phase 61 full fail-closed query-consumption.
  </acceptance_criteria>
  <done>Final automated gate proves drift, baseline and compatibility coverage for the whole Phase 60 plan set.</done>
</task>

</tasks>

<must_haves>
- GOAL: centralizacao de constantes e validacao deterministica fica protegida por testes de drift, baseline e compatibilidade.
- REQ GCON-05: contrato autoritativo e profile sancionado sao testados por fonte e valor exato.
- REQ GCON-06: builder/query/CLI sao protegidos contra regressao para helpers locais.
- REQ GVAL-06: validacao sancionada do artefato live emite sucesso deterministico para o baseline protegido e falhas estruturadas nos testes unitarios.
- CONTEXT: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16 e D-17 sao rastreaveis nesta fase por testes, must-haves e acceptance criteria.
- SCOPE: a forma `{ query_kind, params, result, path }` permanece estavel e a implementacao completa de proof-generation/fail-closed query-consumption fica para Phase 61.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source code -> future maintainers | Drift checks are the maintainer's proof that constants remain centralized. |
| live compiled artifacts -> sanctioned validation | Protected v2 artifacts are used to prove the current sanctioned baseline. |
| CLI/query compatibility -> downstream phases | Phase 61 depends on stable proof envelopes and validation surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-13 | Tampering | source drift tests | mitigate | Add static tests for local ID templates, regex prefix stripping and ad-hoc validation code drift. |
| T-60-14 | Tampering | live baseline validation | mitigate | Validate live v2.11 graph through `validateSanctionedV211Graph` and exact baseline assertions. |
| T-60-15 | Denial of Service | test suite/runtime | accept | Targeted commands remain under normal graph-read-model test scope; full suite is the phase gate. |
| T-60-SC | Tampering | npm installs | mitigate | No package install tasks; all verification uses existing TypeScript/Vitest. |
</threat_model>

<verification>
Overall phase checks:
- `env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/contract.test.ts tests/graph_read_model/graph_id.test.ts tests/graph_read_model/build_graph.test.ts tests/graph_read_model/validate_graph.test.ts tests/graph_read_model/query_graph.test.ts tests/graph_read_model/live_artifact_baseline.test.ts tests/cli/graph_read_model.test.ts`
- `npm --prefix src run typecheck`
- `env TMPDIR=/tmp npm --prefix src test`
</verification>

<success_criteria>
- Static drift tests protect contract constants, graph ID helpers and validation error factories.
- Live sanctioned baseline remains exactly `10/18/341/18/13`.
- CLI and query compatibility stay green.
- Every Phase 60 requirement ID is represented in plan frontmatter and automated verification.
- Phase 61 fail-closed query-consumption remains excluded.
</success_criteria>

<output>
Create `.planning/phases/60-contract-constants-validation-hardening/60-06-SUMMARY.md` when done.
</output>
