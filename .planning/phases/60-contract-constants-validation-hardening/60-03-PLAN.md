---
phase: 60-contract-constants-validation-hardening
plan: 03
type: execute
wave: 3
depends_on: [60-02]
files_modified:
  - src/graph_read_model/types.ts
  - src/graph_read_model/validation_errors.ts
  - src/tests/graph_read_model/validate_graph.test.ts
autonomous: true
requirements: [GCON-05, GVAL-06]
must_haves:
  truths:
    - "GVAL-06: validator errors can be produced by typed factories with stable code, optional invariant_id, normalized path, stable message, expected and actual."
    - "D-14 D-15 D-16 D-17 are covered by factory behavior and JSON-safe payload assertions."
  artifacts:
    - "src/graph_read_model/validation_errors.ts centralizes typed validation error factories."
    - "src/tests/graph_read_model/validate_graph.test.ts covers factory output before validator migration."
  key_links:
    - "validation_errors.ts uses contract vocabularies from contract.ts and GraphValidationError/JsonValue from types.ts."
---

<objective>
Criar factories funcionais tipadas para erros de validacao antes de reescrever o validator.

Purpose: evitar que a separacao estrutural/profile-aware replique strings, mensagens instaveis ou payloads nao serializaveis.
Output: modulo central de factories de erro e testes de payload deterministico.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
@.planning/phases/60-contract-constants-validation-hardening/60-RESEARCH.md
@.planning/phases/60-contract-constants-validation-hardening/60-VALIDATION.md
@.planning/phases/60-contract-constants-validation-hardening/60-PATTERNS.md
@.planning/phases/60-contract-constants-validation-hardening/60-02-SUMMARY.md
@src/graph_read_model/contract.ts
@src/graph_read_model/types.ts
@src/graph_read_model/validate_graph.ts
@src/tests/graph_read_model/validate_graph.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add central typed validation error factory surface</name>
  <files>src/graph_read_model/types.ts, src/graph_read_model/validation_errors.ts</files>
  <read_first>
    - .planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
    - src/graph_read_model/contract.ts
    - src/graph_read_model/types.ts
    - src/graph_read_model/validate_graph.ts
  </read_first>
  <behavior>
    - Factories fix `code`, fill `invariant_id` when a normative mapping exists, normalize `path`, and emit stable messages.
    - Factories expose JSON-safe `expected` and `actual` payloads with predictable shapes.
    - Current observable codes remain unchanged wherever possible.
  </behavior>
  <action>Create `src/graph_read_model/validation_errors.ts`. Export a generic `makeGraphValidationError` plus named factories: `makeInvalidSchemaVersionError`, `makeInvalidGraphIdError`, `makeDuplicateNodeIdError`, `makeDuplicateEdgeIdError`, `makeMissingEdgeEndpointError`, `makeWrongEndpointKindError`, `makeInvalidAliasTargetError`, `makeInvalidSimilarityEndpointError`, `makeInconsistentStatsError`, `makeProfileBaselineMismatchError`, and `makeGraphNotValidatedError`. Keep `makeGraphError` in `types.ts` as a compatibility wrapper that delegates to the typed generic factory if that avoids duplicate normalization. All factories must use codes from `GRAPH_VALIDATION_ERROR_CODES` and invariant IDs from `GRAPH_VALIDATION_CODE_TO_INVARIANT_ID` when available.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/validate_graph.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `validation_errors.ts` exports all named factories listed in this task.
    - Source assertion: no factory accepts `unknown` for `expected` or `actual`; payload inputs are `JsonValue`.
    - Behavior assertion: `makeInvalidSchemaVersionError` emits code `invalid_schema_version` and invariant ID tied to schema version matching.
    - Traceability assertion: D-16 and D-17 are named in test descriptions or source-adjacent assertions.
  </acceptance_criteria>
  <done>All critical validation error categories have named typed factories backed by contract vocabularies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove JSON-safe payloads and compatibility shape</name>
  <files>src/tests/graph_read_model/validate_graph.test.ts, src/graph_read_model/validation_errors.ts</files>
  <read_first>
    - .planning/phases/60-contract-constants-validation-hardening/60-VALIDATION.md
    - src/graph_read_model/types.ts
    - src/graph_read_model/validation_errors.ts
    - src/tests/graph_read_model/validate_graph.test.ts
  </read_first>
  <behavior>
    - Factory results preserve `{ code, path, message, invariant_id?, node_id?, edge_id?, expected?, actual? }`.
    - `JSON.stringify` succeeds for representative errors and output does not contain forbidden runtime-only values.
    - Legacy fields `node_id` and `edge_id` remain conditional and absent when not supplied.
  </behavior>
  <action>Extend `validate_graph.test.ts` with a dedicated describe block for validation error factories. Assert exact objects for schema version, duplicate node/edge, missing endpoint, wrong endpoint kind, inconsistent stats, profile baseline mismatch, invalid graph ID, and `graph_not_validated`. Include a helper that recursively rejects `undefined`, functions, `Date`, `Map`, `Set`, `BigInt`, classes and stack-like fields from `expected`/`actual`. Do not migrate validator branches yet; that is layer 4.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/validate_graph.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: representative factory outputs match exact object snapshots in tests.
    - Payload assertion: every tested `expected` and `actual` passes the JSON-safe helper and `JSON.stringify`.
    - Compatibility assertion: base fields `code`, `path`, and `message` remain present on every error.
    - Traceability assertion: D-14, D-15, D-16 and D-17 appear in acceptance comments or test names.
  </acceptance_criteria>
  <done>Factory tests prove deterministic JSON-safe errors without changing validator execution yet.</done>
</task>

</tasks>

<must_haves>
- GOAL: validacao passa a ter factories centrais antes de qualquer separacao estrutural/profile-aware.
- REQ GCON-05: factories consomem vocabulario autoritativo do contrato.
- REQ GVAL-06: payloads estruturados de falha sao deterministicos, tipados e serializaveis.
- CONTEXT: D-14, D-15, D-16 e D-17 ficam cobertos; D-05, D-06 e D-07 sao preservados por codigos contract-bound.
- RESEARCH: nao adicionar libs de schema/runtime; manter TypeScript puro.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validator internals -> error factories | Validator branches will delegate observable error construction to factories. |
| error factories -> CLI/future consumers | Factory outputs may be serialized and inspected downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-05 | Repudiation | validation error codes | mitigate | Emit stable contract codes and invariant IDs through named factories. |
| T-60-06 | Information Disclosure | `expected`/`actual` payloads | mitigate | Constrain payloads to `JsonValue`; test for forbidden runtime values. |
| T-60-SC | Tampering | npm installs | mitigate | No package install tasks; no new runtime dependency. |
</threat_model>

<verification>
Plan-level checks:
- `env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/validate_graph.test.ts`
- `npm --prefix src run typecheck`
</verification>

<success_criteria>
- `validation_errors.ts` exists and centralizes all required factories by exact name.
- Tests prove JSON-safe payloads and stable compatibility shape.
- Validator branch migration is intentionally deferred to Plan 04 to preserve dependency order.
</success_criteria>

<output>
Create `.planning/phases/60-contract-constants-validation-hardening/60-03-SUMMARY.md` when done.
</output>
