---
phase: 60-contract-constants-validation-hardening
plan: 05
type: execute
wave: 5
depends_on: [60-04]
files_modified:
  - src/graph_read_model/build_graph.ts
  - src/graph_read_model/query_graph.ts
  - src/cli/graph_read_model.ts
  - docs/olfactory_graph_read_model.md
autonomous: true
requirements: [GCON-06, GVAL-06]
must_haves:
  truths:
    - "GCON-06: builder, query and CLI consume central graph ID and sanctioned validation surfaces instead of duplicated local prefixes or ad-hoc validation selection."
    - "D-04 and D-13 are covered by source migrations; Phase 61 proof-generation/fail-closed query consumption remains excluded."
  artifacts:
    - "build_graph.ts imports ID makers from graph_id.ts."
    - "query_graph.ts imports ID makers/strip helper from graph_id.ts and preserves current proof envelope shape."
    - "graph_read_model.ts imports validateSanctionedV211Graph for the sanctioned CLI path."
    - "docs/olfactory_graph_read_model.md mentions sanctioned v2.11 validation wrapper without documenting Phase 61 behavior."
  key_links:
    - "CLI graph build path: buildOlfactoryGraph -> validateSanctionedV211Graph -> writeGraphOutput."
---

<objective>
Migrar builder, query e CLI para consumir as superficies centrais criadas nas ondas anteriores.

Purpose: remover duplicacao pratica de prefixes/codigos e alinhar o caminho sancionado ao wrapper de validacao sem alterar o contrato de provas de query.
Output: builder/query/CLI/docs consumindo `graph_id.ts` e `validateSanctionedV211Graph`.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
@.planning/phases/60-contract-constants-validation-hardening/60-RESEARCH.md
@.planning/phases/60-contract-constants-validation-hardening/60-VALIDATION.md
@.planning/phases/60-contract-constants-validation-hardening/60-PATTERNS.md
@.planning/phases/60-contract-constants-validation-hardening/60-04-SUMMARY.md
@src/graph_read_model/graph_id.ts
@src/graph_read_model/build_graph.ts
@src/graph_read_model/query_graph.ts
@src/graph_read_model/validate_graph.ts
@src/cli/graph_read_model.ts
@docs/olfactory_graph_read_model.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate builder graph ID construction</name>
  <files>src/graph_read_model/build_graph.ts</files>
  <read_first>
    - .planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
    - src/graph_read_model/graph_id.ts
    - src/graph_read_model/build_graph.ts
    - src/tests/graph_read_model/build_graph.test.ts
  </read_first>
  <behavior>
    - Builder uses `makeFamilyGraphId`, `makeSubfamilyGraphId`, `makeDescriptorGraphId`, and `makeAliasGraphId`.
    - Generated node IDs, edge IDs, sorting and stats remain byte-for-byte compatible with existing tests.
    - No local graph ID maker templates remain in `build_graph.ts`.
  </behavior>
  <action>Replace local `familyNodeId`, `subfamilyNodeId`, `descriptorNodeId`, and `aliasNodeId` helpers in `build_graph.ts` with imports from `./graph_id.js`. Preserve the existing `edgeId` helper and exact emitted ID strings. Do not change graph shape, stats derivation, sorting, or source input semantics.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/build_graph.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `build_graph.ts` imports the four maker functions from `./graph_id.js`.
    - Source assertion: `build_graph.ts` no longer declares local `familyNodeId`, `subfamilyNodeId`, `descriptorNodeId`, or `aliasNodeId`.
    - Behavior assertion: `env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/build_graph.test.ts` passes without fixture expectation changes to graph IDs.
    - Traceability assertion: D-04 is covered by this migration.
  </acceptance_criteria>
  <done>Builder graph IDs are produced through the central graph ID boundary with existing output compatibility preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate query graph ID helpers while preserving proof envelopes</name>
  <files>src/graph_read_model/query_graph.ts</files>
  <read_first>
    - .planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
    - .planning/REQUIREMENTS.md
    - src/graph_read_model/graph_id.ts
    - src/graph_read_model/query_graph.ts
    - src/graph_read_model/types.ts
    - src/tests/graph_read_model/query_graph.test.ts
  </read_first>
  <behavior>
    - Query module imports central ID makers and strip helper instead of local templates and regex/replace prefix stripping.
    - Existing successful proof envelope shape remains `{ query_kind, params, result, path }`.
    - Missing target behavior remains current null/empty structured proofs, not Phase 61 fail-closed behavior.
  </behavior>
  <action>Replace local `familyNodeId`, `subfamilyNodeId`, `descriptorNodeId`, and `aliasNodeId` helpers in `query_graph.ts` with imports from `./graph_id.js`. Replace `replace(/^subfamily:/, '')` and equivalent prefix stripping with `stripGraphIdPrefix` and safe fallback handling that preserves current output for valid graph IDs. Do not add validation-before-query wrappers, do not alter `GraphQueryProof`, and do not change query return semantics for invalid or unvalidated graphs in this phase.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/query_graph.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `query_graph.ts` imports central ID helpers from `./graph_id.js`.
    - Source assertion: `query_graph.ts` contains no `replace(/^subfamily:/` prefix-stripping patterns.
    - Behavior assertion: existing query tests for hierarchy, alias, related descriptors, similarity neighborhoods, bridges and hubs still pass.
    - Scope assertion: `GraphQueryProof` and success proof envelopes remain unchanged; no complete Phase 61 fail-closed query-consumption block is implemented.
    - Traceability assertion: D-04 and the strict Phase 61 scope fence are covered.
  </acceptance_criteria>
  <done>Query helper usage is centralized without changing the proof contract or implementing Phase 61.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Switch CLI and docs to sanctioned validation wrapper</name>
  <files>src/cli/graph_read_model.ts, docs/olfactory_graph_read_model.md</files>
  <read_first>
    - .planning/phases/60-contract-constants-validation-hardening/60-CONTEXT.md
    - src/graph_read_model/validate_graph.ts
    - src/cli/graph_read_model.ts
    - src/tests/cli/graph_read_model.test.ts
    - docs/olfactory_graph_read_model.md
  </read_first>
  <behavior>
    - CLI sanctioned build path calls `validateSanctionedV211Graph(graph)` rather than selecting profiles manually or using implicit strict flags.
    - CLI output formatting remains compatible for existing success/failure tests.
    - Maintainer docs describe the sanctioned v2.11 wrapper/profile only for the build-validation workflow.
  </behavior>
  <action>Update `src/cli/graph_read_model.ts` to import and call `validateSanctionedV211Graph` in the existing Step 4 validation boundary. Do not add query execution to the CLI. Update `docs/olfactory_graph_read_model.md` where it describes `graph:build` validation so it names `validateSanctionedV211Graph` and `SANCTIONED_V2_11_GRAPH_VALIDATION_PROFILE`; do not document Phase 61 fail-closed query proof behavior as delivered.</action>
  <verify>
    <automated>env TMPDIR=/tmp npm --prefix src test -- tests/cli/graph_read_model.test.ts</automated>
    <automated>npm --prefix src run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/cli/graph_read_model.ts` imports `validateSanctionedV211Graph` from `../graph_read_model/validate_graph.js`.
    - Source assertion: CLI validation call uses `validateSanctionedV211Graph(graph)`.
    - Behavior assertion: CLI dry-run tests still pass.
    - Documentation assertion: docs mention sanctioned v2.11 wrapper/profile but do not claim invalid/unvalidated query proof guardrails are implemented in Phase 60.
    - Traceability assertion: D-13 is covered by CLI migration.
  </acceptance_criteria>
  <done>The sanctioned CLI boundary consumes the sanctioned validation wrapper, and docs stay aligned without expanding scope.</done>
</task>

</tasks>

<must_haves>
- GOAL: consumers internos passam a usar constantes e boundaries centrais onde pratico.
- REQ GCON-06: builder/query deixam de depender de templates locais e regex/replace para IDs.
- REQ GVAL-06: CLI usa wrapper sancionado para expectativas deterministicas do artefato v2.11.
- CONTEXT: D-04 e D-13 ficam cobertos; Phase 61 fail-closed query proof generation permanece fora.
- RESEARCH: query migration limita-se a helper usage; proof envelope `{ query_kind, params, result, path }` e semantica atual de query permanecem estaveis.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled artifacts -> builder | Builder turns compiled IDs into graph IDs trusted by validator/query. |
| graph -> query proofs | Query functions consume graph IDs to produce proof envelopes. |
| CLI graph build -> output writer | CLI decides whether graph is sanctioned before write path proceeds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-10 | Tampering | `build_graph.ts` ID generation | mitigate | Replace local templates with central makers and retain exact output tests. |
| T-60-11 | Tampering | `query_graph.ts` prefix stripping | mitigate | Use `stripGraphIdPrefix`; preserve proof envelope and avoid Phase 61 behavior changes. |
| T-60-12 | Tampering | CLI validation boundary | mitigate | Use `validateSanctionedV211Graph` before write path. |
| T-60-SC | Tampering | npm installs | mitigate | No package install tasks; no new CLI dependency. |
</threat_model>

<verification>
Plan-level checks:
- `env TMPDIR=/tmp npm --prefix src test -- tests/graph_read_model/build_graph.test.ts tests/graph_read_model/query_graph.test.ts tests/cli/graph_read_model.test.ts`
- `npm --prefix src run typecheck`
</verification>

<success_criteria>
- Builder, query and CLI consume central ID/validation surfaces.
- Existing graph output and query proof envelopes remain compatible.
- No Phase 61 proof-generation/fail-closed query-consumption block is implemented.
</success_criteria>

<output>
Create `.planning/phases/60-contract-constants-validation-hardening/60-05-SUMMARY.md` when done.
</output>
