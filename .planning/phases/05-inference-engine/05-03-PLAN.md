---
phase: 05-inference-engine
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/types/similarity.ts
  - src/types/inference.ts
  - src/inference/final_score.ts
  - src/inference/index.ts
  - src/tests/inference/final_score.test.ts
  - src/tests/fixtures/inference/scoring_dimensions.json
autonomous: true
requirements: [INFR-03, INFR-04]
must_haves:
  truths:
    - "Final score uses default weights semantic_overlap=0.50, tradition=0.25, accord_compatibility=0.15, alias_evidence=0.10."
    - "Final score renormalizes over available dimensions and never treats missing tradition/accord as zero."
    - "Final score helper applies strict edge eligibility where final_score > 0.25 and final_score === 0.25 is excluded."
    - "Graph-ready types preserve separated dimensions and compact evidence for Plan 05-04."
  artifacts:
    - path: "src/inference/final_score.ts"
      provides: "Weight renormalization and strict edge threshold helper"
    - path: "src/tests/inference/final_score.test.ts"
      provides: "Wave 0 tests for score composition, missing dimension neutrality, and strict threshold"
    - path: "src/types/similarity.ts"
      provides: "Graph-ready final_score, separated dimensions, and compact evidence contracts"
  key_links:
    - from: "src/inference/final_score.ts"
      to: "src/types/inference.ts + src/types/similarity.ts"
      via: "dimension score keys and graph-ready final score contracts"
      pattern: "semantic_overlap|tradition|accord_compatibility|alias_evidence"
    - from: "src/inference/final_score.ts"
      to: "src/inference/build_similarity_graph.ts"
      via: "Plan 05-04 consumes combineScores and shouldKeepEdge"
      pattern: "finalScore > threshold"
---

<objective>
Build the final score composition helpers and graph-ready score contracts.

Purpose: finish the scoring-helper portion of INFR-03 and INFR-04 by combining Plan 05-02 dimension outputs with user-locked weights, neutral missing-dimension behavior, and strict sparse edge eligibility before graph composition in Plan 05-04.

Output: `final_score` helper module, final-score tests, and compatible similarity/inference type extensions for graph edges.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-inference-engine/05-CONTEXT.md
@.planning/phases/05-inference-engine/05-RESEARCH.md
@.planning/phases/05-inference-engine/05-PATTERNS.md
@.planning/phases/05-inference-engine/05-VALIDATION.md
@.planning/phases/05-inference-engine/05-01-SUMMARY.md
@.planning/phases/05-inference-engine/05-02-SUMMARY.md
@src/types/similarity.ts
@src/types/inference.ts
@src/inference/semantic_overlap.ts
@src/inference/tradition_score.ts
@src/inference/accord_compatibility.ts
@src/inference/alias_evidence.ts

<interfaces>
Plan 05-02 provides separated dimension calculators and evidence. Existing `SimilarityGraph` currently has `edges[].score` and `edges[].dimensions`; extend it compatibly to support `final_score`, optional compact evidence, and separated dimensions without removing existing graph fields unless tests are updated to the new canonical shape. For Phase 5 v1 compatibility, emitted graph edges must set `edge.score = edge.final_score`; Phase 6 can decide whether to deprecate `score`. Use exact dimension keys `semantic_overlap`, `tradition`, `accord_compatibility`, and `alias_evidence` from INFR-D-22.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 final score tests and graph-ready type contracts</name>
  <files>src/tests/inference/final_score.test.ts, src/tests/fixtures/inference/scoring_dimensions.json, src/types/similarity.ts, src/types/inference.ts</files>
  <read_first>
    - .planning/phases/05-inference-engine/05-CONTEXT.md
    - .planning/phases/05-inference-engine/05-VALIDATION.md
    - .planning/phases/05-inference-engine/05-PATTERNS.md
    - .planning/phases/05-inference-engine/05-02-SUMMARY.md
    - src/types/similarity.ts
    - src/types/inference.ts
    - src/tests/inference/semantic_overlap.test.ts
  </read_first>
  <behavior>
    - `combineScores` uses default weights `0.50/0.25/0.15/0.10`, excludes undefined dimensions from denominator, and never treats missing tradition/accord as zero per INFR-D-22 through INFR-D-24.
    - If no dimensions are available, `combineScores({})` returns `0` and exposes/records the reason `no_available_dimensions` when returning evidence metadata; `shouldKeepEdge(0)` returns false. This preserves the v1 numeric return shape while documenting that missing evidence is not positive evidence.
    - Out-of-range dimension scores are clamped into `[0,1]` for pipeline robustness: negative values become `0` and values above `1` become `1`.
    - `shouldKeepEdge` returns false for `0.25` and true for `0.2500001` with default threshold `0.25` per INFR-D-25.
    - Type contracts expose separated dimension keys and compact evidence fields for Plan 05-04 without collapsing evidence into an opaque score per INFR-D-14 and INFR-D-18.
  </behavior>
  <action>Create Wave 0 Vitest coverage in `final_score.test.ts`. Extend `src/types/similarity.ts` only as needed for tests to assert `final_score`, compatibility alias `score`, separated dimension keys `semantic_overlap`, `tradition`, `accord_compatibility`, `alias_evidence`, and reusable compact edge evidence types for Plan 05-04. Extend `src/types/inference.ts` only if the final score helper needs a reusable dimension-score input contract. Reuse `scoring_dimensions.json` from Plan 05-02 rather than creating a second fixture file. Tests must import planned helpers from `../../inference/index.js` and fail until Task 2 implements the exports.</action>
  <verify>
    <automated>cd src && npm test -- tests/inference/final_score.test.ts</automated>
    <automated>cd src && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert exact default weights: `semantic_overlap: 0.50`, `tradition: 0.25`, `accord_compatibility: 0.15`, `alias_evidence: 0.10`.
    - Tests assert `combineScores({ semantic_overlap: 0.5, tradition: undefined, accord_compatibility: undefined, alias_evidence: 0.25 })` equals `((0.5 * 0.50) + (0.25 * 0.10)) / 0.60` within Vitest `toBeCloseTo` tolerance.
    - Tests assert `combineScores({})` returns `0` and `shouldKeepEdge(0)` returns false for the no-available-dimensions case.
    - Tests assert clamp behavior for invalid upstream scores: `-1` is scored as `0` and `1.2` is scored as `1`.
    - Tests assert exact threshold exclusion for `final_score === 0.25` and inclusion for `final_score > 0.25`.
    - Tests include no watch-mode flags and no dependency installs.
  </acceptance_criteria>
  <done>Wave 0 tests define renormalized final scoring, default weights, graph-ready score contracts, and strict sparse graph threshold behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement final score helpers and exports</name>
  <files>src/inference/final_score.ts, src/inference/index.ts, src/tests/inference/final_score.test.ts, src/types/similarity.ts</files>
  <read_first>
    - src/inference/final_score.ts (if it exists)
    - src/inference/index.ts
    - src/tests/inference/final_score.test.ts
    - src/types/inference.ts
    - src/types/similarity.ts
    - .planning/phases/05-inference-engine/05-CONTEXT.md
  </read_first>
  <action>Implement `DEFAULT_SCORE_WEIGHTS` with exact values from INFR-D-22, `combineScores(dimensions, weights = DEFAULT_SCORE_WEIGHTS)`, and `shouldKeepEdge(finalScore, threshold = 0.25)`. `combineScores` must validate/clamp dimension scores to `[0,1]`, omit dimensions whose score is `undefined` from numerator and denominator, return `0` when no dimensions are available, and otherwise return a deterministic normalized final score. When graph edges are materialized downstream, preserve compatibility by setting `edge.score = edge.final_score`. `shouldKeepEdge` must use strict `finalScore > threshold`, not `>=`, and thresholding must remain outside individual dimension calculators per INFR-D-25. Export helpers from `src/inference/index.ts` for Plan 05-04. Do not import `data/inference/curated_relations.v1.json` or `data/inference/accord_map.v1.json` here; those are explicit inputs to dimension calculators and graph builder, not final-score constants.</action>
  <verify>
    <automated>cd src && npm test -- tests/inference/final_score.test.ts</automated>
    <automated>cd src && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `combineScores` denominator is the sum of weights for dimensions whose score is not `undefined`; missing tradition/accord data does not contribute `0` to numerator or denominator.
    - `combineScores({})` returns `0`, and tests document this as `no_available_dimensions` behavior rather than a zero-evidence penalty.
    - Out-of-range dimension scores are clamped and covered by tests.
    - `src/inference/final_score.ts` contains `finalScore > threshold` logic, not `>=`.
    - `DEFAULT_SCORE_WEIGHTS` is exported and contains exactly the four locked weights from INFR-D-22.
    - No final-score helper reads curated relation/accord JSON files or embeds perfumery relation data.
  </acceptance_criteria>
  <done>Final-score tests pass and INFR-D-22 through INFR-D-25 scoring defaults, neutrality, and strict threshold helpers are implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dimension calculator outputs → final score helpers | Invalid or missing optional dimensions can create out-of-range scores or accidental penalties. |
| final score helpers → sparse graph builder | Incorrect threshold comparison can emit too many edges or drop eligible edges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | `combineScores` | mitigate | Clamp/validate dimension scores to `[0,1]`, renormalize over available dimensions, and test missing accord/tradition neutrality. |
| T-05-12 | Tampering | `shouldKeepEdge` | mitigate | Use strict `finalScore > threshold` and test exact `0.25` exclusion. |
| T-05-SC | Tampering | npm installs | accept | No package installs are planned; Package Legitimacy Audit says no new package gate is required. |
</threat_model>

<verification>
Run after all tasks: `cd src && npm run build && npm test -- tests/inference/final_score.test.ts`.
</verification>

<success_criteria>
- INFR-03: semantic overlap dimensions can be composed into normalized final similarity scores.
- INFR-04: tradition and accord compatibility remain neutral when missing and are incorporated through weight renormalization when present.
- Score helpers expose strict `final_score > 0.25` eligibility for Plan 05-04 sparse graph composition.
</success_criteria>

<output>
Create `.planning/phases/05-inference-engine/05-03-SUMMARY.md` when done.
</output>
