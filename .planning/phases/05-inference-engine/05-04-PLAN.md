---
phase: 05-inference-engine
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/inference/build_similarity_graph.ts
  - src/inference/index.ts
  - src/types/similarity.ts
  - src/types/inference.ts
  - src/tests/inference/build_similarity_graph.test.ts
  - src/tests/fixtures/inference/sparse_graph_threshold.json
autonomous: true
requirements: [INFR-03, INFR-04]
must_haves:
  truths:
    - "Similarity graph composes semantic, tradition, accord, and alias dimensions without collapsing evidence into an opaque score."
    - "Sparse graph emits only edges with final_score > 0.25; final_score === 0.25 is excluded."
    - "Graph output is deterministic, sparse, and includes compact evidence and typed review_queue items for review."
    - "ReviewQueueItem shape is stable: type, severity, affected, evidence, suggested_action, optional confidence/source/reason."
  artifacts:
    - path: "src/inference/build_similarity_graph.ts"
      provides: "Pure graph builder composing Phase 5 dimensions into SimilarityGraph with typed review_queue"
    - path: "src/tests/inference/build_similarity_graph.test.ts"
      provides: "Sparse graph composition, determinism, and threshold tests"
    - path: "src/tests/fixtures/inference/sparse_graph_threshold.json"
      provides: "Exact-threshold and above-threshold graph fixture"
  key_links:
    - from: "src/inference/build_similarity_graph.ts"
      to: "src/inference/final_score.ts"
      via: "combineScores then strict shouldKeepEdge"
      pattern: "final_score > threshold"
    - from: "src/inference/build_similarity_graph.ts"
      to: "src/inference/semantic_overlap.ts + tradition_score.ts + accord_compatibility.ts + alias_evidence.ts"
      via: "dimension calculators preserve separated evidence"
      pattern: "semantic_overlap|tradition|accord_compatibility|alias_evidence"
---

<objective>
Compose the sparse subfamily similarity graph from Phase 5 scoring dimensions.

Purpose: complete INFR-03 and INFR-04 by turning Plan 05-02 dimension calculators and Plan 05-03 final-score helpers into deterministic, explainable sparse graph edges for Phase 6 compilation.

Output: evidence-capable similarity graph builder, sparse threshold fixture, graph composition tests, and final full-suite verification.
</objective>

<execution_context>
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jlima/Projetos/TaxonomySystem/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-inference-engine/05-CONTEXT.md
@.planning/phases/05-inference-engine/05-RESEARCH.md
@.planning/phases/05-inference-engine/05-PATTERNS.md
@.planning/phases/05-inference-engine/05-VALIDATION.md
@.planning/phases/05-inference-engine/05-01-SUMMARY.md
@.planning/phases/05-inference-engine/05-02-SUMMARY.md
@.planning/phases/05-inference-engine/05-03-SUMMARY.md
@src/types/similarity.ts
@src/types/inference.ts
@src/inference/seed_profile.ts
@src/inference/noise.ts
@src/inference/semantic_overlap.ts
@src/inference/tradition_score.ts
@src/inference/accord_compatibility.ts
@src/inference/alias_evidence.ts
@src/inference/final_score.ts
@src/analyzer/pair_key.ts

<interfaces>
Plan 05-01 provides `SeedCorpusProfileResult`, descriptor profiles, cluster evidence, and review queue types including `ReviewQueueItem`. Plan 05-02 provides `computeSemanticOverlap`, `computeTraditionScore`, `computeAccordCompatibility`, and `computeAliasEvidence`. Plan 05-03 provides `combineScores`, `shouldKeepEdge`, `DEFAULT_SCORE_WEIGHTS`, and final-score edge/type contracts. Existing `SimilarityGraph` keeps `version`, `generated_at`, `threshold`, `dimensions`, `edges`, and `stats`; extend edges compatibly with `final_score`, separated dimension keys, compact evidence, and `score` as an alias of `final_score`. This plan stores `review_queue` inside `SimilarityGraph`, so `src/types/similarity.ts` must include `readonly review_queue: readonly ReviewQueueItem[]` and import/reuse the Plan 05-01 review type shape (`type`, `severity`, `affected`, `evidence`, `suggested_action`, optional `confidence`/`source`/`reason`) without emitting dense matrices.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 sparse graph composition tests</name>
  <files>src/tests/inference/build_similarity_graph.test.ts, src/tests/fixtures/inference/sparse_graph_threshold.json, src/types/similarity.ts, src/types/inference.ts</files>
  <read_first>
    - .planning/phases/05-inference-engine/05-CONTEXT.md
    - .planning/phases/05-inference-engine/05-VALIDATION.md
    - .planning/phases/05-inference-engine/05-PATTERNS.md
    - .planning/phases/05-inference-engine/05-02-SUMMARY.md
    - .planning/phases/05-inference-engine/05-03-SUMMARY.md
    - src/types/similarity.ts
    - src/types/inference.ts
    - src/tests/inference/semantic_overlap.test.ts
    - src/tests/inference/tradition_score.test.ts
    - src/tests/inference/accord_compatibility.test.ts
    - src/tests/inference/final_score.test.ts
  </read_first>
  <behavior>
    - `buildSimilarityGraph` preserves dimension scores separately and includes compact evidence summary per INFR-D-14 and INFR-D-18.
    - `buildSimilarityGraph` applies `shouldKeepEdge` after `combineScores`, excluding `final_score === 0.25` and keeping `final_score > 0.25` per INFR-D-25.
    - Curated relation and accord data are passed as inputs and are not embedded as scoring constants per INFR-D-26.
    - `generated_at` is deterministic: use `options.generatedAt` when provided and default to fixed `'1970-01-01T00:00:00.000Z'` when omitted; never call `new Date()` inside the graph builder.
    - `stats.density` is `0` when possible unique pairs is `0` (`subfamily_count < 2`) to avoid division by zero.
  </behavior>
  <action>Create `build_similarity_graph` Vitest coverage and `sparse_graph_threshold.json`. Extend `src/types/similarity.ts` and `src/types/inference.ts` only as needed for graph edge evidence assertions not already added by Plan 05-02. Ensure the `SimilarityGraph` type explicitly includes `readonly review_queue: readonly ReviewQueueItem[]` because this plan stores the queue inside the graph. Tests must import `buildSimilarityGraph` from `../../inference/index.js`, assert separated dimension keys `semantic_overlap`, `tradition`, `accord_compatibility`, `alias_evidence`, assert compact evidence fields `shared_descriptors`, `cooccurrence_support`, `curated_relation`, `accord_reference`, and `alias_evidence` when available, assert at least one emitted edge contains at least two defined dimension scores, assert `stats.density === 0` when `subfamily_count < 2`, and intentionally fail until Task 2 implements the graph builder.</action>
  <verify>
    <automated>cd src && npm test -- tests/inference/build_similarity_graph.test.ts</automated>
    <automated>cd src && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert exact threshold exclusion for a pair whose final score is `0.25`.
    - Tests assert a pair just above `0.25` is emitted with separated dimensions and compact evidence.
    - Tests assert `SimilarityGraph.review_queue` exists and is typed/used as `readonly ReviewQueueItem[]`.
    - Tests assert `generated_at` defaults to `'1970-01-01T00:00:00.000Z'` when `options.generatedAt` is omitted and respects `options.generatedAt` when provided.
    - Tests assert `stats.density === 0` when possible unique pairs is `0`.
    - Tests assert at least one emitted edge contains at least two defined dimension scores.
    - Tests include no watch-mode flags and no dependency installs.
  </acceptance_criteria>
  <done>Wave 0 graph tests define explainable sparse edge composition and strict threshold behavior in the actual graph plan 05-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose sparse subfamily similarity graph</name>
  <files>src/inference/build_similarity_graph.ts, src/inference/index.ts, src/types/similarity.ts, src/tests/inference/build_similarity_graph.test.ts</files>
  <read_first>
    - src/inference/build_similarity_graph.ts (if it exists)
    - src/inference/index.ts
    - src/types/similarity.ts
    - src/types/inference.ts
    - src/tests/inference/build_similarity_graph.test.ts
    - src/inference/seed_profile.ts
    - src/inference/noise.ts
    - src/inference/semantic_overlap.ts
    - src/inference/tradition_score.ts
    - src/inference/accord_compatibility.ts
    - src/inference/alias_evidence.ts
    - src/inference/final_score.ts
    - src/analyzer/pair_key.ts
    - data/inference/semantic_noise.v1.json
  </read_first>
  <action>Implement `buildSimilarityGraph(seed, analysis, inputs, options)` in `src/inference/build_similarity_graph.ts` and export it from `src/inference/index.ts`. Generate unique subfamily pairs sorted lexicographically by `${family.id}:${subfamily.id}`. For every pair, call semantic, tradition, accord, and alias calculators; preserve separated dimension scores and evidence summary fields `shared_descriptors`, `cooccurrence_support`, `curated_relation`, `accord_reference`, and `alias_evidence` when available. Compute `final_score` using `combineScores`, set compatibility `score` equal to `final_score`, then filter only after final scoring with `shouldKeepEdge(final_score, options.threshold ?? 0.25)`. Return a `SimilarityGraph` with `version: '1.0.0'`, deterministic `generated_at: options.generatedAt ?? '1970-01-01T00:00:00.000Z'` (do NOT call `new Date()` inside the graph builder), `threshold`, dimensions with the four default weights, sorted sparse `edges`, `review_queue` inside the graph typed as `readonly ReviewQueueItem[]` (with `type`, `severity`, `affected`, `evidence`, `suggested_action`, and optional `confidence`/`source`/`reason`), and `stats` containing `subfamily_count`, `edge_count`, and `density` based on possible unique pairs where `density = 0` when possible pairs is `0`. Do not emit dense matrices.</action>
  <verify>
    <automated>cd src && npm test -- tests/inference/build_similarity_graph.test.ts</automated>
    <automated>cd src && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Graph edges all satisfy `edge.final_score > 0.25` with default options; fixture pair at exactly `0.25` is absent.
    - Each emitted edge includes dimension scores under separate keys and a compact evidence object with at least one populated evidence field.
    - At least one emitted edge includes at least two defined dimension scores.
    - Each emitted edge sets `score` equal to `final_score` for compatibility with existing `SimilarityGraph` consumers.
    - Edge ordering is deterministic: source ascending, then target ascending, with no self-pairs.
    - `review_queue` items are typed as `ReviewQueueItem` with `type`, `severity`, `affected`, `evidence`, `suggested_action`, and optional `confidence`/`source`/`reason`.
    - `src/types/similarity.ts` includes `readonly review_queue: readonly ReviewQueueItem[]` on `SimilarityGraph`.
    - Repeated calls with identical inputs and `options.generatedAt` produce deep-equal `edges`, `review_queue`, and `stats`.
    - `generated_at` is not hardcoded as `new Date()` inside the graph builder; it comes from `options.generatedAt` or the fixed default `'1970-01-01T00:00:00.000Z'`.
    - `stats.density` is `0` when `subfamily_count < 2`.
    - Full suite command `cd src && npm run build && npm test` passes.
  </acceptance_criteria>
  <done>Sparse multi-dimensional similarity graph is implemented, explainable, deterministic, and ready for Phase 6 compilation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated relation/accord inputs → graph builder | Malformed curated inputs can create invalid edge evidence or hidden domain assumptions. |
| subfamily pair generation → similarity graph | Pairwise scoring can become resource-heavy if implemented as a dense emitted matrix. |
| corpus-derived evidence → graph edges | Corpus evidence must not obscure curated tradition/accord priorities or become unreviewable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | `buildSimilarityGraph` evidence | mitigate | Preserve separated dimensions and compact evidence; never collapse curated/corpus signals into an opaque score. |
| T-05-09 | Denial of Service | `buildSimilarityGraph` pair generation | mitigate | Generate unique sorted subfamily pairs and emit only sparse edges after `final_score > threshold`; never return dense matrices. |
| T-05-10 | Information Disclosure | similarity edge evidence | accept | Evidence contains descriptor ids and aggregate scores only; no secrets or PII are processed in Phase 5. |
| T-05-SC | Tampering | npm installs | accept | No package installs are planned; Package Legitimacy Audit says no new package gate is required. |
</threat_model>

<verification>
Run after all tasks: `cd src && npm run build && npm test`.
</verification>

<success_criteria>
- INFR-03: semantic overlap between subfamilies is consumed as normalized, deterministic dimension evidence in graph edges.
- INFR-04: tradition and accord compatibility are incorporated from explicit inputs, with alias candidates as weak evidence only.
- Phase success criteria are met: at least two dimensions are used and only edges with `final_score > 0.25` are preserved.
</success_criteria>

<output>
Create `.planning/phases/05-inference-engine/05-04-SUMMARY.md` when done.
</output>
