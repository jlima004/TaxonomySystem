---
plan_id: "06-01"
phase: 6
title: "Schema validation dos outputs"
wave: 1
depends_on: []
files_modified:
  - src/compiler/validate_output.ts
  - src/compiler/index.ts
  - src/tests/compiler/validate_output.test.ts
requirements:
  - COMP-04
autonomous: true
---

# Plan 06-01: Schema Validation dos Outputs

<objective>
Implementar validadores de schema para os 3 artefatos compilados: `CompiledTaxonomy`, `DescriptorAliasMap` (com envelope versionado) e `SimilarityGraph`. Os validadores são funções puras que recebem `unknown` e retornam `ValidationResult` com erros estruturados, seguindo o padrão existente em `src/loader/types.ts`.
</objective>

<must_haves>
## Goal-Backward Verification

truths:
  - Validadores retornam structured errors com artifact name, JSON path, error code e message
  - No `null` nos outputs a menos que o schema explicitamente permita
  - Campos opcionais são omitidos, não serializados como null
  - Zero runtime dependencies — validadores são pure functions TypeScript
  - Seguir exatamente os types existentes em `src/types/taxonomy.ts` e `src/types/similarity.ts`

must_verify:
  - `tsc --noEmit` compila sem erros
  - `vitest run src/tests/compiler/validate_output.test.ts` passa
  - Payloads válidos retornam `{ ok: true, errors: [] }`
  - Payloads inválidos retornam erros com JSON path correto
</must_haves>

## Tasks

<task id="06-01-T1">
<title>Criar tipo e helpers de validação do compilador</title>

<read_first>
- src/loader/types.ts (padrão ValidationError / ValidationResult existente)
- src/types/taxonomy.ts (CompiledTaxonomy, TaxonomyFamily, TaxonomySubfamily, CanonicalDescriptor, DescriptorAliasMap, TaxonomyStats)
- src/types/similarity.ts (SimilarityGraph, SimilarityEdge, SimilarityEdgeEvidence, SimilarityStats, SimilarityDimension)
- src/types/inference.ts (FinalScoreDimensions, ReviewQueueItem)
</read_first>

<action>
Criar `src/compiler/validate_output.ts` com:

1. **Tipo `CompilerValidationError`** estendendo o padrão existente com `artifact` e `code`:
   - `artifact: 'taxonomy' | 'aliases' | 'similarity'`
   - `code: string` — e.g. `MISSING_FIELD`, `INVALID_TYPE`, `INVALID_VALUE`, `DUPLICATE_ID`, `EMPTY_ARRAY`
   - `path: string` — JSON path como `families[0].subfamilies[1].id`
   - `message: string` — descrição legível

2. **Tipo `CompilerValidationResult`** — `{ ok: boolean, errors: CompilerValidationError[], warnings: CompilerValidationError[] }`

3. **`validateCompiledTaxonomy(data: unknown): CompilerValidationResult`**:
   - Validar `version` (non-empty string), `generated_at` (non-empty string), `stats` (objeto com family_count, subfamily_count, descriptor_count — todos number >= 0)
   - `families`: non-empty array de `TaxonomyFamily`
   - Cada family: `id` (snake_case string), `name` (non-empty string), `subfamilies` (non-empty array)
   - Cada subfamily: `id` (snake_case), `name` (non-empty), `family_id` (matches parent id), `descriptors` (array, pode ser vazio para subfamílias sem dados corpus)
   - Cada descriptor: `id` (non-empty), `source` ('seed' | 'corpus' | 'inferred'), `frequency` (number >= 0)
   - Verificar unicidade de family ids e subfamily ids globalmente
   - Verificar stats consistency: `family_count` == `families.length`, etc.
   - Rejeitar `null` em qualquer campo — campos não presentes devem ser omitidos, não null

4. **`validateDescriptorAliases(data: unknown): CompilerValidationResult`**:
   - Validar `version` (non-empty string), `generated_at` (non-empty string)
   - `aliases`: objeto com chaves string → valores string (não vazios)
   - Nenhum alias aponta para si mesmo
   - Nenhum valor `null` ou `undefined`

5. **`validateSimilarityGraph(data: unknown): CompilerValidationResult`**:
   - Validar `version`, `generated_at`, `threshold` (number > 0)
   - `dimensions`: array de `{ id: string, name: string, weight: number }`
   - `edges`: array de `SimilarityEdge` — cada com source, target (non-empty strings), score (number), dimensions (object)
   - `review_queue`: array (pode ser vazio)
   - `stats`: `{ subfamily_count, edge_count, density }` — todos number >= 0
   - Verificar edge consistency: `edge_count` == `edges.length`
   - `final_score` se presente deve ser number > 0

6. **`validateAllOutputs(taxonomy: unknown, aliases: unknown, similarity: unknown): CompilerValidationResult`** — roda os 3 validadores e combina resultados, retornando `ok: false` se qualquer um falhar.
</action>

<acceptance_criteria>
- `src/compiler/validate_output.ts` existe e exporta `validateCompiledTaxonomy`, `validateDescriptorAliases`, `validateSimilarityGraph`, `validateAllOutputs`
- `CompilerValidationError` contém campos `artifact`, `code`, `path`, `message`
- Payload válido de `CompiledTaxonomy` retorna `{ ok: true, errors: [], warnings: [] }`
- Payload com `families: null` retorna erro com `code: 'INVALID_TYPE'` e `path: 'families'`
- Payload com `families[0].id` duplicado retorna erro com `code: 'DUPLICATE_ID'`
- Payload com descriptor `source: 'invalid'` retorna erro com `code: 'INVALID_VALUE'`
- `tsc --noEmit` compila sem erros
</acceptance_criteria>
</task>

<task id="06-01-T2">
<title>Criar barrel export do compiler</title>

<read_first>
- src/inference/index.ts (padrão barrel export existente)
- src/compiler/validate_output.ts (arquivo criado em T1)
</read_first>

<action>
Criar `src/compiler/index.ts` como barrel export:
- Exportar tudo de `./validate_output.js`
- Remover `src/compiler/.gitkeep`
</action>

<acceptance_criteria>
- `src/compiler/index.ts` existe com `export { ... } from './validate_output.js'`
- `src/compiler/.gitkeep` removido
- `import { validateCompiledTaxonomy } from '../compiler/index.js'` resolve corretamente
</acceptance_criteria>
</task>

<task id="06-01-T3">
<title>Testes unitários para validadores de schema</title>

<read_first>
- src/compiler/validate_output.ts (validadores a testar)
- src/tests/seed_validator.test.ts (padrão de teste de validação existente)
- src/types/taxonomy.ts (shapes válidas)
- src/types/similarity.ts (shapes válidas)
</read_first>

<action>
Criar `src/tests/compiler/validate_output.test.ts` com:

1. **Suite `validateCompiledTaxonomy`**:
   - Fixture válida mínima: 1 family, 1 subfamily, 1 descriptor → `ok: true`
   - Fixture com version missing → erro `MISSING_FIELD`
   - Fixture com families como `null` → erro `INVALID_TYPE`
   - Fixture com family_id duplicado → erro `DUPLICATE_ID`
   - Fixture com descriptor source inválido → erro `INVALID_VALUE`
   - Fixture com stats inconsistente (family_count != families.length) → erro ou warning
   - Fixture com campo `null` em posição não permitida → erro

2. **Suite `validateDescriptorAliases`**:
   - Fixture válida: `{ version: '1.0.0', generated_at: '...', aliases: { a: 'b' } }` → `ok: true`
   - Fixture com alias apontando para si mesmo → erro
   - Fixture com valor null → erro

3. **Suite `validateSimilarityGraph`**:
   - Fixture válida mínima → `ok: true`
   - Fixture com edge sem source → erro
   - Fixture com stats.edge_count != edges.length → erro

4. **Suite `validateAllOutputs`**:
   - Todos válidos → `ok: true`
   - Um inválido → `ok: false` com erros do artefato que falhou
</action>

<acceptance_criteria>
- `vitest run src/tests/compiler/validate_output.test.ts` executa e passa
- Pelo menos 12 test cases cobrindo happy path e cenários de erro para cada validador
- Nenhum test usa mocks de filesystem — testes puramente in-memory com fixtures
</acceptance_criteria>
</task>

<verification>
## Verification

```bash
cd src && npx tsc --noEmit
cd src && npx vitest run tests/compiler/validate_output.test.ts
```

- Zero erros de compilação TypeScript
- Todos os testes passam
- Validadores cobrem os 3 artefatos de output
</verification>
