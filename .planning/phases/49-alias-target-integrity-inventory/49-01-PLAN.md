---
phase: 49-alias-target-integrity-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/49-alias-target-integrity-inventory/49-ALIAS-TARGET-INVENTORY.md
  - .planning/phases/49-alias-target-integrity-inventory/49-01-SUMMARY.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "49-ALIAS-TARGET-INVENTORY.md exists in the phase directory"
    - "The inventory audits exactly 18 seed aliases from descriptor_aliases.seed.json"
    - "The inventory audits exactly 18 compiled aliases from descriptor_aliases.json and confirms seed == compiled"
    - "The inventory confirms exactly 340 descriptor IDs in the compiled taxonomy"
    - "Exactly 17 alias targets are classified as valid (resolve to compiled descriptor IDs)"
    - "Exactly 1 alias target is classified as dangling: ylang ylang -> ylang_ylang"
    - "ylang_ylang is classified as remediation_required, not possible_exception_candidate"
    - "Near-match analysis documents that ylang exists as corpus candidate (frequency 41, review_required true) but is semantically distinct from ylang_ylang"
    - "Zero mutations are confirmed — no taxonomy, alias, compiled artifact, or code changes"
    - "Handoff section points to Phase 50 (gate automation) and Phase 51 (remediation)"
  artifacts:
    - path: ".planning/phases/49-alias-target-integrity-inventory/49-ALIAS-TARGET-INVENTORY.md"
      provides: "Complete alias target integrity inventory for downstream Phase 50 gate and Phase 51 remediation"
      contains: "dangling_target_analysis"
  key_links:
    - from: "49-ALIAS-TARGET-INVENTORY.md"
      to: "data/taxonomy/descriptor_aliases.seed.json"
      via: "alias seed extraction"
      pattern: "alias -> target"
    - from: "49-ALIAS-TARGET-INVENTORY.md"
      to: "data/compiled/v2/descriptor_aliases.json"
      via: "compiled alias verification"
      pattern: "seed == compiled equivalence"
    - from: "49-ALIAS-TARGET-INVENTORY.md"
      to: "data/compiled/v2/taxonomy.json"
      via: "descriptor ID lookup"
      pattern: "families[].subfamilies[].descriptors[].id"
---

<objective>
Produce the Phase 49 alias target integrity inventory artifact.

Purpose: Audit `descriptor_aliases.seed.json`, compiled `descriptor_aliases.json`, and `taxonomy.json` to produce `49-ALIAS-TARGET-INVENTORY.md` — an evidence-heavy, read-only document that lists all 18 alias targets, separates the 17 valid from the 1 dangling (`ylang ylang -> ylang_ylang`), classifies the dangling target by risk type, proves completeness, and confirms zero mutations.

Output: `49-ALIAS-TARGET-INVENTORY.md` with all 11 sections defined in D-01 of 49-CONTEXT.md.
</objective>

<execution_context>
@.agents/get-shit-done/workflows/execute-plan.md
@.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-alias-target-integrity-inventory/49-CONTEXT.md
@data/taxonomy/descriptor_aliases.seed.json
@data/compiled/v2/descriptor_aliases.json
@data/compiled/v2/taxonomy.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate alias target integrity inventory</name>
  <files>.planning/phases/49-alias-target-integrity-inventory/49-ALIAS-TARGET-INVENTORY.md</files>

  <read_first>
    - .planning/phases/49-alias-target-integrity-inventory/49-CONTEXT.md
    - data/taxonomy/descriptor_aliases.seed.json
    - data/compiled/v2/descriptor_aliases.json
    - data/compiled/v2/taxonomy.json
  </read_first>

  <action>
  Produce `49-ALIAS-TARGET-INVENTORY.md` with exactly these 11 sections (per D-01):

  **Section 1 — Sources Inspected:**
  List the three audited files with relative paths:
  - `data/taxonomy/descriptor_aliases.seed.json`
  - `data/compiled/v2/descriptor_aliases.json`
  - `data/compiled/v2/taxonomy.json`

  **Section 2 — Method:**
  Describe the audit method: direct JSON read of all three files, key-by-key comparison of seed vs compiled aliases dict, lookup of each alias target against the set of 340 descriptor IDs extracted from `taxonomy.json` via `families[].subfamilies[].descriptors[].id`.

  **Section 3 — Summary Counts:**
  Table with these exact values (verified against live data):

  | Metric | Value |
  |--------|-------|
  | Seed aliases audited | 18 |
  | Compiled aliases audited | 18 |
  | Seed == Compiled | ✅ identical |
  | Compiled descriptor IDs | 340 |
  | Valid alias targets | 17 |
  | Dangling alias targets | 1 |
  | Mutations applied | 0 |

  **Section 4 — Seed vs Compiled Alias Equivalence:**
  State explicitly that the alias mapping dictionary in `data/taxonomy/descriptor_aliases.seed.json` is dictionary-identical to the `aliases` dictionary in `data/compiled/v2/descriptor_aliases.json`. No diff needed.

  **Section 5 — Valid Alias Targets Table:**
  Full table with all 17 valid aliases sorted alphabetically by alias key:

  | Alias | Target | Status |
  |-------|--------|--------|
  | ambergri | ambergris | ✅ Valid |
  | banana_ripe_banana | banana | ✅ Valid |
  | boi_de_rose | rosewood | ✅ Valid |
  | bois_de_rose | rosewood | ✅ Valid |
  | cedar | cedarwood | ✅ Valid |
  | cedar wood | cedarwood | ✅ Valid |
  | jasmin | jasmine | ✅ Valid |
  | musky | musk | ✅ Valid |
  | oak moss | oakmoss | ✅ Valid |
  | orange blossom | orange_blossom | ✅ Valid |
  | orange flower | orange_blossom | ✅ Valid |
  | orange_bitter_orange | bitter_orange | ✅ Valid |
  | orangeflower | orange_blossom | ✅ Valid |
  | patchouly | patchouli | ✅ Valid |
  | petit grain | petitgrain | ✅ Valid |
  | rose_red_rose | rose | ✅ Valid |
  | sandal wood | sandalwood | ✅ Valid |

  **Section 6 — Dangling Alias Targets Table:**

  | Alias | Target | Status | Classification |
  |-------|--------|--------|---------------|
  | ylang ylang | ylang_ylang | ❌ Dangling | remediation_required |

  **Section 7 — Near-match / Descriptor Context:**
  Document that `ylang` exists in `data/compiled/v2/taxonomy.json` with these properties:
  - `id: "ylang"`, `source: "corpus"`, `frequency: 41`, `status: "candidate"`, `review_required: true`, `corpus_derived: true`
  - `ylang` is a corpus candidate, not a curated descriptor — semantically distinct from the expected target `ylang_ylang`
  - `ylang_ylang` does NOT appear anywhere in the compiled taxonomy or taxonomy seed

  **Section 8 — Candidate Exception Classification:**
  Apply the D-02 classification framework (three categories: `remediation_required`, `possible_exception_candidate`, `invalid_or_noise_candidate`).
  Classify `ylang ylang -> ylang_ylang` as `remediation_required` with rationale:
  - The alias "ylang ylang" is a semantically valid olfactory ingredient name
  - The target `ylang_ylang` does not exist in any compiled source
  - `ylang` (corpus candidate) is not equivalent to `ylang_ylang` (expected target)
  - No documented evidence of intentional absence → not an exception candidate

  **Section 9 — Completeness Proof:**
  Declare: "All 18 aliases from both seed and compiled sources have been verified. 17 resolve to valid compiled descriptor IDs. 1 (`ylang ylang -> ylang_ylang`) is dangling. 18/18 aliases audited = 100% coverage."

  **Section 10 — Zero-Mutation Statement:**
  Declare: "Nenhum arquivo de dados, seed ou compiled foi modificado durante a execução desta fase. Este inventário é estritamente read-only. Nenhuma decisão de remediação é tomada nesta fase."

  **Section 11 — Handoff to Phase 50/51:**
  - Phase 50 (Alias Target Integrity Automation): Will implement the automated validation gate and exception policy support
  - Phase 51 (Legacy Alias Remediation): Will resolve `ylang ylang -> ylang_ylang` and any other dangling targets confirmed in this inventory, using the Phase 50 gate as proof
  </action>

  <acceptance_criteria>
    - `python3 -c "import json; d=json.load(open('data/taxonomy/descriptor_aliases.seed.json')); print(len(d))"` outputs `18`
    - `python3 -c "import json; d=json.load(open('data/compiled/v2/descriptor_aliases.json')); print(len(d['aliases']))"` outputs `18`
    - `python3 -c "import json; s=json.load(open('data/taxonomy/descriptor_aliases.seed.json')); c=json.load(open('data/compiled/v2/descriptor_aliases.json'))['aliases']; print(s==c)"` outputs `True`
    - `python3 -c "import json; t=json.load(open('data/compiled/v2/taxonomy.json')); ids={d['id'] for f in t['families'] for s in f['subfamilies'] for d in s['descriptors']}; print(len(ids))"` outputs `340`
    - `python3 -c "import json; t=json.load(open('data/compiled/v2/taxonomy.json')); ids={d['id'] for f in t['families'] for s in f['subfamilies'] for d in s['descriptors']}; a=json.load(open('data/taxonomy/descriptor_aliases.seed.json')); valid=sum(1 for v in a.values() if v in ids); print(valid)"` outputs `17`
    - `python3 -c "import json; t=json.load(open('data/compiled/v2/taxonomy.json')); ids={d['id'] for f in t['families'] for s in f['subfamilies'] for d in s['descriptors']}; a=json.load(open('data/taxonomy/descriptor_aliases.seed.json')); dangling={k:v for k,v in a.items() if v not in ids}; print(dangling)"` outputs `{'ylang ylang': 'ylang_ylang'}`
    - 49-ALIAS-TARGET-INVENTORY.md contains the section header "Sources Inspected"
    - 49-ALIAS-TARGET-INVENTORY.md contains the section header "Completeness Proof"
    - 49-ALIAS-TARGET-INVENTORY.md contains the section header "Zero-Mutation Statement"
    - 49-ALIAS-TARGET-INVENTORY.md contains the string "remediation_required"
    - 49-ALIAS-TARGET-INVENTORY.md contains the string "Nenhuma decisão de remediação"
    - 49-ALIAS-TARGET-INVENTORY.md contains the section header "Handoff to Phase 50/51"
    - No files outside `.planning/phases/49-alias-target-integrity-inventory/` were modified
  </acceptance_criteria>

  <verify>
    <automated>python3 -c "
import json, sys

# 1. Seed count (flat dict: {alias: target})
seed_aliases = json.load(open('data/taxonomy/descriptor_aliases.seed.json'))
assert len(seed_aliases) == 18, f'Seed count {len(seed_aliases)} != 18'

# 2. Compiled count (wrapped: {aliases: {alias: target}})
c = json.load(open('data/compiled/v2/descriptor_aliases.json'))
comp_aliases = c['aliases']
assert len(comp_aliases) == 18, f'Compiled count {len(comp_aliases)} != 18'

# 3. Seed == Compiled
assert seed_aliases == comp_aliases, 'Seed != Compiled'

# 4. Descriptor ID count
t = json.load(open('data/compiled/v2/taxonomy.json'))
ids = {d['id'] for f in t['families'] for sub in f['subfamilies'] for d in sub['descriptors']}
assert len(ids) == 340, f'Descriptor IDs {len(ids)} != 340'

# 5. Valid and dangling
valid = {k: v for k, v in seed_aliases.items() if v in ids}
dangling = {k: v for k, v in seed_aliases.items() if v not in ids}
assert len(valid) == 17, f'Valid {len(valid)} != 17'
assert len(dangling) == 1, f'Dangling {len(dangling)} != 1'
assert 'ylang ylang' in dangling, 'ylang ylang not in dangling'
assert dangling['ylang ylang'] == 'ylang_ylang', 'Wrong dangling target'

# 6. Inventory exists
import os
inv = '.planning/phases/49-alias-target-integrity-inventory/49-ALIAS-TARGET-INVENTORY.md'
assert os.path.exists(inv), f'{inv} missing'
content = open(inv).read()
assert 'Sources Inspected' in content, 'Missing Sources Inspected section'
assert 'Completeness Proof' in content, 'Missing Completeness Proof section'
assert 'Zero-Mutation Statement' in content, 'Missing Zero-Mutation Statement section'
assert 'remediation_required' in content, 'Missing remediation_required classification'
assert 'Handoff to Phase 50/51' in content, 'Missing Handoff section'

print('ALL CHECKS PASSED')
"</automated>
  </verify>

  <done>
  49-ALIAS-TARGET-INVENTORY.md exists with all 11 required sections. Audits all 18 aliases from seed and compiled sources. Confirms 17 valid, 1 dangling (ylang ylang -> ylang_ylang classified as remediation_required). Proves completeness (18/18 = 100%). Confirms zero mutations. Handoff to Phase 50/51 documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This is a read-only inventory phase producing a documentation artifact. No user input, API, network, auth, or data mutation boundaries exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | 49-ALIAS-TARGET-INVENTORY.md | accept | Read-only documentation artifact; git history provides integrity. No runtime code, no user-facing surface. |
| T-49-02 | Information Disclosure | descriptor_aliases.seed.json, taxonomy.json | accept | Already published compiled artifacts in the repository. No secrets or PII. |
</threat_model>

<verification>
- `descriptor_aliases.seed.json` contains exactly 18 alias entries
- `descriptor_aliases.json` (compiled) contains exactly 18 alias entries identical to seed
- `taxonomy.json` contains exactly 340 descriptor IDs
- 17 alias targets resolve to valid descriptor IDs
- 1 alias target (`ylang_ylang`) does not resolve — classified as `remediation_required`
- `ylang` exists as corpus candidate but is semantically distinct from `ylang_ylang`
- No files outside `.planning/phases/49-alias-target-integrity-inventory/` were modified
- No taxonomy, alias, compiled artifact, inference, or code mutations occurred
</verification>

<success_criteria>
- 49-ALIAS-TARGET-INVENTORY.md exists in the phase directory
- Inventory audits all 18 seed aliases and all 18 compiled aliases
- Seed == Compiled equivalence confirmed
- 17 valid + 1 dangling = 18 total (100% coverage)
- ylang ylang -> ylang_ylang classified as remediation_required
- Near-match analysis documents ylang as corpus candidate
- Zero mutations confirmed
- Handoff to Phase 50/51 documented
</success_criteria>

<output>
After completion, create `.planning/phases/49-alias-target-integrity-inventory/49-01-SUMMARY.md`
</output>
